{
  "extends": "../../tsconfig.base.json",
  "// Почему rootDir '.'": "E-00, по образцу `renderer-hyperframes` (H-01): тесты пакета лежат в `test/` (vitest.config.ts включает `packages/*/test/**`), точка входа `vpe` — в `bin/` (вне зоны линта D4, где законны часы). При rootDir './src' оба каталога выпадали бы из `tsc --build`, то есть `pnpm typecheck` их не видел. Цена — `dist/` содержит `src/`, `test/` и `bin/`, а `main` смещается в `dist/src/`; пакет приватный. Долг тот же — docs/DEBTS.md №6.",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "bin/**/*.ts"],
  "references": [
    { "path": "../compile" },
    { "path": "../core-model" },
    { "path": "../media" },
    { "path": "../renderer-hyperframes" },
    { "path": "../schema" },
    { "path": "../templates-spec" },
    { "path": "../voice" }
  ]
}
