{
  "note": "Видео во весь кадр целиком, с полями сверху и снизу. Берётся там, где кадрировать нельзя: в горизонтальном ролике важен весь кадр (титр, схема, две фигуры по краям). Цвет полей — чёрный, тот же, что фон композиции, поэтому граница поля не читается как рамка.",
  "params": {
    "asset": "clip",
    "frame": "full",
    "fit": "contain",
    "bg": "#000000",
    "audio": "off"
  }
}
