{
  "extends": "../../tsconfig.base.json",
  "// Почему rootDir '.'": "TS-01, по образцу `packages/voice` (V-01), `packages/media` (M-01) и `packages/compile` (CP-01): тесты пакета лежат в `test/` (vitest.config.ts включает `packages/*/test/**`). При rootDir './src' они выпадали бы из `tsc --build`, то есть `pnpm typecheck` их не видел. Цена — `dist/` содержит и `src/`, и `test/`, а `main` смещается в `dist/src/`; пакет приватный. Долг тот же — docs/DEBTS.md №6, новая строка не заводится.",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts"],
  "references": [
    { "path": "../core-model" }
  ]
}
