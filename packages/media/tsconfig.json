{
  "extends": "../../tsconfig.base.json",
  "// Почему rootDir '.'": "M-01, по образцу `packages/core-model` (C-01) и `packages/schema` (R-02): тесты пакета лежат в `test/` (vitest.config.ts включает `packages/*/test/**`). При rootDir './src' они выпадали бы из `tsc --build`, то есть `pnpm typecheck` их не видел. Цена — `dist/` содержит и `src/`, и `test/`, а `main` смещается в `dist/src/`; пакет приватный, потребителей у него сегодня нет ни одного. Долг тот же, что у core-model, — docs/DEBTS.md №6.",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },
  "// Почему src/**/*.json": "M-05: `cacheKeyView` — данные (K2), три файла `src/cache/views/*.json`. Без них в `include` компилятор отказывается их импортировать (TS6307), а `resolveJsonModule` копирует их в `dist/` вместе с кодом.",
  "include": ["src/**/*.ts", "src/**/*.json", "test/**/*.ts"],
  "references": [
    { "path": "../core-model" },
    { "path": "../schema" }
  ]
}
