# PROGRESS — SP-3f Visual Ceiling (цельный фрагмент ролика на HyperFrames) · старт 2026-08-21T17:34+06:00
План: 5 шагов. (1) чтение контекста + скелет sp3f, вендоринг GSAP/Three; (2) композиция 450 кадров, 6 слоёв режиссуры, пробный рендер; (3) матрица V×10 + V-draft×3 + 3 прогона цены слоёв; (4) приборы (sha256/framemd5/PSNR/ВЧ/where/loc/RSS) + кадры PNG; (5) отчёты. Бюджет: 60 мин стенки, жёстко; композиция+пробный рендер до 25-й мин, матрица 25–50, отчёты 50–60.
- `17:34` [1/5] начато — чтение CLAUDE.md, Charter rev4, ADR-0003, ADR-0008, фикстуры, findings SP-3c/3d/3e, кода sp3e/src/hyperframes и приборов
- `17:37` [1/5] готово за 3 мин — контекст прочитан. Ключевое: GSAP 3.15.0 + SplitText + MorphSVGPlugin ЕСТЬ в sp3c/node_modules (вендорим локально, сеть не нужна); three.js НЕТ → частицы на canvas 2D; HyperFrames квантует `Math.floor(t*fps+1e-9)/fps`; приборы sp3/lib + sp3e/{where,loc,hfenergy}.mjs берутся импортом
- `17:38` [2/5] начато — скелет sp3f, вендоринг, генерация слоёв глубины из backdrop.jpg, композиция 450 кадров
- `17:42` [2/5] проба WebGL: композиция `src-probe` (экран зелёный при живом WebGL, красный при отказе) отрендерена за 12.3 с, кадр 0 = RGB(0,160,0) → **WebGL в софтверном Chrome (SwiftShader) ЗАВЁЛСЯ**, шейдерный фон остаётся на GLSL, откат на CSS-градиенты не понадобился
- `17:43` [2/5] начато — пробный полный рендер 450 кадров (V-cal)
- `17:45` [2/5] готово за 11 мин (план 25) — композиция 450 кадров рендерится: `V-cal` 82.2 с, **5.806 кадра/с** фазы рендера, sha `9ac469f8c5cf7982`. опережение плана на 14 мин
- `17:46` [2/5] просмотр кадров пробного прогона: типографика, стекло, морфинг, финальный кадр и субтитры выразились; частицы слишком бледные, финальная строка заходила под скрим субтитров — правки внесены (частицы ярче и крупнее, финальный блок выше, скрим длиннее)
- `17:47` [3/5] начато — матрица 17 прогонов в фоне (4 коротких на цену слоёв + 10 V + 3 V-draft), дедлайн +15 мин; отчёты пишутся параллельно. прогоны идут фоном, приборы пишутся параллельно
- `17:50` ПОПРАВКА К ЖУРНАЛУ: временные метки строк 17:37–17:47 были проставлены по ошибочной оценке модели («18:xx») и один раз исправлены на показания `date`; содержание строк не менялось. Дальше метки берутся из `date`
- `17:50` [3/5] цена слоёв, первый заход (60 кадров): full60 1.641, noshader 1.715, noparticles 1.615, noglass 1.385 кадра/с. ОШИБКА МЕТОДА: на кадрах 0..60 частиц и карточки на экране нет (окна 120..215 и 200..305), `L-noglass` дал тот же sha256, что и `L-full60` — измерена стоимость инициализации, не отрисовки. Добавлены три полнодлинных прогона `L450-*`
- `17:52` [3/5] V-w4-r1..r4 — один и тот же sha256 `250f2d220a2255f6`, 5.49–5.70 кадра/с фазы рендера
- `17:55` [4/5] приборы написаны: determinism/where/hfenergy(4 кадра по слоям)/captiontest(T8)/loc/frames/machine; `results/machine.json` записан: HyperFrames 0.8.5, GSAP 3.15.0 (SplitText + MorphSVGPlugin), three.js не ставился, сеть не открывалась
- `17:57` [4/5] тест ADR-0003 T8: первая версия прибора (md5 кропа) дала 311 «смен» вместо 37 — это шум кодека, а не сдвиг границ; прибор переписан на средний модуль разности кропа между соседними кадрами. Результат: **37 смен из 37, 10 страниц из 10, 36 границ слов из 36, лишних нет**. Разрыв между сигналом и шумом — 2.366 против 0.197 (×12)
- `17:57` [3/5] V-draft (540×960, crf 28): 18.4 с, 30.64 кадра/с
- `18:05` [3/5] матрица закончена: **V ×10 — один sha256 `250f2d220a2255f6` на все десять**, 5.37–5.90 кадра/с; **V-draft ×3 — один sha256 `e2ecf34f928c1a94`**, 30.6–35.3 кадра/с
- `18:10` ОШИБКА: сторож, который должен был запустить остаток матрицы, зациклился (его `pgrep -f` находил собственную командную строку) — потеряно ~4 мин. Остаток (проба плагинов + три полнодлинных прогона цены слоёв) запущен напрямую
- `18:13` [3/5] проба возможностей браузера (три полосы): WebGL **зелёная**, SplitText **зелёная**, MorphSVGPlugin **зелёная** — ни один заготовленный откат не понадобился
- `18:16` [3/5] цена слоёв, второй заход (450 кадров): полная 5.669, без шейдера 6.140, без частиц 5.936, без стекла 5.640 кадра/с. Прогоны прекращены на 42-й минуте, все 20 рендеров сделаны
- `18:19` [4/5] гейт: **1 sha256 из 10** (final) и **1 из 3** (draftHalf); framemd5 по одному; PSNR на контрольных парах — 0 различающихся кадров из 450; `where` по восьми окнам слоёв — 0 из 450, bbox пуст; ВЧ-энергия по четырём кадрам-слоям — по одному уровню на каждый, перехода между макросостояниями растеризатора нет
- `18:22` [5/5] отчёты записаны: `summary.md` (числа), `findings.md` (пять пунктов задания), `decisions.md` (14 решений), `machine.json`, `sha256.txt`, `captions.json`, `results/frames/` — 8 PNG
**Итог:** сцена из шести элементов режиссуры собралась и держит нулевой порог: 10 прогонов — один файл, 3 черновых — один файл, ни один слой не выброшен. Скорость 5.669 кадра/с (полоса ADR-0008 «≥ 4» пройдена с запасом ×1.42), черновик 33.9 кадра/с (×6.0 быстрее). Тест ADR-0003 T8 зелёный: 37 границ субтитров из 37 на расчётном кадре. WebGL, SplitText и MorphSVGPlugin в софтверном Chrome доступны. Детали — `docs/spikes/sp3f/results/`.
- `18:24` [5/5] дописаны `results/fixture.json` (sha256 двоичных файлов композиции) и README
ГОТОВО, 50 минут, 13 из 13 (+7 служебных: 2 пробы, 4 коротких и 3 полнодлинных на цену слоёв), кадры в results/frames/
