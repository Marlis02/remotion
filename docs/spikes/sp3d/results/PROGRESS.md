# PROGRESS — SP-3d: Docker-режим HyperFrames, держится ли AC4 · старт 2026-08-21T06:20Z (12:20 местного)

Docker: сокет доступен. `docker version` от имени `ct` напрямую даёт
`permission denied while trying to connect to the docker API at unix:///var/run/docker.sock`,
потому что группа `docker` (gid 983) в сессии не подхвачена (`id` её не показывает), хотя
`getent group docker` → `docker:x:983:ct`, то есть владелец в группе уже состоит. Все вызовы
идут через `sg docker -c '...'` — это подхват уже выданной группы, а не sudo. Спайк идёт.

План: 5 шагов. (1) sp3d + сборка образа механизмом самого CLI + machine.json/fixture.json;
(2) матрица Docker 27 прогонов; (3) Q3 версии в образе, Q5 `--network none`, Q4 сравнение с
локальным софтверным путём (mp4 / PNG-сиквенс / свой энкодер); (4) добор — 10 подряд w4 final
и прямой 60 с; (5) сведение: summary.md, findings.md, decisions.md.
Бюджет: платных вызовов нет, аудио и .env не трогаются.
- `12:22` [1/5] сборка образа механизмом самого CLI (`ensureDockerImage`, `docker build` из `hyperframes/dist/docker/Dockerfile.render`) — начато
- `12:29` [1/5] готово за 7м24с (из них рендер 37.4 с): образ `hyperframes-renderer:0.8.5`, id `sha256:6fea83b0…`, 1.28 ГБ content / 4.15 ГБ на диске. Образ НЕ тянется из реестра — CLI собирает его локально
- `12:31` НАХОДКА 1: в Docker захват идёт в режиме **screenshot** (`forceScreenshot: true`, `captureMode: "screenshot"`), а не beginFrame — при том, что `headlessShell=true`. Локально SwiftShader шёл beginFrame
- `12:31` НАХОДКА 2: Chrome в образе `HeadlessChrome/152.0.7977.54`, локально `152.0.7977.42`; ffmpeg в контейнере `Lavc59.37.100` (Debian bookworm), локально `Lavc60.3.100` (ffmpeg-static 6.0)
- `12:33` НАХОДКА 3 (Q4, предварительно): framemd5 пробного Docker-рендера **побайтово равен** локальному SwiftShader `hfB-final-w4-sw-r1` (300 кадров, один md5-файл). sha256 mp4 различаются только из-за строки версии энкодера в контейнере (45378918 против 45378917 байт)
- `12:35` [1/5] готово: machine.json (docker 29.7.2, образ id sha256:6fea83b0…, база node:22-bookworm-slim@sha256:d649c27d…), image-probe.json. Ключевое для Q3: chrome-headless-shell **152.0.7977.54** (Chrome for Testing), ffmpeg **5.1.9-0+deb12u1** (Debian), 10 шрифтовых пакетов, 1909 начертаний, Debian 12 bookworm, node v22.23.2 внутри
- `12:36` [2/5] матрица Docker 27 прогонов — начато, ожидаю ~30 мин
- `12:47` ВАЖНО ДЛЯ Q2: хост НЕ простаивает. Посторонние процессы владельца: `next-server (v16.2.6)` — ~700 % CPU и 53 % RAM, и `qemu-system-x86_64` (Win10 VM, 4 vCPU, 8 ГБ) — ~95 % CPU. loadavg по прогонам 18–92 при 12 потоках, свободной памяти 1.3–3.2 ГиБ. Ни один из них не мой и остановить их я не могу. Следствие: **числа кадров/с SP-3d НЕ сравнимы с ночными числами SP-3c** и помечаются отдельно; на детерминизм это работает в плюс — это и есть стресс, а sha256 пока один и тот же на всех прогонах
- `12:48` запущен фоновый лог loadavg (results/hostload.jsonl), чтобы каждая цифра скорости имела рядом состояние машины
- `12:51` [2/5] блок A+B (точная композиция, final w1/2/4/8 ×3 и draft w4 ×3) готов: **15 из 15, ни одного падения**. Все 12 прогонов профиля final дали ОДИН sha256 `5339fca603207613…` — при w=1, 2, 4 и 8, при loadavg хоста от 18 до 92. draft — свой единственный `182231cffcf39626…`
- `12:52` [2/5] блок C (идиоматичная композиция, w1 и w4) — начато
- `12:57` [2/5] блок C (идиоматичная композиция) готов: **6 из 6**, все w1 и w4 дали ОДИН sha256 `14b800759214c730…`. Локально в SP-3c эта же композиция на софтверном пути давала 2 варианта из 3 при w=1 и 2 из 3 при w=4
- `12:57` [2/5] блок D (под нагрузкой хоста 6 потоков поверх уже занятой машины) — начато
- `13:00` [2/5] блок D (нагрузка) готов: 6 из 6. **НАХОДКА, отвечающая на Q1 отрицательно:** `dD-idiom-final-w4-load6-r3` дал ДРУГОЙ файл — `4ebb5bcc09a945a6…`, 40 628 419 байт против 43 382 724 у двух других. Расхождение с кадра 1. То есть в Docker нулевой порог AC4 **не** держится безусловно: на идиоматичной композиции при w=4 под нагрузкой хоста — 2 варианта из 3
- `13:01` добавлен блок J: частота этого расхождения (идиоматичная w4 под нагрузкой ×10, идиоматичная w1 под нагрузкой ×3, точная w4 под нагрузкой до 9 прогонов) — три прогона отвечают «есть или нет», нужна частота
- `13:03` УТОЧНЕНИЕ к строке `12:47`: «свободной памяти 1.3–3.2 ГиБ» — это `os.freemem()` (память без кэша), а не то, что доступно процессам. По `MemAvailable` доступно было 18–20 ГиБ, дефицита памяти не было; `lowMemoryMode` в трассе всех прогонов — `false`. Загрузка CPU при этом реальна: loadavg(1 мин) медианно 15.4 при 12 потоках. `next-server` к 13:03 освободил память, `qemu` (Win10 VM) продолжает занимать ядро
- `13:09` [2/5] НАХОДКА, закрывающая Q1 и Q4 разом. Идиоматичная композиция в Docker даёт **три** состояния — 43 382 724 / 40 628 419 / 26 533 152 байт. Локально в SP-3c на SwiftShader та же композиция давала ровно три состояния с теми же размерами минус один байт: 43 382 723 / 40 628 418 / 26 533 151. Разница ровно в один байт — длина строки версии энкодера в контейнере (`Lavc59.37.100` против `Lavc60.3.100`)
- `13:09` [2/5] framemd5 сверен попарно: Docker `dH-idiom-final-w1-x06` **побайтово равен по кадрам** локальному `hfN-idiom-final-w1-sw-r1`; Docker `dC-idiom-final-w4-r1` равен локальному `hfM-idiom-final-w4-sw-r1`. То есть Docker воспроизводит локальный софтверный путь вместе с его недетерминизмом, а не чинит его
- `13:09` [2/5] третий вариант получен при `workers=1` **вхолостую** (`dH-idiom-final-w1-x06`) — значит дело не в параллелизме и не в нагрузке. Точная композиция при этом: 25 прогонов final, один sha256
- `13:14` [2/5] ЛОКАЛИЗАЦИЯ расхождения (SP-3c оставлял механизм UNKNOWN). Три варианта — это ДВА состояния растеризации, и прогон может переключиться между ними ПОСРЕДИ ролика. Битрейт по секундам: normal 2402/3987/4597/4816/… КБ, v26 1933/2679/2610/2791/… (лёгкое состояние всю дорогу), v40 1933/2679/**3692**/4816/… (переключился на второй-третьей секунде). Высокочастотная энергия (средний |Δ| соседних пикселей, кроп 520×520): кадр 20 — normal 10.29, v26 8.46, v40 8.46; кадр 150 — 10.43 / 7.49 / 10.42; кадр 290 — 6.19 / 4.32 / 6.19
- `13:14` [2/5] Глазами: у «тяжёлого» состояния по градиентам идёт мелкая крапчатость, у «лёгкого» её нет. Кадры сохранены в out/.look. Кандидаты (INFERENCE, не разделены): дизеринг градиентов Chrome либо качество фильтрации при масштабировании фонового JPEG. Точная композиция при этом на 25 прогонах не переключилась ни разу
- `13:15` [2/5] ВЧ-энергия кадра 150 по объектам: Docker идиоматичная тяжёлое 10.429 / лёгкое 7.488; Docker ТОЧНАЯ 10.338; SP-3c локально SwiftShader идиоматичная 10.429 и 7.488 (те же два состояния); SP-3c локально SwiftShader точная 10.338; SP-3c локально **аппаратный GPU** точная **7.021**. То есть «лёгкое» состояние софтверного пути похоже на то, как рисует аппаратный GPU: расходятся именно градиенты
- `13:17` [2/5] МЕХАНИЗМ УСТАНОВЛЕН. Воркеры получают непрерывные диапазоны кадров (`framesPerWorker = Math.ceil(totalFrames/workerCount)`, producer/dist/index.js:156896), при 300 кадрах и w=4 это 0–74 / 75–149 / 150–224 / 225–299. У смешанного прогона `dD-idiom-final-w4-load6-r3` ВЧ-энергия скачет РОВНО на кадре 75: кадры 68–74 — 4.63/5.59/6.59/4.18/7.17/4.83/4.47 (совпадает с «лёгким» эталоном до сотых), кадры 75–82 — 9.96/10.17/10.15/10.27/10.30/10.27/10.30/10.28 (совпадает с «тяжёлым»). Контроль: у «тяжёлого» эталона те же кадры 68–82 ровно 10.25–10.36, то есть низкие значения — это состояние, а не содержимое
- `13:17` [2/5] Вывод: состояние растеризации — свойство ОТДЕЛЬНОГО процесса-воркера Chrome, устойчивое на всём его диапазоне кадров. Отсюда всё остальное: при w=1 два возможных выхода, при w=4 — до 2^4 сочетаний. Это закрывает UNKNOWN из SP-3c §1.1 «что именно расходится между процессами Chrome»
- `13:18` [2/5] блок H готов (14 из 14). Частота на идиоматичной композиции в Docker: **w=1 вхолостую — 10 прогонов, 2 варианта (8 «тяжёлых» / 2 «лёгких»); w=4 вхолостую — 10 прогонов, 2 варианта (9 / 1 смешанный); w=4 под нагрузкой — 3 прогона, 2 варианта.** Точная композиция за то же время: 25 прогонов, 1 файл
- `13:18` [3/5] парный локальный софтверный путь (9 прогонов) — начато; дальше Q5 (сеть), прямой 60 с, блок J и PNG-сиквенсы
- `13:23` [3/5] парный локальный софтверный путь готов: **9 из 9**, все дали `d0f3fe7e0b8e3c8d…` — ровно тот sha256, что SP-3c записал ночью. Окружение за сутки не сдвинулось, сравнение честное
- `13:23` [3/5] Q5 пройден: негативный контроль — из контейнера с `--network none` наружу не выйти; полный рендер 300 кадров в таком контейнере проходит; кадры и sha256 совпали с эталоном матрицы `dA-final-w4-r1`. Оговорка: флага сетевого режима у CLI НЕТ, контейнер по умолчанию идёт в `bridge`, строку запуска пришлось собирать самому
- `13:26` [3/5] прямой прогон 60 с (1800 кадров): wall 147.5 с = 2.46 мин, 33.05 кадр/с по кадрам, 16.12 кадр/с по всему процессу, пик RSS контейнера 2597 МБ, cgroup 2144 МБ. AC2 (≤ 15 мин) проходит с запасом ×6
- `13:26` [4/5] блок J (частота расхождения под нагрузкой, 16 прогонов) и PNG-сиквенсы — начато
- `13:28` [4/5] Q4 закрыт точно. Элементарный поток h264 (контейнер отброшен) Docker против локального софтверного пути: во всех пяти парах различается **ровно 10 байт в позициях 43–99** — строка SEI с версией x264 (`core 164 r3095 baee400 … 2003-2022` против `core 164 r3106 eaa68fa … 2003-2023`). Все остальные 45 376 328 байт кодированного видео равны побайтово. Плюс один байт в контейнере — тег `encoder` (`Lavc59.37.100` против `Lavc60.3.100`)
- `13:40` [4/5] блок J готов (16 из 16). Итоговые частоты Docker: **точная композиция final — 31 прогон, 1 вариант** (w1/2/4/8, вхолостую и под нагрузкой, loadavg хоста 8.6–92); точная w4 под нагрузкой — 9 прогонов, 1 вариант (у SP-3c на аппаратном GPU та же клетка давала 3 варианта из 9). **Идиоматичная: w1 вхолостую 10→2 (8/2), w4 вхолостую 10→2 (9/1), w4 под нагрузкой 10→2 (7/3), w1 под нагрузкой 3→1.** Всего по идиоматичной 33 прогона и три различных выхода
- `13:40` [4/5] освобождение диска перед PNG: удалено 17 mp4 (711 МБ), sha256 и framemd5 каждого остались в results. Блок P (PNG-сиквенсы) — начато
- `13:41` [4/5] Q4 до энкодера: PNG-сиквенс из Docker дал `dirHash 47ed518df1aaef8b80f055811c17a333c44e3e899d2a5adefc9535226f10beff` — **совпал с тем, что SP-3c записал ночью** для локального SwiftShader (`hfE-png-w4-sw`). То есть равны не только декодированные кадры mp4, но и сырые PNG до энкодера
- `13:42` [4/5] блок P готов: PNG-сиквенс из Docker и локальный сегодня дали ОДИН `dirHash 47ed518d…`, и он же записан в SP-3c ночью. Три независимых прогона в трёх окружениях — один и тот же набор из 300 PNG побайтово
- `13:42` [4/5] блок K (чередующаяся пара Docker ↔ локально, 12 прогонов) — начато; это последний блок
- `13:51` [4/5] блок K готов (12 из 12), матрица закончена. Чередующаяся пара, кадров/с по кадрам: w=1 Docker 7.25/7.22/7.46 против локально 8.21/8.28/8.65; w=4 Docker 28.88/28.85/28.39 против локально 29.67/29.56/28.20
- `13:51` [5/5] сведение (determinism, crosscompare, Q4, PNG + свой энкод, summary) — начато
- `13:56` [5/5] сведение готово: determinism (19 групп), crosscompare (12 клеток), q4-compare (10 пар), png-compare (4 сравнения + свой энкод), bitstream-diff (5 пар), scale + pixeldiff (масштаб расхождения), summary.md
- `14:00` [5/5] масштаб расхождения: по всему ролику совпало 150 кадров из 300 (граница совпадения — ключевой кадр 151, то есть измерение ограничено GOP, а не диапазоном воркера); PSNR p50 43.03 dB, min 34.80, 61 кадр ниже 40 dB. На сырых кадрах: 71.45 % субпикселей различаются, но 36.7 % из них — ровно на один уровень, 61.4 % — на два и меньше, макс 55. Глазом не видно
- `14:04` [5/5] исправлено по журналу хоста: loadavg на старте прогонов 8.78–91.85 (медиана 18.96), MemAvailable медианно 9.0 GiB, минимум 1.7; `lowMemoryMode` = false во всех 91 прогоне. Ранняя строка `12:47` про «свободной памяти 1.3–3.2 ГиБ» относилась к `os.freemem()` и не является мерой доступной памяти
**Итог:** Q1 — нулевой порог AC4 в Docker **не держится безусловно**: точная композиция 37 из 37 один файл, идиоматичная — 33 прогона и три варианта, причём и при `workers=1` вхолостую. Q4 — растеризатор тот же, что у локального софтверного пути: сырые PNG равны 300 из 300, кодированные данные равны, различаются 10 байт подписи x264. Механизм расхождения локализован: состояние принадлежит отдельному воркеру Chrome. Детали — `docs/spikes/sp3d/results/`.
- `14:08` очистка: два PNG-сиквенса по 829 МБ удалены после сведения (диск был на 97 %). dirHash, sha256 каждого из 300 PNG и framemd5 остались в results/raw/dP-*.json; mp4 нашего энкода из обоих сиквенсов остались в out/
ГОТОВО, 14:08 местного (2026-08-21T08:08Z), 91 из 91, отчёты в docs/spikes/sp3d/results/
