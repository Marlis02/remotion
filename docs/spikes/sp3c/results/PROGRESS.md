# PROGRESS — SP-3c HyperFrames как кандидат в рендереры · старт 2026-08-21T03:52Z (09:52 местного)
План: 7 шагов. [1] разведка машины + установка HyperFrames и ffmpeg (только npm) → machine.json;
[2] композиция один-в-один с SP-3 на HTML+GSAP, фиксация расхождений (Q6);
[3] разведочный рендер: beginFrame-режим завести, иначе fallback (бюджет диагностики 1 час);
[4] матрица: 3 прогона × c{1,2,4} × {final,draft} + 3 под нагрузкой CPU + PNG-сиквенсы (Q1,Q2,Q3);
[5] контроль Remotion на ЭТОЙ машине (SP-3-композиция, скрипты sp3c) — иначе кадров/с несравнимы;
[6] V9 netns, старт на сегмент, воспроизводимость бандла, размеры (Q4,Q5,Q7);
[7] отчёты: summary.md, findings.md (Q1–Q7 + сводная таблица), decisions.md, PROGRESS.md.
Бюджет: платных вызовов нет. Аудио/TTS/.env не трогаются вовсе.
- `09:49` [1/7] начато — разведка окружения
- `09:49` [1/7] FACT: **это НЕ та машина, на которой снят SP-3.** SP-3: AMD Ryzen 5 5600H (ноутбук), 6c/12t, 15.03 GiB, Ubuntu 22.04.5, kernel 6.8.0-136, governor powersave, батарея есть. Здесь: Intel Core i5-10400 (ASUS, chassis_type 3 = desktop), 6c/12t, 31 GiB RAM + 8 GiB swap, Ubuntu 24.04.3, kernel 7.0.0-28, /sys/class/power_supply ПУСТ (батареи нет). Следствия: (а) кадров/с SP-3 и SP-3c несравнимы напрямую → добавляю шаг [5], контрольный прогон Remotion здесь же; (б) Q3 «прогон от батареи» физически невозможен — батареи нет, не «не делается по решению», а «нечем»; (в) sha256 mp4 из SP-3 воспроизвести нельзя by design.
- `09:49` [1/7] FACT: ffmpeg/ffprobe на этой машине НЕ установлены, passwordless sudo нет. Беру статические сборки через npm (ffmpeg-static/ffprobe-static) — остаёмся внутри «только npm». Версия будет отличаться от SP-3 (4.4.2) — записано в decisions.
- `09:49` [1/7] FACT: node v25.6.1 (nvm default). SP-3 снят на v22.22.1. HyperFrames требует node >= 22.
- `10:02` [1/7] готово за 13 мин — hyperframes 0.8.5 + @hyperframes/{core,engine,producer} 0.8.5 установлены в docs/spikes/sp3c/node_modules (214 пакетов, 3 мин). ffmpeg 6.0-static и ffprobe 4.0.2-static — через npm (ffmpeg-static/ffprobe-static), симлинки в sp3c/bin. chrome-headless-shell 152.0.7977.42 скачан puppeteer'ом в ~/.cache/puppeteer (сеть сверх npm — записано в decisions). `hyperframes doctor`: всё обязательное ✓, аккаунт/ключ HeyGen НЕ требуется. Телеметрия выключена (`hyperframes telemetry disable`).
- `10:02` [1/7] FACT (чтение кода): beginFrame-режим — базовый путь на Linux; флаги Chrome в browserManager.js:731 ровно те, что в статье. Дефолт `browserGpuMode: "software"` → `--use-gl=angle --use-angle=swiftshader` (то есть аналог swangle из SP-3, а не angle). ENCODER_PRESETS: draft=ultrafast/crf28, standard=medium/crf18, high=slow/crf15. `-bf 0`, `-vf scale=in_range=pc:out_range=tv`, `-x264-params aq-mode=3:aq-strength=0.8:deblock=1,1:colorprim=bt709...`, `-video_track_timescale 90000`, метаданные hyperframes_renderer/version. `-fflags +bitexact` НЕТ ⇒ mvhd creation_time = стенное время ⇒ mp4 их энкодера побайтово равным быть не может в принципе. Вывод: mp4 меряю ДВУМЯ приборами — их sha256 (ожидаемо разный) и framemd5 (настоящий вопрос), плюс отдельно PNG-сиквенс и наш собственный энкод по рецепту SP-3.
- `10:03` [2/7] начато — композиция один-в-один с SP-3 на HTML+GSAP
- `10:10` ПОПРАВКА к меткам времени: строки, помеченные `10:02`/`10:03`, писались в 09:56–09:58 по местному времени (я проставил время на глаз). Дальше время берётся из `date`, машинально. Старые строки не правлю по CLAUDE.md §3.
- `10:12` [2/7] готово — композиция `src/index.html`: 1080×1920, 30 fps, 300 кадров, GSAP 3.15.0 локально (не CDN), шрифт и фон — те же файлы, что в SP-3 (sha256 совпали побайтово: backdrop 170a64ce…, DejaVu d1c3ff99…, captions 01ea6473…). Ken Burns/затемнение/появление страниц — таблица `src/motion.js`, сгенерированная `gen-motion.mjs` из САМОГО remotion (`interpolate` + `Easing.inOut(Easing.ease)`), поэтому геометрия совпадает точно, а не «похоже». `hyperframes lint`: 0 ошибок, 0 предупреждений.
- `10:12` [3/7] готово — beginFrame-режим завёлся с первого раза, диагностика не потребовалась. FACT: `[BrowserManager] Browser launched (HeadlessChrome/152.0.7928.2, beginframe, gl=--use-gl=angle --use-angle=gl-egl, headlessShell=true, platform=linux)`. FACT: режим GPU по умолчанию — `auto`, и проба выбрала **аппаратный** GPU: `browserGpuMode probe → hardware (ANGLE (Intel, Mesa Intel(R) UHD Graphics 630 (CML GT2)))`. То есть путь по умолчанию у HyperFrames — аналог remotion `gl=angle`, а не `swangle`; SwiftShader включается флагом `--no-browser-gpu`. Меряю обе ветки.
- `10:12` [3/7] FACT: `--format png-sequence` уводит захват в **screenshot**-режим (`Browser launched (…, screenshot, …)`), а не beginFrame, и стоит 225 с против 19 с. Значит «сырые PNG до энкодера» через CLI снимаются только для fallback-пути; для beginFrame сырьё до энкодера через CLI недоступно, и вопрос Q1 по нему закрывается парой (sha256 mp4, framemd5), плюс отдельной проверкой детерминизма самого энкодера.
- `10:12` [3/7] FACT: три полных прогона подряд (`--workers 1`, quality standard, GPU auto→hardware) дали **побайтово равный** mp4, sha256 `8ada9f9b297e886a…`, и один и тот же framemd5. Прогон с `--workers 4` дал **тот же** sha256 в первый раз и **другой** (`8afcca5a08a1b0ff…`) во второй — то есть на w=4 есть разброс прогон-к-прогону. Это и разбирает матрица.
- `10:13` [4/7] начато — матрица: блоки A (18), C (3), F (4), E (6), D (6), B (18) = 55 прогонов
- `10:16` [4/7] блок A (18 прогонов, beginFrame + аппаратный GPU) готов. FACT: девять прогонов профиля `final` при workers 1, 2 и 4 дали **один и тот же** mp4 побайтово, sha256 `8ada9f9b297e886a…`, один framemd5 `15ce608fb84fcc2a…`. То есть на этом пути `workers` не влияет на результат вообще — как `concurrency` на `angle` у Remotion (SP-3 §2.1).
- `10:20` [4/7] блок C (нагрузка CPU, 6 занятых потоков, workers 4) — FACT: **три прогона под нагрузкой дали ДРУГОЙ файл**, sha256 `8afcca5a08a1b0ff…` и другой framemd5 `c89f96cb04e698a3…`. Различаются именно ДЕКОДИРОВАННЫЕ кадры, а не метаданные контейнера. Вариант устойчив: три прогона под нагрузкой совпали между собой. Это ровно тот класс отказа, которого у Remotion `angle` НЕ было (SP-3 блок I: под нагрузкой тот же sha256). Ставлю блок H (нагрузка при workers 1 и 2) и блок G (10 повторов без нагрузки), чтобы отделить «нагрузка» от «нагрузка × параллелизм» и померить частоту.
- `10:20` [4/7] FACT (снимает подозреваемого): в beginFrame-режиме HyperFrames сам пишет `static-frame dedup: disabled (capture mode beginframe, not screenshot)` — дедупликация кадров не участвует и на расхождение влиять не может.
- `10:23` [4/7] блок E (PNG-сиквенс) идёт: 70.7 с при workers 4 против 225 с при workers 1; захват 4.7 кадра/с — это fallback-путь (screenshot), а не beginFrame.
- `10:27` [4/7] очередь ночи выстроена тремя цепочками: (1) блоки A/C/F/E/D/B; (2) блоки G/H, контроль Remotion 27 прогонов + 2 PNG-сиквенса, V9 netns, собственный энкод, прямой замер 60 с у обоих рендереров, воспроизводимость сборки, попиксельные сравнения, сведение; (3) идиоматичный вариант композиции (Q6) и досводка. Каждая цепочка ждёт предыдущую, чтобы замеры не мешали друг другу.
- `10:27` FACT (лицензия, Q7): CLI-пакет `hyperframes@0.8.5` объявляет `"license": "Apache-2.0"`; все `@hyperframes/*` несут файл LICENSE — чистый Apache-2.0, `Copyright 2026 HeyGen, Inc.`, без Commons Clause и без NOTICE. Но **поля `license` в их package.json нет**, и в метаданных npm-реестра лицензия у них пустая: автоматический сканер лицензий пометит их как unknown. Это не юридическая проблема, но операционная.
- `10:27` FACT: `hfE-png-w4-gpu` r1 и r2 дали один dirHash `2281ccb6a131dd01…`, r3 — другой `27358ec11948dc0f…`. То есть на fallback-пути (screenshot) сырые PNG расходятся прогон-к-прогону — картина как у `swangle` в SP-3.
- `10:41` [4/7] картина по блокам A–F сложилась, и она НЕ «детерминирован/не детерминирован», а тоньше:
  * `workers=1` — стабильно во всех режимах: 3/3 beginFrame + аппаратный GPU, 3/3 fallback-screenshot, 3/3 SwiftShader, и все три дали ОДИН И ТОТ ЖЕ sha256 `8ada9f9b…` (то есть beginFrame и screenshot при w=1 дают побайтово равный mp4);
  * SwiftShader (`--no-browser-gpu`) — 9/9 прогонов профиля final при workers 1, 2, 4 дали один sha256 `d0f3fe7e0b8e3c8d…`; параллелизм на нём ни на что не влияет;
  * аппаратный GPU + `workers=4` — есть разброс: под нагрузкой CPU 2 из 3 дали `8afcca5a…` вместо `8ada9f9b…`; в fallback-режиме 1 из 3 дал `53c98bf8…`; на половинном draft 1 из 3 дал другой файл; PNG-сиквенс 1 из 3 дал другой dirHash. Варианты устойчивые (повторяются дословно), как дискретные варианты `swangle` в SP-3.
  * `workers=8`: 6.3 ГБ пик RSS против 3.1 ГБ при w=4 — и всего 58.7 кадра/с захвата против 60.4. Смысла нет, ровно как у Remotion c=8 в SP-3.
- `10:41` Блоки G (10 повторов w4 без нагрузки) и H (нагрузка при w1 и w2) поставлены именно ради этого: нужно отделить «параллелизм» от «нагрузка» и получить ЧАСТОТУ, а не факт.
- `10:50` [4/7] FACT (локализация разброса, важно): блок G — **10 повторов подряд** на аппаратном GPU при `workers=4` без нагрузки дали **один и тот же** mp4 `8ada9f9b…`, 10/10. Блок H — **под той же нагрузкой CPU, что ломала w=4, при `workers` 1 и 2 разброса нет**: 6/6 дали `8ada9f9b…`. То есть разброс возникает не от нагрузки как таковой, а от сочетания «аппаратный GPU × несколько процессов Chrome»; нагрузка лишь делает его частым. Ставлю блоки J (SwiftShader w4 ПОД нагрузкой — та самая конфигурация, которую пришлось бы пришпилить ради AC4), K (ещё 6 прогонов аппаратного GPU w4 под нагрузкой — нужна частота, а не факт) и L (7 прогонов w2 без нагрузки — где граница).
- `10:52` ОШИБКА и правка на ходу: первые 6 контрольных прогонов Remotion сняты без пика RSS и без верификации — `control/runner.mjs` их не считал. Скрипт исправлен прямо во время матрицы (matrix.mjs читает файл на каждый прогон, поэтому с 7-го всё пишется). Шесть первых будут перепрогнаны в четвёртой цепочке. Матрица не останавливалась.
- `10:53` FACT (контроль, первое число): Remotion 4.0.513, `gl=angle`, c=4, профиль final **на этой машине** — 26.4 кадра/с фазы кадров, 16.8 кадра/с весь процесс, пик RSS 2.10 ГБ, sha256 `ed786a41d7918b10…` совпал у r1 и r2. Chromium у контроля — 149.0.7790.0, то есть ровно та версия, что в SP-3.
- `11:12` [4/7] вторая цепочка готова: 100 прогонов, 0 упавших. Ключевые числа этой части:
  * **Прямой замер 60 с (1800 кадров, один сегмент, без экстраполяции):** HyperFrames final w4 — **61.2 с** (64.5 кадра/с фазы захвата); контрольный Remotion `angle` c4 — **101.1 с** (24.9 кадра/с). Порог AC2 (15 минут) обе цифры проходят с запасом больше ×8.
  * **V9:** рендер 300 кадров прошёл в сетевом namespace без интерфейсов, кроме loopback; негативный контроль подтвердил, что наружу не выйти.
  * **Энкодер на этой машине детерминирован** (двойной энкод одного PNG-сиквенса даёт побайтово равный mp4), `threads=1` и `threads=4` дают разный битстрим — как блок D в SP-3.
  * **Q7:** `compositionHash` один и тот же во всех прогонах и в двух холодных компиляциях; две холодные компиляции дали побайтово равный mp4. `node_modules` 1447 МБ, chrome-headless-shell 260 МБ, композиция целиком 3.4 МБ.
  * **Старт на сегмент:** HyperFrames 1115 мс, контрольный Remotion здесь же 2063 мс (в SP-3 на ноутбуке было 3.2 с).
  * **Контроль воспроизвёл главную находку SP-3 на другом железе:** Remotion `swangle` недетерминирован (final c1: 2 варианта из 3; final c4: **три разных файла из трёх**), Remotion `angle` внутри настройки детерминирован (final c4 3/3, draft 9/9).
- `11:15` [4/7] Q6 измерен числом: HyperFrames против Remotion `angle`, кадр 100, сырые PNG — различается 27.6 % субпикселей, но **91.6 % расхождений ровно на 1 уровень из 255, 97.7 % — не больше двух**; хвост до 218 уровней сидит **только в буквах**: средний |Δ| по строкам фона 0.1–0.3, в полосе субтитров (строки 1344–1536) — до 9.1. То есть фон, Ken Burns и затемнение перенеслись один-в-один, а расходится растеризация текста и тени (Chrome 152 против 149). Для сравнения: Remotion `angle` против `swangle` на том же кадре — 27.4 % субпикселей, максимум 4 уровня.
- `11:15` [4/7] FACT (Q6, цена авторской модели): «идиоматичный» вариант той же композиции (родные ease GSAP, тайминги в секундах, клипы `data-start`/`data-duration`) отличается от точного на **298 кадрах из 300**, медиана 82.8 % субпикселей, PSNR 30.0 dB, максимум 138 уровней. На первом кадре расхождение ещё ±1 уровень, дальше расходится — это разная кривая easing, а не растеризация.
- `11:16` [4/7] НОВОЕ и важное: контрольный Remotion `angle` при `concurrency=1` дал sha256 `864f10d2…`, а при `concurrency=4` — `ed786a41…`. В SP-3 (на ноутбуке с Radeon) c=1, c=2 и c=4 давали ОДИН файл. Если это подтвердится на трёх прогонах, вывод SP-3 §2.1 «на angle concurrency не влияет вообще» окажется свойством той машины, а не рендерера. Жду блок перепрогона.
- `11:26` [4/7] четвёртая цепочка готова: 120 прогонов, 0 упавших. Картина детерминизма замкнулась, и она симметричнее, чем ожидалось:
  * **HyperFrames SwiftShader — один вариант на 12 прогонов**: w1/w2/w4, и вхолостую, и под нагрузкой 6 потоков (блок J) — везде sha256 `d0f3fe7e0b8e3c8d…`. Это прямой аналог блока I из SP-3, и он пройден.
  * **HyperFrames аппаратный GPU: w=1 — 6/6 один вариант, w=2 — 13/13 один вариант** (10 вхолостую + 3 под нагрузкой). Разброс начинается ровно на **w=4**: вхолостую final 13/13 совпали, но draft 2/3, draftHalf 2/3, PNG 2/3, screenshot 2/3, идиоматичная композиция — **три разных файла из трёх**; под нагрузкой при w=4 на девяти прогонах вышло **три устойчивых варианта** (4 + 4 + 1).
  * **Контрольный Remotion `angle` на ЭТОЙ машине оказался хуже, чем в SP-3**: профиль final при c=1 — 3/3 один файл, при c=4 — 3/3 один файл, но при **c=2 — три разных файла из трёх**, и c=1 ≠ c=4 (расхождение с кадра 177). В SP-3 на ноутбуке с Radeon c=1, c=2 и c=4 давали один файл на 13 прогонах. Профиль draft здесь при этом 9/9 один файл.
  * **Контрольный Remotion `swangle`** воспроизвёл SP-3 дословно: расхождение с кадра 1, 2 совпавших кадра из 300, PSNR p50 44 dB, максимум 5–6 уровней.
  * **Масштаб всех расхождений одинаков по классу**: PSNR p50 41.7–54 dB, максимум |Δ| 5–21 уровня из 255 — то же, что нашёл SP-3 на `swangle`. Глазом не видно, нулевой порог AC4 ломает.
- `11:26` Ставлю блок M: идиоматичная композиция на SwiftShader и при workers=1 — надо убедиться, что вывод «SwiftShader и w=1 детерминированы» держится не только на нашей композиции.
- `11:29` [4/7] блок M закрыл вопрос и переформулировал вывод: идиоматичная композиция **на SwiftShader при w=4 дала 2 варианта из 3** (`710be65f…` дважды, `076591e9…` один раз), а **при w=1 на аппаратном GPU — 3/3 один файл** (`5d75aa11…`). То есть «SwiftShader детерминирован» — неверно как общее утверждение: он детерминирован для НАШЕЙ композиции. Единственная настройка, которая не дала разброса ни разу и ни на одной композиции, — **`workers = 1`**. Ставлю блок N: идиоматичная при w=2 (пять прогонов) и при w=1 на SwiftShader (три), чтобы клетки таблицы не остались пустыми.
- `11:34` [4/7] блок N закрыл клетки: идиоматичная при `workers=2` на аппаратном GPU — **5/5 один файл**; идиоматичная при `workers=1` на SwiftShader — **2 варианта из 3**. Итоговая раскладка: аппаратный GPU при `workers` 1 или 2 — 38 прогонов в 11 группах, **ноль расхождений**; `workers ≥ 4` — 52 прогона в 14 группах, разброс в семи.
- `11:38` [5/7] контроль Remotion на этой машине готов (27 прогонов + 2 PNG-сиквенса + 60-секундная ступень). Числа — в `results/summary.md`, раздел «Контроль».
- `11:40` [6/7] Q4/Q5/Q7 сняты: V9 подтверждён механикой и mp4 из namespace **побайтово равен** обычному; старт на сегмент 1.12 с (HyperFrames) против 2.07 с (Remotion здесь же) и 3.2 с (Remotion в SP-3); `compositionHash` один на все прогоны и на две холодные компиляции, две холодные компиляции дали побайтово равный mp4; размеры — `node_modules` 1447 МБ, chrome-headless-shell 260 МБ, композиция 3.4 МБ.
- `11:43` [7/7] отчёты написаны: `results/summary.md` (только числа, 134 прогона), `results/findings.md` (Q1–Q7 с пометками FACT/INFERENCE/UNKNOWN, сводная таблица «Remotion (SP-3) против HyperFrames (SP-3c)», долги, раздел «Что это меняет для ADR-0008 и R2»), `results/decisions.md` (18 решений ночи), `results/machine.json`, `results/fixture.json`, `results/raw/` (145 файлов), `results/framemd5/` (138 файлов).
- `11:45` Уборка: удалены семь из девяти PNG-сиквенсов (5 ГБ) — диск был занят на 94 %, стало 19 ГБ свободно. Оставлены два, на которых считалось попиксельное сравнение. sha256 каждого отдельного PNG всех сиквенсов остался в `results/raw/*.json`.
- `11:45` Проверено: `git status docs/spikes/sp3/` **пуст** — SP-3 не тронут ни одним файлом. Библиотеки SP-3 переиспользованы импортом.
**Итог:** HyperFrames заводится на этой машине с первого раза в beginFrame-режиме, без аккаунта и ключа HeyGen, проходит V9 механикой и укладывается в AC2 с запасом больше ×8 (60 секунд за 1.02 минуты против 1.68 у контрольного Remotion). Детерминизм у него есть не как свойство движка, а как свойство конфигурации: аппаратный GPU при `workers ≤ 2` не разошёлся ни разу на 38 прогонах, `workers ≥ 4` разошёлся в семи группах из четырнадцати. Главная неожиданность ночи — контрольный Remotion: `swangle` воспроизвёл недетерминизм SP-3 дословно на другом железе, а `angle`, на котором построено решение владельца, дал здесь **три разных файла из трёх** при `concurrency=2`. Детали — `docs/spikes/sp3c/results/findings.md`.
ГОТОВО, 11:45 (2026-08-21T05:45Z), снято 134 прогона из 134 запланированных, упавших нет, отчёты в docs/spikes/sp3c/results/

<!-- Корневой PROGRESS.md по CLAUDE.md §3 очищен; в нём оставлена одна строка-указатель
     на этот файл, потому что задание просило, чтобы строка «ГОТОВО» была видна с первого
     взгляда, а корневой файл — то место, куда владелец смотрит первым. -->
