# SP-2 — что измерено и что из этого следует

Пометки по Charter §7: **FACT** — измерено, число лежит в `results/`; **INFERENCE** — вывод из
измеренного; **UNKNOWN** — не измерялось и не выводится. Числа — в [summary.md](summary.md),
сырьё — в [raw/](raw/), журнал — в [PROGRESS.md](PROGRESS.md).

**Спайк только отвечает на вопросы.** Ни один ADR и ни одна строка Charter здесь не меняются:
решения принимает владелец отдельным коммитом.

**Условие, общее для всего документа.** Голос из задания
(`Michael C. Vincent — Confident, Expressive`, `category: professional`) на тарифе Free через API
недоступен: HTTP 402 `paid_plan_required`, списано 0. Замеры сняты на
`Daniel — Steady Broadcaster` (`category: premade`). Что от этого зависит, а что нет, разобрано
в [voice-and-tier.md](voice-and-tier.md) и повторено ниже в каждом затронутом разделе.

**Досъёмка SP-2b (тариф Creator).** После смены тарифа голос из задания стал доступен, и всё
голосозависимое пересняли на нём же. Разделы U4–D10 ниже описывают замер на Daniel и остаются
как есть; что из них подтвердилось на боевом голосе, что опровергнуто и что это меняет для T7 —
в разделе [«Досъёмка SP-2b: боевой голос»](#досъёмка-sp-2b-боевой-голос) перед итоговыми
таблицами. Там же закрыт долг 4 (здоровье alignment выше 1514 символов).

---

## U4 — тождество `characters.join('') === input`, единица массива, лид-ин/хвост

### U4.1 Тождество выполняется

**FACT.** На 28 строках, покрывающих F1–F16 из ADR-0010 §10 плюс числа, даты, деньги и дату
`03/04`, `alignment.characters.join('')` **побайтово равен отправленному тексту — 28 из 28**.
Ни одна ловушка его не сломала, включая NFD-диакритику, NBSP, суррогатную пару, эмодзи с
модификатором тона, curly quotes, эллипсис одним символом, URL и все числовые классы.
Попутно: три массива `alignment` одной длины во всех 28 случаях, `start` неубывает и
`start[i] ≤ end[i]` во всех 28. Файл: [raw/block1-block2.json](raw/block1-block2.json).

**FACT.** То же тождество держится на длинных чанках: 202, 403, 810 и 1514 символов —
`charIdentity` истинно на всех четырёх ступенях ([raw/block4-health.json](raw/block4-health.json)).

**INFERENCE.** `charIdentity` из ADR-0010 §1 — не «защита от вероятного», а **дешёвый ассерт на
редкое событие**. Это не довод его убрать: гарантия по-прежнему нигде не декларирована
(`FACT` r1 §1.4), а цена проверки — одно сравнение строк.

**INFERENCE.** Риск, записанный в ADR-0010 («если SP-2 покажет, что `charIdentity` не
выполняется на английском даже при `off`, придётся ужесточать линт либо включать fuzzy-алигнер»),
**не реализовался**. Fuzzy-алигнер original↔normalized в v1 не нужен ни как дефолт, ни как
включаемая опция для ElevenLabs.

**UNKNOWN.** Поведение при `alignment: null` (оба поля nullable, `FACT` r1 §1.3) не наблюдалось:
за 49 успешных вызовов `alignment` пришёл всегда. Это не значит, что он приходит всегда —
приёмка обязана обрабатывать `null`, и в `tts:mock@1` соответствующий тест написан.

### U4.2 Единица массива — code points

**FACT.** Длина `alignment.characters` совпала с числом **code points** входа во всех 28 строках;
с числом UTF-16 units — в 26; с числом графем — в 26. Три строки различают гипотезы, и все три
дают один ответ:

| строка | UTF-16 | code points | графемы | массив |
|---|---|---|---|---|
| `cafe`+U+0301 (NFD) | 45 | 45 | 44 | 45 → не графемы |
| `🚢 ahead` | 43 | 42 | 42 | 42 → не UTF-16 |
| `👍🏽 ok` | 47 | 45 | 44 | 45 → не UTF-16 и не графемы |

**FACT.** Тот же счётчик используется в **тарификации**: сумма code points по всем отправленным
строкам = **5222** = списанию провайдера (`character_count` 605 → 5827); сумма UTF-16 units = 5225 и
списанию не равна. Разница ровно 3 приходится на две строки с эмодзи (−1 и −2).

**INFERENCE.** Это прямо поддерживает F13 из ADR-0010 §10: span-map трансдьюсера обязана быть
монотонной **в code points**, а не в UTF-16 units. Реализация, индексирующая spoken-текст через
`str[i]` или `str.length`, разъедется с `alignment` на первом же эмодзи — и разъедется молча,
потому что `charIdentity` при этом останется истинным.

**INFERENCE.** Практическое следствие для кода: индексация spoken-текста ведётся по
`[...text]` (или по итератору строки), а `Intl.Segmenter` в движке не нужен вовсе —
он дал бы **неверную** единицу. Это удачно совпадает с запретом `Intl` из Charter V8.

### U4.3 Лид-ин и хвост: таймкоды говорят «ноль», аудио говорит другое

**FACT.** По таймкодам `start[0] = 0` ровно у **28 из 28** строк; `audioDuration − end[last]`
имеет медиану 0.042 мс и весь разброс в пределах ±0.5 мс. То есть alignment по своей арифметике
покрывает возвращённое аудио целиком, без зазоров с обеих сторон.

**FACT.** По самому PCM (RMS, окно 10 мс, порог −45 dBFS) на тех же 28 файлах:
акустический лид-ин — медиана **100 мс** (40–110), акустический хвост — медиана **310 мс**
(245–392). Файл: [raw/block2-acoustic.json](raw/block2-acoustic.json).

**FACT.** Эта тишина приписана крайним символам: длительность **первого** символа — медиана
93 мс (46–128), длительность **последнего** — медиана 337 мс (278–395). У обычных букв в
середине строки длительность порядка 23–120 мс.

**INFERENCE — и это главный результат спайка.** Формулировка T7 (ADR-0003) —
«PCM обрезается до `[start первого токена, end последнего токена]`, после чего вся тишина между
чанками принадлежит движку» — **работает наполовину**:

* **хвост снимается.** Последний символ прозы — точка, а точка по правилу интервала токена
  (D10 п.6) в слово не входит. Конец последнего слова оказывается на ~314 мс раньше конца файла,
  и обрезка действительно срезает провайдерский хвост.
* **лид-ин не снимается.** `start[0] = 0` **всегда**, поэтому «start первого токена» — это
  нулевой сэмпл, и обрезка слева удаляет ровно ноль. Медианные 93 мс тишины остаются **внутри**
  интервала первого слова чанка.

**INFERENCE.** Цена этого — два разных дефекта, и оба выше бюджета AC5 (p95 ≤ 80 мс):

1. **привязка первого слова каждого чанка уезжает вперёд на 46–128 мс.** Субтитр появляется
   раньше звука. Остальные слова чанка не затронуты — у них старты честные.
2. **`[pause: 400ms]` даёт не 400 мс.** К заказанной паузе добавляется несрезанный лид-ин
   следующего чанка. На ролике из ~8 чанков это ~0.7 с незаказанной тишины, распределённой по
   границам абзацев. Именно это T7 и был написан предотвратить.

**INFERENCE, кандидат в решение (за владельцем).** T7 должен обрезать голову **по акустике**, а
не по таймкодам: детектор начала энергии на импорте дубля, результат — в `leadInSamples`.
Константу вместо детектора брать нельзя: разброс 40–110 мс шире, чем весь бюджет AC5.
Хвост при этом можно продолжать снимать правилом D10 п.6 — но см. следующий пункт.

**FACT.** Если чанк кончается **не пунктуацией**, хвост достаётся последней **букве**, и правило
D10 п.6 его не снимает: на строке `The pilot called out one last time` (без точки) правило
срезало **0.4 мс** вместо 313.9 мс, и **278 мс тишины осталось внутри интервала последнего
слова**. Контрольная строка с точкой на том же материале — 313.9 мс срезано, 11 мс осталось.
Файл: [raw/block2b-nopunct.json](raw/block2b-nopunct.json).

**INFERENCE.** В нашем формате чанк = абзац, а абзац прозы кончается точкой, поэтому штатно
этот случай не наступает. Но он наступает при делении абзаца по `[pause:]`, если автор поставил
паузу не на границе предложения, и при чанке, кончающемся на `[say:]`. Дешёвая защита: ассерт на
импорте дубля «последний символ spoken-текста — пунктуация, иначе применить акустическую обрезку
хвоста тоже». Это тот же детектор, что нужен для головы.

**FACT.** У 12 строк из 28 `end[last]` **превышает** длину PCM, максимум на **12 сэмплов**
(0.5 мс). Ассерт T7 `end[last] ≤ numSamples` в буквальном виде **упал бы** на 12 строках из 28.

**INFERENCE.** Ассерту нужен допуск. Разумная форма: `end[last] ≤ numSamples + ⌈sampleRate/1000⌉`
(один миллисекундный запас на округление), а не точное неравенство.

**Голосозависимость.** Числа 100 мс, 310 мс, 93 мс, 337 мс — характеристика темпа диктора и на
боевом голосе будут другими. Структура (тишина существует; она приписана крайним символам;
`start[0] = 0`) — `INFERENCE` — структурна и голосом определяться не должна. Перемер блока 1 на
боевом голосе стоит 1215 кредитов.

---

## U16 — что делает `apply_text_normalization: "off"` с английскими числами

**FACT.** `alignment` равен входу побайтово у всех 28 строк, включая все девять числовых классов:
`Dr. Smith`, `in 1793`, `the 3rd of May`, `21st century`, `$5`, `5%`, `03/04`, `1,000,000`, `3.14`.

**FACT.** `normalized_alignment` не равен входу **ни в одной** из 28 строк. Но у 20 строк
единственное отличие — **по одному пробелу с каждого края** (форма паддинга `1/1` у всех 28).

**FACT.** Настоящие подстановки в `normalized_alignment` есть у 8 строк, и **ни одна из них
не касается чисел**:

| вход | становится |
|---|---|
| `’` U+2019 | `'` U+0027 |
| `—` U+2014 | `--` |
| `–` U+2013 | `-` |
| `“` `”` | `"` |
| `…` U+2026 | `...` |
| NBSP U+00A0 | пробел U+0020 |
| `🚢`, `👍🏽` | удалены |

**FACT.** Все девять числовых классов доходят до `normalized_alignment` **дословно** — после
снятия обрамляющих пробелов строка равна входу.

**INFERENCE.** `apply_text_normalization: "off"` действительно **выключает нормализатор
чисел, дат, денег и сокращений**. Но он не выключает второй, отдельный слой — **канонизацию
типографики и удаление непроизносимых символов**, — и этот слой виден только в
`normalized_alignment`. Это уточняет `FACT` r1 §1.4 («нормализатор переписывает текст»):
переписывателей два, и `off` гасит только один.

**INFERENCE.** Для движка это безопасно ровно потому, что привязки строятся по `alignment`, а не
по `normalized_alignment`. `normalized_alignment` проекту **не нужен вообще** и не должен
попадать ни в дубль, ни в приёмку: его единственная роль — окно в чужой нормализатор.

**INFERENCE — прямой ответ на критерий core.md §16.** Критерий сформулирован так: «если при `off`
строка возвращается побайтово равной входу для всех шести классов — линт-запрет цифр остаётся как
есть, и Charter V5 закрывается без изменений». Проверены все шесть названных классов
(`1793`, `3rd`, `$5`, `5%`, `03/04`, `Dr.`) плюс три сверх списка. **Все девять равны входу
побайтово.** Смягчение по этому критерию возможно только в третьем случае — «`off` честно не
трогает текст **и** доля токенов под запретом велика». Доля измерена в блоке 9 и составляет
**1.20 %** при пороге ADR-0002 «> ~2 %». Второе условие **не выполнено**.

**Вывод по U16:** линт-запрет цифр **остаётся как есть**, пометка-вопрос Charter V5 закрывается
**отрицательно** — смягчения нет. Решение — за владельцем, отдельным коммитом.

**Оговорка, которую нельзя опускать.** Доля 1.20 % посчитана на **единственном** английском
драфте репозитория (166 токенов), и этот драфт написан **уже под линт**: автор трижды применил
`[say:]`, то есть выборка систематически смещена в сторону «запрет соблюдаем». Число 1.20 % —
нижняя оценка. Верхняя граница на том же тексте (если считать сокращением каждый токен с точкой
на конце) — 6.63 %. Честный вывод: доля **скорее всего** ниже порога, но одного фикстурного
абзаца для этого утверждения мало. Настоящая проверка — первый живой LLM-драфт.

**Не выполнено по сравнению с core.md §16.** Прогон того же текста с `apply_text_normalization:
"auto"` для сравнения `off` vs `auto` **не делался**: задание SP-2 фиксирует параметры платных
вызовов и прямо запрещает менять их между блоками. Это записано в [decisions.md](decisions.md) п.4
и в долги. Цена досъёмки: 6 строк × ~45 симв. ≈ **270 кредитов**.

---

## U6 — безопасный предел длины чанка по качеству alignment

**FACT.** `uniqueTimestampRatio = 1.000` и `maxEqualRun = 1` на **всех** ступенях: 202, 403, 810 и
1514 символов, что дало 14.6, 28.6, 59.3 и **113.7 секунды** аудио. `charIdentity` истинно на всех
четырёх. Порог деградации `ratio < 0.9` не достигнут.

**INFERENCE.** Картина деградации из r1 §2.1 (падение до 1–3 % уникальных таймстемпов после
~250 символов / ~15 с) на `eleven_multilingual_v2` **не воспроизводится**. Это согласуется с
источником: issue #707 и #760 заведены на `eleven_v3` и `text-to-dialogue`. Решение ADR-0010
(«не брать v3, пока #707 открыт») измерением **подтверждено**, а не просто сохранено.

**INFERENCE.** Проверенный потолок — 1514 символов / 113.7 с — с большим запасом перекрывает
рабочий диапазон: абзац ролика AC1 (~150 слов на ролик целиком) до него не дотягивается на
порядок. Безопасный предел деления абзаца (D10 п.3) **упирается не в качество alignment**.
Значит `maxChunkChars` в профиле остаётся, но его значение выбирается из других соображений —
просодия, стоимость перегенерации, гранулярность AC3 — а не из здоровья таймкодов.

**FACT.** На здоровом материале `uniqueTimestampRatio` равен **ровно 1.000**, а `maxEqualRun`
равен **1** — у 26 строк из 28. Обе строки с `ratio < 1` — строки с **эмодзи**, и причина у них
одна: непроизносимые code points получают интервал **нулевой длины** и делят старт с соседом.
`🚢` даёт `ratio` 0.976 и `maxEqualRun` 2 (нулевой пробел после эмодзи); `👍🏽` даёт 0.956 и 3
(нулевые модификатор тона и пробел). Ни одна строка без эмодзи ниже 1.000 не опустилась.

**INFERENCE.** Порог приёмки можно держать **высоким**: рабочая точка — 1.000, а порог 0.9 из
задания лежит от неё далеко и сработает только на настоящей патологии, то есть ложных
срабатываний не даст. `maxEqualRun` — тот же вывод, но с оговоркой: **агрессивный порог по
`maxEqualRun` спотыкается об эмодзи**. Порог-кандидат ADR-0010 (8) безопасен; порог 2 сломал бы
приёмку на любом абзаце с эмодзи. Эмодзи в прозе линтом ADR-0002 §3 **не запрещены**, так что
случай достижим.

**INFERENCE.** Отдельное следствие для стадии `bind`: непроизносимый символ приходит с интервалом
нулевой длины, а не отсутствует. Значит токен, состоящий только из таких символов, обязан
получать `status: 'absent'` (ADR-0010 §5), а не интервал `[t, t]`, — иначе субтитр получит слово
нулевой длительности и это пройдёт мимо всех проверок.

**UNKNOWN.** Не проверено, что происходит **выше** 1514 символов — в частности, вблизи
2674 символов / 60 с, где issue #707 фиксировал залипание у v3. Досъёмка одной ступени 2700
символов стоит **2700 кредитов** и в бюджет спайка не влезала.

**UNKNOWN.** Не проверено поведение при **многократных** длинных запросах подряд и под нагрузкой
аккаунта — деградация в issue #760 описывалась как зависящая от суммарной длительности сессии,
а не только от одного запроса.

---

## D10 п.6 — куда падает межпредложенческая пауза

**FACT.** На одной фразе с пятью разделителями пауза между `stop` и `then` целиком лежит на
**знаке и пробелах**; ни буква перед разделителем, ни буква после не удлиняются:

| разделитель | пауза всего | на знаке | на пробелах | доля на знаке |
|---|---|---|---|---|
| `, then` | 453 мс | 174 | 279 | 0.384 |
| `; then` | 476 мс | 151 | 325 | 0.317 |
| `. Then` | 499 мс | 174 | 325 | 0.349 |
| ` — then` | 557 мс | 58 | 174 + 325 | 0.104 |
| `… then` | 673 мс | 232 | 441 | 0.345 |

**FACT.** Длительность буквы **до** разделителя по пяти вариантам: 82–117 мс, разброс **35 мс**.
Длительность буквы **после**: 23–58 мс, разброс **35 мс**. Обе величины остаются в обычном
диапазоне букв и от знака практически не зависят.

**INFERENCE.** Правило ADR-0010 §6 — «интервал = [start первого небелого символа, end последнего
небелого символа]; пробелы и пунктуация в интервал слова не входят» — **верно и достаточно**.
Оно исключает ровно те две категории символов, на которые провайдер и кладёт паузу.
Формулировка «куда падает пауза — на точку, на пробел или распределяется» получает ответ
**«на точку и на пробел, в переменной пропорции, и НЕ распределяется по соседним словам»** —
а переменность пропорции для правила безразлична, потому что обе категории исключены.

**INFERENCE.** Ранжирование пауз по знакам даёт автору управляющий рычаг без единого нового
маркера: `,` 453 → `;` 476 → `.` 499 → `—` 557 → `…` 673 мс. Разница между запятой и эллипсисом —
**220 мс**, то есть сопоставима с `defaultParagraphGapSamples` (180 мс, ADR-0003 T8). Это стоит
записать в документацию формата: пунктуация — уже работающий инструмент тайминга.

**Голосозависимость.** Абсолютные значения (453–673 мс) — характеристика диктора. Структурный
вывод (пауза не попадает в слово) — `INFERENCE` — голосом определяться не должен.

---

## U5 — слышимость отказа от `request_ids`

**FACT.** `previous_text` / `next_text` **влияют на звук**: PCM отдельного предложения не совпал
с версией без контекста **ни разу** из трёх, а длительность отличается до **139.3 мс (3.06 %)**.

**FACT.** Суммарная длительность склейки против одного запроса: с контекстом **−232 мс**, без
контекста **−464 мс** (эталон 15.093 с).

**INFERENCE.** Контекст **восстанавливает примерно половину** того, что теряется при разбиении:
из 464 мс расхождения с одиночным запросом 232 мс возвращаются просто передачей соседних
предложений. Это объективное свидетельство, что `previous_text`/`next_text` — не декоративный
параметр, и что решение ADR-0010 §4 (стичинг только текстом) имеет измеримое основание.

**FACT.** `previous_text` и `next_text` **не тарифицируются**: 264 символа контекста не попали
в списание (5222 = сумма code points только полей `text`).

**INFERENCE.** Стичинг текстом **бесплатен**. Довод «контекст стоит денег» из рассмотрения
исключается целиком; остаётся только довод про ключ кэша (ADR-0006), и он не про деньги.

**UNKNOWN — и остаётся UNKNOWN по построению.** Слышна ли разница на шве — вопрос к ушам, и
спайк его не решает. Файлы готовы:

* `out/b5-a.wav` — эталон, три предложения одним запросом;
* `out/b5-b.wav` — три запроса с `previous_text`/`next_text`, склеены встык, швы на **4.690 с** и **10.263 с**;
* `out/b5-c.wav` — три запроса без контекста, склеены встык, швы на **4.551 с** и **10.124 с**.

Слушать на отметках шва: скачок высоты или громкости, оборванная интонация конца фразы, разная
скорость соседних предложений. Если разница между `b` и `c` не слышна — вопрос о fallback-ключе
кэша (смежное предложение в `voiceKey`, ADR-0006) закрывается отрицательно и `conditionedOn`
остаётся чисто диагностическим полем.

---

## Детерминизм seed

**FACT.** Три вызова одной строки с одним `seed: 20260821` и совпадающими параметрами дали
**побайтово одинаковый PCM** (sha256 совпал 3/3, длина 117 029 сэмплов у всех трёх) и **побайтово
одинаковый `alignment`**.

**INFERENCE.** Это **не** опровергает `FACT` (r1 §2.3) «Determinism is not guaranteed»: вендор
даёт best-effort, три совпадения подряд best-effort не отменяют. Практический вывод обратный и
скучный: **на детерминизм нельзя опираться, но и лестница ретраев ADR-0010 §1 не бесполезна** —
если бы ответ был случаен, ретрай того же запроса не имел бы смысла; раз он в норме
детерминирован, ретрай проверяет именно сбой, а не подбрасывает монету.

**INFERENCE.** Для AC4 это ничего не меняет: аудио остаётся закоммиченным артефактом, а не
результатом вызова на рендере (V9). Совпадение sha256 — приятный факт, а не разрешение
ходить в сеть.

---

## C1 — словарь произношения с alias

**FACT.** Замер **не выполнен**. `POST /v1/pronunciation-dictionaries/add-from-rules` вернул
**HTTP 401**: `The API key you used is missing the permission pronunciation_dictionaries_write`.
Списано 0. Словарь не создавался, удалять было нечего.

**FACT.** Это ограничение **прав API-ключа**, а не тарифа: сообщение говорит о разрешении ключа,
а не о плане (сравни с 402 `paid_plan_required` на голосе).

**UNKNOWN.** Сохраняется ли `charIdentity` при включённом alias-правиле — **не измерено**.
Запрет alias в ADR-0010 §7a остаётся обоснованным **рассуждением** (alias меняет длину
нормализованного текста ⇒ тождество обязано нарушиться), но не измерением. Досъёмка: включить
`pronunciation_dictionaries_write` в дашборде ElevenLabs, затем `./run.sh block7.mjs` —
**76 кредитов** и два бесплатных вызова.

**INFERENCE.** Косвенное свидетельство в пользу того же вывода спайк всё-таки даёт: блок 1
показал, что `alignment` **не отражает** ничего из того, что делает нормализатор — все
подстановки видны только в `normalized_alignment`. Если словарь работает на том же слое, alias
проявится в `normalized_alignment` и **не** сломает `charIdentity`. Если он работает раньше —
сломает. Различить эти две гипотезы без замера нельзя, поэтому это `UNKNOWN`, а не `INFERENCE`.

---

## U14 — `alignerNoiseFloor`

**FACT.** Калибровка **не выполнялась** — по прямому указанию задания.

**FACT.** Материал для неё готов: `tts:mock@1` даёт PCM и alignment, истинный **по построению**
(alignment — то самое расписание, по которому синтезирован звук), 18 unit-тестов зелёные.

**UNKNOWN.** Собственная ошибка выбранного forced aligner'а на нашем материале. Порог AC5-b
до этой калибровки не формулируется: без полки он измеряет сумму «ошибка провайдера ⊕ ошибка
прибора». Оценка стоимости — в разделе «Долги».

---

## Тарификация и учёт (побочные факты, полезные движку)

**FACT.** Тарификация — по **code points** отправленной строки. Поштучного минимума на запрос
нет: первая строка в 46 символов списала ровно 46.

**FACT.** `previous_text` / `next_text` не тарифицируются (264 символа контекста — 0 списания).

**FACT.** `GET /v1/user/subscription` обновляет `character_count` **с задержкой 20–40 секунд**.
Дельта, снятая вокруг вызова, читается как 0.

**INFERENCE.** Поле `billedUnits` в provenance дубля (ADR-0010 §2) **нельзя** заполнять дельтой
подписки вокруг запроса — там будет ноль. Его надо заполнять числом code points отправленного
`spokenChunkText`, а подписку использовать только как редкую сверку на границе сборки.
Заодно это делает `billedUnits` вычислимым офлайн и проверяемым тестом.

---

## Досъёмка SP-2b: боевой голос

Спайк переснят на голосе из задания после смены тарифа. **Условие досъёмки:** тариф
`creator` (был `free`), `character_limit` **125 173**, голос
**`Michael C. Vincent — Confident, Expressive`**, `category: professional`,
`sharing.status: copied` — через API доступен. Параметры платных вызовов
**те же, что в SP-2**, отличается ровно `voice_id`; те же строки, тот же порядок, тот же
`seed: 20260821`. Снято 34 платных вызова (блок 1 — 28, блок 3 — 5, блок 4 — 1),
отправлено 4122 code points. Снимок аккаунта — `machine.json` → `snapshot_sp2b`.

### SP-2b.1 Daniel против Michael — всё, что выражено в миллисекундах

Все строки — **FACT**. Метод акустики не менялся (RMS, окно 10 мс, порог −45 dBFS), иначе
числа несравнимы; те же 28 строк блока 1 и те же 5 строк блока 3.

| величина | Daniel — Steady Broadcaster | **Michael C. Vincent** | файл |
|---|---|---|---|
| акустический лид-ин, медиана (min–max) | 100 мс (40–110) | **95 мс (10–180)** | [raw/block2-acoustic-prod.json](raw/block2-acoustic-prod.json) |
| акустический лид-ин, p95 | 110 мс | **110 мс** | [raw/block2-acoustic-prod.json](raw/block2-acoustic-prod.json) |
| акустический хвост, медиана (min–max) | 310 мс (245–392) | **296 мс (164–414)** | [raw/block2-acoustic-prod.json](raw/block2-acoustic-prod.json) |
| длительность **первого** символа, медиана (min–max) | 93 мс (46–128) | **93 мс (35–163)** | [raw/block1-block2-prod.json](raw/block1-block2-prod.json) |
| длительность **последнего** символа, медиана (min–max) | 337 мс (278–395) | **279 мс (163–372)** | [raw/block1-block2-prod.json](raw/block1-block2-prod.json) |
| пауза на `,` | 453 мс | **301 мс** | [raw/block3-pause-prod.json](raw/block3-pause-prod.json) |
| пауза на `;` | 476 мс | **302 мс** | [raw/block3-pause-prod.json](raw/block3-pause-prod.json) |
| пауза на `.` | 499 мс | **301 мс** | [raw/block3-pause-prod.json](raw/block3-pause-prod.json) |
| пауза на `—` | 557 мс | **430 мс** | [raw/block3-pause-prod.json](raw/block3-pause-prod.json) |
| пауза на `…` | 673 мс | **418 мс** | [raw/block3-pause-prod.json](raw/block3-pause-prod.json) |
| скорость, символов/с, медиана (min–max) | 14.2 (8.4–17.5) | **16.0 (10.8–19.3)** | [raw/block1-block2-prod.json](raw/block1-block2-prod.json) |

**FACT.** Те же 28 строк на Michael звучат **78.9 с** против **90.4 с** на Daniel — на 12.7 %
короче ([raw/block1-block2-prod.json](raw/block1-block2-prod.json), `vsDaniel.perStringDuration`).

**INFERENCE.** Пункт 2 из `voice-and-tier.md` §5 («константы `audioProfile` в миллисекундах
привязаны к паре (голос, модель)») получил измерение: ни одна величина из таблицы не совпала,
разброс по паузам достигает **255 мс** (`…`). Смена голоса обязана инвалидировать `audioProfile`
так же, как смена модели.

**INFERENCE.** Одно различие в форме, а не только в величине: у Michael разброс лид-ина
**шире** (10–180 против 40–110 мс) при почти той же медиане. Медиана голосом почти не меняется,
а хвосты распределения — меняются; это довод против «померить лид-ин один раз и зашить
константу» более сильный, чем был на Daniel.

### SP-2b.2 Построчная проверка таблицы `voice-and-tier.md` §4.1

Проверка машинная, по тем же raw-файлам обоих голосов:
[raw/voice-independence-check.json](raw/voice-independence-check.json). Итог по 12 строкам:
**8 подтверждено, 2 опровергнуто, 2 не перемерялись**. Оба опровержения — не про то, про что
строка писалась: ставка тарификации зависит от **тарифа** (SP-2b.7), а ранжирование пауз —
от **голоса** (SP-2b.3).

| строка §4.1 | вердикт | чем подтверждено / что разошлось |
|---|---|---|
| `alignment.characters.join('') === input` | **FACT: подтверждено** | Michael 28/28, как и Daniel 28/28 |
| массив `alignment` приходит в code points | **FACT: подтверждено** | 28/28 совпали с числом code points; три различающие строки (`café` NFD, `🚢`, `👍🏽`) дали ровно тот же ответ, что на Daniel |
| единица тарификации — code points, а не UTF-16 units | **FACT: подтверждено** | формула списания сходится по code points и не сходится по UTF-16 units (668 против 670 на блоке 1) |
| списано = сумме отправленных code points *(равенство из SP-2)* | **FACT: опровергнуто** | отправлено 4122, списано **2268**: на Creator ставка `round(codePoints × 0.55)` покалльно, на Free была ×1.00. Разбор — [SP-2b.7](#sp-2b7-тарификация-на-creator-списывается-055-code-point-за-code-point) |
| `previous_text` / `next_text` не тарифицируются | *не перемерялось* | блок 5 на боевом голосе не снимался (задание, шаг 6) |
| `normalized_alignment` переписывает типографику (`’→'`, `—→--`, `–→-`, `“”→"`, `…→...`, NBSP→пробел, эмодзи выбрасывает) | **FACT: подтверждено** | `normalized_alignment` совпал с Daniel **побайтово на 28/28** строк; переписано те же 8 строк, паддинг той же формы `1/1` |
| при `off` не переписываются числа, годы, порядковые, `$`, `%`, даты, сокращения | **FACT: подтверждено** | 9/9 числовых классов дословны и в `alignment`, и в `normalized_alignment` |
| деградации `uniqueTimestampRatio` нет до 1514 симв. / 113 с | **FACT: подтверждено и расширено** | на Michael проверено выше: **2689 симв. / 155.4 с**, ratio 1.000, `maxEqualRun` 1 |
| пауза ложится на знак и пробел, соседние буквы её не забирают | **FACT: подтверждено** | знак + пробел = вся пауза на 5/5 вариантах; буква до разделителя 81–93 мс, после — 35–59 мс, обе в обычном диапазоне букв |
| *(следствие из D10 п.6)* ранжирование пауз по знакам как рычаг автора | **FACT: опровергнуто** | порядок другой и разрешение другое — разбор в SP-2b.3 |
| тишина в начале приписана первому символу, в конце — последнему | **FACT: подтверждено** | `start[0] = 0` у 28/28; первый символ несёт медианные 93 мс, последний — 279 мс |
| одинаковый `seed` даёт побайтово одинаковый PCM | *не перемерялось* | блок 6 на боевом голосе не снимался (задание, шаг 6): 192 кредита, свойство инфраструктуры модели |

**INFERENCE.** Три строки, которые в SP-2 были помечены как `INFERENCE` «структурно, но проверено
на одном голосе» (пауза не в словах; тишина на крайних символах; `start[0] = 0`), теперь
подтверждены на втором голосе другого класса (`premade` против `professional`). Это не
доказательство для всех голосов, но это уже не единственное наблюдение.

### SP-2b.3 Пауза из пунктуации — свойство голоса, а не формата

**FACT.** Структура держится: вся межпредложенческая пауза лежит на знаке и пробелах, сумма
«знак + пробелы» равна зазору на 5/5 вариантах, соседние буквы не удлиняются
([raw/block3-pause-prod.json](raw/block3-pause-prod.json)).

**FACT.** Ранжирование — не держится. У Daniel пять знаков дают пять различимых значений с
разбросом **220 мс**; у Michael точка, запятая и точка с запятой неразличимы (**301 / 301 /
302 мс**, разброс 1 мс), а тире и эллипсис меняются местами:

| разделитель | Daniel | Michael | Δ |
|---|---|---|---|
| `, then` | 453 мс | **301 мс** | −152 |
| `; then` | 476 мс | **302 мс** | −174 |
| `. Then` | 499 мс | **301 мс** | −198 |
| ` — then` | 557 мс | **430 мс** | −127 |
| `… then` | 673 мс | **418 мс** | −255 |
| порядок по возрастанию | `,` `;` `.` `—` `…` | **`.` `,` `;` `…` `—`** | другой |
| разброс между крайними | 220 мс | **129 мс** | — |

**INFERENCE — вывод для ADR-0010 (по прямому указанию владельца).** Формулировка SP-2
«пунктуация — уже работающий инструмент тайминга: `,` 453 → `…` 673, разница 220 мс сопоставима
с `defaultParagraphGapSamples`» **не переносится на другой голос и в документацию формата в таком
виде идти не должна**. На Michael три знака из пяти дают одну и ту же паузу, то есть «поставь
точку с запятой вместо запятой, чтобы получить паузу длиннее» — совет, который на боевом голосе
не работает.

Практическое следствие для ADR-0010: **длительность паузы, полученной из пунктуации, — свойство
пары (голос, модель), а не свойство исходного формата.** Гарантированная пауза выражается только
маркером `[pause: Nms]`, который проходит через T7/T8 и задаётся движком в сэмплах. Пунктуация
остаётся средством просодии (она влияет на интонацию и на то, что пауза вообще будет), но не
средством тайминга: на неё нельзя опираться ни в расчёте длительности сегмента, ни в
рекомендациях автору. Решение — за владельцем, отдельным коммитом.

### SP-2b.4 C1 — словарь с alias: замер снова не выполнен

**FACT.** `POST /v1/pronunciation-dictionaries/add-from-rules` вернул **HTTP 401**:
`The API key you used is missing the permission pronunciation_dictionaries_write`. Списано 0,
словарь не создавался, удалять было нечего
([raw/block7-dictionary-prod.json](raw/block7-dictionary-prod.json)).

**FACT.** Диагностика, которой в SP-2 не было: у ключа нет и **чтения** —
`GET /v1/pronunciation-dictionaries` тоже возвращает 401
(`missing the permission pronunciation_dictionaries_read`). Тариф здесь ни при чём: тот же ключ
на том же тарифе успешно сделал 34 платных вызова TTS.

**UNKNOWN.** Сохраняется ли `charIdentity` при включённом alias-правиле и что при этом лежит в
`normalized_alignment` — по-прежнему **не измерено**. Долг 1 остаётся открытым; рецепт починки
тот же (право `pronunciation_dictionaries_write` в дашборде), цена та же — **76 кредитов** и два
бесплатных вызова.

**INFERENCE.** Косвенный довод из SP-2b.2 усилился: `normalized_alignment` на двух голосах
совпал **побайтово** на 28/28 строк, то есть слой канонизации от голоса не зависит вовсе.
Если словарь работает на этом слое, alias проявится только в `normalized_alignment` и
`charIdentity` не сломает. Различить это с гипотезой «словарь работает раньше» без замера
по-прежнему нельзя.

### SP-2b.5 Долг 4 — здоровье alignment выше 1514 символов

**FACT.** Один чанк **2689 code points** на боевом голосе дал **155.4 с** аудио
(17.3 симв./с) и полностью здоровый alignment:

| метрика | значение |
|---|---|
| `uniqueTimestampRatio` | **1.0000** (2689 уникальных стартов из 2689) |
| `maxEqualRun` | **1** — серий равных стартов нет ни одной |
| `charIdentity` | **ДА** |
| `normalized_alignment` (без обрамляющих пробелов) | равен входу |
| единица массива | code points (на этом тексте все три счётчика совпадают: чистый ASCII) |
| `start[0]` | 0 |

Файл: [raw/block4-health-2700-prod.json](raw/block4-health-2700-prod.json).

**INFERENCE.** Окрестность порога из issue #707 (**2674 символа / 60 с**) пройдена без следа
деградации, причём по длительности — с превышением в 2.6 раза (155 с против 60 с). Вместе с
четырьмя ступенями SP-2 это даёт непрерывный ряд 202 → 403 → 810 → 1514 → 2689 символов, на
котором `uniqueTimestampRatio` не отклонился от 1.000 ни разу. Вывод SP-2 «безопасный предел
деления абзаца упирается не в качество alignment» подтверждён на вдвое большей длине.

**UNKNOWN.** По-прежнему не проверено поведение при **многократных** длинных запросах подряд
(issue #760 описывал деградацию как функцию суммарной длительности сессии). Один длинный запрос —
не то же самое, что десять.

**Оговорка.** Абзац `block4.mjs` кончается на 1514 символах, поэтому для ступени 2689 он **дописан
семнадцатью предложениями** в том же стиле и по тому же правилу (без цифр, `%`, `$` и сокращений
с точкой) — [`corpus-prod.mjs`](../corpus-prod.mjs), см. [decisions.md](decisions.md) п. 19.

### SP-2b.6 Что это меняет для T7 (ADR-0003)

**FACT.** Картина «таймкоды говорят ноль, аудио говорит другое» на втором голосе **совпадает
полностью**. Прямой замер T7 на 28 строках обоих голосов
([raw/block2-t7-prod.json](raw/block2-t7-prod.json)):

| | Daniel | **Michael** |
|---|---|---|
| `start` первого токена (то, по чему T7 режет голову) | 0 у 28/28 | **0 у 28/28** |
| T7 срезает слева | **0 мс** | **0 мс** |
| тишины остаётся **внутри интервала первого слова** | 100 мс (p95 110, max 110) | **95 мс (p95 110, max 180)** |
| правило D10 п.6 срезает справа | 336.6 мс | **278.8 мс** |
| тишины остаётся внутри интервала последнего слова | −14 мс | **7.5 мс** (max 89) |
| `end[last] > numSamples` (ассерт T7 в буквальном виде) | 12/28, максимум 12 сэмплов | **13/28, максимум 12 сэмплов** |

*(Отрицательные и близкие к нулю остатки — следствие разрешения прибора: акустический край
меряется окнами по 10 мс.)*

**INFERENCE.** Кандидат-решение «**резать голову по акустике, а не по таймкодам**» остаётся в
силе и становится обоснованнее:

1. Дефект воспроизвёлся на голосе другого класса: медианные **95 мс** несрезанной тишины внутри
   интервала первого слова против бюджета AC5 **p95 ≤ 80 мс**. Это не особенность Daniel.
2. Замена детектора константой невозможна тем более: на Daniel разброс был 40–110 мс, на
   Michael — **10–180 мс**. Константа, подобранная по одному голосу, на другом ошибётся на
   величину, сравнимую со всем бюджетом AC5.
3. Хвост по-прежнему снимается правилом интервала токена (D10 п.6) — и по-прежнему **только
   пока чанк кончается пунктуацией**. Предусловие из SP-2 в силе; на боевом голосе оно не
   перепроверялось отдельно (замер без финальной точки — долг, 69 кредитов).
4. Допуск в ассерте нужен: `end[last] ≤ numSamples` в буквальном виде упал бы на **13 строках
   из 28** (было 12 из 28), максимум превышения тот же — **12 сэмплов** (0.5 мс). Форма допуска
   из SP-2 (`+ ⌈sampleRate/1000⌉`) числами SP-2b подтверждается.

**Спайк решения не принимает.** T7 остаётся `proposed` до решения владельца; SP-2b лишь
показывает, что вход для этого решения не зависел от выбора голоса в SP-2.

### SP-2b.7 Тарификация: на Creator списывается 0.55 code point за code point

**FACT.** Отправлено **4122 code points** (34 вызова). Провайдер списал **2268**:
`character_count` 0 → 2268, и то же число независимо даёт `GET /v1/usage/character-stats`.

**FACT.** `GET /v1/usage/character-stats` суммирует расход по произвольному временному окну, и это
позволило разложить списание по блокам бесплатно, без единого нового вызова
([raw/billing-prod.json](raw/billing-prod.json); границы окон взяты из журнала
[progress.jsonl](progress.jsonl), а не зашиты числами):

| блок | вызовов | отправлено, code points | списано | отношение |
|---|---|---|---|---|
| 1 | 28 | 1212 | **668** | 0.5512 |
| 3 | 5 | 221 | **121** | 0.5475 |
| 4 | 1 | 2689 | **1479** | 0.5500 |
| **итого** | **34** | **4122** | **2268** | **0.55022** |

**FACT.** Из проверенных формул списание воспроизводит **ровно одна**:

> `billed = Σ round(codePoints_i × 0.55)` — множитель применяется **к каждому запросу
> отдельно**, результат округляется по каждому запросу, а не по блоку.

Отсеяны все остальные: `floor` даёт 653/120/1478, `ceil` — 681/125/1479, округление от суммы
блока — 667/122/1479. Ни один вариант, кроме указанного, не совпал на всех трёх группах сразу.

**FACT.** **Единица осталась прежней — code points.** Та же формула по UTF-16 units даёт для
блока 1 **670** вместо списанных 668: две строки с эмодзи (`🚢`, `👍🏽`) различают счётчики и
здесь. То есть SP-2 был прав насчёт *единицы* и ошибся только насчёт *ставки*.

**FACT.** В SP-2 на тарифе Free та же модель, тот же эндпоинт и тот же способ учёта дали
отношение **ровно 1.00000** (5222 отправлено = 5222 списано). Разница между замерами — тариф
(`free` → `creator`) и голос (`premade` → `professional`).

**INFERENCE.** Ставка списания — свойство **тарифа**, а не текста, не модели и не голоса
(голос и модель в SP-2 и SP-2b различались по-разному, а поведение внутри каждого замера
однородно: один и тот же множитель на строках 35–48 символов и на строке 2689 символов).
Практически это значит, что оценка стоимости, вычисленная офлайн по длине текста, **верна
с точностью до множителя, который надо знать**.

**INFERENCE — прямое следствие для ADR-0010 §2.** Вывод SP-2 «`billedUnits` заполнять числом
code points отправленного `spokenChunkText`, потому что это вычислимо офлайн и проверяемо
тестом» на Creator даёт число, завышенное **почти вдвое**. Но чинится это без изменения формы
данных: в provenance дубля уже есть **`planTierAtGeneration`** (ADR-0010 §2) — поле, введённое
ровно для того, чтобы задним числом было понятно, на каком плане снят дубль. Достаточно
трактовать `billedUnits` как «единицы, отправленные провайдеру» (по-прежнему code points,
по-прежнему офлайн и проверяемо), а ставку хранить рядом с тарифом. Разделение «сколько
отправлено» и «сколько стоило» — это одно поле, а не пересмотр решения.

**UNKNOWN.** Откуда именно берётся 0.55 — в ответах API этого нет: `token_cost_factor` у модели
равен **1** в обоих снимках `machine.json`, то есть множитель приходит не оттуда. Стабилен ли он
во времени, одинаков ли для других моделей и не изменится ли при смене плана — **не измерено**.
Поэтому множитель нельзя зашивать константой в код: его место — в снимке аккаунта рядом с
`planTierAtGeneration`, с пересчётом при расхождении.

**UNKNOWN.** Поштучная сверка (сколько списано за конкретный запрос) недоступна:
`GET /v1/history` у ключа закрыт — `missing the permission speech_history_read`. Разложение по
блокам получено окнами времени, и оно совпадает с общим счётчиком (2268 = 668 + 121 + 1479),
но внутри блока 1 отдельные вызовы не различимы.

---

## Таблица: U-вопрос → ответ → файл измерения

| U | Вопрос | Ответ | Файл | Боевой голос (SP-2b) |
|---|---|---|---|---|
| **U4** | тождество `join('') === input` на английском | **ДА, 28/28**, включая все ловушки F1–F16 | [raw/block1-block2.json](raw/block1-block2.json) | **подтверждено 28/28** — [raw/block1-block2-prod.json](raw/block1-block2-prod.json) |
| **U4** | единица массива `alignment` | **code points** (не UTF-16 units, не графемы); тот же счётчик в тарификации | [raw/block1-block2.json](raw/block1-block2.json) | единица **подтверждена**; «тот же счётчик в тарификации» — **опровергнуто** (SP-2b.7) — [raw/voice-independence-check.json](raw/voice-independence-check.json) |
| **U4** | лид-ин и хвост (T7) | по таймкодам 0 мс с обеих сторон; **акустически лид-ин 100 мс (40–110), хвост 310 мс (245–392)**; T7 снимает хвост и **не снимает голову** | [raw/block2-acoustic.json](raw/block2-acoustic.json), [raw/block2b-nopunct.json](raw/block2b-nopunct.json) | картина та же: `start[0]=0` 28/28, **лид-ин 95 мс (10–180), хвост 296 мс**; T7 срезает слева 0 мс — [raw/block2-t7-prod.json](raw/block2-t7-prod.json) |
| **U5** | слышимость отказа от `request_ids` | **UNKNOWN** (за ушами владельца). Объективно: контекст меняет звук всегда, возвращает 232 мс из 464 мс расхождения, **стоит 0 кредитов** | [raw/block5-seam.json](raw/block5-seam.json), `out/b5-{a,b,c}.wav` | не переснимался (задание, шаг 6); WAV в `out/` — на Daniel |
| **U6** | безопасный предел длины чанка | **не найден до 1514 симв. / 113.7 с**: ratio 1.000 на всех ступенях. Предел упирается не в alignment | [raw/block4-health.json](raw/block4-health.json) | **расширен: не найден до 2689 симв. / 155.4 с** — [raw/block4-health-2700-prod.json](raw/block4-health-2700-prod.json) |
| **U14** | `alignerNoiseFloor` | **не измерялся** (по заданию). Материал `tts:mock@1` готов | [../mock.mjs](../mock.mjs), [../mock.test.mjs](../mock.test.mjs) | не входил в SP-2b |
| **U16** | что делает `off` с английскими числами | **не трогает их: 9/9 классов дословно** в обоих доменах. Но `off` не выключает канонизацию типографики (`’→'`, `—→--`, `…→...`, NBSP→пробел, эмодзи удаляются) — она видна только в `normalized_alignment` | [raw/block1-block2.json](raw/block1-block2.json) | **подтверждено 9/9**; `normalized_alignment` совпал с Daniel **побайтово 28/28** — [raw/voice-independence-check.json](raw/voice-independence-check.json) |
| **D10 п.6** | куда падает пауза | **на знак и пробел** (переменная пропорция), соседние слова её **не забирают**. Правило верно и достаточно | [raw/block3-pause.json](raw/block3-pause.json) | правило **подтверждено**; ранжирование пауз по знакам — **опровергнуто** (SP-2b.3) — [raw/block3-pause-prod.json](raw/block3-pause-prod.json) |
| **C1** | `charIdentity` при alias-словаре | **UNKNOWN** — HTTP 401, у ключа нет `pronunciation_dictionaries_write` | [raw/block7-dictionary.json](raw/block7-dictionary.json) | **по-прежнему UNKNOWN**: 401 и на write, и на read — [raw/block7-dictionary-prod.json](raw/block7-dictionary-prod.json) |
| **D2 п.3** | доля токенов под линт-запретом | **1.20 %** (естественная орфография) при пороге ADR-0002 «> ~2 %». Выборка — один драфт на 166 токенов, написанный уже под линт | [raw/block9-lint.json](raw/block9-lint.json) | не зависит от голоса, не переснимался |

---

## Долги

| # | Что не сделано | Почему | Цена досъёмки |
|---|---|---|---|
| 1 | Блок 7: `charIdentity` при alias-словаре (C1) | у API-ключа нет права `pronunciation_dictionaries_write` (HTTP 401). **SP-2b: право не появилось**, и у ключа нет также `pronunciation_dictionaries_read` — [raw/block7-dictionary-prod.json](raw/block7-dictionary-prod.json) | включить право в дашборде ElevenLabs, затем `./run.sh block7-prod.mjs` — **76 кредитов** + 2 бесплатных вызова |
| 2 | Калибровка `alignerNoiseFloor` (U14) | прямо исключена заданием | выбор алигнера — отдельное решение. **WhisperX** (BSD-2) — локально, 0 кредитов, но нужен GPU/CPU-прогон и pip-окружение. **`POST /v1/forced-alignment`** у ElevenLabs — вызов существует (`FACT` r1 §4.1), тарификация в нашем аккаунте не проверялась, поэтому цена — **UNKNOWN**, а не число. Материал `tts:mock@1` готов, писать нечего |
| 3 | Сравнение `off` vs `auto` на шести классах (core.md §16) | задание фиксирует параметры платных вызовов и запрещает менять их между блоками | 6 строк × ~45 симв. = **≈ 270 кредитов** |
| 4 | ~~Здоровье alignment выше 1514 символов~~ **ЗАКРЫТ SP-2b** | ~~не влезало в бюджет 7000~~ | снято: ступень **2689 симв. / 155.4 с**, `uniqueTimestampRatio` 1.000, `maxEqualRun` 1, деградации нет — [SP-2b.5](#sp-2b5-долг-4--здоровье-alignment-выше-1514-символов), [raw/block4-health-2700-prod.json](raw/block4-health-2700-prod.json) |
| 5 | ~~Все замеры на **боевом** голосе~~ **ЗАКРЫТ SP-2b (частично: блоки 1, 2, 3, 4)** | ~~голос из `.env` недоступен на Free через API (402)~~ — на тарифе Creator доступен | снято 34 вызова, 4122 code points: блоки 1 и 2 (**1212**), блок 3 (**221**), ступень 2689. Проверка §4.1 построчно: 7 подтверждено, 2 опровергнуто, 2 не перемерялись — [SP-2b.2](#sp-2b2-построчная-проверка-таблицы-voice-and-tiermd-41), [raw/voice-independence-check.json](raw/voice-independence-check.json) |
| 6 | Слуховая оценка шва (U5) | по построению за владельцем | 0 кредитов, три WAV лежат в `out/` — **на Daniel**; на боевом голосе см. долг 9 |
| 7 | Доля токенов под линтом на **живом** LLM-драфте | в репозитории один английский драфт, и он написан уже под линт | 0 кредитов, нужен первый настоящий драфт |
| 8 | Поведение при `alignment: null` | за 49 вызовов ни разу не наблюдалось | не воспроизводится по заказу; покрыто тестом в `tts:mock@1` |
| 9 | Блок 5 (шов, U5) и блок 6 (детерминизм seed) на **боевом** голосе | задание SP-2b, шаг 6: не переснимать. `previous_text`/`next_text` — свойство API, а детерминизм — свойство инфраструктуры модели | блок 5 ≈ **599**, блок 6 ≈ **192**; вместе ≈ **800 кредитов**. Отдельным решением владельца: слушать шов имеет смысл на том голосе, которым будет озвучен ролик |
| 10 | Чанк без финальной пунктуации (предусловие правила D10 п.6) на **боевом** голосе | в SP-2b снимались только те же строки, что в SP-2, — все с точкой | 2 вызова × ~35 симв. = **≈ 69 кредитов** |
| 11 | ~~Почему провайдер списал **2268** при отправленных **4122** code points~~ **ЗАКРЫТ SP-2b, 0 кредитов** | ~~не различимо правами ключа~~ — разложено по блокам через `GET /v1/usage/character-stats` по временным окнам | установлено: `round(codePoints × 0.55)` покалльно, единица — code points — [SP-2b.7](#sp-2b7-тарификация-на-creator-списывается-055-code-point-за-code-point), [raw/billing-prod.json](raw/billing-prod.json). Осталось UNKNOWN: **откуда 0.55** и стабилен ли множитель — новый долг 13 |
| 12 | Многократные длинные запросы подряд (issue #760: деградация как функция суммарной длительности сессии) | SP-2b снял **один** длинный чанк | серия из ~5 чанков по 2700 = **≈ 13 500 кредитов**; дешевле — серия по 800 симв. ≈ **4000** |
| 13 | Природа множителя 0.55: откуда берётся, стабилен ли, одинаков ли для других моделей | в ответах API его нет (`token_cost_factor` модели = 1 в обоих снимках) | 0 кредитов при следующем прогоне: сверять `usage/character-stats` с отправленным и падать, если множитель уехал |

---

## Что из этого блокирует и что разблокировано

Спайк **не принимает решений** — ниже только состояние вопросов, на которые он отвечал.

| Что ждало SP-2 | Состояние после спайка |
|---|---|
| **Charter V5, пометка-вопрос** (смягчать ли линт-запрет цифр) | Ответ получен и **отрицательный**: `off` не трогает числа, но доля токенов 1.20 % ниже порога 2 %, то есть второе условие смягчения не выполнено. Решение — за владельцем, отдельным коммитом |
| **ADR-0010 §1**, пороги приёмки | Рабочая точка измерена: `uniqueTimestampRatio` = 1.000, `maxEqualRun` = 1. Пороги 0.9 и 8 ложных срабатываний не дадут. **SP-2b:** та же рабочая точка на боевом голосе, включая чанк 2689 симв. |
| **ADR-0010 §3**, правило деления абзаца | Качество alignment предел не задаёт (до 1514 симв.). `maxChunkChars` выбирается из других соображений. **SP-2b:** предел не задаётся и до **2689 симв. / 155 с** |
| **ADR-0010 §6**, правило интервала токена | **Подтверждено измерением.** С одной оговоркой: работает, пока чанк кончается пунктуацией. **SP-2b:** подтверждено на втором голосе. Но **длительность паузы из пунктуации голосозависима** (SP-2b.3): гарантированная пауза — только `[pause:]` |
| **ADR-0010 §7a**, запрет alias-словарей | Обоснование остаётся рассуждением: замер упёрся в права ключа (долг 1). **SP-2b:** без изменений — 401 и на write, и на read |
| **ADR-0003 T7** | **Опровергнут наполовину**: хвост снимается, голова — нет. Требуется решение владельца (акустическая обрезка головы) и допуск в ассерте `end[last] ≤ numSamples`. **SP-2b:** воспроизведено на втором голосе — слева срезается 0 мс, внутри первого слова остаётся 95 мс (max 180). Кандидат «резать по акустике» в силе (SP-2b.6) |
| **ADR-0006**, fallback-ключ `voice` со смежным предложением | Ждёт ушей владельца. Объективная часть: контекст бесплатен и звук меняет. **SP-2b:** не переснимался (долг 9) |
| **ADR-0010 §2**, `billedUnits` в provenance дубля | **SP-2b уточнил:** единица прежняя (code points), но ставка зависит от тарифа — Free ×1.00, Creator ×0.55 покалльно с округлением. `billedUnits` = «отправлено», ставка живёт рядом с уже существующим `planTierAtGeneration`. Формы данных это не меняет (SP-2b.7) |
| **AC5-b**, порог | По-прежнему не формулируется: `alignerNoiseFloor` не измерен (долг 2) |
