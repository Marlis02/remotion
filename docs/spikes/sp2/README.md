# SP-2 — «Alignment на английском: тождество, здоровье, границы»

Спайк из `docs/architecture/core.md §16`. **Блокирующий**: до его результатов ADR-0003 и ADR-0010
не закрыты в частях T7, §1, §3, §6, а пометка-вопрос Charter V5 (линт-запрет цифр) висит открытой.

**Спайк только отвечает на U-вопросы.** ADR и Charter он не меняет — решения принимает владелец
отдельным коммитом.

**Код этого каталога — одноразовый**, кроме `mock.mjs` и `mock.test.mjs`: они написаны по
интерфейсу ADR-0010 §7 и переезжают в пакет провайдеров. Остаются **артефакты замеров**
в `results/`.

---

## 1. Что здесь лежит

| путь | что это | судьба |
|---|---|---|
| `run.sh` | единая точка запуска: node из nvm + `--env-file` | выбрасывается |
| `lib/api.mjs` | единственная точка обращения к ElevenLabs: параметры платного вызова, редакция секретов, ворота бюджета, журнал | выбрасывается |
| `lib/analyze.mjs` | разбор alignment: тождество, единица массива, здоровье, PCM→WAV | выбрасывается |
| `lib/progress.mjs` | журнал прогонов, дописывается после каждого вызова | выбрасывается |
| `lib/voice.mjs` | резолв голоса **по имени** (см. `results/voice-and-tier.md`) | выбрасывается |
| `corpus.mjs` | весь тестовый материал; ловушки заданы `\u`-эскейпами | выбрасывается |
| `estimate.mjs` | смета платных вызовов, ни сети, ни кредитов | выбрасывается |
| `machine.mjs`, `finalize-machine.mjs` | снимок аккаунта, модели, голоса, итог расхода | выбрасывается |
| `diag-voice.mjs` | диагностика 402 на голосе, только GET'ы | выбрасывается |
| `block1.mjs` + `analyze1.mjs` | блоки 1 и 2: тождество, единица массива, U16, лид-ин/хвост | выбрасывается |
| `acoustic.mjs` | акустический лид-ин/хвост по PCM (бесплатно) | выбрасывается |
| `block2b-nopunct.mjs` | хвост при чанке без финальной пунктуации | выбрасывается |
| `block3.mjs` + `analyze3.mjs` | куда падает пауза (D10 п.6) | выбрасывается |
| `block4.mjs` | здоровье alignment по длине чанка (U6) | выбрасывается |
| `block5.mjs` | шов без `request_ids` (U5), три WAV в `out/` | выбрасывается |
| `block6.mjs` | детерминизм seed | выбрасывается |
| `block7.mjs` | словарь с alias (C1) — упёрся в права ключа | выбрасывается |
| `lint-digits.mjs` | блок 9: доля токенов под линт-запретом | выбрасывается |
| **`mock.mjs`** | **`tts:mock@1` по интерфейсу ADR-0010 §7** | **переезжает в пакет провайдеров** |
| **`mock.test.mjs`** | **18 unit-тестов интерфейса** | **переезжают вместе с ним** |
| **`results/`** | **все измерения** | **остаётся в репозитории** |

## 2. Что читать

| файл | что внутри |
|---|---|
| [`results/summary.md`](results/summary.md) | **только числа** |
| [`results/findings.md`](results/findings.md) | по разделу на U-вопрос, каждое утверждение с FACT / INFERENCE / UNKNOWN; в конце таблица «U → ответ → файл» и список долгов |
| [`results/decisions.md`](results/decisions.md) | каждая неоднозначность задания: «неясно → выбрано → почему» |
| [`results/voice-and-tier.md`](results/voice-and-tier.md) | почему голос из `.env` не сработал и что изменится при покупке подписки |
| [`results/PROGRESS.md`](results/PROGRESS.md) | журнал вызовов и расход |
| [`results/machine.json`](results/machine.json) | аккаунт, модель, голос, итог расхода. **Ни ключа, ни боевого voice_id** |
| [`results/raw/`](results/raw/) | сырьё: ответы провайдера и посчитанные сводки |

## 3. Как запустить

```bash
cd docs/spikes/sp2

./run.sh machine.mjs          # бесплатно: tier, лимиты, модель, голос
./run.sh estimate.mjs         # бесплатно: смета всех платных вызовов
./run.sh --test mock.test.mjs # бесплатно: блок 8, 18 тестов
./run.sh lint-digits.mjs      # бесплатно: блок 9

./run.sh block1.mjs           # платно: блок 1 (28 вызовов, 1215)
./run.sh analyze1.mjs         # бесплатно: разбор блоков 1 и 2 по raw/
./run.sh acoustic.mjs         # бесплатно: акустический лид-ин/хвост
./run.sh block2b-nopunct.mjs  # платно: 2 вызова, 69
./run.sh block3.mjs && ./run.sh analyze3.mjs   # платно: 5 вызовов, 221
./run.sh block4.mjs           # платно: 4 вызова, 2929
./run.sh block5.mjs           # платно: 7 вызовов, 599
./run.sh block6.mjs           # платно: 3 вызова, 192
./run.sh block7.mjs           # платно: 1 вызов, 76 — нужно право ключа

./run.sh finalize-machine.mjs # бесплатно: итог расхода в machine.json
```

Скрипты **идемпотентны**: если `results/raw/<id>.json` уже есть, вызов пропускается и деньги
не тратятся. Перед каждым платным вызовом `assertBudget()` падает, если вызов выводит спайк
за 7000 символов.

## 4. Гигиена секретов

* Ключ и голос читаются **только** из `process.env` (`run.sh` передаёт `--env-file`);
  ни один скрипт не открывает `.env` сам.
* Любая строка, уходящая в лог или в файл, проходит через `redact()`: ключ и боевой voice_id
  заменяются на `<API_KEY>` / `<VOICE_ID>`.
* В `machine.json` записано **имя** голоса из `GET /v1/voices/{id}`, а не id.
* Публичный id premade-голоса, на котором снят спайк, записан открыто — он есть в документации
  ElevenLabs и нужен для воспроизводимости.
* `out/` целиком в `.gitignore`: там PCM, WAV и нетронутые ответы с `audio_base64`.

## 5. Итог одной строкой

Тождество `alignment` к входу держится **28/28**, массив приходит в **code points**, при `off`
провайдер **не трогает числа, даты, деньги и проценты**, деградации alignment нет **до 1514
символов / 113.7 с**, пауза падает на знак и пробел и в слова **не попадает**. Одно опровержение:
**T7 снимает хвост, но не снимает лид-ин** — по таймкодам его нет, акустически он есть и равен
медианным 100 мс, что больше всего бюджета AC5.
