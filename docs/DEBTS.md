# DEBTS — реестр долгов

**Что это.** Одно место, где живут долги реализации: то, что осознанно НЕ сделано, с адресом,
по которому это будет сделано, или с условием, при котором вопрос откроется заново. До этого
файла долги были размазаны по «известным ограничениям» отчётов `docs/impl/*/report.md` и по
трём разделам roadmap.

**Правила.** Одна строка — один долг; рассуждение НЕ копируется, вместо него ссылка на отчёт.
У каждой строки обязан быть либо адрес «Куда» (задача roadmap), либо «Условие открытия» — без
одного из двух это не долг, а `UNKNOWN`, и место ему в [roadmap §11.2](roadmap.md) («Что осталось `UNKNOWN` в самом roadmap»).
Закрывающая задача меняет статус **в той же сессии**, в которой закрывает. Новые долги идут
**только сюда**.

**Что здесь НЕ повторяется.** Долги спайков и дрейф документов остаются на своих местах и
переписыванию не подлежат: [roadmap §7.3](roadmap.md)
(долги SP-2 и SP-3 — десять и тринадцать позиций), [roadmap §9](roadmap.md) (найденный дрейф
документов) и [roadmap §11.2](roadmap.md) (`UNKNOWN` самого roadmap). Строки ниже на них ссылаются, но их не дублируют.

---

| # | Долг | Откуда (отчёт) | Куда (задача) | Условие / цена | Статус |
|---|---|---|---|---|---|
| 1 | `import/no-restricted-paths` молчит на импортах с `.js` — нужен `eslint-import-resolver-typescript`. **Измерено `M-03` (2026-08-23), нарушение 36 протокола:** зонд `media/src/audio` → `media/src/cache` со специфаером `'../cache/__probe__.js'` даёт **0 ошибок**, тот же зонд без расширения — 1 ошибку M5. То есть охранник M5 стережёт форму, которой в репозитории не пишут (`moduleResolution: NodeNext`) **Измерено `CP-01` (2026-08-26):** кросс-зонный импорт `timeline/** ↔ render-ir/**` в задаче НЕ появился — каталог `render-ir/` пуст, зона `timeline/` не импортирует из него ни строкой (греп в [`determinism.test.ts`](../packages/compile/test/determinism.test.ts) проверяет соседнее: отсутствие `@vpe/schema` и диска в `compose.ts`). Зонд M5 на `compile` поэтому не ставился: измерять было нечего. **ЗАКРЫТО `CP-04` (2026-08-27):** зонд поставлен на своих зонах и долг воспроизведён (`.js` — **0 ошибок**, без расширения — 1); поставлен `eslint-import-resolver-typescript@4.4.5` (версия пришпилена как у остальных devDeps), в `import/resolver` первым, node вторым, `project: 'tsconfig.json'`; повторный замер — **1 ошибка** на обеих формах, контроль «импорт внутри своей зоны» молчит. Установка+конфиг+замер = **~1.5 мин** из 15 отведённых; `pnpm lint` зелёный за 4.9 с. Лок изменён ровно этим пакетом (+11 −2, десять транзитивных; `pnpm` сообщил `Ignored build scripts: unrs-resolver@1.12.2` — prebuilt-бинарник подхватился, скрипт сборки не одобрялся; заглушку `allowBuilds: unrs-resolver: set this to true or false`, вписанную самим `pnpm add` в `pnpm-workspace.yaml`, разобрали измерением — `false`, с условием пересмотра прямо в файле). Охранник усилен: каждая зона проверяется ОБЕИМИ формами спецификатора, 6 тестов → 10 | [`R-01`](impl/R-01/report.md) огр. 3, измерения — [`M-03`](impl/M-03/report.md) §6 и [`CP-04`](impl/CP-04/report.md) §8 | — | `closed` |
| 2 | Runtime-guard заморозки глобалей — вторая половина **D4** | [`R-01`](impl/R-01/report.md) | `H-05` | **ЗАКРЫТ `H-05` (2026-08-28) — с областью действия, названной вслух.** Guard [`composition/freeze.js`](../packages/renderer-hyperframes/src/composition/freeze.js) встроен в `index.html` и замораживает весь список ADR-0007 §4 (`Date`, `Date.now`, три `Date.prototype.toLocale*`, `Intl` — броском на ЧТЕНИИ, `Math.random`, `Number.prototype.toLocaleString`, `String.prototype.localeCompare`, `Array.prototype.toLocaleString`, `performance.now`) БРОСКОМ с именем API и адресом клипа, а не подменой константой. **Вариант владельца «а» (безусловный бросок) ИЗМЕРЕН и отвергнут прогоном:** инжектируемый рантайм самого HyperFrames читает часы на своей инициализации (`Date.now`×8, `Intl.`×3, `Math.random`×2 в `RUNTIME_IIFE`), и безусловный guard даёт `[Browser:PAGEERROR] … Date.now …` → `window.__hf not ready after 45000ms` → рендер не стартует. Реализован вариант «б»: guard стоит на всей странице, но бросает только во ВЗВЕДЁННОМ окне, и взвод покрывает три места по условию владельца — модульный код шаблонов (построение `__VPE_TEMPLATES`), `mount(host, ctx)` и колбэки, которые шаблон кладёт в таймлайн (`guardedTimeline`). Guard входит в `bundle.hash` (измерено: `e30e511d…901d` без него → `89a65c8a…142f` с ним). Непокрытое вынесено долгом №167 | `closed` |
| 3 | `canonicalJson` рекурсивен, без лимита глубины | [`S-01`](impl/S-01/report.md) огр. 2 | — | условие: вход глубже ~1e4 уровней; цена — константа из ADR | `open` |
| 4 | Линт «никаких `as Frames` вне `brands.ts`» | [`S-01`](impl/S-01/report.md) огр. 3 | **закрыт `C-01`** — правило в `eslint.config.js`, охранник в `tests/lints/brand-casts.test.ts` | — | `closed` |
| 5 | Блок **K6** живёт в `render-profile.test.ts`, а охраняет 4 семейства | [`S-02-fix`](impl/S-02/report.md) | `H-03` — вторая половина K6, переезд в `test/k6.test.ts` | 10 мин | `open` |
| 6 | `main` → `dist/src/index.js` у `core-model` после `rootDir` с тестами; **то же у `media`** *(дополнено: `M-01`, 2026-08-23 — тесты пакета переехали в `packages/media/test/`, `rootDir` стал `.`; долг тот же, новая строка не заводится)*; **то же у `voice`** *(дополнено: `V-01`, 2026-08-24 — пакет получил тесты в `packages/voice/test/`, `rootDir` стал `.` по тому же образцу и по той же причине: при `rootDir: './src'` их не видел бы `pnpm typecheck`)* *(дополнено: `CP-01`, 2026-08-26 — **то же у `compile`**: пакет получил тесты в `packages/compile/test/`, `rootDir` стал `.` по образцу `voice`, `main`/`types`/`exports` смещены в `dist/src/`.)* | [`C-01`](impl/C-01/report.md), [`M-01`](impl/M-01/report.md), [`V-01`](impl/V-01/report.md) | `CP-01` — **проверено импортом**: тесты `compile` резолвят `@vpe/core-model`, `@vpe/media` и `@vpe/voice` через их `main` (`dist/src/index.js`) и работают; долг остаётся открытым как КОСМЕТИКА раскладки, адрес — `L-01` | 0 — работает; цена в том, что `dist/` несёт и `src/`, и `test/` | `open` |
| 7 | Правило укладки клипа при `frameStart == segmentDurationInFrames`. **ЗАКРЫТО `CP-04` (2026-08-27), решение владельца 2026-08-26, вариант (а):** клип прижимается к `[d−1, d)` длиной 1 кадр **с записью** `clip-at-segment-tail` в отчёт сборки. Отвергнуты: выбросить клип (потеря произведения) и ошибка компиляции (падение сборки на округлении в ½ кадра). Цена названа — кадр, которого автор не просил. Охранник — property-тест по всей зоне `x ∈ [(d−½)·S, L)` ([`render-ir.test.ts`](../packages/compile/test/render-ir.test.ts)) | [`C-01`](impl/C-01/report.md) `UNKNOWN` 1, закрытие — [`CP-04`](impl/CP-04/report.md) §5 | — | `closed` |
| 8 | Схлопывание пробельных внутри абзаца — исполняется, в ADR-0002 §8 не записано | [`C-02`](impl/C-02/report.md) | ~~`CP-05` — вместе с таблицей `[emph]`~~ **`DOC-06`/первая задача в `core-model/source`** *(переадресовано измерением: `CP-05`, 2026-08-27)* — схлопывание пробельных исполняет ЛЕКСЕР (`C-02`), а правка нормативной таблицы ADR-0002 §8 — работа с ДОКУМЕНТОМ; в стадии звука нет ни лексера, ни прозы: её вход — уже уложенная Timeline и числа манифеста. Дрейф roadmap §9: адрес `CP-05` достался этой строке от СТАРОГО значения `CP-05` | правка ADR | `open` |
| 9 | `“Stop.” [pause:]` — закрывающая кавычка перед паузой даёт ошибку границы предложения | [`C-02`](impl/C-02/report.md) §6.2 | — | условие: первый реальный текст, где упрёмся; цена — одна строка | `open` |
| 10 | Ordinal `b:img-<alias>-<n>`: что считается и где — сцена или файл | [`C-02`](impl/C-02/report.md) §6.1 | **закрыт `C-04`** — решение владельца: счёт **по alias**, область — **файл**, 1-based (обе «сценные» трактовки дают один и тот же id одному alias в двух сценах, то есть ломают A3) | — | `closed` |
| 11 | Видео-слой не измерен (долг SP-3 №4) — спайк `X-video` | [roadmap §7.3](roadmap.md) SP-3 №4 | **`X-video`** — спайк назван и описан в [roadmap §7.3](roadmap.md), SP-3 №4 (`DOC-01`, 2026-08-23); вход `F-01` | условие: первый ролик с видео-вставкой; цена — 1–2 сессии + ADR | `open` |
| 12 | Провайдер AI-арта не выбран | владелец | **закрыт `IMPL-M-02`, 2026-08-23** — решение владельца дословно: **провайдер AI-арта заранее НЕ выбирается.** `providerId` — свободная строка (решение 6, уже в норме), имя сервиса вписывается по факту, при первой настоящей AI-картинке. Движок хранит паспорт/флаг «сгенерировано ИИ» (`disclosure.aiImagery`, provenance §9b) — этого достаточно; вход `A-01` **НЕ заблокирован**: её механика пишется на тестовой записи, живой сервис ей не нужен | пометка `⛔ждёт: провайдер` снята со строки `A-01` в [roadmap §3](roadmap.md); остальные четыре упоминания того же `UNKNOWN` в roadmap правкой не покрыты — долг №51 | `closed` |
| 13 | Шрифт канала не выбран *(дополнено: `M-02`, 2026-08-23 — механика записи готова: ветка `intrinsic` для шрифта в `asset-record/1` и первая запись `fixtures/minimal/fonts/records/0000…0005.json` на **временном** DejaVu Sans Bold. Долг тот же, новая строка не заводится)* *(уточнено `TS-01`, 2026-08-27 — механика ОБЪЯВЛЕНИЯ готова и не привязывает выбор: `FontRef.family` НЕОБЯЗАТЕЛЕН, и `captionEmphasis@1` семейства **не называет** — шаблон объявляет РОЛЬ (`caption`), семейство подставляет проект. Шаблон, вписавший `DejaVu Sans`, увёз бы временный выбор в `engineFingerprint` вместе с первым гейтом. Долг тот же, новая строка не заводится, решение по-прежнему за владельцем)* *(дополнено: `H-07`, 2026-08-31 — **предъявлено кадрами, решение по-прежнему за владельцем**. Механика ingest'а исполнена на настоящем файле: демо `examples/vertical-v1` несёт ОДНУ запись `fonts/records/<sha>.json` на ВАРИАТИВНЫЙ шрифт, то есть правило v1 «ровно один font-record» выполняется без всякого смягчения — начертания живут осью `wght`, а не вторым файлом. Два кандидата прогнаны на одном тексте, отличие прогонов — РОВНО запись шрифта: **Inter** 4.001 (`opsz` 14…32, `wght` 100…900, `fsType` 0, SIL OFL 1.1, sha `29160a80…`) и **Montserrat** 9.000 (`wght` 100…900, `fsType` 0, SIL OFL 1.1, sha `0f7b311b…`). Кадры обоих — [`impl/H-07/frames/`](impl/H-07/report.md); в коммит идёт Inter (решение владельца по плану `H-07`), Montserrat живёт кадрами и роликом на диске. Статус НЕ меняется: буква решения — за владельцем, по роликам на телефоне)* | владелец | ~~до `TS-01`~~ **владелец / `H-07` — предъявлено, ждёт буквы** *(уточнено `TS-01`, 2026-08-27; дополнено `H-07`, 2026-08-31)* | решение владельца. **Что придётся сделать при ответе:** ingest настоящего файла (sha в фикстуре — плейсхолдер по образцу четырёх соседних записей, байтов в контуре нет), сверка `fsType` и лицензии выбранного шрифта, запись в `store.lock` с `kind: font` **ЗАКРЫТ `V-06` (2026-08-31) — «пока»: смена шрифта канала стоит ОДНОЙ записи.** Решение владельца по кадрам `H-07`: **Montserrat Variable**. Исполнено шагом 0 задачи: запись `examples/vertical-v1/fonts/records/0f7b311b….json` (`asset-record/1`, provenance OFL 1.1), запись Inter удалена, `store.lock` переписан на sha `0f7b311b…` и size **744936**. Данные записи СНЯТЫ С БАЙТОВ из стора, а не переписаны из отчёта: `name[1]` = «Montserrat Thin», `name[2]` = «Regular», `name[16]` = «Montserrat», `name[5]` = «Version 9.000», `fsType` = 0, ось `fvar wght` 100…900 (умолчание 100). В `intrinsic.family` записано ИЗМЕРЕННОЕ (`name[1]`), как и у Inter: в запрос рендерера едет семейство ФАЙЛА (ADR-0008). Правило v1 «ровно один font-record» выполняется без смягчения — начертания живут осью | `closed` |
| 14 | Список сокращений ADR-0002 §3 закрыт **восемью** словами; `etc.`, `vs.`, `Jr.`, `Ph.D.` проходят как проза | [`C-03`](impl/C-03/report.md) огр. 1 | — | условие: первый реальный текст, где упрёмся; цена — одна строка списка | `open` |
| 15 | Римские цифры краснеют на капсовых аббревиатурах из тех же букв (`DVD`, `MIX`, `CIVIC`) | [`C-03`](impl/C-03/report.md) огр. 2 | — | условие: первый текст, где капсовая аббревиатура нужна в прозе, а не под `[say:]`; цена — решение владельца, правило уже выбрано осознанно | `open` |
| 16 | URL без схемы (`example.org`) линт не ловит: «точка между буквами» неотличима от сокращения | [`C-03`](impl/C-03/report.md) огр. 3 | — | условие: первый URL в прозе, прошедший мимо линта; цена — новое правило и его цена в ложных срабатываниях | `open` |
| 17 | Обратное направление карты (display-текст → исходник) не реализовано: у прогона есть `displaySource`, функции `displayToSource` нет | [`C-03`](impl/C-03/report.md) огр. 4 | `CP-*` — первый потребитель субтитров (AC5) | одна функция + property-тест | `open` |
| 18 | ADR-0002 §4 и ADR-0004 §2a пишут `b:img-<alias>-<ordinalВСцене>`, а исполняется счёт **по alias и по файлу** (решение владельца `C-04`) | [`C-04`](impl/C-04/report.md) §3.6 | **закрыт `DOC-01`, 2026-08-23** — ADR-0002 §4 и ADR-0004 §2a: `b:img-<alias>-<n>`, «`<n>` — порядковый номер `[img:]` с этим alias среди всех `[img:]` файла, 1-based» | в обоих ADR добавлено предложение с причиной: «сценные» трактовки дают два **живых** якоря с одним id при законном повторе alias в двух сценах (A3). Поведение кода не менялось | `closed` |
| 19 | Бренд `AnchorId` (ADR-0001 пишет `anchor: AnchorId`, в коде был `type AnchorId = string`) | [`C-01`](impl/C-01/report.md) огр. 3 | **закрыт `C-04`** — `AnchorId`/`PublicAnchorId` в [`brands.ts`](../packages/schema/src/types/brands.ts), конструкторы валидируют схемами семейств; линт каста расширен обоими именами | — | `closed` |
| 20 | `publicAnchor()` возвращает `z.ZodString`, поэтому `AnchorTimePoint.anchor` остался `string`: сузить его до `PublicAnchorId` — значит сузить выход схемы и порвать тип-тест «`direction/1 → at` присваивается в `TimePoint`» | [`C-04`](impl/C-04/report.md) §7 огр. 1 | **закрыт `C-05` — ИНАЧЕ, ЧЕМ ПРЕДПОЛАГАЛОСЬ** (решение владельца): схема не тронута, сужение стоит на ГРАНИЦЕ МОДЕЛИ — `AnchorRef` ([`model/entities.ts`](../packages/core-model/src/model/entities.ts)) объявляет `anchor: PublicAnchorId`, переход схема → модель идёт единственным конструктором `asPublicAnchorId` ([`model/direction.ts`](../packages/core-model/src/model/direction.ts)). Правка `publicAnchor()` отвергнута по существу, а не по запрету: сужение требует `.transform()` (`ZodString` → `ZodPipe`), а `types/brands.ts` валидирует бренд ЭТОЙ ЖЕ схемой — круг. `TimePoint` остаётся типом ФОРМАТА и слоя Timeline | — | `closed` |
| 21 | `recordId` у порождённой `[img:]`-записи: ADR-0002 §4 его не называет, `direction/1` требует у каждой записи, случайный сломал бы D1/AC4 | [`C-04`](impl/C-04/report.md) §6.1 | **закрыт `C-05` — решение владельца (а)**: порождённая запись остаётся объектом МОДЕЛИ, а не записью `direction/1`. Основание — не молчание ADR, а его текст: ADR-0007 §1 определяет `recordId` как id, **выданный CLI при создании записи** и **записанный в `direction/*.yaml`**, а у порождённой записи нет ни одного из двух событий (ADR-0002 §4: «компилятором, не форматтером и не в файле»). ADR описывает другой объект; отсутствие правила здесь — граница, а не пробел. Утверждается тестом `DirectionSchema.safeParse(generated).success === false`, а не комментарием | — | `closed` |
| 22 | `ch:` в ledger не пишется: `anchors/1` требует непустой `sceneId`, а у якоря главы сцены нет | [`C-04`](impl/C-04/report.md) §6.2 | `O-01`/`L-03` — первый потребитель `ch:` через ledger | либо поле файла/сцены в схеме (бамп `anchors/2`), либо резолв `ch:` по структуре AST — как сейчас | `open` |
| 23 | «Мёртвые хранятся N ревизий» (ADR-0004 §4): `N` не задано, а удаление противоречит add-only — не удаляется ничего | [`C-04`](impl/C-04/report.md) §6.3 | **закрыт `DOC-01`, 2026-08-23** — ADR-0004 §4: «хранятся N ревизий» → «**мёртвые записи не удаляются; компакция ledger'а в v1 не определена**» | удаление противоречило бы add-only (ADR-0005 §10), а число ревизий не задавал ни один документ. Сноска-дубль — `core.md` A3 | `closed` |
| 24 | Ledger растёт при правках текста: состояние якоря (`ordinal`/`prev`/`next`) дописывается новой строкой, поэтому вставка слова добавляет по строке на каждый последующий якорь СЦЕНЫ; компакции нет | [`C-04`](impl/C-04/report.md) §7 огр. 2 | — | условие: ledger вырос настолько, что мешает ревью diff'а; цена — команда компакции + правка ADR (add-only она нарушает) | `open` |
| 25 | A3 не различает случай «две ветки выдали один id ОДНОМУ И ТОМУ ЖЕ слову в одной сцене»: личности совпадают. Признак «две записи в одной ревизии» был написан, замерен и снят — он краснел на штатной параллельной правке | [`C-04`](impl/C-04/report.md) §3.4 | — | условие: первый реальный случай (вероятность 2⁻⁸⁰ × совпадение слова и сцены); цена — различающий признак, не дающий ложных срабатываний | `open` |
| 26 | Ledger не хранит имя файла, поэтому `syncLedger` хоронит якоря только тех глав, что пришли в разбираемом документе; два файла с одинаковым `chapterId` затрут якоря друг друга | [`C-04`](impl/C-04/report.md) §7 огр. 3 | `L-03` — `vpe check` видит все файлы разом | проверка «`chapterId` уникален по проекту» или поле файла в схеме | `open` |
| 27 | Запрет `node:crypto` в `core-model` вне файла минта — расширение **D4**, в ADR-0007 §4 не записано (там `Math.random`, `Date.now` и соседи) | [`C-04`](impl/C-04/report.md) §3.5 | **закрыт `DOC-01`, 2026-08-23** — ADR-0007 §4: `node:crypto` записан **с областью** — «в модели (`core-model`) только в единственном модуле, объявленном реестром линта»; путь `core-model/src/anchors/mint.ts` — примером | дословное «добавить в общий список» сделало бы ADR строже реализации: `media` обязан считать sha256 (ADR-0005 §8), и там `node:crypto` законен. Пометка D4 в [invariants.md](invariants.md) приведена встречно | `closed` |
| 28 | Критерий `L-03` «нет **подстроки** `w:` в `direction/**` и `overrides/**`» краснеет на комментарии `fixtures/minimal/direction/01-intro.yaml`, объясняющем это же правило | [`C-04`](impl/C-04/report.md) §5, нарушение 20 | `L-03` — `vpe fmt --check` | проверять значения, а не байты: сканер `C-04` отрезает YAML-комментарии | `open` |
| 29 | Word-diff — LCS `O(n·m)` с жёстким пределом `4·10⁶` ячеек на сцену; Myers/patience (их называет ADR-0004 §4) не реализованы | [`C-04`](impl/C-04/report.md) §7 огр. 4 | — | условие: первая сцена, упёршаяся в предел; цена — замена одной функции, вызовы не меняются | `open` |
| 30 | Seed'а у порождённой `[img:]`-записи нет и быть не может: `recordId` — вход формулы ADR-0007 §1, а у неё его нет (следствие решения (а) по долгу №21) | [`C-05`](impl/C-05/report.md) §6.1 | **закрыт `DOC-01`, 2026-08-23** — ADR-0002 §4: «порождённая запись — **объект модели, а не запись `direction/1`**: `recordId` у неё нет, и seed для неё не определён (v1: её шаблон `still@1` случайности не требует)» | долг закрыт **записью границы**, а не правилом вывода: правило появится вместе с первым шаблоном на порождённой записи, которому нужна случайность | `closed` |
| 31 | ADR-0007 §1 пишет вход seed'а конкатенацией (`‖`), исполняется **канонический JSON**: склейка не инъективна (`("ab","c")` и `("a","bc")` — один поток байтов), а `purpose` и оба id — свободные строки | [`C-05`](impl/C-05/report.md) §3.2 | **закрыт `DOC-01`, 2026-08-23** — ADR-0007 §1: формула и абзац под ней — «канонический JSON массива `[seedRoot, chapterId, sceneId, recordId, purpose]` (`canonicalJson`, ADR-0005)» | причина записана одним предложением: склейка не инъективна (`("ab","c")` = `("a","bc")`), а `purpose`/`chapterId`/`sceneId` — свободные строки. Сноска-дубль — `core.md` RECOMMENDED | `closed` |
| 32 | `uint64(blake3(...))`: какие байты дайджеста и в каком порядке — ADR-0007 §1 не говорит. Исполняется **первые 8 байт, big-endian**, тип результата — `bigint` (`INFERENCE`, решение владельца `C-05`) | [`C-05`](impl/C-05/report.md) §3.3 | **закрыт `DOC-01`, 2026-08-23** — ADR-0007 §1: «`uint64` — **первые 8 байт дайджеста, big-endian**; тип результата — целое без потери точности (`bigint`)» | основание записано там же: 64 бита не помещаются в `Number.isSafeInteger`. Порядок байтов уже прибит golden-значением в тесте `C-05` | `closed` |
| 33 | Запись режиссуры на якоре ГЛАВЫ (`at: ch:main`) сцены не имеет, и в seed идёт `sceneId: null`. ADR-0007 §1 пишет формулу для узла внутри сцены и этот случай не разбирает (`INFERENCE`) | [`C-05`](impl/C-05/report.md) §6.2 | **закрыт `DOC-01`, 2026-08-23** — ADR-0007 §1: «у записи, стоящей на **якоре главы** (`at: ch:<id>`), сцены нет: `sceneId` в формуле — `null`» | противоречия в ADR не нашлось (проверено до правки): §1 писал формулу для узла внутри сцены и случай не разбирал — это пробел, а не запрет. Записано и почему не пустая строка | `closed` |
| 34 | Связь `r:<recordId>` (пространство `r:` = запись режиссуры) ADR-0004 §1 прямо не записывает — он называет только само пространство. Резолв исполняется по множеству `recordId` (`INFERENCE`, решение владельца `C-05`) | [`C-05`](impl/C-05/report.md) §6.3 | **закрыт `DOC-01`, 2026-08-23** — ADR-0004 §1: «`r:<recordId>` — ссылка на запись режиссуры; резолвится по множеству `recordId` проекта, в ledger не минтится» | `INFERENCE` из `C-05` стал нормой ADR: резолв в коде не менялся, изменилась только его опора | `closed` |
| 35 | `gridPoint` ищется **сканом** значений `params` на объекты с полем `kind` — временная мера: контракт `params` принадлежит манифесту шаблона. **СУЖЕНО `TS-01` (2026-08-27): контракт назван, скан остаётся.** У шаблонов `gridPoint` больше не «ищется» — он **невыразим**: каждый спек объявляет свои поля-точки ЯВНО, `TimePointParamSchema` есть сумма из ДВУХ вариантов (`anchor`, `mediaTime`), а `bed@1.inPoint` сужен до `mediaTime` (ADR-0001/V1: точка внутри ассета). Отказ живёт в сумме, а не в проверке — то же решение, по которому `Clip.at` объявлен `RealizableTimePoint`. **Скан `assertNoGridPoint` в `direction.ts` НЕ снят, и это измерение:** сегодня `readDirection` — единственное место, где `params` вообще проверяются, и снятие скана оставило бы `gridPoint` непроверенным до появления вызывающего. **УСЛОВИЕ ЗАКРЫТИЯ, НАЗНАЧЕННОЕ `TS-01`, НАСТУПИЛО (`CP-07`, 2026-08-27), но скан ОСТАВЛЕН по заданию.** Схема шаблона теперь стоит ПЕРЕД сканом: `gridPoint` в `still@1` отвергается `paramsSchema.safeParse` в стадии контракта, до `assertNoGridPoint` (тест). Снять скан задание запретило прямо (`core-model/model/direction.ts` — закрытая зона), и он остаётся вторым эшелоном: он видит `params` до реестра и ловит случай «шаблон объявил поле-точку, а автор подсунул `gridPoint`». Остаток долга — не `gridPoint`, а **два эшелона на одно правило** | [`C-05`](impl/C-05/report.md) §3.4 | ~~**`CP-06`**~~ **`L-03`** — снятие второго эшелона *(сужено `CP-07`)* | скан заменяется контрактом параметров шаблона; отказ (`assertRealizable`) остаётся на месте, меняется только способ ДОЙТИ до значения | `open` |
| 36 | `recordId` — `string`, а не бренд: единственный конструктор-валидатор жил бы в `@vpe/schema/types/brands.ts` рядом с остальными шестью, а пакет в `C-05` трогать было запрещено. **`TS-01` КАНДИДАТУРУ СНИМАЕТ (2026-08-27).** Долг называл «`TS-01` или `L-03`»; измерение: `templates-spec` по карте ADR-0009 имеет ровно одну стрелку (`core-model`) и `@vpe/schema` не резолвит вовсе, а задание сессии объявило пакет закрытой зоной. Трогать `brands.ts` из задачи, которой он недоступен, значило бы завести стрелку ради одного конструктора. | [`C-05`](impl/C-05/report.md) §7 огр. 1 | ~~первая задача, которой разрешено трогать `@vpe/schema` (`TS-01` или `L-03`)~~ **`L-03`** *(сужено `TS-01`, 2026-08-27)* | `asRecordId` + расширение `BRAND_NAMES` в линте; форму (`^[0-9a-f]{8}$`) проверяет схема `direction/1` уже сейчас | `open` |
| 37 | `TemplateCall` объявлен типом, но функции разбора `template: "kenburns@1"` → `{templateId, templateVersion}` нет: грамматика имени (включая префикс `local:`, Charter V3) принадлежит манифесту. **ЗАКРЫТО `TS-01` (2026-08-27):** грамматика написана и покрыта — [`parseTemplateName`/`formatTemplateName`](../packages/templates-spec/src/name.ts), ЕДИНСТВЕННАЯ регулярка имени в репозитории (`^(?:(local):)?[a-z][A-Za-z0-9]*@[1-9][0-9]*$`), таблица из девяти принятых и семнадцати отвергнутых имён (без версии, `@0`, `@01`, пробелы, второй `@`, неизвестный namespace, дефис/подчёркивание). `TemplateCall` из `core-model` потребляется КАК ЕСТЬ: `resolve` собирает `${templateId}@${templateVersion}` обратно и прогоняет той же грамматикой, поэтому и `kenburns`, и `local:kenburns` в поле `templateId` законны и разводятся ключом реестра. `core-model` и `@vpe/schema` не тронуты ни символом. | [`C-05`](impl/C-05/report.md) §7 огр. 2 | **закрыт `TS-01`** — грамматика в `templates-spec/src/name.ts` | разбор строки + тест; писать его здесь значило бы завести вторую грамматику раньше первой | `closed` |
| 38 | Греп D2 смотрит на файл формулы (`model/seed.ts`) и не видит вызывающих: позицию можно подмешать в `Scope`, который формуле подают, — измерено нарушением 1 протокола `C-05` (D1 краснеет, греп зелён). **СУЖЕН `CP-04` (2026-08-27) на один уровень, измерением.** Греп расширен на файл, где `SeedNode` СОБИРАЕТСЯ ([`render-ir/seeds.ts`](../packages/compile/src/render-ir/seeds.ts)), плюс заведена проверка полноты списка («другого продакшн-вызывающего `seedOf` в репозитории нет», перебором `packages/*/src/**`). Протокол `CP-04`, нарушение 3: подмешивание `segmentId` ВЫЗЫВАЮЩИМ кодом теперь **краснеет у грепа** — в `C-05` тот же по смыслу обход оставался зелёным. **Остаток:** обход уровнем выше (подмена `scope.sceneId` номером сегмента) грепу по-прежнему не виден; закрывает его не греп, а AC4-b — и он теперь исполним, строки **T3**/**D2** переведены в `guarded` | [`C-05`](impl/C-05/report.md) §5, сужение — [`CP-04`](impl/CP-04/report.md) §8 | остаток закрыт по существу (AC4-b); строка держится ради формулировки грепа | `open` |
| 39 | Осиротевший tmp после `kill` процесса не убирается никем: в CAS — потому что `vpe store gc` не будет написан (K10), в дереве проекта (`store.lock.tmp-<pid>-<n>`) — потому что `.gitignore` о нём не знает | [`M-01`](impl/M-01/report.md) §7 | `L-02` — `vpe store verify` (единственная команда, которая и так обходит стор) | уборка по маске имени + строка в `.gitignore`; сегодня цена нулевая: обрыв на пути ошибки убирает tmp сам, сирота остаётся только после `kill`  **Пометка `L-02` (2026-08-30): адресат пришёл и долг НЕ закрыл, причина механическая.** `vpe store verify` стор НЕ ОБХОДИТ: он спрашивает `has`/`read` по списку `store.lock`, то есть каталоги шардов не перечисляет вовсе и осиротевший `<sha>.tmp-<pid>-<n>` не видит физически. Ожидание строки «единственная команда, которая и так обходит стор» не сбылось — обход по локу и обход по дереву это разные обходы, и второй пришлось бы заводить. Адрес не меняется | `open` |
| 40 | Флаг `wx` при создании tmp (отказ вместо перезаписи чужого tmp) не охраняется ничем — измерено нарушением 30: замена на `w` оставляет все 95 тестов зелёными | [`M-01`](impl/M-01/report.md) §5 | — | условие: первая параллельная запись одного sha двумя процессами; цена — тест с двумя процессами либо отказ от проверки | `open` |
| 41 | `read` не перехэширует содержимое: порча блоба на диске обнаружится только `vpe store verify`, а не при чтении | [`M-01`](impl/M-01/report.md) §3.5 | `L-02` — `vpe store verify` | перехэширование на каждом чтении прогоняло бы весь оплаченный PCM через sha256 на каждой сборке; отложено осознанно  **ЗАКРЫТ `L-02` (2026-08-30) — не отменой границы, а появлением места, где она платится.** `Store.read` по-прежнему НЕ перехэширует (граница `M-01` не тронута ни строкой), а [`vpe store verify`](../packages/cli/src/store.ts) читает каждый лежащий блоб и сверяет `sha256Of(байты)` с адресом: порча называется своим sha рядом с фактическим хэшем и даёт ненулевой код. ИЗМЕРЕНО протоколом Н1: порча ОДНОГО байта не меняет ни размера, ни ответа `has`, ни ответа `read` — её не видит ничто, кроме этой сверки. Цена названа вслух: проверка прочитывает стор целиком, и потому живёт в команде, которой её попросили, а не на пути сборки. Охранник — [`store.test.ts`](../packages/cli/test/store.test.ts), показан красным снятием перехэширования (Н3в) | `closed` |
| 42 | `lastVerifiedAt` проверяется формой, но не календарём: `2026-13-45T99:99:99Z` проходит | [`M-01`](impl/M-01/report.md) §3.2 | `L-02` — там же, где значение пишется | проверка требует `new Date`, запрещённого линтом V8/D4; цена — либо свой календарь, либо файл в реестре читателей часов  **Пометка `L-02` (2026-08-30): писатель появился, дыра осталась ТОЙ ЖЕ ширины.** `vpe store verify --write-verified` пишет момент (решение владельца, В2: умолчание — не писать, потому что `verify` есть команда чтения и не должна трогать коммитимый файл при первом же прогоне). Форма приводится регуляркой на границе — доли секунды отсекаются, свободный ISO (`+03:00`) отвергается отказом входа, а не обрезается молча. Календарь по-прежнему не проверяется НИКЕМ: `--now 2026-13-45T99:99:99Z` пройдёт. Реестр читателей часов остался пуст — момент приезжает в команду входом (`--now` → `VPE_NOW` → часы `bin/vpe.ts`) | `open` |
| 43 | Строки DoD «после каждого ролика — `vpe store push`» в `00-PROCESS.md` **нет**, хотя ADR-0005 §8a п.3 прямо требует её внести. Это вторая половина охранника **P8** | [`M-01`](impl/M-01/report.md) §4 | **закрыт `DOC-02`, 2026-08-23** — строка внесена в [`00-PROCESS.md`](../00-PROCESS.md), раздел «ЭТАП 7 — Первое видео», отдельной прозаической строкой со сноской об источнике | раздела `§8`, который называет ADR-0005 §8a п.3, в файле нет вовсе (долг №47); место выбрано по смыслу правила — ролики появляются в ЭТАПе 7 и в циклах после него, а единственный похожий на DoD список ЭТАПа 5–6 писан про **модуль**, и перенос туда молча заменил бы «ролик» на «модуль». **P8 остаётся `named`:** её пометка называет второе недостающее — долг №44 | `closed` |
| 44 | `LocalStore` принимает любой абсолютный путь: `resolveStorePath` — дверь, а не стена, и путь внутрь дерева проекта можно подать конструктору мимо резолва | [`M-01`](impl/M-01/report.md) §7 | `L-01`/`L-02` — когда появится единственный вызывающий (CLI) | либо CLI как единственный вход, либо бренд `StoreRoot` с единственным конструктором  **Пометка `L-02` (2026-08-30): закрыт У ОДНОГО вызывающего из двух.** В [`vpe store`](../packages/cli/src/store.ts) через `resolveStorePath` проходят ВСЕ три пути — `--store-dir`, `--from`, `--to`, — то есть флаг подчиняется **P8** наравне со значением из `project.yaml`, и каталог внутри дерева отвергается сообщением про `git clean -xdf` (два теста). `vpe build --store-dir` по-прежнему зовёт `path.resolve` ([`build-stages/inputs.ts`](../packages/cli/src/build-stages/inputs.ts)) — правка его поведения есть правка входа сборки и в задание `L-02` не входила. Долг открыт до сведения обоих входов к одному резолву | `open` |
| 45 | Греп-половина охранника брендов (`tests/lints/brand-casts.test.ts`) дублирует AST-правило `BRAND_SYNTAX` **вручную** и может разъезжаться с ним — что и произошло: `Promise<Sha256>` краснел в грепе и молчал в ESLint | [`M-01`](impl/M-01/report.md) §6 | — | условие открытия: следующее ложное срабатывание; цена — генерация грепа из списка селекторов либо отказ от греп-половины в пользу прогона ESLint по всему дереву | `open` |
| 46 | ADR-0005 §8 перечисляет **пять** видов блоба (`voice\|asset\|font\|snapshot\|c2pa`), а §8a того же ADR после RM1 и строка **P7** реестра говорят про `voice\|snapshot\|ai-image` — то есть про шестой. Исполняется перечень из шести | [`M-01`](impl/M-01/report.md) §4 | **закрыт `DOC-03`, 2026-08-24** — в [ADR-0005](adr/0005-project-format.md) §8 комментарий сигнатуры дополнен шестым видом (`ai-image`), под код-блоком — предложение «видов блоба шесть» со ссылкой на §8a | одно предложение в §8: перечислить `ai-image` и сослаться на §8a. Поведение кода от правки не изменилось — проверено прогоном: 57 файлов, 1039 тестов | `closed` |
| 47 | Ссылка ADR-0005 §8a п.3 на «00-PROCESS §8» висячая: нумерованных разделов в [`00-PROCESS.md`](../00-PROCESS.md) нет вовсе — только `ЭТАП 0`…`ЭТАП 7` и именованные разделы | [`DOC-02`](impl/DOC-02/report.md) | **закрыт `DOC-03`, 2026-08-24** — в [ADR-0005](adr/0005-project-format.md) §8a п. 3 адрес заменён на «`00-PROCESS.md`, ЭТАП 7» со сноской о том, что нумерованных разделов в файле нет вовсе | одно слово: `§8` → «ЭТАП 7». Требование самого пункта **выполнено** (`DOC-02`), неверен был только адрес внутри него | `closed` |
| 48 | «Заметка о лицензиях стека» не исполнена: GSAP и плагины `SplitText`/`MorphSVGPlugin` под GSAP Standard «no charge» License обязаны попасть в provenance наравне со шрифтами ([roadmap §4.4](roadmap.md), `M-02`). **АДРЕС УТОЧНЁН ИЗМЕРЕНИЕМ `TS-01` (2026-08-27).** Долг адресовал его сюда как «`declareAssets`/`declareFonts` — единственное место, где шаблон объявляет, что приносит с собой». Измерено: `declare*` объявляют **ассеты РОЛИКА** — alias каталога и роль шрифта, то есть то, у чего есть запись `asset-record/1`; у `gsap` и плагинов ни alias'а, ни записи нет и быть не может, а `templates-spec` `gsap` **не видит вовсе** (охранник **M6**: пакет объявляет ровно `@vpe/core-model` и `zod`). Provenance про КОД собирает тот, кто код объявляет. | [`M-02`](impl/M-02/report.md) §5 | ~~`TS-01` — манифест шаблона (`declareAssets`/`declareFonts`)~~ **`H-01`** *(переадресовано измерением: `TS-01`, 2026-08-27)* — пакет, который объявляет `gsap` | решение владельца (`M-02`): в объём `M-02` не входит — это записи про КОД, а не про ассеты ролика, механика та же и появится с первым шаблоном. Оговорка роадмапа: у `@hyperframes/*` нет поля `license` в `package.json`, автоматический сканер увидит `unknown`, закрывать вручную **ИЗМЕРЕНО `H-01` (2026-08-28), долг НЕ закрыт — собраны данные.** Стек установлен, и обе лицензии сняты с фактического дерева: `hyperframes@0.8.5` объявляет `"license": "Apache-2.0"` **полем `package.json`**, но **файла `LICENSE` в пакете НЕТ**; `gsap@3.15.0` в поле `license` несёт **не SPDX-идентификатор, а строку** «`Standard 'no charge' license: https://gsap.com/standard-license.`», файла тоже нет. То есть предсказание Charter §6 («сканер пометит `unknown` — закрывать вручную») подтвердилось для ОБОИХ, а не только для `@hyperframes/*`, и по другой причине, чем ожидалось: `@hyperframes/*` в дереве нет вовсе — у 0.8.5 CLI самодостаточен (`pnpm why @hyperframes/core` пусто). Плагины `SplitText`/`MorphSVGPlugin` НЕ устанавливались (они `E-03`/`E-05`), поэтому вторая половина долга пока беспредметна. Механизм provenance — не здесь. | `open` |
| 49 | Охранником «шрифт без лицензии не публикуется» roadmap называет **PG-D11**, а это в каталоге [r3 §5](research/r3-youtube-licensing.md) — **WARN про музыку вне YouTube Audio Library и риск Content ID**. Шрифт без лицензии останавливают **PG-D1** («у каждого ассета есть provenance») и **PG-D2** (`licenseId != UNKNOWN`), обе BLOCK | [`M-02`](impl/M-02/report.md) §5 | **закрыт `DOC-03`, 2026-08-24** — заменены **все шесть** упоминаний (решение владельца по вопросу 1 плана `DOC-03`): [roadmap](roadmap.md) §3 строка `M-02`, §4.4 строка `M-02` (там же длинная сноска с разбором), §4.8 подраздел `H-06`, §8.13 строка 2, §11.2 строка 14 и [spikes/rm1-closure.md](spikes/rm1-closure.md). **PG-D11 не меняется** — он остаётся WARN про музыку; правилась только ссылка | долг называл три места; грепом их оказалось **шесть**, и правка трёх оставила бы долг открытым по его же формулировке («roadmap называет PG-D11»). Поведение кода от правки не меняется | `closed` |
| 50 | Что обязана говорить СОБСТВЕННАЯ `provenance` производной записи, если лицензия читается по ссылке, не записано нигде: схема требует `provenance` у каждой записи, включая производную, а `resolveEffectiveLicense` её не читает вовсе | [`M-02`](impl/M-02/report.md) §4 | `A-01` — первый настоящий производный ассет (AI-арт по референсу); в v1 их нет вовсе (ADR-0005 §9a: «ресайза на ingest нет») | либо статус вида `inherited` с проверкой схемой, либо явное «поля производного игнорируются» в §9a. Цена — предложение в ADR плюс тест | `open` |
| 51 | После закрытия долга №12 в roadmap осталось **четыре** упоминания снятого `UNKNOWN`: §1 (строка 10), врезка перед §3 (строка 98), заголовок §4.7 `A-01` (строка 1205) и §11.2 строка 1 (строка 1646) | [`M-02`](impl/M-02/report.md) §5 | **закрыт `DOC-03`, 2026-08-24** — приведены к факту **девять** мест [roadmap](roadmap.md) (решение владельца по вопросу 2 плана `DOC-03` — вариант «все восемь», плюс девятое, найденное при внесении): §0 «Шапка», врезка перед §3 (**полная формулировка долга №12 дословно, остальные ссылаются сюда**), §6.2 рецепт `fixtures/reference`, §8 вводный абзац, §8.13 строка 1, §11.2 строка 13, заголовок подраздела `A-01` и буллит «⛔ Открыт провайдер» в нём же | адреса в столбце «Долг» — номера строк **до отката автоформата** (`d354ba0`); опознаны по содержимому, сверка — в [отчёте `DOC-03`](impl/DOC-03/report.md) §3. Названия секций в исходной формулировке долга неточны: «§1» — это §0 «Шапка», «§11.2 строка 1» — это §8.13 строка 1 | `closed` |
| 52 | `asset-record/1 → kind` — свободная строка (`identifier()`), тогда как `store-lock/1 → kind` после `M-01` — перечень **шести** видов. Опечатка `imgae`/`fnot` в записи ассета проходит валидацию, а те же байты в `store.lock` — нет | [`M-02`](impl/M-02/report.md) §5 | `A-01` — следующая задача, которой разрешено трогать `asset-record/1` (ей и нужен `kind: ai-image`) | перечень один на две схемы (`BLOB_KINDS` уже экспортируется типом `BlobKind`); в `M-02` пакет `@vpe/schema` был открыт ровно на третью ветку `intrinsic` | `open` |
| 53 | Сортировка обхода `assets/records`/`fonts/records` не охраняется ничем — измерено нарушением 19 протокола `M-02`: снятие `.sort()` оставляет все тесты зелёными, потому что `readdirSync` на этой ФС и так отдаёт имена по порядку | [`M-02`](impl/M-02/violation-transcript.txt) нарушение 19 | — | условие: первая ФС, отдающая порядок не по имени (тогда порядок проблем в отчёте станет разным на разных машинах). Цена настоящего охранника — подмена `node:fs` в тесте; сортировка при этом ОСТАВЛЕНА | `open` |
| 54 | `attributionRequired: true` в записи шрифта фикстуры — **`INFERENCE`, а не `FACT`**: лицензия Bitstream Vera требует включать уведомление в копии *Font Software*, а рендер ролика распространяет не шрифт, а изображение глифов. Выбрано консервативное значение (лишний кредит безвреден, пропущенный — риск PG-D5 BLOCK) *(`TS-01`, 2026-08-27: не тронут — `FACT` требует файла шрифта и его лицензии, а спек шаблона ни того, ни другого не видит; адрес прежний, вместе с №13)* | [`M-02`](impl/M-02/report.md) §3 | ~~`TS-01` — вместе с выбором шрифта (№13)~~ **владелец / `H-07`** — вместе с №13 | условие: первый ролик на этом шрифте; цена — одно булево поле и строка `attributionText` | `open` |
| 55 | `PcmS16.samples` — изменяемый `Int16Array`: `readonly` охраняет ссылку, а не содержимое, и конструктор массив НЕ копирует (договор «принимает владение») | [`M-03`](impl/M-03/report.md) §7 | — | условие открытия: первый случай, когда одну дорожку удерживают два владельца; цена — копия в конструкторе (~1.4 МБ на вызов для 60-секундного ролика) либо обёртка с индексатором | `open` |
| 56 | Версия и конфигурация ffmpeg возвращаются отчётом ingest (`IngestResult.ffmpeg`), но никуда не записываются, тогда как ADR-0006 §5 кладёт их в ключи `audioTrack`/`final` | [`M-03`](impl/M-03/report.md) §7 | `M-05` | ключей кэша ещё нет; цена — поле в `cacheKeyView` | `open` — **переадресован `M-05` (2026-08-25)**: ключей `audioTrack`/`final` НЕ СУЩЕСТВУЕТ и не появится — эти стадии не кэшируются (ADR-0006 Decision 1). Версия и конфигурация ffmpeg — измеренное окружение, и его место `engineFingerprint` (**K6**, ADR-0006 §3), то есть адрес долга — `H-*`, а не кэш. Заодно кандидат в правку ADR-0006 §5 (колонка «входит в ключ» у `audioProfile`) |
| 57 | Интегральная громкость ITU-R BS.1770-4 не измеряется: `LoudnessReport.lufs` имеет тип `null`; true peak заменён **консервативным снизу** пиком по сэмплам | [`M-03`](impl/M-03/report.md) §3.7, §7 | `X-02`, **после `M-04`** — адрес подтверждён владельцем, 2026-08-24 (`DOC-03`) | решение владельца (вариант «в», вопрос 3 сессии `M-03`): K-взвешивание + гейтирование + передискретизация — +0.5…1 сессии, и мерить надо на ДОСТАВКЕ, после единственного энкода `M-04` | `open` |
| 58 | Охранник **V6** на выходе `writeWavFile` структурно не может сработать — `encodeWav` всегда начинает файл с `RIFF`; его снятие ловится только грепом (измерено нарушением 11) | [`M-03`](impl/M-03/report.md) §5 п. 3 | — | условие открытия: появление в тракте писателя, способного произвести не-RIFF | `open` |
| 59 | Детектор mp3 неприменим к сырому PCM: сэмпл −1281 в s16le даёт байты `FF FA` — «валидный» заголовок кадра MPEG-1 Layer III (измерено). Значит, «ни один промежуточный артефакт не имеет mp3-контейнера» проверяется только там, где артефакт является ФАЙЛОМ | [`M-03`](impl/M-03/report.md) §7 | `M-04` / `M-05` | условие: появление шагов трубы с собственными артефактами; цена — проверка на границе каждого шага, а не внутри потока | `open` — **сужен `M-05` (2026-08-25)**: кэш стадий собственных артефактов-файлов трубы не добавляет (значение стадии `voice` — каноническая запись, байты остаются в CAS), поэтому условие открытия не наступило. Адрес прежний: первая задача, где у шага трубы появится свой файл |
| 60 | Формат тракта (PCM s16 little-endian, моно) — **`INFERENCE`**: не записан ни в одном ADR и не выражен схемой `audio-profile/1`; живёт двумя константами в `media/src/audio/pcm.ts` | [`M-03`](impl/M-03/report.md) §3.1, §8 п. 1 | **закрыт `DOC-03`, 2026-08-24** — в [ADR-0003](adr/0003-time-model.md), раздел «Разделение sampleRate», записано: формат тракта — PCM `s16` little-endian, моно, `projectSampleRate`; WAV только контейнер на границах | одно предложение в ADR-0003 «Разделение sampleRate»; поведение кода от правки не изменилось. `INFERENCE` в коде снят — теперь это норма ADR | `closed` |
| 61 | ADR-0003 T7 пишет «детерминированным **равномощным** микрофейдом», поле профиля названо `crossfadeSamples`; исполнен **линейный краевой** фейд без перекрытия (перекрытие укоротило бы дорожку на `N` сэмплов, что тот же T7 запрещает) | [`M-03`](impl/M-03/report.md) §8 п. 2 | **закрыт `DOC-03`, 2026-08-24** — в [ADR-0003](adr/0003-time-model.md) T7 «равномощный микрофейд» заменён на **линейный краевой фейд без перекрытия**, с одной фразой почему (равномощный закон осмыслен для перекрывающегося кросс-фейда, а перекрытие запрещает сам T7) | решение владельца (вопрос 4 сессии `M-03`); равномощный закон потребовал бы float-кривой либо таблицы целых коэффициентов на 72 отсчёта. **Имя поля `crossfadeSamples` объявлено историческим; ни поле, ни схема `audio-profile/1` не переименованы** — это `fixtures/` и `packages/`, которых задача не касалась | `closed` |
| 62 | ADR-0006 §5 называет в `audioProfile` «**нормализацию** громкости»; исполнен **контроль** — отчёт со списком расхождений, громкость не подкручивается | [`M-03`](impl/M-03/report.md) §8 п. 3 | **закрыт `DOC-03`, 2026-08-24** — в [ADR-0006](adr/0006-content-cache.md) §5, строка `audioProfile`: «нормализация громкости» → «параметры громкости (`targetLufs`, `truePeakDb`) как вход ключа кэша; исполняется контроль, не нормализация» | решение владельца (вопрос 8 сессии `M-03`); одно слово в таблице ADR-0006 §5 | `closed` |
| 63 | Насыщение при миксе не является отказом: `MixResult.clippedSamples` — факт в отчёте, и никто пока не решает, «так и должно быть» | [`M-03`](impl/M-03/report.md) §2 п. 1 | `CP-05` — **адрес подтверждён владельцем, 2026-08-24** (`DOC-03`): решение «падать / ослаблять / писать в отчёт сборки» принимает `CP-05` | решение владельца (вопрос 1 сессии `M-03`): решает тот, у кого есть план дорожки; цена — ветка в `compileAudio` **СУЖЕН `CP-05` (2026-08-27), решение владельца 2, вариант «а»: правило ПРИНЯТО — `clippedSamples > 0` есть ОШИБКА СБОРКИ, а не факт в отчёте.** Насыщение — дефект входа: громкость приводится ДО микса (`checkLoudness`, `targetLufs: -14`), и клип, который пришлось обрезать, означает, что этого не сделали. Правило записано СЕЙЧАС в двух местах: кандидат в ADR (отчёт `CP-05` §кандидаты) и комментарий в [`compile/src/audio/render.ts`](../packages/compile/src/audio/render.ts) — там, где его прочтут, когда придут звать `mixSaturating`. **Долг НЕ закрыт, и причина измерена:** микса в дорожке v1 НЕТ вовсе (решение владельца 1, вариант «а» — музыка едет данными), `mixSaturating` не зовётся ни разу, то есть ветки, которую правило описывает, ещё не существует. Остаток: исполнить правило кодом на первом же вызове микса | `open` |
| 64 | **Ресемплинг при муксе нигде не описан.** Тракт живёт на `projectSampleRate = 24000`, `audio-profile/1 → deliverySampleRate` = 48000 ⇒ при муксе финала происходит ВТОРОЙ ресемплинг (первый — музыка на ingest, `M-03`). Ни один ADR этого шага не называет; правило «ресемплинг ровно один раз, на ingest» (ADR-0010 §9) про музыку и формально не нарушено, но тракт теперь ресемплирует дважды. Смягчено тем, что параметры soxr стоят в командной строке явно (решение владельца, `M-04` вопрос 4b) | [`M-04`](impl/M-04/report.md) §7 п. 7, §9 п. 4 | **`X-02`** — там же живёт `UNKNOWN` самого профиля («почему 48000, а не 24000 — тогда ресемплинга нет») | правка ADR-0003/ADR-0010 либо смена `deliverySampleRate` на 24000 | `open` |
| 65 | `-movflags +faststart` в муксе не ставится: он делает второй проход по готовому файлу ради веб-раздачи, ADR о нём молчит, и заводить «второй проход» в задаче про единственность энкода молча — плохая идея (решение владельца, `M-04` вопрос 4c) | [`M-04`](impl/M-04/report.md) §7 п. 6 | ~~**`CP-05`/публикация**~~ **`L-0x`/публикация** *(переадресовано измерением: `CP-05`, 2026-08-27)* — `-movflags +faststart` это флаг МУКСА и свойство веб-доставки готового mp4; стадия звука кончается непрерывной PCM-дорожкой и контейнера не производит вовсе (ADR-0008: аудио кодируется ровно один раз при муксе финала, и это не здесь). Половина адреса, названная владельцем, — «публикация» — остаётся; половина «`CP-05`» снята | один флаг + пересъём измерений размера/времени мукса | `open` |
| 66 | **Полная строка энкодера исполняется не дословно:** x264 молча зажимает `rc_lookahead` до `keyint`. Профиль просит `rcLookahead: 40` при `gopSize: 30`, в подписи битстрима — `rc_lookahead=30` (измерено `M-04`). То есть поле профиля, заведённое находкой C5 ради воспроизводимости, на этой паре значений не исполняется, и никакая проверка этого не видит | [`M-04`](impl/M-04/report.md) §3.4, протокол | **пакет правок доков** (предложение сессии) либо `X-02` | либо согласовать значения в трёх профилях фикстуры, либо ассерт «`rc_lookahead` из подписи == `rcLookahead` профиля» — второе честнее, но упрётся в тот же зажим | **`closed` (`DOC-04`, 2026-08-25)** — записано в ADR-0008 «Сборка» как СВОЙСТВО ЭНКОДЕРА, а не дефект вызова: поле профиля задаёт намерение, фактическое значение живёт в подписи битстрима, дословный ассерт упрётся в тот же зажим, а воспроизводимость не страдает — зажим детерминирован и является функцией той же пары значений профиля. Согласование `rcLookahead` с `gopSize` в профилях фикстуры — вопрос эргономики, а не корректности, и отдельного долга не требует |
| 67 | **Одно поле профиля — три флага колориметрии.** `pixelProfile.colorSpace` разворачивается в `-colorspace`/`-color_primaries`/`-color_trc`: `FACT` (`M-04`) при одном лишь `-colorspace` файл выходит помеченным наполовину (`color_space: bt709` есть, `color_primaries` и `color_transfer` отсутствуют), а половинная разметка в публикуемом ролике — дефект. Это `INFERENCE` сессии, а не строка ADR | [`M-04`](impl/M-04/report.md) §3.5, §9 п. 3 | **пакет правок доков** — ADR-0005 §9 фиксирует чтение поля, либо в схему приходят три поля | правка ADR (в схему не лез: `@vpe/schema` закрыт) | **`closed` (`DOC-04`, 2026-08-25)** — ADR-0008 «Сборка» фиксирует чтение поля: `colorSpace` означает матрицу, первичные и передаточную характеристику РАЗОМ; трёх полей в схеме не заводится. **Адрес долга был записан неверно** (ADR-0005 §9 — про то, кто правит файл, а не про то, как энкодер читает поле); нормативный вызов энкодера живёт в ADR-0008 |
| 68 | Мукс ставит `-fflags +bitexact` **безусловно**, а не из профиля: поля, которое этим управляло бы, в схеме нет (`encoder.bitexact` живёт в `pixelProfile` и относится к энкодеру видео, которого на шаге конката нет). Обоснование: артефакт, несущий версию сборки ffmpeg, ломал бы AC4 на ровном месте | [`M-04`](impl/M-04/report.md) §7 п. 9 | **`M-05`** — там появляется `BuildRecord`, и вместе с ним место, где такое решение записывается как решение | либо поле в `audio-profile/1`, либо строка в ADR-0008 «Сборка» | `open` — **переадресован `M-05` (2026-08-25)**: `BuildRecord` в этой задаче НЕ ПОЯВИЛСЯ — roadmap `M-05` его не обещает (выход: три ключа, `cacheKeyView`, матрица, сверка), а отчёт кэша и `BuildRecord` — ADR-0006 §12, то есть `H-*`/`F-01`. Предпосылка прежнего адреса («там появляется `BuildRecord`») была неверна |
| 69 | `Take.pcm.sha256` **всегда `null`**, и тип у поля — `string \| null`, а не бренд `Sha256`. Две причины, и обе названы: CAS в `V-01` не пишется (решение владельца, вопрос 2 — дубль остаётся значением в памяти), а `@vpe/schema` из пакета `voice` не резолвится вовсе (карта ADR-0009: стрелки `voice` — только `core-model` и `media`; `packages/voice/node_modules/@vpe/` содержит два симлинка). Разрешение владельца на реэкспорт из `core-model` было **на одну строку** и потрачено на `Samples`/`asSamples` | [`V-01`](impl/V-01/report.md) §7 п. 1 | **`V-03`** — там появляется `chunkKey`/`voiceKey` и первая настоящая запись блоба `kind: voice` | **закрыт `V-03` (2026-08-24).** `recordSpeechPlan` кладёт байты дубля в CAS (`store.put(bytes, 'voice')`) и пишет ВОЗВРАЩЁННЫЙ адрес в `Take.pcm.sha256`; охранник — [`plan-record.test.ts`](../packages/voice/test/plan-record.test.ts) («`pcm.sha256` — настоящий адрес байтов в CAS, а не `null`», плюс проверка, что блоб по этому адресу действительно лежит), показан падающим внесением 18 протокола. Реэкспорт из `core-model` состоялся вторым АДРЕСНЫМ БЛОКОМ (решение владельца 2026-08-24, вопрос 7): `blake3`, `blake3Bytes`, `base32`, `canonicalJson`, `Blake3`, `isSentenceEnd`. **Вторая половина долга (тип поля) закрыта НЕ реэкспортом, а тем, что он не нужен:** `LocalStore.put` возвращает бренд `Sha256`, и он проверен — внесение 26 протокола (опечатка `kind: 'voise'`) роняет `tsc`, то есть типы `@vpe/schema` в пакете `voice` настоящие, а не `any`. Само поле `Take.pcm.sha256` остаётся `string \| null` — см. новый долг №82 | `closed` |
| 70 | `seedFrom(spokenText, seed)` (FNV-1a по UTF-16 units, вход `mulberry32`) — **частная функция `tts:mock@1`**, общей формы с `seedOf` (ADR-0007 §1) у неё нет. Решение владельца по вопросу 5: не унифицировать — это две разные величины (иерархия seed'ов узлов рендера от `seedRoot` против входа TTS-провайдера из `project.yaml → voice.seed`), и подстановка `seedOf` потребовала бы `blake3` (наружу `core-model` его не отдаёт) и правила сведения `bigint → uint32` ради величины, которая на alignment не влияет вовсе | [`V-01`](impl/V-01/report.md) §2 п. 5 | **`V-06`** — второй провайдер покажет, нужна ли общая форма | одна функция сведения + охранник на неё; сегодня цена больше пользы | `open` |
| 71 | **`node:crypto` в пакете `voice` не охраняется ничем** — измерено нарушением 22 протокола `V-01`: вставка `import { randomBytes } from 'node:crypto'` в `providers/mock.ts` оставляет `pnpm lint` и охранник **V9** ЗЕЛЁНЫМИ. Это не дефект переноса (сам перенос crypto не тянет: `seedFrom` — свой FNV-1a, 20 строк) и не дыра этой сессии: расширение D4 (`C-04`) по построению покрывает только `packages/core-model/src/**`, а `tests/lints/d4-crypto-mint.test.ts` своим контрольным случаем (`packages/voice/src/__crypto_control__.ts`) прямо утверждает, что в `voice` крипто законно | [`V-01`](impl/V-01/report.md) §7 п. 2, нарушение 22 | **`V-06`** — там у `voice` появится второй файл, которому может понадобиться источник случайности | решение: расширить реестр D4 на все пакеты либо оставить как есть и записать почему | `open` |
| 72 | Capability **`requestStitching` — скаляр**, а реальный провайдер поддерживает два режима сразу: ElevenLabs умеет и текстовый стичинг, и `previous_request_ids`, а поле заставляет назвать один. Форма перенесена из ADR-0010 §8 и спайка как есть; соседние capability того же смысла (`pcmFormats`, `timestampDomains`) — списки | [`V-01`](impl/V-01/report.md) §4.1 | **`V-06`** — кандидат в правку ADR-0010 §8 (сделать поле списком); до второго провайдера расхождение ненаблюдаемо | одно поле схемы capability + ветка `stitchingMode` | `open` |
| 73 | `takeHealth.tailResidualSamples` считается через `providerSecondsToSamples`, то есть с округлением чужого таймкода **до миллисекунды**: на выходе `tts:mock@1` расхождений ноль (его времена целые в мс по построению), на живом alignment — до `sampleRate/1000` сэмплов (12 при 24 кГц). Принято сознательно (решение владельца, вопрос 7, вариант «а»): альтернатива — новое исключение из линта T1 ради точности, которая mock'у не нужна. Слоп того же порядка ADR-0010 уже допускает на этой границе (ассерт `V-04`: `end[last] ≤ numSamples + ⌈sampleRate/1000⌉`) | [`V-01`](impl/V-01/report.md) §4.3 | ~~**`V-02`**~~ → **`V-06`** *(переадресован `V-02`, 2026-08-24)*: живого пути в `V-02` нет ни одного — приёмка написана и охраняется на `tts:mock@1`, у которого расхождение **ноль** по построению (времена целые в мс). Решать «нужна ли точность до сэмпла» не на чем, пока нет чужого alignment; существо долга не изменилось. См. [`V-02`](impl/V-02/report.md) §10 | либо исключение из T1 на один файл, либо описанная в ADR-0003 третья единица времени | `open` — **сужен `V-04` (2026-08-24)**: вторая половина долга исполнена, слоп ассерта T7 введён в код (`tailResidualSlopSamples`, `providers/time.ts`) и охраняется тестами «12 сэмплов законно, 25 — нет». Осталась ровно ПЕРВАЯ половина: точность самого перевода `providerSecondsToSamples` до миллисекунды. См. [`V-04`](impl/V-04/report.md) §3 |
| 74 | `voiceCategory: 'none'` у `tts:mock@1` — **`INFERENCE`**: ADR-0010 §2 перечисляет `premade` / `professional` / клонированный и про «никакой» молчит, а у mock'а голоса провайдера нет вовсе. Поле при этом обязательно (ADR-0010 §2, `V-05`), и оставлять его пустым нельзя: пустое место означало бы, что `V-06` может о нём забыть | [`V-01`](impl/V-01/report.md) §7 п. 6 | **`V-06`** — кандидат в правку ADR-0010 §2 (назвать значение для провайдера без голосов) | одно слово в перечне ADR | `open` |
| 75 | **Статус `absent` порождается только по КЛАССУ символов** — «токен из одних непроизносимых code point'ов» (ADR-0010 §1). ВТОРОЙ генератор того же статуса из §5 — «TTS проглотил слово», то есть токен ИСХОДНИКА без соответствия в ответе, — в `V-02` был недостижим по построению: привязки выводились ИЗ ответа провайдера, а токенов исходника (`SourceTokenRef`) у стадии не было вовсе | [`V-02`](impl/V-02/report.md) §7, §9 п. 1 | **`V-05` — ЗАКРЫТО (2026-08-25).** Тип `SourceTokenRef` заведён (`packages/voice/src/bind/types.ts`), стадия `bind` принимает `tokens` входом, и сравнение «токен исходника ↔ ответ» стало выразимым: сведение идёт двумя указателями ПО ТЕКСТУ слов, несведённый токен получает `absent` | оба генератора статуса покрыты: «проглоченное слово» — [`bind.test.ts`](../packages/voice/test/bind.test.ts) (4 пробы), «непроизносимый токен» — [`token-status.test.ts`](../packages/voice/test/token-status.test.ts); показано падающим — нарушение 1 протокола `V-05` | `closed` |
| 76 | **`interpolated` охраняется грепом, а не поведением.** Значение зарезервировано ТИПОМ (иначе первый интерполирующий биндер добавит ПОЛЕ вместо ВЕТКИ), и что его никто не порождает, проверяет [`tests/lints/adr0010-take-acceptance.test.ts`](../tests/lints/adr0010-take-acceptance.test.ts). Поведенческого охранника у него быть не может: биндер в v1 один (`provider-timestamps@1`) и он ничего не интерполирует | [`V-02`](impl/V-02/report.md) §7 | **`A-03`** — первый биндер, который интерполирует (акустический, `torchaudio` MMS); там же появляется тест на статус. *(Сужено `V-05`: адрес был «`V-05`», но биндер этой сессии — `provider-timestamps@1`, и он по построению не интерполирует; греп при этом ДОКАЗАН падающим — нарушение 2 протокола `V-05`, чего у `V-02` не было)* | тест на биндере; греп остаётся как есть | `open` |
| 77 | **`TokenBinding` разошёлся с ADR-0010 §5**: в ADR `startSample`/`endSample` объявлены `Samples` без `null`, то есть на бумаге `absent` обязан нести какое-то время. Исполняется размеченное объединение, где у `absent` времени нет вовсе, — иначе интервал `[t, t]`, прямо запрещённый §1, остаётся ВЫРАЗИМЫМ, и запрет держится на дисциплине | [`V-02`](impl/V-02/report.md) §11 п. 1 | **правка ADR-0010 §5.** *(Сужено `V-05`, решение владельца 2026-08-25: «форма **V8** первична, `[t, t]` у проглоченного слова не должен быть выразим». Расхождение подтверждено сознательным, ТЕКСТ ПРАВКИ ПОДГОТОВЛЕН дословно — [`V-05`](impl/V-05/report.md) §6; исполнителя выбирает владелец.)* Вместе с типом правится и `confidence`: `provider-timestamps@1` пишет `null` — «биндер не измеряет уверенность», не «уверенность плохая» | несколько строк ADR; кода не касается | **`closed` (`DOC-04`, 2026-08-25)** — ADR-0010 §5 переписан на размеченное объединение дословно по тексту [`V-05`](impl/V-05/report.md) §6.1: у `absent` времени нет вовсе, интервал `[t, t]` стал НЕВЫРАЗИМЫМ формой, а не запрещённым проверкой. Там же записано, что `confidence: null` означает «биндер не измеряет уверенность», а не «уверенность низкая» |
| 78 | **Правило произносимости нигде не записано.** ADR-0010 §1 и §5 говорят «непроизносимые code points», но класса не определяют; исполняется «есть хотя бы одна буква или цифра» (`\p{L}`/`\p{N}` — обычные regexp-эскейпы ECMAScript, не `Intl` и не локаль). Цена названа: таблица Unicode — свойство движка, версия Node пиньётся `engines`, но само правило остаётся `INFERENCE` сессии | [`V-02`](impl/V-02/report.md) §9 п. 3, §11 п. 2 | **правка ADR-0010 §5 или §6**; при отказе — условие открытия: первый текст, где класс символа спорен (например, `ß`, римские цифры, математические алфавиты) | одно предложение ADR | `open` |
| 79 | **Счётчик графем спайка потерян.** `identity` из `sp2/lib/analyze.mjs` считал три длины (UTF-16 units, code points, графемы) и печатал, какая совпала с длиной массива `characters`; `charIdentityReport` считает две — `Intl.Segmenter` запрещён линтом V8/D4, и roadmap §4.0 прямо велит его не переносить. Единица массива при этом ИЗМЕРЕНА (`FACT` SP-2 U4.2: code points, 28/28 на двух голосах) | [`V-02`](impl/V-02/report.md) §3 отклонение 3 | — | условие открытия: провайдер, у которого единица массива — графема (тогда понадобится счётчик без `Intl`); цена — своя таблица кластеров либо исключение из D4 | `open` |
| 80 | **Испорченный ответ уходит МИМО лестницы.** `timeAt` бросает `VoiceError('ADR-0010 §1')`, когда массив разрежен при заявленной длине; `acceptTakeWithRetries` этот бросок не ловит — ретраев не будет, ошибка уйдёт наружу с правилом БЕЗ M12. Отличать «дубль плох» (вердикт, лестница) от «ответ испорчен» (исключение) осмысленно, но правила для второго случая ADR-0010 не содержит | [`V-02`](impl/V-02/report.md) §9 п. 2, §11 п. 3 | **`V-06`** — живой провайдер, где разреженный массив вообще достижим | решение владельца: ретраить испорченный ответ или падать сразу; далее `try/catch` в лестнице + тест | `open` |
| 81 | **`takeHealth` остался в `mock.ts` делегатом** — второе имя `assessTake` в публичной поверхности пакета. Оставлено ради потребителей `V-01` (четыре вызова в тестах) и ради того, что провайдеру законно уметь оценить собственный ответ; второй реализации метрик в репозитории при этом нет ни одной | [`V-02`](impl/V-02/report.md) §9 п. 4 | **`V-05`**/**`V-06`** — когда потребители переедут на `assessTake` напрямую | удаление функции и правка вызовов; сегодня цена больше пользы | `open` |
| 82 | **`Take.pcm.sha256` типизирован `string \| null`, а не брендом `Sha256`.** Значение теперь настоящее (долг №69 закрыт), но ТИП по-прежнему допускает любую строку: подделка адреса не краснеет у компилятора. Бренд в `voice` резолвится (проверено внесением 26 протокола `V-03`), то есть цена смены типа — одна строка реэкспорта плюс правка формы `Take`; она не сделана, потому что `null` остаётся законным значением до `V-05`, где take-файл становится коммитимым артефактом со своей формой | [`V-03`](impl/V-03/report.md) §7 п. 1 | **`V-05`** — там же решается семейство `take/1` | одна строка реэкспорта `type Sha256` + сужение поля до `Sha256 \| null` | `open` |
| 83 | **Семейства `take/1` нет: take-файл пишется каноническим JSON без шапки `schema:`.** Решение владельца (`V-03`, вопрос 4): реестр содержит двенадцать семейств, ADR-0005 §1 называет `voice/takes/<chunkKey>.json` БЕЗ шапки *(сужено: `CP-01`, решение владельца 2026-08-26, вопрос 3 — вариант «а»: решение `V-03` оставлено, читатель для компиляции написан БЕЗ семейства.)* Первый посторонний читатель появился и работает — `parseTakeFile` ([`plan/take-file.ts`](../packages/voice/src/plan/take-file.ts)): структурная проверка каждого поля, ошибка называет файл и путь поля, `anchorId` проходит через `asAnchorId`. То, что дало бы семейство, исполнено — кроме `vpe fmt --check` и миграций. | [`V-03`](impl/V-03/report.md) §4 | **`G-02`** — снимки и миграции (`vpe open`, корпус `fixtures/legacy/v1/`): именно там снимок старого проекта обязан открыться текущим движком, то есть там цена впервые становится НАБЛЮДАЕМОЙ, а не декларируемой | **цена, пока долг открыт (дословно):** файл не читается штатным `readFamily`, НЕ ПОПАДАЕТ ПОД `vpe fmt --check` и не участвует в миграциях. Цена (б) названа и отвергнута владельцем: правка `@vpe/schema` (реестр 12 → 13), writer `V-05` и все тесты take | `open` |
| 84 | **Смена ЗНАЧЕНИЯ переменной окружения меняет голос, не меняя `voiceKey` — экземпляр дыры `Lexicon`** (ADR-0010 §7a, дыра 1). В ключ по решению владельца (`V-03`, вопрос 9) идёт ИМЯ переменной: ADR-0006 §9 требует, чтобы промах ключа `voice` падал сборкой, а не звал сеть, значит ключ считается и там, где секрета в окружении нет вовсе. Для `tts:mock@1` дыра **холостая** — значение переменной на звук не влияет вовсе (mock синтезирует из текста и seed'а); она открывается вместе с живым провайдером | [`V-03`](impl/V-03/report.md) §7 п. 2 | **`V-06`** — обязана ПРЕДЛОЖИТЬ механизм (дайджест значения при наличии секрета либо verify-проверка снимка аккаунта), а не унаследовать имя молча | механизм + правка ADR-0006 §2 | `open` |
| 85 | **`leadInSamples`/`tailSamples` в take-файле — ВХОД укладки, а не измерение.** `recordSpeechPlan` берёт их параметром и не считает: акустический детектор T7 приходит с `V-04`. В тестах `V-03` передаётся ноль, и это честно ровно как «ещё не измерено»; `FACT` (SP-2 U4.3) настоящий лид-ин 95–100 мс, то есть ноль в артефакте боевого дубля был бы ложью | [`V-03`](impl/V-03/report.md) §7 п. 3 | **`V-04`** — **исполнено 2026-08-24**: `recordSpeechPlan` больше не принимает края параметром вовсе (поле `edges` из `RecordSpeechInput` удалено), а измеряет их сам акустическим детектором из байтов принятого дубля. Ноль перестал быть выразимым входом: подделать край вызывающему нечем. См. [`V-04`](impl/V-04/report.md) §4 | охранник — греп (г) `tests/lints/adr0003-t7-acoustic.test.ts` («у входа укладки нет поля готовых краёв», «оба края берутся у измерения») плюс поведенческий тест «край в take-файле совпадает с независимым прогоном детектора» | `closed` |
| 86 | **`bindings[]` в take-файле пуст: стадия `bind` — `V-05`.** Форма поля в типе есть (`TokenBinding`, размеченное объединение из `V-02`), наполнение — вход `recordSpeechPlan`. Следствие: take-файл `V-03` самоописателен по тексту и звуку, но НЕ по времени, то есть AC6 на нём ещё не выполняется | [`V-03`](impl/V-03/report.md) §7 п. 4 | **`V-05` — ЗАКРЫТО (2026-08-25).** Привязки ВЫЧИСЛЯЮТСЯ стадией `bind` внутри укладки (входом приходят токены с якорями и сам биндер), а рядом с ними легли входы их пересчёта — блок `bind`. Пустой список остался законным значением ровно одного смысла: «стадия `bind` над этим дублём не работала» | take-файл самодостаточен по времени: пересчёт из одного файла + байтов CAS равен записанному ([`bind-take.test.ts`](../packages/voice/test/bind-take.test.ts)) | `closed` |
| 87 | **`maxChunkChars` действует на ключи ЧЕРЕЗ ТЕКСТ и не входит в `cacheKeyView` стадии `voice`.** Механическая мутация поля в матрице ключей (**K1**) ИЗМЕНИТ `voiceKey` (сменится раскрой ⇒ сменится `spokenChunkText`), хотя самого поля в ключе нет. Правило матрицы «поле вне `cacheKeyView` ⇒ ключ обязан не измениться» на нём в лоб не работает и обязано получить оговорку | [`V-03`](impl/V-03/report.md) §4 п. 1 | **`M-05`** — матрица мутации ключей | оговорка в матрице либо явное включение поля в `cacheKeyView` | **`closed` (`M-05`, 2026-08-25)** — введена ТРЕТЬЯ категория правила K1: `upstream`. Поле вне `cacheKeyView`, меняющее ЗНАЧЕНИЕ поля view, объявляется данными (`views/voice.json`, блок `upstream`) вместе с перечнем полей, через которые действует, и матрица доказывает это ДИФФОМ ПРОЕКЦИИ: отличаться обязано ровно названное множество ([`cache-matrix-voice.test.ts`](../packages/voice/test/cache-matrix-voice.test.ts)). Правило не ослаблено: поле вне view обязано либо не двигать ключ, либо быть объявленным. Решение владельца 2026-08-25, вопрос 4 |
| 88 | **`conditionedOn` считается по соседям ВНУТРИ СЦЕНЫ — это решение `V-03`, а не строка ADR.** ADR-0010 §4 требует записать `conditionedOn` и предупреждать при несогласованной истории сшивки, но НЕ говорит, докуда тянется контекст. Взята сцена (граница просодии); альтернатива «абзац» дала бы меньше контекста, «документ» — протянула бы его через смену темы | [`V-03`](impl/V-03/report.md) §6 | **`V-06`** вместе с живым `previous_text`/`next_text`; кандидат в правку ADR-0010 §4 | — **СУЖЕН `V-06` (2026-08-31):** контекст теперь ДЕЙСТВИТЕЛЬНО отправляется — в `previous_text`/`next_text` уходит текст чанков, названных `conditionedOn` (берётся из плана по их `chunkKey`, вторым правилом не считается). Записано в ADR-0010 §4. Открытым остаётся ровно исходное: РАДИУС (сцена) — решение `V-03`, ADR его не называет; кандидат в правку §4 ждёт слуховой оценки шва (SP-2 №6) | `open` — сужен |
| 89 | **Попадание в УЖЕ ОПЛАЧЕННЫЙ дубль прошлой сборки не реализовано.** Индекс `voiceKey → sha256` живёт в памяти одного прогона `recordSpeechPlan`; кэш `voice` на диске и правило ADR-0006 §9 («промах ключа не зовёт сеть молча, сборка падает без `--allow-tts`») — за пределами `V-03`. Оговорка входит в статус **V4** дословно | [`V-03`](impl/V-03/report.md) §7 п. 5 | **`M-05`** | — | **`closed` (`M-05`, 2026-08-25)** — межсборочный кэш `.cache/voice/` (`plan/voice-cache.ts`) плюс `voiceKey` ПОЛЕМ take-файла (решение владельца, вопрос 3): манифест стал производным, восстановимым сканом `voice/takes/*.json` (`voiceCacheFromTakes`), и `rm -rf .cache` перестал стоить денег. Холодный/прогретый прогоны: `sourceCalls` 3 → 0, артефакты побайтово равны ([`cache-warm.test.ts`](../packages/voice/test/cache-warm.test.ts)). Гейт `--allow-tts` (**K8**) остаётся у вызывающего — он в CLI, не здесь |
| 90 | **`sourceHash` определён сессией, а не ADR.** ADR-0010 §2 называет поле, но не определяет его. Принято: blake3 среза исходника от первого до последнего произносимого code point'а чанка — значит маркеры ПЕРЕД первым произносимым символом (`[img: harbour]` в начале абзаца) в срез не попадают, а `[say: d \| s]` попадает целиком вместе с display-стороной. Определение проверяемо тестом, но живёт в коде, а не в ADR | [`V-03`](impl/V-03/report.md) §8 | **`DOC-04`** (пакет правок ADR) либо `V-05` | абзац в ADR-0010 §2 | **`closed` (`DOC-04`, 2026-08-25)** — определение внесено в ADR-0010 §2 абзацем: blake3 среза исходника от первого до последнего произносимого code point'а чанка, вместе с обоими названными следствиями (маркеры перед первым произносимым символом в срез не попадают; `[say: …]` попадает целиком) |
| 91 | **`splitIndex` — один плоский счётчик на два источника деления**, и ADR-0010 §3a этого не говорит: там он «номер части при структурном делении длинного абзаца», а `C-02` уже растит его от `[pause:]`. Прочтение выбрано (иначе у абзаца были бы две независимые нумерации и два чанка с одним адресом) и охраняется тестом, но ADR остаётся неточным | [`V-03`](impl/V-03/report.md) §8 | **`DOC-04`** — кандидат в правку ADR-0010 §3a | одна фраза в ADR | `open` |
| 92 | **Правило границы предложения слабее своего обещания ровно на длину списка `ABBREVIATIONS`.** Деление длинного абзаца ВЫЗЫВАЕТ правило `C-02` (второго набора знаков в репозитории нет — греп-охранник), но `etc.`, `vs.`, `Jr.`, `Ph.D.` линт прозы пропускает, и на них раскрой сработает по ложной границе. Ключей это не ломает (границы остаются функцией байтов), лечится `[pause:]` в тексте *(`TS-01`, 2026-08-27: адрес подтверждён отказом — этот пакет прозы не видит ни строкой, его вход начинается там, где шаблон уже назван по имени)* | [`V-03`](impl/V-03/report.md) §5 п. 2; исходный долг списка — `C-03` | ~~**`CP-05`** вместе с пересмотром списка~~ **`TS-01`-независимо: первая задача в `core-model/source`** *(переадресовано измерением: `CP-05`, 2026-08-27)* — `ABBREVIATIONS` живёт в раскрое абзаца на чанки (`C-03`/`V-03`), то есть В ПРОЗЕ; стадия звука прозы не видит вовсе — её вход начинается там, где чанки уже стали дублями. Дрейф roadmap §9: адрес достался строке от СТАРОГО значения `CP-05` | — | `open` |
| 93 | **Новый файл фикстуры `voice/roles.yaml` потребовал двух правок ТЕСТОВ `@vpe/schema`** — `FIXTURE_FILES` в `families.test.ts` и `WRITABLE` в `canonical-form.test.ts`: оба списка явные, «чтобы новый файл не проехал незамеченным», и файл вне них остался бы без охранника каноничности. Правки за пределами дословного разрешения промпта `V-03` (оно называло файл семейства `audio-profile` и его тесты) и потому названы здесь поимённо | [`V-03`](impl/V-03/report.md) §2 | — | **условие открытия:** если владелец сочтёт правку чужих тестов недопустимой — откатить и завести охранник каноничности вне `@vpe/schema` | `open` |
| 94 | **`speechEdges.sides` реализовано ровно значением `both`**, любое другое падает `VoiceError` (`ADR-0003 T7`). Схема `audio-profile/1` типизирует поле как `identifier()`, то есть перечня допустимых значений у него нет, а ADR-0003 T7 после SP-2 односторонней обрезки не знает вовсе. Цена: профиль синтаксически допускает значение, которого движок не понимает, — падение наступает в рантайме укладки, а не при чтении профиля | [`V-04`](impl/V-04/report.md) §7 п. 1 | условие открытия: ADR описал односторонний режим ЛИБО схема получила перечень значений (тогда — правка `@vpe/schema`, сегодня закрытого) | `open` |
| 95 | **Диапазон дрейфа лид-ина (10–180 мс) живёт КОНСТАНТАМИ ПАКЕТА, а не профилем** (решение владельца, `V-04` вопрос 4а: `@vpe/schema` в сессии закрыт). Он привязан к той же паре (голос, модель), что и `speechEdges`, но правится пересборкой пакета, а не правкой профиля, — то есть при смене голоса две связанные величины правятся в двух разных местах | [`V-04`](impl/V-04/report.md) §5 | **`V-06`** — на первом живом голосе станет видно, нужна ли диапазону та же судьба, что и параметрам детектора | цена переноса: четвёртое поле блока `speechEdges` в `audio-profile/1` | `open` |
| 96 | **`RecordSpeechResult.edgeDrift` никем НЕ ЧИТАЕТСЯ.** Поле вычисляется и несёт значения, порог и текст, но печатать отчёт сборки сегодня некому: CLI ещё нет. WARN, которого никто не смотрит, отличается от отсутствующего только тем, что его легко начать смотреть | [`V-04`](impl/V-04/report.md) §5 | **`X-01`** (первый сквозной прогон) — там же появляется читатель отчёта | — | `open` |
| 97 | **`tailSamples` — это ВСЯ тишина в конце дорожки, а не «тишина, добавленная провайдером».** Измерено: на моке хвост чанка, кончающегося точкой, включает и паузу самого знака (у `tts:mock@1` пунктуация — тишина; у живых голосов `FACT` SP-2: длительность последнего символа — медиана 337 мс у Daniel и 279 мс у Michael). Это поведение верное (акустика мерит звук, а не расписание), но потребитель обязан знать, что величина НЕ равна дрейфу провайдера **Прочитано и исполнено `CP-01` (2026-08-26).** Интервал речи клипа — `[leadInSamples, numSamples − tailSamples)`, и на фикстуре это видно числом: при вставленных 300 мс тишины `tailSamples` = 15240…15360 сэмплов (635…640 мс), потому что хвост последнего предложения у `tts:mock@1` — тоже тишина. Ассерт `Σ речевых клипов == Σ (numSamples − leadIn − tail)` показан падающим (нарушение 2 протокола). | [`V-04`](impl/V-04/report.md) §6 | **закрыт** — потребитель, ради которого долг записан, прочёл величину правильно | — | `closed` |
| 98 | **Хвост завышается на неосмотренный остаток `numSamples mod windowSamples`** (до 239 сэмплов ≈ 10 мс при 24 кГц): цикл спайка идёт, пока `i + W ≤ n`, и последнее неполное окно не осматривается вовсе. Метод перенесён ДОСЛОВНО и правке не подлежит («иначе числа несравнимы с измеренными»), поэтому это цена метода, а не дефект переноса. Против бюджета AC5 (`p95 ≤ 80 мс`) величина мала, но она односторонняя | [`V-04`](impl/V-04/report.md) §2 п. 3 | **`A-03`** — там измеряется AC5-b, и там же станет видно, съедает ли этот остаток заметную долю бюджета | цена устранения: правка метода ⇒ несравнимость с числами SP-2 | `open` |
| 99 | **Весь-тихий дубль не отвергается никем.** Детектор его РАЗЛИЧАЕТ (`allSilent: true`), но приёмка (`V-02`) судит `alignment`, а не звук, и молчащий дубль с исправными таймкодами будет принят, уложен в CAS и записан в take-файл с краями `0`/`0`. Поле `allSilent` при этом в take-файл не идёт (оно — свойство измерения, а не дубля), то есть на диске «весь тихий» и «тишины по краям нет» неразличимы **Решено владельцем 2026-08-26 (`CP-01`, вопрос 4): ОШИБКА КОМПИЛЯЦИИ.** `leadIn + tail ≥ numSamples` ⇒ интервал речи пуст или вывернут ⇒ `CompileError` (`ADR-0003 T7`) со списком `chunkKey`; текст объясняет, что приёмка `V-02` пропустила дубль ЗАКОННО (она судит `alignment`, а не звук) и что чинить надо стадию `voice`. | [`V-04`](impl/V-04/report.md) §7 п. 2 | **закрыт решением владельца** — со стороны Timeline; вторая половина («приёмка `V-02` могла бы судить и звук») отдельным долгом не заводится: `V-02` судит то, что ей дано, и менять её объём владелец не просил | — | `closed` |

| 100 | **`anchorId` в `SourceTokenRef` и `TokenBinding` — `string`, а не бренд `AnchorId`.** Подделка адреса якоря не краснеет у компилятора; охраняется тем, что собрать её в `bind/**` запрещено грепом ([`adr0004-bind-mints-nothing`](../tests/lints/adr0004-bind-mints-nothing.test.ts)), а не типом. Причина не в удобстве: бренд живёт в `@vpe/schema`, который из `voice` не резолвится, а расширять адресный реэкспорт `core-model` заданием `V-05` запрещено **Закрыто `CP-01` (решение владельца 2026-08-26, вопрос 5).** `core-model/src/index.ts` получил третий адресный блок реэкспорта (`asAnchorId` + `AnchorId`, одна строка, форма как у `Samples`); `SourceTokenRef.anchorId` и обе ветки `TokenBinding.anchorId` объявлены `AnchorId`. ИЗМЕРЕНО: в `voice` изменились четыре аннотации в `bind/tokens.ts` и ни одной строки тестов — `AnchorBinding.id` уже был `AnchorId`, то есть бренд не добавлен, а перестал теряться. | [`V-05`](impl/V-05/report.md) §7 п. 1 | **закрыт** | фактическая цена: 1 строка реэкспорта + 3 поля типов + 4 аннотации; в тестах `voice` — ноль правок | `closed` |
| 101 | **При ПОВТОРЯЮЩЕМСЯ слове совпадение достаётся первому вхождению.** `matchInOrder` сводит слова исходника и ответа двумя указателями по тексту; если в тексте два `the`, а прозвучал один, `measured` получит ПЕРВЫЙ, а второй уйдёт `absent`. Различить их без второго источника времени нечем — таймкоды провайдера про это молчат | [`V-05`](impl/V-05/report.md) §7 п. 2 | **`A-03`** — акустический биндер меряет по звуку и различает вхождения по положению, а не по тексту | цена ошибки ограничена: оба токена остаются в списке, ни один не получает выдуманного времени | `open` |
| 102 | **Частично прозвучавший токен остаётся `measured` по объединению НАЙДЕННЫХ слов.** У токена `[say: 200 \| two hundred]` два произнесённых слова; прозвучи одно — интервал соберётся по нему, и потеря второго не отражена ни статусом, ни полем. Альтернатива («весь токен `absent`») отвергнута: она выбросила бы настоящее измеренное время | [`V-05`](impl/V-05/report.md) §7 п. 3 | **правка ADR-0010 §5** вместе с долгом №77 — либо четвёртый статус, либо поле «сколько слов из скольких» | форма данных меняется ⇒ вместе с семейством `take/1` (№83) | `open` |
| 103 | **Блок `bind` в take-файле расширяет состав ADR-0010 §2 и roadmap §4.5.** Поля `bind{binderId, tokens[], providerAlignment}` приняты решением владельца (`V-05`, вопрос 4) как единственный способ сделать критерий «привязки пересчитываются» исполнимым, но в ADR они пока не записаны | [`V-05`](impl/V-05/report.md) §6 | **правка ADR-0010 §2** — текст подготовлен дословно в отчёте `V-05` | несколько строк ADR; кода не касается | **`closed` (`DOC-04`, 2026-08-25)** — блок `bind{binderId, tokens[], providerAlignment}` внесён в состав ADR-0010 §2 дословно по тексту [`V-05`](impl/V-05/report.md) §6.2, вместе с обязательной оговоркой: `providerAlignment` — диагностический вход пересчёта, а не вторая истина о времени; единственный потребляемый выход стадии — `bindings[]` |
| 104 | **Цена самодостаточности измерена на `tts:mock@1` и является НИЖНЕЙ оценкой.** `FACT` (`V-05`): блок `bind` стоит **35.3 байта на code point** (5539 байт на 157 code points, 4 take-файла), экстраполяция на чанк предела `maxChunkChars: 600` — **20.7 КБ**. Но у mock'а времена — короткие десятичные дроби (`0.155`), а живой провайдер отдаёт восьмизначные (`0.11609977`): та же форма на нём стоит дороже | [`V-05`](impl/V-05/report.md) §5 | **`V-06`** — первый живой дубль; число переснимается и записывается рядом | если размер окажется неприемлемым, дешевле всего убрать `providerAlignment`, потеряв пересчёт | `open` |
| 105 | **`tokensOfPlan` обязана получить ТОТ ЖЕ `maxChunkChars`, с которым строился план**, а `SpeechPlan` его не хранит. Расхождение ловится сверкой текста каждой части с `spokenChunkText` чанка (жёсткий отказ), то есть молча не проходит, — но передаётся значение вызывающим, и второй его источник существует | [`V-05`](impl/V-05/report.md) §7 п. 4 | **`M-05`** — там же, где раскрывается `cacheKeyView` стадии `voice` (долг №87 про `maxChunkChars` в ключе) | поле в `SpeechPlan` либо параметр в `PlannedChunk` | **`closed` (`M-05`, 2026-08-25)** — `SpeechPlan` хранит `maxChunkChars`, которым построен (решение владельца, вопрос 4). Второго источника у `tokensOfPlan` больше нет; поле понадобилось и матрице: утверждение «`maxChunkChars` действует ровно через `spokenChunkText`» проверяется на плане, который помнит свой предел |
| 106 | **Классы символов правила §6 — свойства таблицы Unicode версии Node**: `\p{P}` (пунктуация) и `\p{White_Space}`. Та же цена, что у долга №78 (произносимость через `\p{L}`/`\p{N}`): смена мажорной версии Node меняет таблицу, а с ней — границы слов. Версия пиньётся `engines` и `.nvmrc` | [`V-05`](impl/V-05/report.md) §3 | **условие открытия:** смена мажорной версии Node — прогнать §6 на пяти разделителях и статусы заново | — | `open` |
| 107 | **Пересчёт не сверяет, ТЕМ ЛИ биндером считает.** `rebindTake` берёт биндер значением и не сравнивает его `binderId` с записанным: сравнение по имени запрещено (**V16**, охранник `adr0010-binder-branching`). Расхождение поэтому наблюдаемо результатом (привязки не равны записанным), а не явным отказом | [`V-05`](impl/V-05/report.md) §7 п. 5 | **`A-03`** — когда биндеров станет два, нужна форма сверки ПО ВОЗМОЖНОСТЯМ, а не по имени | — | `open` |
| 108 | **Укладка держит в памяти PCM всех РАЗЛИЧНЫХ дублей прогона.** Байты нужны стадии `bind` на попадании `voiceKey`: у рефрена звук один, а привязки у двух мест разные, и второй чанк обязан получить те же байты. Цена: ролик AC1 (60 с речи, 24 кГц, s16 моно) — около 2.9 МБ; для long-form (10–20 мин) это уже десятки мегабайт | [`V-05`](impl/V-05/report.md) §7 п. 6 | **`L-01`** — там же, где решается, читать ли байты обратно из CAS вместо удержания в памяти | чтение из CAS по `sha256` вместо поля `Recorded.pcm` | `open` |
| 109 | **`Take` собирается в ДВУХ местах**: `makeTake` (`providers/mock.ts`, значение в памяти для тестов `V-01`) и `recordSpeechPlan` (`plan/record.ts`, настоящая укладка). Поле, добавленное в одном, можно забыть в другом: `V-05` добавляла `bind` в оба. Расхождение ловится только тестами состава | [`V-05`](impl/V-05/report.md) §7 п. 7 | **`V-06`** — там `makeTake` теряет последнего потребителя-провайдера либо становится тонкой обёрткой над укладкой | — | `open` |
| 110 | **Манифест кэша перечитывается и пишется ЦЕЛИКОМ на каждый `put`.** Два ОДНОВРЕМЕННЫХ `put` в одно пространство имён потеряют одну запись: read-modify-write без блокировки. В v1 сборка однопроцессна, а `chapterParallelism` — литерал `1` (ADR-0008), поэтому состояние недостижимо | [`M-05`](impl/M-05/report.md) §7 п. 1 | ~~**`CP-05`** — там появляется параллельный рендер сегментов~~ **`H-*`** *(переадресовано измерением: `CP-05`, 2026-08-27)* — в стадии звука параллельного рендера НЕТ и быть не может: дорожка одна на ролик (T5), `compileAudio` чистая и в CAS не пишет вовсе, `renderAudioTrack` кладёт байты в один `Int16Array` последовательно. Параллельный `put` появится там, где появится параллельный рендер сегментов, — в адаптере рендерера | цена: файл-замок либо запись по одному файлу на ключ вместо общего манифеста | `open` |
| 111 | **Take-файл без блока `bind` не восстанавливает межсборочный кэш.** Дубль с `bind: null` законен (решение владельца `V-05`, вопрос 5), но ответа провайдера в нём нет, а он невоспроизводим; `voiceCacheFromTakes` такой файл пропускает ГРОМКО (списком `unrestorable`), а не молча **Переадресовано `CP-01` (2026-08-26) с причиной:** `compose` читает `bindings[]` и НЕ читает `bind` — блок нужен пересчёту привязок и восстановлению кэша, а не компиляции. Со стороны Timeline дубль с `bind: null` законен: он несёт готовые привязки, а компилятор в `providerAlignment` не заглядывает (ADR-0010 §2 после `DOC-04`). | [`M-05`](impl/M-05/report.md) §7 п. 2 | **`V-06`** — там появляется живой провайдер, то есть впервые возникает невоспроизводимый ответ, ради которого блок и заведён; `CP-01` из адреса снят по названной причине | цена: либо `bind` обязателен, либо `alignment` кладётся в take-файл отдельным полем | `open` |
| 112 | **`project.yaml → voice.voiceId` мёртв, пока к чанку применима роль.** `voice_id` в `voice-roles/1` ОБЯЗАТЕЛЕН, поэтому применимая роль перекрывает голос проекта всегда; матрица ключей это показала. Пока назначений ролей нет (их резолвит `A-02`), поле работает | [`M-05`](impl/M-05/report.md) §6 п. 4 | **`A-02`** — там появляются настоящие назначения ролей | цена: либо `voice_id` в роли становится необязательным, либо это записывается решением | **`closed` (`M-05fix`, 2026-08-25) — РЕШЕНИЕМ ВЛАДЕЛЬЦА, а не правкой кода.** Формулировка дословно: **`voice_id` в роли остаётся обязательным; роль — ПОЛНАЯ спецификация голоса, `project.yaml → voice.voiceId` — УМОЛЧАНИЕ для текста без роли.** Перекрытие голоса применимой ролью — не дефект, а объявленное поведение; матрица ключей проверяет строку `project/1:voice.voiceId` в условии, где поле действует (база без назначения роли), и отдельным утверждением фиксирует само перекрытие — обратное изменение схемы (сделать `voice_id` необязательным) станет видно как смена поведения ключей |
| 113 | **Тесты пакета `voice` видят ДАННЫЕ пакета `media` только после `tsc --build`.** `@vpe/voice` резолвит `@vpe/media` в `dist/`, поэтому правка `cacheKeyView` (JSON) без пересборки не видна матрице стадии `voice`; найдено нарушением Н9 протокола. Свойство не новое (так же устроены импорты `V-03`/`V-05`), но впервые касается данных, а не кода | [`M-05`](impl/M-05/report.md) §7 п. 3 | **`L-03`** — `vpe check` строит перед прогоном | цена: либо `build` перед `test` в скрипте, либо резолв на `src` в vitest | `open` |
| 114 | **`compileProfile` входит в `segmentKey` целиком и, вероятно, дублирует `segmentIrHash`.** Поля вроде `defaultParagraphGapSamples` и `captions.*` влияют на IR, то есть учитываются и в хэше IR, и отдельными строками ключа. Это переинвалидация, а не тихая ошибка, но ADR-0006 §3 требует «ни одна величина не учитывается дважды» | [`M-05`](impl/M-05/report.md) §6 п. 2 | ~~**`CP-03`** — там появляется настоящий `segmentIrHash`~~ **`CP-04`** *(сужено измерением: `CP-03`, 2026-08-26)* — `segmentIrHash` есть хэш **IR**, а IR (`RenderIrSegment[]`) появляется в `CP-04`; каталог `render-ir/` пакета `compile` пуст и после `CP-03`, так что мерить пересечение здесь было не с чем | цена: измерить пересечение и убрать из view поля, полностью поглощённые IR. **Вход измерения готов:** матрица «что из `compileProfile` двигает РАЗБИЕНИЕ» ([`CP-03`](impl/CP-03/report.md) §11) — из одиннадцати полей `CompileProfileInput` разбиение двигают **шесть** (`projectSampleRate`, `fps`, три gap'а, `minSegmentDurationFrames`), пять не двигают (`captions.*` — измерено мутацией; `width`, `height`, `safeAreas`, `templateRegistryVersion`, `maxDurationFrames` — входа в `compose` не имеют вовсе). «Двигает разбиение» ≠ «поглощается `segmentIrHash`»: второе измеримо только когда IR существует. **ИЗМЕРЕНО `CP-04` (2026-08-27), K4-матрица** ([отчёт §7](impl/CP-04/report.md)): **поглощены** хэшем IR семь полей — `fps`, `projectSampleRate`, `defaultParagraphGapSamples`, `defaultSceneGapSamples`, `minSegmentDurationFrames`, `captions.tokensPerGroupMin/Max`, `captions.maxGroupChars`; **НЕ поглощены** — `width`, `height`, `safeAreas`, `templateRegistryVersion`, `maxDurationFrames` (входа в стадию не имеют вовсе), `captions.minGroupDurationFrames` (порог отчёта, в IR не попадает) и `defaultChapterGapSamples` (на одноглавой фикстуре не двигает ничего, на многоглавном проекте поглощается — поле УСЛОВНОЕ). **Рекомендация (`media` не тронут, поправка владельца П4):** явными строками в `cacheKeyView` стадии `segment` оставить пять непоглощённых плюс два условных/безвредных (`captions.minGroupDurationFrames`, `defaultChapterGapSamples` — условная строка в ключе хуже лишней); семь поглощённых — кандидаты на удаление из проекции. ~~Правку выполняет **`CP-05`/`M-06`**~~ **Правку выполняет `M-06`** *(переадресовано измерением: `CP-05`, 2026-08-27)* — `segmentKey` живёт в `media/cache`, а стадия звука его не трогает ни одной строкой; `segmentKey` в `AudioPlan` не входит. **Одно измерение эта сессия всё же добавила:** `maxDurationFrames` перестал быть полем «без входа вовсе» — у него появился потребитель (`AudioProfileInput`, **T9**). В `cacheKeyView` стадии `segment` он остаётся явной строкой: `segmentIrHash` его не поглощает (в IR он не попадает), а предел хронометража на содержимое сегмента влияет через отказ сборки, а не через её результат; цена бездействия — переинвалидация, а не тихая ошибка | `open` |
| 115 | **`composeKey` и `segmentKey` не подключены ни к одной сборке.** Продюсеров у них нет: `segmentIrHash` — `CP-03`, `engineFingerprint` — `H-*`, `compositionHash` — рендерер (`CP-05`). Ключи существуют чистыми функциями от типизированных входов и покрыты матрицей, но ни разу не считаются на настоящих величинах. **СУЖЕН `CP-04` (2026-08-27): продюсер `segmentIrHash` ЕСТЬ** — [`render-ir/hash.ts`](../packages/compile/src/render-ir/hash.ts), `blake3(canonicalJson(RenderIrSegment))`; величина посчитана на настоящих данных фикстуры (`acf91306…` у `seg:intro`, `5bcdb8bd…` у `seg:turn`) и входит в `SegmentKeyInput.segmentIrHash` строкой. **Остаток:** `engineFingerprint` (`H-*`), `compositionHash` (рендерер) и подключение `segmentKey` к настоящей сборке (`L-01`) — то есть ключ целиком по-прежнему ни к одной сборке не подключён | [`M-05`](impl/M-05/report.md) §7 п. 4 | ~~**`CP-03`**~~ **`CP-04`**, ~~**`CP-05`**~~ **`H-01`**, **`L-01`** *(сужено измерением: `CP-05`, 2026-08-27 — `compositionHash` считает РЕНДЕРЕР, а стадия звука рендерера не видит; остаток адресов не меняется)*, **`H-*`** *(сужено измерением: `CP-03`, 2026-08-26)* — продюсер `segmentIrHash` живёт там же, где IR, то есть в `CP-04`; `CP-03` производит РАЗБИЕНИЕ (`Timeline.segments`), а не хэш содержимого сегмента | — **СУЖЕН `H-01` (2026-08-28): продюсер `compositionHash` появился.** [`materializeComposition`](../packages/renderer-hyperframes/src/materialize.ts) возвращает `compositionHash` (sha256 канонического перечня каталога) и сверяет его с `bundle.hash` запроса; [`renderSegment`](../packages/renderer-hyperframes/src/run.ts) дополнительно возвращает `engineCompositionHash` — величину самого рендерера из трассы. То есть у `SegmentKeyInput.compositionHash` источник теперь есть. Остаток долга — ровно два слагаемых: `engineFingerprint` (`H-03`) и вызывающий, который соберёт ключ (`L-01`). Побочно долг раздвоился — какая из двух величин входит в ключ, ADR не говорит: №155. — **СУЖЕН `H-03` (2026-08-28): продюсер `engineFingerprint` появился.** [`computeEngineFingerprint`](../packages/renderer-hyperframes/src/fingerprint.ts) считает `blake3(canonicalJson(проба))` на измеренных величинах; на этой машине — `c775344b76b4fd2ed32240c3c6e0a785253e6bea1eb899755b3f4ceed4c40521` (проба целиком — [отчёт §4](impl/H-03/report.md)); `renderSegment` возвращает его полем `RenderResponse.engineFingerprint`. **Остаток долга — ОДИН вызывающий:** `vpe build` (`L-01`), который соберёт `segmentKey` на настоящих величинах и подаст `renderSegment` записанный отпечаток опцией `recordedEngineProbe` — без него **R14** сверяет только полноту пробы, а не расхождение. | `open` |
| 116 | **`verifyComposition` не вызывается никем.** Охранник «скомпилировалось не то» написан и покрыт тестами, но стадии `compose` не существует, и звать его неоткуда | [`M-05`](impl/M-05/report.md) §7 п. 4 | ~~**`CP-05`**~~ **`H-01`** *(переадресовано измерением: `CP-05`, 2026-08-27)* — `verifyComposition` стережёт КОМПОЗИЦИЮ («скомпилировалось не то»), а её материализует адаптер рендерера (`H-01`, `bundle.hash == compositionHash`); стадия звука композиции не видит: её вход — Timeline и манифест, выход — PCM. Звать охранника по-прежнему неоткуда, и место вызова появляется вместе с адаптером | — **ПОМЕТКА `H-01` (2026-08-28): потребитель обеспечен, вызывающего по-прежнему нет.** Адаптер ПРОИЗВОДИТ обе величины, которые `verifyComposition` сравнивает, и возвращает их наружу (`bundle.hash` сверяется внутри, `engineCompositionHash` — поле ответа). Звать `verifyComposition` обязан `L-01`, и до его появления охранник остаётся написанным-но-непозванным. Условие корректного вызова — решение долга №155. | `open` |
| 117 | **GC кэша отсутствует: `.cache` растёт неограниченно.** Решение владельца (`M-05`, вопрос 5): LRU + пины (ADR-0006 §13) в объём не входят, «пустой каталог удаляется руками». Пометка **K10** из `M-01` адресата в `M-05` не получила — удалений в `media/src/cache/**` нет ни одного | [`M-05`](impl/M-05/report.md) §6 п. 5 | **отдельная задача** (в roadmap её нет) | условие открытия: кэш перестал помещаться на диск на настоящем проекте | `open` |
| 118 | **Ревизия приёмки в ADR-0010 §1/§6 — четыре кандидата без адреса.** (1) Правило произносимости: §1 и §5 говорят «непроизносимые code points», но класс не определяют; исполняется «есть хотя бы одна буква или цифра» (`\p{L}`/`\p{N}`). (2) Испорченный ответ ≠ больной дубль: §1 описывает лестницу для провала ПРИЁМКИ, а разрежённый массив приёмка не судит, а отвергает исключением мимо лестницы. (3) `rejectReason` объявлен `string`, исполняется литеральный union из семи причин. ~~(4) Уточнение roadmap §4.0 (не ADR): `takeHealth` физически лежит в `sp2/mock.mjs`, а таблица переносов отдаёт его `V-02` вместе с `sp2/lib/analyze.mjs`.~~ Тексты всех четырёх готовы в отчётах | [`V-01`](impl/V-01/report.md) §8, [`V-02`](impl/V-02/report.md) §11 | **сужен `DOC-05`, 2026-08-26: п. 4 исполнен** (roadmap §4.0, пометка в строке `sp2/lib/analyze.mjs → V-02`) **— адрес больше не нужен; пп. 1–3 остаются `V-06`** (решение владельца `DOC-04` по адресу пп. 1–3 не меняется) | решение владельца (`DOC-04`, 2026-08-25): первые три — содержательная ревизия лестницы приёмки, ей нужен свой заход, когда живой провайдер испытает лестницу; четвёртая — правка roadmap, не ADR, и она внесена этой сессией **ЗАКРЫТ `V-06` (2026-08-31): пп. 1–3 внесены в ADR-0010 конвенцией зачёркивания.** (1) §1 получил определение непроизносимого: «есть хотя бы одна буква или цифра Unicode» (`\p{L}`/`\p{N}`), по code point'ам, с названной ценой (токен из одних математических символов считается непроизносимым). (2) §1 получил абзац «испорченный ответ ≠ больной дубль»: разрежённый `alignment`, тело без `audio_base64` и не-JSON отвергаются ИСКЛЮЧЕНИЕМ мимо лестницы, тогда как `alignment: null` остаётся законным ответом и судится приёмкой (`no-alignment`). (3) §1 объявляет `rejectReason` литеральным union'ом из семи причин со значением `null` у принятого дубля; прежняя запись `rejectReason?: string` зачёркнута | `closed` |
| 119 | **Длительность клипа режиссуры без `until` берётся из области, а не из объявленного параметра шаблона.** Решение владельца 2026-08-26 (`CP-01`, вопрос 7, вариант «а»): `until` по умолчанию = конец области, содержащей `at` — так уже записано в `DirectionRecordBase` (`core-model/model/entities.ts`) и в ADR-0002 §4 для `[img:]`. Наблюдаемое следствие: `flash@1` фикстуры несёт `params.durationSamples: 4800` (0.2 с), а клип получает 281880 сэмплов (11.7 с) — до конца `sc:intro`. **СУЖЕНО `TS-01` (2026-08-27): контракт есть, потребителя нет.** Манифест `flash@1` объявляет параметр, и он типизирован — `durationSamples: z.int().positive()` (ноль отвергнут: вспышка нулевой длины не является вспышкой, а T4 пустых интервалов не знает). Читать `params` компилятор по-прежнему права не имеет — это `CP-06`. **ЗАКРЫТ `CP-07` (2026-08-27).** `flash@1` объявляет свою длительность (`declareDuration`, третья необязательная декларация контракта — расширение `templates-spec`, объяснённое в отчёте §4), `compose` её берёт: клип стал **4800 сэмплов = 6 кадров** вместо 281 880. Порядок источников конца назван в коде и в тесте: `until` автора → объявленная шаблоном → конец области; `until` ВМЕСТЕ с объявленной — ошибка компиляции (решение владельца E: автор противоречит сам себе). Компилятор при этом `params` по имени поля НЕ читает — читает их спек про себя, и это охраняется грепом. Разбор — [`CP-07`](impl/CP-07/report.md) §5. Побочная цена названа: клип перестал доставать до разреза 551760, тест `CP-03` «стоящие вплотную» переписан с объяснением | [`CP-01`](impl/CP-01/report.md) §5 | ~~**`CP-06`**~~ — закрыт | цена: клип длиннее, чем шаблон рисует. Для `still@1`/`kenburns@1`/`bed@1` это и есть намерение; для `flash@1` — расхождение, видимое в отчёте, но не в картинке | `closed` |
| 120 | **Alias'ы внутри `params` чужих шаблонов не разрешаются, и форма поля с ними расходится с моделью.** Решение владельца 2026-08-26 (`CP-01`, вопрос 8): `compose` разрешает ровно один alias — у порождённой `[img:]`-записи, потому что манифеста у неё нет и быть не может. В фикстуре при этом `bed@1.params.asset: "pad-loop"` и `bed@1.params.inPoint: { kind: mediaTime, asset: "pad-loop", … }`, тогда как `MediaTimePoint.asset` типизирован `Sha256` — то есть на месте sha стоит alias, и сегодня это не ловит НИКТО (`assertNoGridPoint` в `C-05` проверяет только `kind`). **ЗАФИКСИРОВАНО `TS-01` (2026-08-27), решение владельца: расхождение записано ЯВНО, а не устранено.** `AssetRef` — **alias-форма** (`{alias, role}`), тип в `core-model` НЕ заводится. `bed@1.params.asset` и `bed@1.params.inPoint.asset` типизированы alias'ом и **обязаны совпадать** (`superRefine`): in-point есть точка внутри ассета, а ассет у `bed@1` ровно один — разные alias'ы означали бы второй ассет, которого шаблон не объявляет. Расхождение с `MediaTimePoint.asset: Sha256` осталось и названо в шапке [`refs.ts`](../packages/templates-spec/src/refs.ts). **ЗАКРЫТ `CP-07` (2026-08-27).** Alias живёт в **авторских** `params` (решение владельца C: подмена внутри `params` есть интерпретация шаблона и ломает AC4-b при переименовании alias'а), sha — в `contract.assets` с ролью, которую назвал `declareAssets`. **Особая ветка `resolveAlias` у порождённой `[img:]`-записи УДАЛЕНА:** путь один на все клипы, поля `alias`/`assetSha` из `ClipFill` ушли. Расхождение `MediaTimePoint.asset: Sha256` против alias'а осталось и вынесено ОТДЕЛЬНЫМ долгом №152 — оно уже не про компилятор. Разбор — [`CP-07`](impl/CP-07/report.md) §1 | [`CP-01`](impl/CP-01/report.md) §5 | ~~**`CP-06`**~~ — закрыт | цена: до `TS-01` `Timeline` «знает ассеты» только про `[img:]`; ассет музыки виден Policy Guard'у (`CP-06`) не как sha, а как строка | `closed` |
| 121 | **`[pause: 0ms]` синтаксически законен, а на границе сцены и главы разрушает T8 и V4.** Найдено зондом `CP-01` (2026-08-26): лексер `C-02` принимает `N = 0` и даёт `Silence` нулевой длины. На границе сцены это ломает **T8** («каждая граница сцены — кандидат на разрез»), на границе главы — **V4** («граница главы обязана быть границей сегмента»), причём валидация профиля этот путь не закрывает вовсе: `defaultSceneGapSamples > 0` проверяет ПРОФИЛЬ, а обнуляет тишину ПРОЗА. Половина закрыта: `compose` отвергает такой вход со списком (решение владельца, поправка П1) | [`CP-01`](impl/CP-01/report.md) §5, §8 | **ревизия ADR-0002 §2 + первая задача, которой разрешено трогать лексер `C-02`** — потребовать `N ≥ 1` на уровне лексера: `[pause: 0ms]` ВНУТРИ абзаца режет чанк и СТОИТ ДЕНЕГ ради нуля сэмплов тишины. Текст правки ADR готов в отчёте `CP-01` §8; лексер `C-02` закрыт, этой задачей не правился | цена: до правки диагностика приходит на стадии `compose`, а не на разборе исходника, то есть позже, чем автор её ждёт | `open` |
| 122 | **Время якоря пространства `r:` не определено ни одним ADR.** ADR-0004 §1 называет пространство и задаёт его резолв в SCOPE (`C-05`, `model/direction.ts`), но не говорит, ЧТО такое `r:X` как момент — начало клипа, его конец или что-то третье. `validateDirection` такую ссылку принимает, а `compose` отвергает вслух (`resolvePoint`, `unsupportedRecordRef`), то есть проект с ней проходит валидацию и падает на компиляции | [`CP-01`](impl/CP-01/report.md) §9 | **`O-01`** — там появляется единственный писатель, которому `r:` реально нужен (`vpe nudge` по записи режиссуры), и там же выбирается смысл момента | цена: отказ громкий и с объяснением, но поздний; в фикстуре ссылок `r:` нет ни одной | `open` |
| 123 | **Порядок клипов сортируется по `sourceOrdinal` = позиции якоря в прозе, а ADR-0007 §5 называет поле, но не определяет его.** §5 требует «первичные ключи — авторские (`track`, `z`, `sourceOrdinal`)», не говоря, чем `sourceOrdinal` является. `CP-01` принял: это индекс якоря `at` в каноническом порядке слотов документа (`INFERENCE`). Альтернатива «индекс записи в файле» отвергнута измеримо: она зависит от порядка чтения каталога, то есть ломает критерий «перестановка файлов не меняет Timeline» | [`CP-01`](impl/CP-01/report.md) §8 | **ревизия ADR-0007 §5** — дописать определение; текст готов в отчёте `CP-01` §8 | цена: до правки определение живёт в коде и в этом долге, а не в ADR | `open` |
| 124 | **Балансировка хвоста групп субтитров (`3 + 1` → `2 + 2`) не делается.** Это ЕДИНСТВЕННОЕ прочтение, при котором «минимум достигается УМЕНЬШЕНИЕМ числа слов» (ADR-0003 «Субтитры») буквально работает: укоротив предыдущую группу, соседний однословный огрызок получает второе слово без единого сдвига. Решение владельца 2026-08-26 (вопрос 1): в `CP-02` не делаем, набивка остаётся жадной. Данные для пересмотра уже собираются — `CaptionReport.tailSingletons` (на фикстуре **1**) | [`CP-02`](impl/CP-02/report.md) §вопрос 1 | **X3** — пересмотр после первого опубликованного ролика | цена сейчас: правило нелокальное (просмотр вперёд до конца клипа), в ADR его нет; на фикстуре чинит 0 групп при потолке 21 и 1 при потолке 20 — то есть предмета почти нет, пока речь мокованная | `open` |
| 125 | **Потолок `maxGroupChars: 21` и кегль 72 px — временные величины под ВРЕМЕННЫЙ шрифт.** Измерены по `DejaVuSans-Bold.ttf` (`unitsPerEm` 2048, `xAvgCharWidth` 1173 = 0.5728 em): 21 — наибольшее N, при котором ни одна группа фикстуры не выходит за полосу 960 px. **Остаточный риск назван дословно:** потолок в символах есть ПРОКСИ ширины, и строка из одних широких букв (`m`, `W`) в 21 символ дала бы ~1210 px. На фикстуре таких групп нет (бюджет при 21 символе — 0.6349 em/символ, и превышают его только короткие однословные кандидаты, чья полная ширина мала), но гарантии это не даёт. Кегль в профиле не живёт — он свойство шаблона *(`TS-01`, 2026-08-27: кегль в этой сессии не появился — `captionEmphasis@1` объявляет РОЛЬ шрифта и стиль (`bold`), а не размер. Пересматривать нечего, адрес сужается до одного)* *(дополнено: `H-07`, 2026-08-31 — **предложение с кадрами, статус не меняется**. Кегль 72 и потолок 21 символа мерились по `DejaVuSans-Bold.ttf`, которого в демо нет. Предложено по глазу под 9:16 и показано кадрами: кегль **68** px при 1080, полоса **920** (поля по 80), межстрочный **1.22**. Потолок символов в профиле демо оставлен фикстурным (21) и НЕ пересматривался: при полосе 920 и кегле 68 он заведомо не тесен, а его пересмотр есть предмет №188 вместе с остальными числами)* | [`CP-02`](impl/CP-02/report.md) §вопрос 3, предложение — [`H-07`](impl/H-07/report.md) §4 | ~~**`TS-01`** / **`H-07`**~~ **владелец** *(сужено `TS-01`, 2026-08-27; предъявлено кадрами `H-07`, 2026-08-31)* — вместе с №13 | связан с №13 (шрифт канала не выбран); цена пересмотра — одно число в профиле и одно в шаблоне | `open` |
| 126 | **`absent` под ПРОИЗНОСИМЫМ словом роняет сборку целиком.** Решение владельца 2026-08-26 (вопрос 4 (г)): показать слово без времени нельзя (зритель прочтёт не то, что слышит), вывести время из соседей нельзя (**V8**, ADR-0010 §5) — значит ошибка компиляции со списком. Цена названа: одно проглоченное провайдером слово останавливает сборку, и автор обязан перегенерировать дубль. Непроизносимый `absent` (знак препинания) ошибкой НЕ является — он приклеивается к соседу слева | [`CP-02`](impl/CP-02/report.md) §вопрос 4 | **`A-03`** — акустический биндер принесёт `interpolated`, то есть время там, где провайдер промолчал | до `A-03` смягчать нечем: любая мягкая ветка есть выдуманное время | `open` |
| 127 | **Два текста разошлись с принятым прочтением минимума и правилом конца предложения.** (1) Комментарий в фикстуре у `minGroupDurationFrames` говорит «минимум — за счёт числа токенов (M6)», тогда как принято чтение (б): это ПОРОГ ЗАПИСИ в отчёт. Строку не правил — разрешение `CP-02` на правку фикстуры покрывало ровно три строки. (2) ADR-0003 «Субтитры (M6)» по-прежнему говорит «минимум обеспечивается уменьшением числа слов» и ничего не говорит о конце предложения как границе группы. Готовые формулировки обеих правок — в отчёте | [`CP-02`](impl/CP-02/report.md) §кандидаты в правку ADR | **закрыт `DOC-05`, 2026-08-26** | **обе половины исполнены.** (1) комментарий `fixtures/minimal/profiles/compile.yaml` у `minGroupDurationFrames` заменён на «порог записи в отчёт сборки: группа короче не сдвигается и не растягивается (решение владельца `CP-02`, ADR-0003 «Субтитры»)» — единственная правка `fixtures/` этой сессии; (2) ADR-0003 «Субтитры (M6)» переписан дословно по тексту `CP-02` §6: минимум как порог записи + жадная набивка, и добавлено новое правило «конец предложения закрывает группу» | `closed` |
| 128 | **Scope маркера `[emph]` выбран временно и держится в одном месте.** «Следующий токен того же чанка» (решение владельца 2026-08-26, вопрос 6) — это ВЫБОР scope, а `C-02` решением владельца оставил его открытым (в AST у маркера scope нет вовсе), и закрывает вопрос `CP-05` (дрейф roadmap §11.2 строка 16). Правило названо ровно в одной функции (`sourceTokens`, `captions.ts`) и в одном поле (`CaptionGroupToken.emph`). **ПЕРЕАДРЕСОВАНО `TS-01` (2026-08-27).** `captionEmphasis@1` объявил СТИЛЬ (`style: bold`, закрытый список), а не scope: scope маркера — вопрос ЛЕКСЕРА и ADR-0002 (в AST у маркера scope нет вовсе), а спек шаблона AST не видит. Расширять список стилей до решения по scope значило бы решать за `DOC-06`. | [`CP-02`](impl/CP-02/report.md) §вопрос 6 | ~~**`DOC-06`/`TS-01`**~~ **`DOC-06`** *(переадресовано `TS-01`, 2026-08-27)* | цена смены: одна ветка обхода; поле остаётся | `open` |
| 129 | **Сокращение вида `Mr.` неотличимо от конца предложения, и теперь это РЕЖЕТ группу субтитров.** Граница предложения в репозитории одна — `isSentenceEnd` (`C-02`, решение владельца `V-03` вопрос 2), и она смотрит на один code point. До `CP-02` цена ошибки была «лишний разрез чанка»; с правилом «конец предложения закрывает группу» (решение владельца 2026-08-26, вопрос 5) добавилась вторая — лишняя короткая группа субтитров. Своего правила субтитры НЕ заводят: вторая копия набора знаков хуже | [`CP-02`](impl/CP-02/report.md) §вопрос 5 | **X3** — пересмотр после первого опубликованного ролика (на английском контенте канала сокращений с точкой пока нет ни одного) | цена: список исключений — это словарь, а словарь в компиляторе требует своего решения владельца | `open` |
| 130 | **Сцена без единого речевого клипа сегментации не видна, и держится это ЧУЖИМ ассертом.** Области сцен строятся по первому речевому клипу (`CP-01`, `areasOf`), поэтому сцена, в которой нет ни одного произносимого токена, не попадёт ни в один `Segment.sceneIds[]` и не даст кандидата на разрез. Сегодня такой исходник недостижим: `anchorTimes` (`CP-01`, правило П3) роняет компиляцию раньше — но правило про СЕГМЕНТЫ охраняется ассертом из соседней стадии, а не своим | [`CP-03`](impl/CP-03/report.md) §9 п. 2 | **`CP-04`** — там у сегмента появляется IR, и «сегмент без сцены» становится видимым по содержимому | одна проверка + фикстура со сценой из одного `[img:]` без прозы | `open` |
| 131 | **`nominalSamples` (`L_i`) сегодня тождественно равен длине сегмента.** Клипов `Silence(kind: 'boundary-correction')` не существует до `CP-04` (решение владельца `CP-01`, вопрос 2), поэтому сумма номинальных длин и разность границ совпадают. Считается величина СУММИРОВАНИЕМ и сверяется с разностью — то есть ассерт содержателен, — но свою настоящую работу (`L_i` БЕЗ поправки `δ`) поле начнёт делать только когда поправка появится. **ЗАКРЫТО `CP-04` (2026-08-27):** тождество разорвано в арифметике — `A_i` и `δ_i` считаются ИЗ `L_i`, и на фикстуре `A_1 = 552000 ≠ 551760 = L_1`, `δ_1 = 240`. Что `δ` не входит в `L`, доказано ПОРЯДКОМ вычисления: `L_i` приходит из `CP-03` и вычисляется до `d_i`, а `δ_i` — после `A_i`. Материализация клипов `Silence(kind: 'boundary-correction')` — отдельный вопрос и отдельный адрес (`CP-05`, решение владельца 4 от 2026-08-26), а не этот долг | [`CP-03`](impl/CP-03/report.md) §9 п. 3, закрытие — [`CP-04`](impl/CP-04/report.md) §8 | — | `closed` |
| 132 | **Порог сравнивается с длиной в СЭМПЛАХ, а рендер считает КАДРЫ.** `minSegmentDurationFrames` переводится в сэмплы один раз (`frameStartSample(grid, asFrames(n))`), и обе части сравниваются с этим числом. Но длина сегмента в кадрах есть `d_i = ceil(L_i / S)` (ADR-0003 T6), и сегмент длиной `порог − 1` сэмпл даст `d_i`, равный порогу в кадрах. Расхождение ≤ 1 кадра, направление известно (`ceil` — в пользу разреза), проверить его можно только там, где `d_i` существует. **ЗАКРЫТО `CP-04` (2026-08-27).** Ассерт в КАДРАХ стоит в продакшн-пути ([`render-ir/metrics.ts`](../packages/compile/src/render-ir/metrics.ts), `assertThreshold`). Формулировка исключений — **решение владельца 9** (2026-08-26), дословно: ассерт применяется к сегменту, обе границы которого — либо край ролика при наличии хотя бы одного принятого разреза, либо принятый разрез с `reason === null`; сегмент, у которого хотя бы одна граница `chapter-forced` (**V4** режет безусловно), и единственный сегмент ролика без принятых разрезов исключаются **по таблице `cutTable`**, а не молча. **Направление расхождения проверено, а не выведено:** из `L_i ≥ frameStartSample(порог)` следует `d_i ≥ порог` — перебором 40 точек вокруг порога на ОБЕИХ сетках, включая дробную 48000 при 30000/1001. То есть сэмпловая проверка `CP-03` не слабее кадровой, и ассерт может покраснеть только от дефекта | [`CP-03`](impl/CP-03/report.md) §9 п. 4, закрытие — [`CP-04`](impl/CP-04/report.md) §8 | — | `closed` |
| 133 | **Синтетические фикстуры `CP-03` держатся на числах профиля мока, а не читают их.** 58320 / 14400 / 6720 сэмплов — это `tts:mock@1` с `msPerChar: 55` и `TAKE_PROFILE` (`leadInMs: 100`, `tailMs: 300`); смена профиля мока потребует пересчёта литералов в [`segments.test.ts`](../packages/compile/test/segments.test.ts). `fixtures/minimal` от этого защищена (`test/fixture.ts` читает профиль регуляркой), синтетика — нет: у неё профиля-источника не существует, она сама себе исходник | [`CP-03`](impl/CP-03/report.md) §9 п. 6 | **`G-02`** — вместе с общим читателем фикстур тестов | цена: считать пороги из построенной дорожки вместо литералов; сегодня литерал ЧЕСТНЕЕ — он падает при смене мока, а вычисление молча подстроилось бы | `open` |
| 134 | **Лексер `C-02` принимает `[pause: 0ms]`, хотя ADR-0002 §2 после `DOC-05` требует `N ≥ 1`.** Правка ADR внесена (`DOC-05`, 2026-08-26, основание `CP-01` §8.4); лексер `C-02` не правился — он закрыт для этой сессии. Сегодня ошибка ловится только в `compose`, и только на границе сцены/главы (решение владельца, поправка П1, `CP-01`): внутри абзаца `[pause: 0ms]` по-прежнему проходит молча | ADR-0002 §2 (правка `DOC-05`), [`CP-01`](impl/CP-01/report.md) §5, §8 | **`L-03`** (`vpe check`) либо первая задача, открывающая `core-model/source` | одна строка правила лексера + тест; до неё диагностика внутри абзаца приходит позже, чем автор её ждёт (или не приходит вовсе) | `open` |
| 135 | **`purpose = templateId` — временная форма seed'а.** ADR-0007 §1 определяет `purpose` как «строковую константу шаблона» (`'kenburns.jitter'`), то есть перечень принадлежит манифесту шаблона, а манифестов нет: `templates-spec`/`TS-01` не написан. `CP-04` взял самую узкую форму, не выдумывающую перечня: один seed на клип, ключ — id шаблона целиком (`'kenburns@1'`). Формула ADR-0007 §1 при этом не меняется ни символом — вырастет число ключей карты. **СУЖЕНО ИЗМЕРЕНИЕМ `TS-01` (2026-08-27), и измерение меняет ЦЕНУ долга.** `purposes[]` — поле манифеста, и у ВСЕХ ПЯТИ шаблонов фикстуры оно **пусто**: случайности не требует ни один (`kenburns@1` задан `from`/`to`/`easing` целиком; `still@1` — ADR-0002 §4 прямо говорит «случайности не требует»). Значит при подключении контракта карта `seeds` станет **ПУСТОЙ** у всех клипов фикстуры, а не сменит ключ, — то есть цена «seed сменится один раз ⇒ кэш инвалидируется» описывает не тот случай. **Следствие, требующее решения `CP-06`, а не правки ожидания:** тест **D1**, считающий четыре seed'а, обязан быть ПЕРЕЧИТАН — вопрос «что D1 охраняет, если ни один шаблон v1 случайности не просит» подгонкой числа не решается. **ЗАКРЫТ `CP-07` (2026-08-27), и цена ЗАПЛАЧЕНА.** `purpose = templateId` удалён; карта строится по `manifest.purposes`, которые приезжают в IR значением. На фикстуре карта стала **ПУСТОЙ у всех клипов** — это измерение, а не деградация. Вклад в смену `segmentIrHash` измерен отдельной строкой лестницы ([`CP-07`](impl/CP-07/report.md) §5): `seg:intro` `acf91306…` → `2005469a…`, `seg:turn` `5bcdb8bd…` → `da95e70a…` при выключенных остальных изменениях. **Тест D1 ПЕРЕЧИТАН, а не подогнан** (находка 9 `TS-01`): охранник переведён на синтетический спек `jitter@1` с непустым перечнем — измерено (протокол Н5), что фикстурная редакция осталась бы ложно-зелёной | [`CP-04`](impl/CP-04/report.md) §8 | ~~**`CP-06`**~~ — закрыт | **цена, принятая решением владельца 1 (2026-08-26):** при появлении настоящих purposes seed каждого шаблона сменится ОДИН раз ⇒ кэш сегментов инвалидируется целиком. До первого ролика это ничего не стоит | `closed` |
| 136 | **У порождённой `[img:]`-записи нет seed'ов вовсе** (`seeds: {}`). Основание — отсутствие `recordId` (ADR-0007 §1 определяет его как id, ВЫДАННЫЙ CLI и ЗАПИСАННЫЙ в `direction/*.yaml`; у порождённой записи нет ни одного из двух событий — решение владельца `C-05`, долг №21) и запрет выдумывать правило его вывода. `still@1` — статичная картинка, случайность ей не нужна. **ПРОВЕРЕНО `TS-01` (2026-08-27): условие открытия НЕ наступило.** `still@1` случайности не получил — `purposes: []`, `easingIds: []`; спек объявляет `asset` и необязательный `fit`, и ничего сверх. Долг остаётся закрытым-по-условию. **ПРОВЕРЕНО `CP-07` (2026-08-27): условие по-прежнему НЕ наступило, но теперь оно ИСПОЛНИМО.** Шаблон с непустыми `purposes`, поставленный на порождённую `[img:]`-запись, есть **ошибка компиляции** с текстом про отсутствующий `recordId` — а не выдуманный id и не молчание (тест `template-contract.test.ts`). То есть в день, когда `still@1` получит случайность, компилятор скажет об этом сам | [`CP-04`](impl/CP-04/report.md) §8, решение владельца 1-bis (2026-08-26) | ~~**`CP-06`/`E-*`**~~ **`E-*`** *(проверено `CP-07`)* | **условие открытия:** шаблон порождённой записи получает случайность (например, jitter в `still@1`) — тогда `TS-01` обязан назвать, что подставляется в слот `recordId`, и это будет решение, а не деталь | `open` |
| 137 | **`IrBuildRecord` — не `BuildRecord`.** Сущность `BuildRecord` (ADR-0001: входы, ключи, версии, `now`, sha256 выходов стадий) **в репозитории не существует**: `core-model/src/model/entities.ts` в шапке относит её к `G-0x`, `grep 'interface BuildRecord'` по `packages/` пуст. ADR-0003 T3 при этом пишет «с записью в BuildRecord». `CP-04` производит ВКЛАД в неё под своим именем и в своей зоне (`render-ir/records.ts`, пять правил, детерминированный порядок) | [`CP-04`](impl/CP-04/report.md) §8, решение владельца 10 (2026-08-26) | **`L-01`**/`G-0x` | цена: собирающая задача обязана вложить эти записи в настоящий `BuildRecord`, а не завести второй формат отчёта сборки | `open` |
| 138 | **`IrAssetRef.role` у порождённой `[img:]` — строка `'asset'`, взятая из имени параметра.** Настоящие роли объявляет `declareAssets` шаблона (ADR-0008); у порождённой записи манифеста нет и быть не может — она родилась из прозы. **ЗАКРЫТО `TS-01` (2026-08-27), И БЕЗ СМЕНЫ `segmentIrHash`.** Правило названо: роль = **имя параметра, который ассет держит** — так уже разворачивает `[img:]` ADR-0002 §4 (`params: { asset }`), и ровно эту строку уже кладёт в IR `compile-ir.ts`. Манифесты `still@1` и `bed@1` объявляют `declaredAssets: ['asset']`, `declareAssets` возвращает `{alias, role: 'asset'}`; тест сверяет манифест с фактическим результатом на `params` фикстуры. Цена долга («если `TS-01` назовёт роль иначе, `segmentIrHash` сменится один раз») **не заплачена**: ни один хэш не меняется. **ПОДТВЕРЖДЁН ИЗМЕРЕНИЕМ (`CP-07`, 2026-08-27, решение владельца G).** Роль теперь называет `declareAssets` спека, а не компилятор: ветка «у порождённой записи роль `asset`, у прочих пусто» из `compile-ir.ts` удалена. Значение то же (`'asset'`), и вклад этой ссылки в смену `segmentIrHash` — **нулевой**: строка лестницы «только `assets` клипов-записей» не двигает `seg:intro` вовсе ([`CP-07`](impl/CP-07/report.md) §5) | [`CP-04`](impl/CP-04/report.md) §8 | **закрыт `TS-01`** — роль = имя параметра (ADR-0002 §4) | цена: если `TS-01` назовёт роль иначе (`'image'`, `'still.source'`), `segmentIrHash` всех сегментов с картинками сменится один раз — тот же класс, что №135 | `closed` |
| 139 | **`RenderIrSegment.fonts` — `readonly never[]`, то есть пометка типом.** Список наполняет `declareFonts` (ADR-0008: «шрифты и эмодзи-шрифт — файлами с checksum»); до `TS-01` единственное честное содержимое — пустой массив, а тип делает «положить сюда что-нибудь» невыразимым до правки самого типа. **СУЖЕНО `TS-01` (2026-08-27): наполнитель написан.** `declareFonts` есть у контракта, и `captionEmphasis@1` — единственный из пяти, кто шрифт просит: `declaredFonts: ['caption']`, `declareFonts() → [{role: 'caption'}]`. **Семейство не называется** (`FontRef.family` необязателен) — шрифт канала не выбран (№13), и шаблон, вписавший `DejaVu Sans`, увёз бы временный выбор в `engineFingerprint` вместе с первым гейтом. Правка типа `readonly never[]` → `IrFontRef[]` остаётся одной строкой и делается потребителем. **ЗАКРЫТ `CP-07` (2026-08-27).** Тип правлен (решение владельца A, только типы): `RenderIrSegment.fonts: readonly IrFontRef[]`, плюс `IrClip.fonts` и новый `IrFontRef` с полями `sha256`/`family`/`role`. Наполняет `declareFonts`; `family` — ИЗМЕРЕННОЕ `intrinsic.family` записи каталога, а не то, что назвал шаблон (`FontRef.family` необязателен — шрифт канала не выбран, №13). На фикстуре `seg:turn` несёт `…0005/DejaVu Sans/caption`, `seg:intro` пуст. Правило выбора записи шрифта — v1, вариант «а»: ровно одна запись `kind: 'font'` на проект; ноль или две+ — ошибка «укажите шрифт роли» | [`CP-04`](impl/CP-04/report.md) §8 | ~~**`CP-06`**~~ — закрыт | цена: одна строка в `core-model/src/model/render-ir.ts` при `TS-01`; альтернатива (`readonly IrFontRef[]` с пустым массивом) была бы приглашением дописывать в неё, не читая ADR — тот же довод, по которому `core-model` до `CP-04` не заводил типа-заготовки RenderIR | `closed` |
| 140 | **Блоб ДЛИННЕЕ обещанного не обнаруживается.** `renderAudioTrack` проверяет, что окно `[from, to)` помещается в присланные байты, — то есть ловит блоб КОРОЧЕ обещанного. Длиннее — не ловит: обещанная длина (`take.pcm.numSamples`) до зоны звука не доезжает, `PlacedSpeech` несёт только края окна (`pcmStartSample`/`pcmEndSample`), а `numSamples` из пары не восстанавливается (`pcmEndSample = numSamples − tailSamples`, и `tailSamples` рядом нет). Практически состояние недостижимо: адрес блоба — `sha256` его байтов, и другая длина означает другой sha | [`CP-05`](impl/CP-05/report.md) §7 п. 1 | **`L-01`** — там источник PCM наполняется из CAS, и проверка адреса естественна на границе чтения | цена: либо сверять `sha256Of(bytes)` с `pcmSha256` при наполнении источника (одна хэш-операция на дубль), либо провести `numSamples` через `PlacedSpeech` (правка типа `CP-01`) | `open` |
| 141 | **Музыка и `sfx` едут в `AudioPlan` данными и НЕ микшируются.** Решение владельца 1 (2026-08-27, вариант «а»): дорожка v1 = речь + тишины движка; клипы аудио-домена лежат в `AudioPlan.music[]` как есть (`clipId`, `template`, `params`, интервал) и посчитаны полем `unmixedClips`, дамп печатает «music: N клипов не смикшированы (TS-01)». **Sha музыки в плане НЕТ, и это измерено:** `params.asset` у `bed@1` — alias (`'pad-loop'` на `fixtures/minimal`), а разрешать alias внутри `params` чужого шаблона компилятор не вправе до манифеста шаблона (решение владельца `CP-01`, вопрос 8). **СУЖЕНО `TS-01` (2026-08-27): `params` музыки типизированы.** `bed@1` объявляет `asset` (alias), `inPoint` (**только** `mediaTime`, в тот же ассет), `gainDb`, `duckUnderSpeechDb`; `declareAssets` возвращает ОДНУ ссылку `{alias: 'pad-loop', role: 'asset'}`, хотя alias встречается в `params` дважды. Откуда берётся sha — `CP-06` (`resolveAlias`, `M-02`); что делают три величины — микс `X-02`. **СУЖЕН `CP-07` (2026-08-27) до `X-02`: sha музыки ПОЯВИЛСЯ.** `AudioMusicClip` получил поле `assets`, и `bed@1` фикстуры несёт `…0004/asset` — alias разрешил `declareAssets` спека, а не обход `params` компилятором (один alias, встреченный в `params` дважды, даёт ОДНУ ссылку). Микса по-прежнему нет: дорожка v1 = речь + тишины движка, `unmixedClips` считает клипы числом, дамп печатает «music: 1 клипов не смикшированы (X-02)» | [`CP-05`](impl/CP-05/report.md) §4 п. 1 | ~~**`CP-06`** (sha из alias) →~~ **`X-02`** (микс) *(сужено `CP-07`, 2026-08-27: sha есть)* | цена бездействия: ролик v1 выходит БЕЗ музыки, и это видно числом, а не молчанием | `open` |
| 142 | **Добивка T5 исполняется только на дробном `S`, и на фикстуре её ветка мертва.** При целом `S` (24000/30 ⇒ `S = 800`) `frameStartSample` аддитивна, `Σ A_i` уже равно `frameStartSample(F)`, и `finalPaddingSamples` тождественно ноль: снятие добивки не роняет НИ ОДНОГО теста на `fixtures/minimal` (протокол `CP-05`, нарушение 3). Ветку держит единственный тест на 24000 при 30000/1001 (`S = 800.8`). Фикстура `minimal` при этом не меняется ни символом (решение `CP-01`) | [`CP-05`](impl/CP-05/report.md) §6 | **`F-02`/`fixtures/reference`** — если референсный проект тоже встанет на целую сетку, ветка останется без НАСТОЯЩЕГО материала | цена: одна строка в профиле референсной фикстуры либо явная запись «на целой сетке добивка тождественно нулевая» в ADR-0003 T5 | `open` |
| 143 | **`AudioPlan` не персистится и ни одной сборкой не производится.** Стадия существует чистой функцией и покрыта тестами, но `audioTrack` в `build/` не пишет никто (ADR-0006 §1 перечисляет стадию среди персистируемых), дорожка в CAS не кладётся, `withAudioTrack` в настоящем манифесте не зовётся. То же состояние, что у `composeKey`/`segmentKey` (долг №115) | [`CP-05`](impl/CP-05/report.md) §7 п. 2 | **`L-01`** — оркестрация стадий и персист в `build/` | — | `open` |
| 144 | **`AudioTrackRef` не входит в `segmentIrHash` и не обязан.** Поле `audioTrack` лежит в `AssemblyManifest`, а `segmentIrHash` считается от `RenderIrSegment` — манифест в него не входит вовсе. Практического следствия сегодня нет (дорожка не участвует в ключе сегмента, и это верно: сегменты немые). Записано затем, чтобы при первом же вопросе «а инвалидирует ли смена дорожки кэш сегмента» ответ был найден, а не выведен заново | [`CP-05`](impl/CP-05/report.md) §7 п. 3 | **`M-06`** (проекции ключей) — вместе с №114 | — | `open` |
| 145 | **Форма хранения манифеста шаблона — TS-литерал, а не файл под схемой.** Решение владельца (`TS-01`, вопрос 1, вариант «а»): семейство `template-manifest/1` в `@vpe/schema` НЕ заводится, манифест живёт данными пакета и валидируется zod-схемой `templates-spec`. **Цена, принятая заранее:** манифест не попадает под `vpe fmt --check` и под миграции V7, а команда `vpe template gate <id>@<N> --profile final\|draftHalf` будет править **TS-файл**, а не YAML — то есть запись гейта ставится правкой исходника | [`TS-01`](impl/TS-01/report.md) §3 | **`E-00`** | условие пересмотра: если правка TS-файла командой окажется дороже, чем миграция семейства, — вопрос открывается заново. Цена перехода: тринадцатое семейство в реестре схем + миграции | `open` |
| 146 | **`msPerFrameBudget` пяти шаблонов фикстуры — `INFERENCE`, не измерен ни один.** Спайки мерили другие композиции: `FACT` (SP-3f §2) шейдер 13.5 мс/кадр, частицы ≈ 37 мс на своих кадрах, стекло +0.9 при разбросе ±4.7 %. Ни `kenburns`, ни `still`, ни `flash`, ни `captionEmphasis`, ни `bed` не участвовали ни в одном из пяти спайков SP-3. Проставлено: `kenburns@1` — 2 (вдвое больше «ниже шума»), три визуальных — 1, `bed@1` — 0 (аудио-домен) **ПОТРЕБИТЕЛЬ ПОЯВИЛСЯ (`CP-07`, 2026-08-27), сам долг не тронут.** Пик суммы по кадрам считается и печатается: на `fixtures/minimal` — **3** у обоих сегментов. Цена неизмеренности стала видимой раньше `E-00`: печатается число, целиком сложенное из `INFERENCE` | [`TS-01`](impl/TS-01/report.md) §2 | **`E-00`** | числа заменяются измерением при прохождении гейта; до тех пор сумма по пересекающимся клипам в отчёте сборки считается по оценкам, а не по замерам | `open` |
| 147 | **Ноль у аудио-шаблона законен; охранника «визуальный шаблон с нулевым бюджетом» нет.** Поправка владельца П1 (`TS-01`): `msPerFrameBudget: z.int().nonnegative()` — поле обязательно (его отсутствие есть отказ реестра, критерий `E-00`), но «> 0» не требуется. `bed@1` несёт 0, потому что кадров не рисует, и «> 0» у него было бы фикцией. Правило «> 0 у ВИЗУАЛЬНОГО шаблона» остаётся ожиданием: реестр трека записи не видит и проверить его не может. **Побочно назван вопрос без ответа в документах:** что означает «гейт шаблона `bed@1`», если гейт сравнивает `sha256` готового mp4 и `framemd5` **СУЖЕН `CP-07` (2026-08-27): место для охранника появилось.** Сумма `msPerFrameBudget` по кадрам сегмента считается (`budgetOf`, `render-ir/build.ts`) и печатается (`budgetMsPerFrame=` в дампе, решение владельца F и 9-RM1: «печатается, не роняет»). Правило «> 0 у визуального шаблона» встанет туда одной строкой, когда `E-00` назовёт порог; сегодня охранника по-прежнему нет | [`TS-01`](impl/TS-01/report.md) §5 п. 3 | **`CP-06`**/**`E-00`** — там, где известен трек записи | цена бездействия: визуальный шаблон с нулевым бюджетом зарегистрируется молча, и сумма по сцене недосчитает его вклад | `open` |
| 148 | **`msPerFrameBudget` объявлен ЦЕЛЫМ, а все измеренные величины дробные.** Поправка владельца П1 назвала `z.int().nonnegative()`; `FACT` (SP-3f §2) даёт 13.5, ≈ 37 и +0.9 мс/кадр. Значит при `E-00` измеренное число придётся округлять, и округление будет молчаливым, если его никто не назовёт (13.5 → 13 занижает бюджет, → 14 завышает) **Не тронут `CP-07` (2026-08-27), но цена уточнена:** сумма по кадрам печатается целым, потому что целыми объявлены слагаемые. Дробные измерения `E-00` попадут в сумму уже округлёнными — то есть округление случится ДО сложения, а не после | [`TS-01`](impl/TS-01/report.md) §3, поправка П1 | **`E-00`** | цена: одна строка схемы (`z.number()` с запретом `NaN`/`Infinity`/`-0` вместо `z.int()`) либо явное правило округления в команде гейта. Решает владелец **ЗАКРЫТ `H-06` (2026-08-29) ПРАВИЛОМ ОКРУГЛЕНИЯ, а не правкой схемы.** Решение владельца (развилка «д», вариант д1): измеренная величина округляется **вверх** (`Math.ceil`), потому что бюджет объявлен ОЦЕНКОЙ СВЕРХУ — занижение (13.5 → 13) лжёт в ту сторону, где сборка не уложится в собственное обещание. Вторая половина правила закрывает случай, которого `TS-01` не предвидел: разность НИЖЕ ШУМА ПРИБОРА либо отрицательная ⇒ **1** у визуального шаблона и **0** у аудио-шаблона, с пометкой «измерено `H-06`: ниже шума». Ноль у визуального читался бы как «шаблона нет». Схема осталась `z.int().nonnegative()` — правки не потребовалось: округление названо, а не молчаливо. ИСПОЛНЕНО на пяти манифестах: `kenburns@1` 32.944 ⇒ 33, `flash@1` 29.727 ⇒ 30, `still@1` 0.936 и `captionEmphasis@1` 0.887 при шуме 1.178 ⇒ по 1, `bed@1` 0 по построению ([измерение](impl/H-06/budget-measurement.txt)) | `closed` |
| 149 | **Форма hex-дайджеста скопирована в `templates-spec` — вторая копия в репозитории.** Первая живёт в `@vpe/schema/types/brands.ts` (`asSha256`/`asBlake3`: 64 строчных hex). Скопирована потому, что `@vpe/schema` из этого пакета **не резолвится вовсе** — карта ADR-0009 даёт `templates-spec` одну стрелку (`core-model`), а `core-model` реэкспортирует `asSamples`/`asFrames`/`asAnchorId`/`Blake3`, но не конструкторы дайджестов. Альтернативы обе хуже: адресный реэкспорт — правка `core-model` (закрытая зона задачи), а `z.string().min(1)` принял бы запись гейта со строкой `"todo"` на месте хэша — ровно то, что **R12** запрещает | [`TS-01`](impl/TS-01/report.md) §1, `manifest.ts` | **`CP-06`**/**`H-03`** — адресный реэкспорт `asSha256`/`asBlake3` из `core-model` по образцу четырёх существующих блоков | цена: три строки реэкспорта; цена бездействия — форма дайджеста живёт в двух местах и может разъехаться | `open` |
| 150 | **`forkedFrom.hash` — ADR-0008 не называет, какой функцией он считается.** ADR пишет `forkedFrom: {id, version, hash}` и не говорит, `sha256` это (байты, CAS) или `blake3` (вычисление, ключи кэша). Схема манифеста проверяет только ФОРМУ (64 строчных hex), общую у обеих | [`TS-01`](impl/TS-01/report.md) §1, `manifest.ts` | **`vpe template fork`** (в v1 не пишется — ADR-0008) / **`E-00`** | условие открытия: первый форк шаблона. До него поле недостижимо — локальных шаблонов в репозитории нет ни одного | `open` |
| 151 | **`fit: cover` и `style: bold` — закрытые списки из ОДНОГО значения.** `still@1.fit` и `captionEmphasis@1.style` принимают ровно то, что стоит в `fixtures/minimal`, потому что второго значения не называет ни один документ проекта. Это отказ выдумывать поведение, а не полнота: `contain`/`fill` и вторые стили эмфазы — решения, которых никто не принимал | [`TS-01`](impl/TS-01/report.md) §10 п. 5 | **`E-*`/`H-06`** — там, где пишется код шаблона | цена расширения — одна строка в константе (список общий у схемы и манифеста); цена бездействия — автор, написавший `fit: contain`, получит отказ компиляции вместо картинки **АДРЕС ИЗРАСХОДОВАН `H-06` (2026-08-29), СПИСКИ НЕ РАСШИРЕНЫ — и это результат.** Код обоих шаблонов написан ([`still@1`](../packages/renderer-hyperframes/src/templates/still@1.ts), [`captionEmphasis@1`](../packages/renderer-hyperframes/src/templates/captionEmphasis@1.ts)), и ни один не потребовал второго значения: `fit: cover` живёт умолчанием В КОДЕ шаблона (порождённая `[img:]` запись `fit` не несёт вовсе), `style: bold` — единственная ветка в `mount`. То есть условие «там, где пишется код шаблона» наступило и ответило «не нужно». Новый адрес — **владелец / `H-07`**: расширение списка теперь есть решение о ПОВЕДЕНИИ (что значит `contain` в кадре 1080×1920), а не о полноте перечисления | `open` |
| 152 | **`MediaTimePoint.asset` типизирован `Sha256`, а в `params` там alias — и после `CP-07` это ЗАКОННО.** Отделено от №120, закрытого `CP-07`: решение владельца C оставило `params` **авторскими** (подмена alias'а на sha внутри `params` есть интерпретация шаблона и ломает AC4-b при переименовании alias'а), поэтому `bed@1.params.inPoint.asset` навсегда остаётся строкой каталога. Значит поле модели `MediaTimePoint.asset: Sha256` и одноимённое поле схемы `bed@1` описывают РАЗНЫЕ вещи. Сегодня безвредно: `inPoint` не читает никто — микса нет. Первый же потребитель получит alias там, где тип обещает sha | [`CP-07`](impl/CP-07/report.md) §9 | **`X-02`** (микс) / **`H-01`** (адаптер) | цена: либо `MediaTimePoint` перестаёт быть общим типом модели и у шаблона появляется свой, либо адаптер резолвит alias по карте `assets` запроса. Решение — за тем, кто первым прочитает in-point | `open` |
| 153 | **`IrFontRef` не несёт веса начертания.** ADR-0008 «Контракт» описывает `SegmentRenderRequest.fonts` как `{sha256, path, family}`; адаптеру для `captionEmphasis@1 {style: 'bold'}` нужно знать, что файл — Bold. Запись `asset-record/1` несёт `intrinsic.subfamily` (`"Bold"` у шрифта фикстуры), а не числовой вес, и правила перевода одного в другой не пишет ни один документ. `CP-07` поле не завёл: выдуманное правило (`"Bold" → 700`) уехало бы в ключ кэша сегмента | [`CP-07`](impl/CP-07/report.md) §4 | **`H-01`** — там, где `fonts` запроса превращаются в `@font-face` | цена бездействия: при ОДНОМ шрифте проекта вопрос не возникает (правило v1 отдаёт единственную запись любой роли); он открывается вместе со вторым шрифтом — то есть вместе с №13 | `open` |
| 154 | **`pixelProfile.imageFormat: jpeg` рендерером НЕ ВЫРАЖАЕТСЯ — профиль `final` сегодня не рендерится.** ИЗМЕРЕНО (`H-01`, `hyperframes@0.8.5`): форматы вывода CLI — `mp4\|webm\|mov\|png-sequence\|gif`; JPEG-последовательности среди них нет, а mp4 запрещён **R10** (`FACT` SP-3d §4.3: штатный энкодер не ставит `-sc_threshold 0`). При этом `fixtures/minimal/profiles/render.final.yaml` несёт `imageFormat: jpeg`, `jpegQuality: 90`, и ADR-0008 после правки DOC-04 прямо говорит, что это «формат ПЕРЕДАЧИ кадров» и «здесь он исполняется». Адаптер ОТКАЗЫВАЕТ правилом «ADR-0008 профиль», а не подставляет png молча: подмена формата передачи сменила бы пиксели, не сменив ключа кэша (ADR-0006 §2). **Три развилки, и выбор не мой:** (а) сменить `final` на `imageFormat: png` — цена: PNG тяжелее, `AC2` замеряли на другой передаче; (б) кодировать JPEG из PNG вторым шагом в `media` — цена: лишний проход по 1800 кадрам; (в) признать поле неисполнимым у этого рендерера и записать это в ADR — цена: поле остаётся мёртвым у `final`. **Обнаружено НЕ на фикстуре, а измерением CLI:** тесты `H-01` идут на `ac4` (`imageFormat: png`) и потому зелёные | [`H-01`](impl/H-01/report.md) §10 п. 5 | `H-06` либо `L-01` (кто первым соберёт настоящий ролик на `final`) | цена бездействия: первая же сборка на профиле `final` падает с понятным текстом — то есть долг ловится, а не тихо портит результат **ЗАКРЫТ `H-06` (2026-08-29) ВАРИАНТОМ (а) — С УСЛОВИЕМ ПЕРЕСМОТРА, НАЗВАННЫМ ВЛАДЕЛЬЦЕМ.** Выбран (а): `fixtures/minimal/profiles/render.final.yaml` несёт `imageFormat: png` без `jpegQuality` — правка сделана `H-03` и лежит в коммите `2e10ecd`, то есть решение исполнено ДО этой сессии, а здесь оно ПРОВЕРЕНО ДЕЛОМ: живой гейт V13 на профиле `final` (`kenburns@1`, N = 10, 1080×1920) — первый настоящий рендер на `final` с момента правки ([`templates-gate-final.test.ts`](../packages/renderer-hyperframes/test/templates-gate-final.test.ts)). **УСЛОВИЕ ПЕРЕСМОТРА (решение владельца `H-06`):** если `G-04` покажет, что сборка на `final` идёт дольше 15 минут, вариант меняется на **(б)** — кодирование JPEG из PNG вторым шагом в `media`; цена (лишний проход по 1800 кадрам) тогда окупается весом передачи. K1 при этом не обнуляется: покрытие по `jpegQuality` держит `render.draft.yaml` (долг №163). Второй половиной той же правки заведён `gate-profiles/draftHalf.yaml` — см. новый долг №183 | `closed` |
| 155 | **ADR-0006 §2 не говорит, КАКАЯ из двух величин композиции входит в `segmentKey`.** После `H-01` их две и они разные: `bundle.hash` — sha256 канонического перечня каталога, который считаем МЫ (64 hex, поле ВХОДА запроса), и `engineCompositionHash` — величина, которую считает САМ рендерер (16 hex, приходит в трассе ПОСЛЕ старта рендера; ИЗМЕРЕНО `H-01`: `a3a5b27f50451f50`, `FACT` SP-3c §7: `5c05d8c4637e8a1c`). ADR-0006 §2 писался, когда величина мыслилась одна. Существенно: SP-3c мерил на воспроизводимость **вторую** («за 134 прогона ровно один»), а по форме контракта ADR-0008 в ключ попадает **первая**, потому что `bundle.hash` типизирован `Sha256` и обязан быть известен ДО рендера. Обе годятся, но выбор обязан быть записан, а не выведен | [`H-01`](impl/H-01/report.md) §10 | `H-03` (там же собирается `engineFingerprint`) либо `L-01` | цена бездействия: `verifyComposition` (долг №116) невозможно позвать правильно — неизвестно, что с чем сверять. **ЗАКРЫТ `DOC-06` (2026-08-29):** выбор внесён в [ADR-0006 §2](adr/0006-content-cache.md) дословной блок-цитатой из отчёта [`H-03`](impl/H-03/report.md) §10 (1) — в `segmentKey` входит **хэш реализации** (sha256 канонического перечня каталога композиции БЕЗ `ir.json`/`assets/`/`fonts/`); `engineCompositionHash` в ключ **не входит** и служит только `verifyComposition`. Правка — только в документации: продюсер этого хэша и вызывающий, который соберёт `segmentKey` на настоящих величинах, остаются за `L-01` | `closed` |
| 156 | **Обоснование инварианта R14 и roadmap `H-03` устарело: версию Chrome пришпиливает САМ пакет.** Строка R14 объясняет себя словами «`FACT` (SP-3c §7) версия Chrome **не пришпилена самим пакетом** — её выбирает `puppeteer`». ИЗМЕРЕНО (`H-01`, `hyperframes@0.8.5`): в коде CLI стоит литерал `CHROME_VERSION = "152.0.7928.2"` (`src/browser/manager.ts`), кэш — `~/.cache/hyperframes/chrome/`, штатная установка — `hyperframes browser ensure`; `~/.cache/puppeteer/` остался фолбэком. Правило НЕ отменяется (сверять фактическое дерево с отпечатком по-прежнему надо, и `npm update` по-прежнему может сменить растеризатор — теперь вместе с версией `hyperframes`), но его «почему» другое, а формулировка «явной версией, не через авто-загрузку puppeteer» в ADR-0008 описывает механику, которой у 0.8.5 уже нет | [`H-01`](impl/H-01/report.md) §10 | `H-03` | **ЗАКРЫТ `H-03` (2026-08-28) кандидатом — но НЕ той формулировкой, которую предлагал `H-01`.** ИЗМЕРЕНО (`H-03`): `hyperframes browser path` вернул `~/.cache/**puppeteer**/chrome-headless-shell/linux-152.0.**7977.42**/…`, хотя пришпиленная пакетом установка `152.0.7928.2` лежала рядом в `~/.cache/hyperframes/` (оба бинаря на диске, 197 МБ каждый). Значит пришпиливание `CHROME_VERSION` **не действует при наличии puppeteer-кэша**, и первоначальный `FACT` SP-3c §7 («версию выбирает `puppeteer`») верен на этой машине прямо сейчас. Кандидат в правку R14/ADR-0008 несёт ОБЕ оговорки (текст — [`H-03` report §10 п. 3](impl/H-03/report.md)); механика уже исполнена кодом: отпечаток спрашивает РЕЗОЛВЕР рендерера, а не константу и не каталог (протокол Н1/Н2). Побочно долг раздвоился: то, что preflight не гарантирует, какой браузер запустится, — новый долг №160 | `closed` |
| 157 | **`--strict` рендерера не включён: неверная разметка композиции даёт не отказ, а неограниченно долгий рендер.** ИЗМЕРЕНО (`H-01`): компилятор HyperFrames читает композицию СТАТИЧЕСКИ и при отсутствии `data-composition-id`/`data-width`/`data-height`/регистрации `window.__timelines` печатает три `✗`, говорит «Continuing render despite lint issues. Use --strict to block errors» и уходит калибровать длительность браузером — 0–2 PNG из 30 за 13 минут вместо ~5 с. То есть дефект разметки выглядит как зависание, а не как ошибка. `H-01` устранил ПРИЧИНУ (разметка теперь верна и это покрыто тестами), но не поставил ОХРАННИКА: `--strict` в аргументы не добавлен, потому что менять поведение рендерера на этапе, где нет ни одного настоящего шаблона, значило бы получить красный гейт на пустом месте | [`H-01`](impl/H-01/report.md) §5 | `H-06` (там появятся настоящие шаблоны) либо `H-04` | цена бездействия: ошибка в разметке нового шаблона проявится зависанием, а не сообщением; выясняется прогоном без `--quiet` **ЗАКРЫТ `H-05` (2026-08-28).** `--strict` стоит в `FIXED_RENDER_ARGS` безусловно, при любом профиле (тест перебирает восемь комбинаций `workers`/`browserGpu`/`scale`). ИЗМЕРЕНО на кривой композиции: отказ **за 3.0 с** с кодом 1 вместо «0–2 PNG из 30 за 13 минут». **Цена флага названа и оплачена:** `--strict` вместе с `--quiet` печатает **НОЛЬ БАЙТ** (`presentRenderLintAbort` молчит при `effectiveQuiet`), а «exit 1 без объяснения» отказом не считается (решение владельца, П2) — поэтому текст добирается отдельным вызовом `hyperframes lint` (2.3 с, статически, без браузера), только на падении и в той же изоляции; отказ несёт собственное правило `ADR-0008 композиция`. Строка запуска сменилась, и вместе с ней `engineFingerprint`: `a48cdce5…ea89` → `61db2ca8…63a0` — это правильно, флаг меняет поведение рендерера | `closed` |
| 158 | **`SegmentArtifact.stats.retries` всегда `0`: механизма повторов нет.** Поле есть в ADR-0008 и заполняется измеренным нулём. Политика повторов — часть ОРКЕСТРАЦИИ (`vpe build`), а не границы рендерера: адаптер рендерит ровно один раз и на ошибке возвращает отказ. Записано, чтобы «0» читалось как «повторов не было», а не как «повторы отключены» | [`H-01`](impl/H-01/report.md) §11 | `L-01` | цена бездействия: нулевая до появления флапающих рендеров; при их появлении отчёт сборки не покажет, сколько раз пересняли | `open` |
| 159 | **Дыра ДВУМЯ половинами, и обе про `media` в ключах.** (а) **Версия пакета `@vpe/media` не входит в `composeKey`:** формула ADR-0006 §2 перечисляет «хэши исходников `renderer-hyperframes` и `templates-*`», а `media` там нет — хотя именно он кодирует сегмент. (б) **Константный скелет строки энкодера не входит НИКУДА:** `-sc_threshold 0`, `open-gop=0`, `-fps_mode cfr`, `-an`, `-f mpegts` и отображение `h264 → libx264` живут литералами в [`media/src/assemble/encode.ts`](../packages/media/src/assemble/encode.ts), МЕНЯЮТ битстрим (`FACT` `M-04`: без `-sc_threshold 0` ключевые кадры встают на 0/30/**45**/75 вместо 0/30/60) и при этом не в `pixelProfile` (не намерение), не в `engineFingerprint` (не окружение; профиль туда нельзя — M9, решение владельца `H-03` вопрос 4) и не в `segmentKey` через `composeKey` — `composeKey` в `segmentKey` не входит вовсе. Итог: правка этих литералов не инвалидирует ни один ключ | [`H-03`](impl/H-03/report.md) §6, §11 | **`L-01`** — там `composeKey` впервые собирается на настоящих величинах | цена бездействия: правка константы энкодера отдаёт кэшированный сегмент, собранный прежней; ловится только глазами в ревью | `open` |
| 160 | **`hyperframes browser ensure` НЕ гарантирует, какой браузер запустится.** ИЗМЕРЕНО (`H-03`): при двух установках на диске (`~/.cache/hyperframes/…/152.0.7928.2` — пришпиленная пакетом, и `~/.cache/puppeteer/…/152.0.7977.42` — чужая) `hyperframes browser path` вернул **вторую**. Значит запись гейта V13 (**R12**) может быть снята на одном растеризаторе, а сборка пойти на другом, и отпечаток это ЗАМЕТИТ (версии разные) — но только если запись гейта и сборка идут через отпечаток. Механизма «использовать именно пришпиленную» у нас нет: команда `browser` умеет `ensure\|path\|clear`, выбора версии не предлагает | [`H-03`](impl/H-03/report.md) §5 п. 1 | **`H-04`** (гейт V13) / **`H-05`** (изоляция окружения) | цена: возможно, `clear` чужого кэша перед `ensure` — но это правка ЧУЖОГО каталога, решение владельца. Цена бездействия: гейт и сборка расходятся молча на машине, где кто-то ставил puppeteer **ЗАКРЫТ `H-05` (2026-08-28); ПРИЧИНА НАЙДЕНА В КОДЕ CLI.** У рендерера ДВА порядка резолва, и они не совпадают: команда `browser path` идёт через `findFromCache` — **puppeteer-кэш ПЕРВЫМ** (`dist/cli.js:114408`), а фактический запуск через `resolveHeadlessShellPath` — env `PRODUCER_HEADLESS_SHELL_PATH` → env `HYPERFRAMES_BROWSER_PATH` → СВОЙ кэш → puppeteer (`dist/cli.js:65272`). То есть preflight мерил один бинарь, а рендерил другой, и это видно прямо в сырых данных спайка: `sp3c/.../network-isolation.json` несёт `versions.chromeHeadlessShell = 152.0.7977.42` и лог того же прогона `Browser launched (HeadlessChrome/152.0.7928.2…)`. **Механизм «использовать именно пришпиленную» появился:** резолвер [`browser.ts`](../packages/renderer-hyperframes/src/browser.ts) выбирает по НАШЕМУ корню (0 — отказ с командой установки, 1 — путь, ≥2 — отказ со списком; `sort().at(-1)` запрещён), чужой puppeteer-кэш не читается вовсе, а выбранный бинарь ПРИШПИЛИВАЕТСЯ рендереру через `HYPERFRAMES_BROWSER_PATH`, причём перебивающая `PRODUCER_HEADLESS_SHELL_PATH` снимается ВСЕГДА. Контур сверяется тестом в трёх точках (резолвер == env == версия из `[BrowserManager] Browser launched`); ответ `browser path` печатается рядом как измерение. Правки чужого каталога (`clear`) не потребовалось | `closed` |
| 161 | **Флаги, которые CLI ставит ВНУТРИ себя, в отпечаток не входят прямо.** `FACT` (SP-3c §6.1): `--font-render-hinting=none` меняет растеризацию текста и назван ADR-0006 §3 как ровно тот флаг, ради которого строка запуска входит в ключ «целиком». Адаптер Chrome не запускает — его запускает `hyperframes` внутри, и снаружи флаг не наблюдаем без запуска. В отпечатке он покрыт КОСВЕННО, версией `pkg.hyperframes`; косвенность значит, что смена флага без смены версии пакета (патч-релиз с тем же номером, форк, переменная окружения CLI) кэш не инвалидирует | [`H-03`](impl/H-03/report.md) §6, кандидат в ADR-0006 §3 | **`H-04`** — измеренная полная строка запуска из трассы рендера, класс проверок `verifyComposition` (не в ключ) | цена бездействия: узкий класс — нужен релиз CLI, меняющий флаги без смены версии **СУЖЕН ДВАЖДЫ.** `H-04` (2026-08-28): строка запуска не только возвращается ответом, но и СВЕРЯЕТСЯ МЕЖДУ ПРОГОНАМИ ГЕЙТА — две разных строки при совпавшей картинке дают класс `FLAKY-по-контейнеру` с причиной, а не PASS ([`gate.ts`](../packages/renderer-hyperframes/src/gate.ts), тест «две разных строки `Browser launched`»). Непокрытое прежнее: **СУЖЕН `H-05` (2026-08-28) измерением.** ИЗМЕРЕНО: CLI печатает `[BrowserManager] Browser launched (HeadlessChrome/<версия>, <режим захвата>, gl=…, headlessShell=…, platform=…)` — И ПРИ `--quiet` тоже. Величина теперь возвращается ответом (`RenderResponse.browserLaunchLine`, `browserLaunchLineOf`) и В КЛЮЧ НЕ ВХОДИТ: её потребитель — класс проверок `verifyComposition` (ADR-0006 §2). Побочно она закрыла контур №160 третьей точкой. **Долг остаётся открытым в суженном виде:** полной командной строки Chrome в ней нет — `--font-render-hinting=none` и прочие флаги, которые CLI ставит внутри себя, снаружи по-прежнему не наблюдаемы | `open` |
| 162 | **Охранник «версия Chrome взята от бинаря, а не от константы» вырождается на машине с ОДНОЙ установкой.** Нарушения Н1 (литерал `CHROME_VERSION`) и Н2 (`dirs.sort().at(-1)` по каталогу кэша) поймал один тест — и поймал потому, что на этой машине версии двух установок РАЗЛИЧАЮТСЯ. Там, где установка одна, константа, сортировка и `--version` дадут одно число, и оба нарушения будут зелёными. Это граница проверяемого, а не дефект теста | [`H-03`](impl/H-03/report.md) §9, [`violation-transcript.txt`](impl/H-03/violation-transcript.txt) | **`H-04`** — условие открытия: перенос сборки на машину с одной установкой Chrome | цена: защитой остаётся ревью кода; альтернатива — подставной резолвер в тесте, но он проверял бы тест, а не окружение **ЗАКРЫТ `H-05` (2026-08-28).** Охранник больше не зависит от того, что лежит на машине: [`browser.test.ts`](../packages/renderer-hyperframes/test/browser.test.ts) СТРОИТ установки сам во временном `HOME` — две в нашем корне ⇒ отказ со списком обеих; одна ⇒ путь; чужой puppeteer-кэш рядом (в том числе с БОЛЕЕ НОВОЙ версией) ⇒ игнор; каталог версии без бинаря ⇒ не установка. Это законно потому, что резолвер бинарь не запускает и версию из него не читает: подставные файлы отвечают на вопрос «какой путь уедет рендереру» ровно так же, как настоящие. Нарушение Н5 протокола воспроизводит дефект №160 живьём: при возврате к `browser path` тест краснеет словами «резолвер выбрал …7928.2…, а запустился 152.0.7977.42» | `closed` |
| 163 | **Правка №154 обнулила покрытие `pixelProfile.jpegQuality` в матрице K1 и уронила четыре теста в ЗАКРЫТЫХ для `H-03` зонах.** После `imageFormat: png` у `final` оба образца матрицы (`final`, `ac4`) стали `png`, и строка view `pixelProfile.jpegQuality` перестала быть достижимой — strict-полнота K1 краснеет. Красные: `schema/test/render-profile.test.ts:249` (якорь `jpeg-quality-missing`), `media/test/assemble-args.test.ts:169` (контрольное `imageFormat === 'jpeg'`), `media/test/cache-matrix.test.ts:207,267`, `media/test/cache-views-golden.test.ts:65`. Правки перечислены поимённо и НЕ применены: `@vpe/schema` — «нет», `media` — «потребляется» (§0 задания), `C-04` §3.2 требует спросить. Ни одно правило при этом не ослабляется — меняется ОБРАЗЕЦ (`render.draft.yaml` остался `imageFormat: jpeg`, `jpegQuality: 80`) | [`H-03`](impl/H-03/report.md) §7 | **ответ владельца** в этой же сессии; иначе **`DOC-06`** | **ЗАКРЫТ `H-03` (2026-08-28) решением владельца (вариант 1 с условием).** Условие «показать грепом, что `render.draft.yaml` — ЖИВОЙ jpeg-профиль» выполнено: `imageFormat: jpeg` + `jpegQuality: 80`, этой задачей не тронут. Сделано: якорь `jpeg-quality-missing` перенесён на `draft`; `FINAL → DRAFT` в негативном тесте `assemble-args`; `draft` добавлен ТРЕТЬИМ образцом `render-profile` в матрицу K1 — покрытие `pixelProfile.jpegQuality` восстановлено, и `golden/cache-matrix.txt` от этого **не изменился ни байтом** (счёт вернулся к прежнему); `golden/cache-key-view.txt` пересчитан штатным `VPE_GOLDEN_UPDATE=1` — дифф РОВНО две строки (`imageFormat: jpeg→png`, `jpegQuality: [90]→[]`). **Одна правка изменила ФОРМУ ПРАВИЛА, а не образец, и названа отдельно:** R-02-утверждение «множества путей `draft` и `final` ОДИНАКОВЫ» после №154 недостижимо по построению (схема при `png` `jpegQuality` запрещает, при `jpeg` требует) и заменено на «различаются РОВНО на `jpegQuality`, и наличие поля у КАЖДОГО профиля равно `imageFormat === 'jpeg'`» — вторая половина обязательна, без неё поле могло бы пропасть из `draft`. Полный прогон: 2029 passed / 1 skipped, `pnpm lint` чист | `closed` |
| 164 | **Охранник «исключение композиции = отказ» читает ТЕКСТОВЫЙ канал.** ИЗМЕРЕНО (`H-05`): исключение в скрипте композиции рендерер отказом не считает — прогон завершается кодом 0 и отдаёт ровно N ЧЁРНЫХ кадров (сегмент валиден по числу кадров, коду выхода и ключу кэша, пуст по содержанию). Поэтому адаптер сам читает `[Browser:PAGEERROR]` из вывода рендерера (`pageErrorsOf`) и отказывает правилом `ADR-0008 композиция`; без этого ОБА критерия готовности `H-05` недостижимы (протокол Н6: негативная фикстура становится зелёной по-неверному). Цена: охранник стоит на ФОРМАТЕ СТРОКИ ЛОГА, а не на структурированном сигнале, и смена формата в новой версии CLI снимет его молча **ПРОВЕРЕН ДЕЛОМ `H-04` (2026-08-28), долг НЕ закрыт.** Живой тест [`gate-render.test.ts`](../packages/renderer-hyperframes/test/gate-render.test.ts) снимает гейт на композиции, бросающей исключение: исход — `error`, а НЕ PASS. Цена бездействия ПЕРЕИЗМЕРЕНА и оказалась выше записанной: без этого охранника N прогонов дали бы N одинаковых наборов ЧЁРНЫХ кадров, то есть один `sha256` и один `framemd5` — **PASS про пустой сегмент**, запись в манифесте и допуск шаблона в библиотеку. Это не «тихая потеря диагностики», а ложно-зелёный гейт| [`H-05`](impl/H-05/report.md) §8 | **`H-04`** либо любое обновление `hyperframes` (условие открытия: смена версии CLI)| цена бездействия: тихая потеря охранника при апгрейде; ловится тестом `isolation-render.test.ts`, который краснеет, если фикстура перестала падать | `open` |
| 165 | **Принудительного потолка памяти нет: «ulimit по RSS» из roadmap на Linux механизмом не существует.** ИЗМЕРЕНО (`H-05`): `prlimit --as=256 МБ` убивает node на инициализации V8 (`Fatal process out of memory: SegmentedTable::InitializeTable`), `--as=64 МБ` — segfault, при копеечном фактическом RSS, потому что `--as` ограничивает ВИРТУАЛЬНОЕ адресное пространство (V8 резервирует его гигабайтами), а не resident set; `ulimit -m` (RLIMIT_RSS) ядром Linux не обеспечивается вовсе. Единственный настоящий потолок — cgroup v2 (`systemd-run --user --scope -p MemoryMax=`), ценой зависимости от systemd-сессии (в headless-CI её может не быть) и SIGKILL от OOM-killer'а вместо диагностируемого отказа. Решение владельца (`H-05`, вопрос 3): потолка нет; принудитель — wall-clock kill, пик RSS дерева МЕРЯЕТСЯ (`stats.peakRssBytes`) | [`H-05`](impl/H-05/report.md) §6 | **условие открытия:** появление CI либо момент, когда бюджет RAM станет проблемой (ADR-0008 «Параллелизм»: `chapterParallelism` × пик RSS ≤ RAM). Кандидат в правку roadmap §4.9 `H-05` | цена бездействия: сегмент, съевший всю память, останавливает машину до срабатывания wall-clock kill; число для проверки бюджета при этом есть |
| 166 | **Сетевой изоляции нет на macOS и Windows.** `unshare -rn --map-root-user` — linux-специфичный механизм; на других ОС `RenderOptions.isolation: 'none'` окажется единственным рабочим режимом, и **R1** там держится только на четырёх `HYPERFRAMES_NO_*`, которые глушат каналы CLI, а не запрещают сеть. Дефолт `netns` (решение владельца, вопрос 1) на такой машине даст отказ preflight'а с инструкцией — это осознанный выбор: отказ лучше тихого рендера с доступной сетью | [`H-05`](impl/H-05/report.md) §2 | **условие открытия:** появление машины не на Linux в контуре сборки | цена бездействия: нулевая, пока сборка идёт только на Linux; при переносе — либо явное `isolation: 'none'` с записанным ослаблением R1, либо второй механизм (sandbox-exec / контейнер) |
| 167 | **Заморозка D4 не покрывает отложенные вызовы шаблона мимо таймлайна.** Guard бросает во взведённом окне, и взвод покрывает три места: модульный код шаблонов, `mount(host, ctx)` и колбэки, переданные в `ctx.timeline` (`guardedTimeline` подменяет функции внутри объектов параметров). Код шаблона, попавший в `setTimeout`, `requestAnimationFrame` или обработчик события, исполняется ВНЕ этих окон и не охраняется. Безусловный guard невозможен по измерению (см. №2). Вендорный код — GSAP и рантайм рендерера — не охраняется намеренно | [`H-05`](impl/H-05/report.md) §3 | **`H-06`** — там появляются настоящие шаблоны, и там же видно, нужен ли шаблонам отложенный вызов вообще; при «не нужен» правило усиливается запретом в ревью шаблона, а не расширением guard'а | цена бездействия: шаблон, спрятавший недетерминизм в `setTimeout`, проходит обе половины D4; ловится гейтом V13 (**R12**) как расхождение прогонов **ИЗМЕРЕНО `H-06` (2026-08-29): ОТЛОЖЕННЫЙ ВЫЗОВ ШАБЛОНАМ НЕ ПОНАДОБИЛСЯ НИ РАЗУ.** Греп по всем пяти реализациям — `setTimeout`, `setInterval`, `requestAnimationFrame`, `queueMicrotask`, `addEventListener`: **ноль совпадений**. То есть ветка «при „не нужен“ правило усиливается запретом в ревью шаблона, а не расширением guard'а» — та, которая наступила, и долг сужается до неё: нужен ЗАПРЕТ (греп-линт по `mountSource`, как у D4/D5), а не безусловный guard, невозможный по измерению №2. Адрес — **`E-00`/`H-07`** (ревью шаблона). Пока запрета нет, дыра та же, но она теперь ИЗМЕРЕНА пустой, а не предполагалась | `open` |
| 168 | **`clip.frames` живёт в ДВУХ ФОРМАХ, и первый же настоящий IR через адаптер даст `NaN`-окна.** ИЗМЕРЕНО (`H-04`): модель типизирует `FrameInterval = {frameStart, frameEnd}` ([`core-model/src/time/interval.ts:37`](../packages/core-model/src/time/interval.ts)), и ровно её кладёт компилятор ([`compile/src/render-ir/build.ts:160`](../packages/compile/src/render-ir/build.ts)); а рантайм композиции читает `clip.frames.start`/`.end` ([`composition/runtime.js:141`](../packages/renderer-hyperframes/src/composition/runtime.js)), и фикстура `H-01` ([`test/fixture.ts`](../packages/renderer-hyperframes/test/fixture.ts)) написана по форме РАНТАЙМА. Обе стороны зелены только потому, что компилятор и адаптер ещё ни разу не встречались на одном значении: `data-start` тогда станет `NaN`, а клип — невидимым. Прибор гейта (`where.windowOf`) читает обе формы НАМЕРЕННО и с этим комментарием: промах дал бы отчёт «ни один клип не виноват» на настоящем расхождении | [`H-04`](impl/H-04/report.md) §8 | **`L-01`** — там формы встретятся впервые; либо `H-06` | цена бездействия: тихо пустой сегмент на первом же настоящем прогоне компиляторского IR. Правка — одна из двух сторон, и выбор стороны есть решение владельца (форма модели или форма рантайма) **ПОМЕТКА `H-06` (2026-08-29), БЕЗ СМЕНЫ АДРЕСА: долг НЕ РАСШИРЕН, и это проверено охранником.** Пять реализаций окно клипа берут ТОЛЬКО из `ctx.frames`, а `clip.frames` не читают ни разу — включая правку R2 в `runtime.js`, которая пользуется `group.frames` (субтитры), а не окном клипа. Ассерт стоит в [`templates.test.ts`](../packages/renderer-hyperframes/test/templates.test.ts) («ни одного лишнего окна из `clip.frames`»), поэтому шестая реализация не сможет добавить третью точку разъезда молча. Формы по-прежнему две, встретятся при `L-01` **ЗАКРЫТ `L-01` (2026-08-30) СТОРОНОЙ МОДЕЛИ — решение владельца `H-04`.** Канон — `FrameInterval` модели (`{frameStart, frameEnd}`); правился ПОТРЕБИТЕЛЬ, и его оказалось больше, чем две точки из формулировки долга: `composition/runtime.js` (окно клипа, окно группы субтитров И интервал подсветки слова — `IrCaptionGroup.frames`/`IrCaptionToken.highlight` тот же `FrameInterval`), четыре реализации шаблонов через `ctx.frames`, обе тестовые `solid@1`, билдеры фикстур рендерера и `cli`, а также `cli/src/budget.ts` — ТРЕТЬЯ форма, жившая своим типом `BudgetClip` с пометкой «нормализует вызывающий». `where.windowOf` НЕ тронут намеренно (решение владельца): двойное чтение остаётся охранником от повторного разъезда. Проверено делом на ПЕРВОМ настоящем IR через адаптер — [`build.test.ts`](../packages/cli/test/build.test.ts) «`NaN`-окон нет»: в `ir.json` каталога композиции у каждого клипа ровно пара `frameStart`/`frameEnd`, все `data-duration` статической разметки — числа, а встроенный рантайм читает модельную форму. Протокол Н4 (вернуть `{start,end}` в `runtime.js` руками) краснит именно этот тест. СЛЕДСТВИЯ, обе штатные: (а) восемь `gate-requests/*.json` перегенерированы билдером; (б) записи гейта в репозитории УСТАРЕЛИ по `bundleHash` — пары до/после в [`L-01`](impl/L-01/report.md) §2, пере-снимает владелец по [runbook](gate-runbook.md) | `closed` |
| 169 | **Склейку порта `GateMedia` повторяет каждый вызывающий.** Кодирование кадров живёт в `media`, стрелки `renderer-hyperframes → media` в карте ADR-0009 нет, поэтому `runGate` принимает измерение ВХОДОМ. Склейка (`buildSegmentArtifact` + `framemd5Of`) сегодня написана в браузерном тесте и в README; вторая её копия появится в `vpe template gate`, и разойтись они могут молча — например, разным `pixelProfile` | [`H-04`](impl/H-04/report.md) §7 | **`E-00`** | цена бездействия: гейт снят на одном профиле энкодера, сборка идёт на другом — обе величины записи описывают не тот файл. Лечится тем, что склейку пишет ОДНА функция `E-00`, а тест её же и зовёт  **ЗАКРЫТ `E-00` (2026-08-29).** Склейка — ОДНА функция [`createGateMedia`](../packages/renderer-hyperframes/src/gate-media.ts) (`buildSegmentArtifact` + `framemd5Of` + `mkdir` каталога выхода); обе функции `media` приезжают ЗНАЧЕНИЕМ, типы параметрические, поэтому каст остаётся на значении профиля, а не на функции. Вызывающих по-прежнему двое, и оба зовут ЕЁ: браузерный тест `H-04` ([`gate-render.test.ts`](../packages/renderer-hyperframes/test/gate-render.test.ts), копия удалена) и команда [`vpe template gate`](../packages/cli/src/template-gate.ts). Стрелка `renderer-hyperframes → media` не появилась — охранник [`boundaries.test.ts`](../packages/renderer-hyperframes/test/boundaries.test.ts) зелёный. Побочно закрыта и названная цена долга («гейт снят на одном профиле энкодера, сборка идёт на другом»): полный `pixelProfile` приезжает в команду ФАЙЛОМ `--render-profile`, и его `profileId` сверяется с `--profile`, а три поля адаптера — с запросом | `closed` |
| 170 | **Записи гейта негде жить.** `runGate` отдаёт `GateRecord`, `GateRecordSchema` её принимает, `assertBuildMayStart` её читает — но файла `<id>@<n>.gates.json` (решение владельца `H-04`, вопрос 1, вариант «б») никто не пишет и никто не читает при регистрации спека. То есть контур R12 замкнут в памяти процесса и разомкнут на диске | [`H-04`](impl/H-04/report.md) §7 п. 1 | **`E-00`** + правка `TS-01` (форма хранения манифеста собирается из двух мест: TS-литерал + json-файл записей) | цена бездействия: нулевая до появления настоящих шаблонов (`H-06`); после — гейт придётся снимать заново на каждой сборке  **ЗАКРЫТ `E-00` (2026-08-29).** Файл `<id>@<N>.gates.json` пишется командой (только `PASS`), читается при сборке каталога и доезжает до `manifest.gates`. Форма — семейство `template-gates/1` ([`gates-file.ts`](../packages/templates-spec/src/gates-file.ts)): `schema`, `templateId`, `templateVersion`, `entries[]`, где запись = семь полей `GateRecord` ПЛЮС `bundleHash` (решение владельца `E-00`, развилка 3). Две половины по границе пакетов: правило слияния — чистое, в `templates-spec` (диск там запрещён охранником `templates-spec-imports`, обоснование R3), `readdir`/`readFile` — в [`renderer-hyperframes/src/library.ts`](../packages/renderer-hyperframes/src/library.ts). Отказы: файл без спека (с полным путём и парой — поправка П1), имя внутри ≠ имени файла, записи И в коде И в файле. Спек без файла — законен (ноль записей, `UNGATED`) | `closed` |
| 171 | **`bin/render-segment` проверяет пару по ФИКСТУРНОМУ реестру спеков.** `createRegistry(FIXTURE_TEMPLATES)` — пять спеков фикстуры с `gates: []`, то есть `require` в подпроцессе сегодня отказывает ВСЕГДА. По существу это верный ответ (ни один шаблон гейта не проходил), но источник реестра обязан смениться вместе с появлением библиотеки | [`H-04`](impl/H-04/report.md) §10 | **`E-00`**/`H-06` | цена бездействия: нулевая (подпроцесса пока зовут только тесты, и они называют `--gate-skip` с причиной); при `L-02` — одна строка  **ЗАКРЫТ `E-00` (2026-08-29).** `bin/render-segment` собирает реестр `loadTemplateLibrary()` — прод-каталог из ДВУХ мест (пять единиц `TEMPLATE_LIBRARY` + записи с диска). Ответ сегодня прежний (`require` отказывает: реализаций нет до `H-06`, записей ноль), но причина другая: раньше источник был заведомо пуст, теперь гейт действительно не снят. Заодно снято враньё имени: `FIXTURE_TEMPLATES` → `TEMPLATE_LIBRARY` целиком, без алиаса (точечное разрешение владельца на механическую замену идентификатора в `packages/compile/test/**`) | `closed` |
| 172 | **`where` меряет bbox/PSNR только на двух прогонах.** Кадры держатся у прогона №1 и первого разошедшегося (потолок 2× вместо N×, решение владельца `H-04`); если на десяти прогонах разойдётся ТРЕТИЙ вариант, его кадров на диске не будет. Номера расходящихся кадров и раскладка по клипам при этом сохраняются — они читаются из `framemd5`, а не из PNG | [`H-04`](impl/H-04/report.md) §5 | **условие открытия:** первый FAIL с тремя и более вариантами картинки | цена бездействия: у третьего варианта нет bbox/PSNR; альтернатива — держать N×кадры всегда, то есть платить дисками за случай, который может не наступить | `open` |
| 173 | **`TRANSFORM_ORDER` объявлен данными, но поведением не проверен ничем.** Реестр `TS-02` фиксирует порядок `translate → scale` явно (`FACT` SP-3c §6.2 п. 3: `gsap/dist/gsap.js` строки 5091–5121 дописывают `translate3d(...)` раньше `scale(...)`; на Ken Burns разница с обратным порядком — до 5.4 px на последнем кадре). Это ОБЪЯВЛЕНИЕ: реализаций шаблонов сегодня нет вовсе (`src/templates/` рендерера пуст до `H-06`), и следовать порядку некому. Ни один охранник не связывает константу с пикселями: линт ищет `Math.*`, parity проверяет кривые, схема манифеста порядка не объявляет. Протокол нарушений это подтвердил делом — сломать `TRANSFORM_ORDER` руками нечем. Плюс вторая половина: названы ДВА члена из пяти, которые собирает GSAP (`perspective → translate → rotate → skew → scale`), — те, которыми пользуются шаблоны v1; для `rotate`/`skew` порядок не объявлен, потому что его никто не двигает | [`TS-02`](impl/TS-02/report.md) §6 | **`H-06`** — первая реализация, двигающая и сдвиг, и масштаб (`kenburns@1`) | цена: тест на кадре с переставленным порядком (до реализаций сравнивать нечего, эталона не существует); цена бездействия: константа, на которую никто не ссылается, — комментарий, а не правило, и первый же шаблон вправе собрать `transform` как ему удобно **ЗАКРЫТ `H-06` (2026-08-29) ДВУСТОРОННИМ ОХРАННИКОМ — и каждая половина ПОКАЗАНА ПАДАЮЩЕЙ.** Половина первая, ПОВЕДЕНИЕ: `kenburns@1` ставит пробу на свой пустой `host` (`{x: 1, y: 1, scale: 2}` — ненулевые, иначе gsap вправе не собрать `translate` вовсе) и требует, чтобы `TRANSFORM_ORDER[0]` стоял в собранной строке раньше `[1]`; иначе `mount` бросает, и гейт даёт `error`. Половина вторая, ТЕКСТ: юнит [`templates.test.ts`](../packages/renderer-hyperframes/test/templates.test.ts) требует, чтобы ни один `mountSource` не ПРИСВАИВАЛ `style.transform`. Протокол нарушений уронил обе поодиночке (Н3а — перевёрнутое ожидание пробы ⇒ `error` гейта; Н3б — ручная сборка ⇒ красный юнит), и ни одна не покрывает другую: первая ловит сменившийся gsap, вторая — руку автора. **`FACT` SP-3c §6.2 п. 3 ПОДТВЕРЖДЁН ЖИВЬЁМ, а не чтением исходников:** отказ пробы назвал строку, которую gsap 3.15.0 собрал в этом браузере — `translate(1px, 1px) scale(2, 2)`. Вторая половина прежней формулировки («для `rotate`/`skew` порядок не объявлен») остаётся как была: их по-прежнему никто не двигает | `closed` |
| 174 | **Линт D5 стережёт ТРИ имени, а семейство implementation-approximated шире.** `FORBIDDEN` в [`d5-easing-render-path.test.ts`](../tests/lints/d5-easing-render-path.test.ts) — `Math.pow`, `Math.sin`, `Math.exp`: ровно те, что названы правилом D5 и ADR-0007 §3 дословно. ECMA-262 не гарантирует воспроизводимости и у соседей — `Math.cos`, `Math.tan`, `Math.log`, `Math.log2`, `Math.expm1`, `Math.hypot`, `Math.cbrt` (`Math.sqrt` в это семейство НЕ входит: он округляется точно по IEEE-754, и запрещать его было бы ошибкой). Сегодня их в рендер-пути ноль, то есть долг — про будущую дыру, а не про текущее нарушение. Расширять список молча нельзя: это правка ФОРМУЛИРОВКИ правила, а не теста | [`TS-02`](impl/TS-02/report.md) §6 | **владелец** (формулировка D5) — исполнение при `H-06`, где появится первый шаблон с тригонометрией | цена: одна строка в `FORBIDDEN` за каждое имя плюс решение, какие имена входят; цена бездействия: кривая, посчитанная через `Math.cos`, пройдёт мимо охранника, хотя ломает V13 ровно так же, как `Math.sin` **ПОМЕТКА `H-06` (2026-08-29): ШАБЛОНА С ТРИГОНОМЕТРИЕЙ НЕ ПОЯВИЛОСЬ.** Пять реализаций не зовут `Math` ВООБЩЕ ни в одной форме — движение целиком отдано именованным кривым gsap (`power2.inOut` у `kenburns@1`, `power3.out` у `flash@1`), то есть исполнение долга по-прежнему не на чем проверить. Адрес прежний — владелец (формулировка D5). **Рядом измерена СОСЕДНЯЯ дыра того же правила, и она шире этой:** имя кривой ВНЕ реестра, написанное литералом в тексте `mountSource`, не ловится ни типом, ни юнитом, ни гейтом — новый долг №184 | `open` |
| 175 | **Комментарий в ЗАКРЫТОЙ зоне цитирует несуществующее имя.** [`compile/src/timeline/compose.ts:71`](../packages/compile/src/timeline/compose.ts) говорит «тесты строят свой (`createRegistry(FIXTURE_TEMPLATES)`…)», а идентификатора `FIXTURE_TEMPLATES` в репозитории больше нет (`E-00` переименовал его в `TEMPLATE_LIBRARY`). Правка одной строки НЕ сделана намеренно: разрешение владельца распространялось на `packages/compile/test/**`, а это `src/**` | [`E-00`](impl/E-00/report.md) §1 | первая задача, открывающая `compile` (`CP-*`/`L-01`), либо `DOC-06` | цена бездействия: греп по `FIXTURE_TEMPLATES` находит одно попадание — комментарий; кода не касается | `open` |
| 176 | **Фикстура шаблона приезжает ФАЙЛОМ запроса, механизма «фикстура шаблона» в проекте нет.** `vpe template gate --request <файл>` (решение владельца `E-00`, развилка 1) принимает готовый `SegmentRenderRequest`; охранник «все клипы зовут названный шаблон» защищает от подмены, но САМ ЗАПРОС автор собирает руками. Настоящая фикстура (params, окно кадров, ассеты) — свойство шаблона и появится вместе с реализациями | [`E-00`](impl/E-00/report.md) §4 | **`H-06`** (спеки получат фикстуру) либо `L-01` (сборка запроса из проекта) | цена бездействия: нулевая до `H-06` — снимать гейт не на чем; после: автор может подать запрос, не покрывающий кадры, где слой рисуется (roadmap §5.4 — «зелёный гейт ни о чём») **ПОМЕТКА `H-06` (2026-08-29): ПОЛОВИНА ПОЯВИЛАСЬ, МЕХАНИЗМА — НЕТ.** `params` пяти шаблонов теперь лежат ОДНИМ местом и взяты ДОСЛОВНО из режиссуры — `FIXTURE_PARAMS` в [`renderer-hyperframes/test/fixture.ts`](../packages/renderer-hyperframes/test/fixture.ts), сверка со схемами спеков стоит юнитом. Но это фикстура ТЕСТА, а не свойство ШАБЛОНА: спек её по-прежнему не несёт, и `vpe template gate --request <файл>` по-прежнему принимает запрос, собранный руками. Новый адрес — **`L-01`** (сборка запроса из проекта) | `open` |
| 177 | **Команда не проверяет `params` клипов схемой спека.** `vpe template gate` валидирует ФОРМУ запроса (`validateRequest`, ADR-0008) и то, что клипы зовут названный шаблон, но `paramsSchema` шаблона к ним не применяет — это делает компилятор (`CP-07`). Гейт можно снять на `params`, которые сборка отвергнет | [`E-00`](impl/E-00/report.md) §4 | `H-06` (там появятся настоящие `params`) либо `L-01` | цена бездействия: запись гейта описывает пару, которой в сборке не будет; ловится первым же прогоном `vpe build` — то есть поздно, но не молча **ПОМЕТКА `H-06` (2026-08-29): НЕ ИСПОЛНЕН, адрес сужен до `L-01`.** Настоящие `params` появились, но проверять их схемой команда не начала — эта сессия команду не трогала. Цена бездействия при этом ИЗМЕРЕНА и оказалась выше объявленной: протокол нарушений (Н1в) показал, что гейт даёт **PASS** на шаблоне, чей `mountSource` зовёт кривую вне реестра, — то есть запись гейта может описывать не только «пару, которой не будет в сборке», но и пару, которая сборкой ЗАПРЕЩЕНА | `open` |
| 178 | **`vpe template list` не проверяет устаревание записей.** Таблица печатает класс записи как он лежит в файле; сверка с парой (профиль, отпечаток, композиция) требует пробы окружения (`H-03`: ffmpeg + дерево зависимостей), а просмотр каталога обязан работать и там, где движок не установлен. Строка «устаревание здесь не проверяется» печатается явно | [`E-00`](impl/E-00/report.md) §6 | условие открытия: у автора появится ≥ 2 машин или запись, устаревшая незаметно; тогда — флаг `--check-env` | цена бездействия: названа в самом выводе команды | `open` |
| 179 | **Вторая фикстура запроса рендерера.** [`packages/cli/test/fixture.ts`](../packages/cli/test/fixture.ts) повторяет форму запроса из [`renderer-hyperframes/test/fixture.ts`](../packages/renderer-hyperframes/test/fixture.ts): импорт тестового файла чужого пакета не собирается `tsc --build` (файл вне `rootDir`), а выносить тестовые помощники в публичную поверхность пакета нельзя. Две фикстуры разъедутся при первой правке контракта ADR-0008 | [`E-00`](impl/E-00/report.md) §4 | условие открытия: ТРЕТЬЯ копия либо правка формы `SegmentRenderRequest`; тогда — общий тестовый пакет `@vpe/test-fixtures` | цена бездействия: правка контракта чинится в двух местах, и вторая покраснеет | `open` |
| 180 | **Файл записей гейта пишется одной строкой (`canonicalJson`).** Форма каноническая (ключи отсортированы, одна форма записи одного факта — ADR-0007 §3; `JSON.stringify` запрещён линтом в `src/**`), но файл коммитит ЧЕЛОВЕК, и построчного диффа у него нет: две записи в файле дают один длинный ряд | [`E-00`](impl/E-00/report.md) §3 | условие открытия: у шаблона появятся ОБЕ записи (`final` + `draftHalf`) и первый конфликт в git; тогда — канонический pretty-print в `@vpe/schema` | цена бездействия: дифф записи читается целиком, а не построчно | `open` |
| 181 | **Охранник команды `vpe template gate` не пропускает СМЕШАННЫЙ запрос, а `kenburns@1` без него неизмерим.** Команда требует, чтобы КАЖДЫЙ клип запроса звал названный шаблон (`E-00` §4) — защита от подмены. Но `kenburns@1` по решению владельца `TS-01` (вопрос 5) `declareAssets` пуст и двигает слой НИЖЕ себя, поэтому запрос из одних `kenburns@1` вырожден по построению: под ним нечего двигать, и его реализация обязана дать `error` (поправка П1-б, проверено тестом). Значит гейт этого шаблона снимается только на паре `[still@1, kenburns@1]` — и ровно такой запрос команда сегодня отвергнет. Тесты `H-06` зовут `runGate` напрямую и потому зелены; ручной гейт владельца упрётся **ЗАКРЫТ `FIX-01` (2026-08-29) СМЯГЧЕНИЕМ ПО ФОРМУЛИРОВКЕ ВЛАДЕЛЬЦА.** Правило теперь: «запрос гейта вправе нести шаблоны-ОСНОВАНИЯ; запись пишется по НАЗВАННОМУ». Три условия вместо одного — названный обязан присутствовать хотя бы одним клипом; соседи обязаны быть ИЗ БИБЛИОТЕКИ (то есть иметь спек и собственную запись); пустой запрос по-прежнему отказ. Функция переименована по смыслу: `assertRequestCallsTemplate` → `assertRequestCarriesTemplate`. Защита от ПОДМЕНЫ, ради которой охранник писался, не ослабла, а разложилась на две проверяемые половины (названный рисует в этом файле; соседи — не произвольный код). Юниты — [`template-gate.test.ts`](../packages/cli/test/template-gate.test.ts): смешанный `still+kenburns` проходит И запись ложится по названному (`still@1.gates.json` есть, `kenburns@1.gates.json` нет), запрос без названного — отказ, сосед вне библиотеки (`solid@1`) — отказ. **ЖИВАЯ РЕПЕТИЦИЯ РУЧНОГО ГЕЙТА ВЛАДЕЛЬЦА ПРОЙДЕНА:** `vpe template gate kenburns@1 --profile draftHalf` на смешанном запросе ⇒ `ГЕЙТ: PASS`, N = 3, один `sha256` `666084110f838c12…2819` и один `framemd5` `5473d656235cd9bd…08a5` из трёх прогонов, запись создана файлом, путь напечатан, код 0. Это первый гейт `kenburns@1`, снятый КОМАНДОЙ, — прежним правилом он был неисполним | [`H-06`](impl/H-06/report.md) §5, [`templates-gate.test.ts`](../packages/renderer-hyperframes/test/templates-gate.test.ts) шапка | **`E-00`** (смягчение охранника) — решение владельца ДО его ручных гейтов | цена: правило «клипы ЗВАНОГО шаблона обязательны, соседние — допустимы» плюс проверка, что званый шаблон в запросе есть; цена бездействия: `kenburns@1` — единственный из пяти, кто двигает пиксели, и единственный, на ком гейт `final` осмыслен (решение владельца, вариант в1), — не получит записи вовсе | `closed` |
| 182 | **Дефект масштаба: при `scale < 1` композиция сжимается в ЛЕВУЮ ВЕРХНЮЮ ЧЕТВЕРТЬ кадра, остальное прозрачно.** ИЗМЕРЕНО (`H-06`): масштаб применяется ДВАЖДЫ — рендерер уменьшает вьюпорт и мы раскрываем `scale` на `#root`. Воспроизводится и на синтетическом `solid@1` (`H-01`), значит дефект существовал до реализаций шаблонов и в зону `H-06` не входит. Гейты `draftHalf` при этом ЗЕЛЁНЫЕ и остаются осмысленными: гейт мерит СОВПАДЕНИЕ прогонов, а кривая картинка воспроизводится не хуже верной. Но всё, что проверяет КАРТИНКУ, обязано идти на `scale: 1` — так и сделано в [`captions-visibility.test.ts`](../packages/renderer-hyperframes/test/captions-visibility.test.ts), ценой 1080×1920 вместо 270×480 **ЗАКРЫТ `FIX-01` (2026-08-29) ИЗМЕРЕНИЕМ, КОТОРОЕ НАЗВАЛО ЛИШНЮЮ ПОЛОВИНУ.** Масштаб раскрывает САМ рендерер (размер выхода он берёт из `data-width`/`data-height` корня), а мы раскрывали его второй раз трансформой `#root { transform: scale() }` — снята. `FACT` (зонд по пикселям на `solid@1`, доля НЕЧЁРНЫХ по квадрантам кадра): было `100 / 0 / 0 / 0` при `scale: 0.5`, стало `100 / 100 / 100 / 100`; геометрия выхода прежняя (540×960), то есть `scale` не потерян, а применён РОВНО ОДИН раз. Какая из двух простановок лишняя — решено опытом, а не вкусом: опыт «Б» (снята только трансформа, `html`/`body` оставлены масштабированными) даёт те же четыре сотни, поэтому несущая правка ровно одна; приведение `html`/`body` к базовой геометрии взято ОТДЕЛЬНЫМ куском за когерентность и при `scale === 1` не меняет ни байта. Охранник — новый браузерный [`scale-render.test.ts`](../packages/renderer-hyperframes/test/scale-render.test.ts) (форма ассерта — квадранты, потому что ЭТАЛОНА верного кадра не существует и PSNR сравнивать не с чем); показан падающим (протокол `FIX-01`, Н1). `bundle.hash` при `scale === 1` НЕ изменился (`702dc5e6…db53` до и после) — правка не касается полного профиля ни байтом | [`H-06`](impl/H-06/report.md) §7 | **`H-01fix`** — отдельной задачей, ДО ручных гейтов владельца | цена: одна из двух простановок масштаба лишняя, выбор стороны — измерение; цена бездействия: `draftHalf` (`scale: 0.5`) и `ac4` (`0.25`) дают кадры, по которым нельзя смотреть глазами, а **R13** (`H-02`) на них неизмерим | `closed` |
| 183 | **Профиль `draftHalf` живёт в ДВУХ файлах, и живая сборка на фикстурном по-прежнему невозможна.** `H-06` завёл [`gate-profiles/draftHalf.yaml`](../packages/renderer-hyperframes/gate-profiles/draftHalf.yaml) (решение владельца, вариант п1), потому что `fixtures/minimal/profiles/render.draft.yaml` несёт `imageFormat: jpeg`, а адаптер такой формат отказывает — то есть команда гейта на фикстурном профиле падала бы на КАЖДОМ шаблоне (тот же №154, но на `draft`). Фикстуру не правили намеренно: она единственная держит покрытие матрицы **K1** по `jpegQuality` (долг №163). Итог — два файла с одним `profileId: draftHalf`, различающиеся ровно `imageFormat`/`jpegQuality`; охранник разъезда — [`gate-profile.test.ts`](../packages/renderer-hyperframes/test/gate-profile.test.ts) (сверяет ВСЕ прочие поля) | [`H-06`](impl/H-06/report.md) §4 | **`L-01`** — там формы встретятся на настоящем ролике | цена: решение владельца, какой из двух файлов есть `draftHalf` проекта, и что делать с покрытием K1 у второго; цена бездействия: два ответа на вопрос «что такое `draftHalf`» держатся тестом, а не одним источником | `open` |
| 184 | **Кривая ВНЕ закрытого реестра **D5**, написанная литералом в ТЕКСТЕ `mountSource`, не ловится НИЧЕМ.** ИЗМЕРЕНО (`H-06`, протокол нарушений Н1в): `ease: 'elastic.out'` в тексте `flash@1` проходит `tsc` (текст функции есть строка, `satisfies EasingId` его не видит), проходит юнит Н1 (он фильтрует литералы через `isEasingId` и потому имя ВНЕ реестра отбрасывает вместе с прочим текстом — он ловит обратный случай, имя ИЗ реестра, не объявленное манифестом), проходит линт D5 (`tests/lints/d5-easing-render-path.test.ts` ищет `Math.pow/sin/exp`, а не имена кривых) и **проходит гейт V13**: три прогона дали один `sha256` `cd853a1f…`, класс PASS. Кадры при этом ДРУГИЕ — объявленная `power3.out` даёт `bb18d149…` **ЗАКРЫТ `FIX-01` (2026-08-29) ИНВЕРСИЕЙ ФИЛЬТРА, А НЕ НОВЫМ ЛИНТОМ.** Дыра была ровно в фильтре: `isEasingId` отбрасывал имя ВНЕ реестра вместе с прочим текстом, то есть юнит проверял обратный случай. Теперь литералы `mountSource` фильтруются по ФОРМЕ («похоже на имя кривой»), и каждый пойманный обязан пройти ДВЕ проверки — членство в реестре **D5** и объявленность манифестом. Форма выбрана измерением: `<слово>.<слово>` с необязательными скобками (без них мимо прошёл бы `back.out(2.5)` при законном `back.out(1.7)` в реестре) плюс точное `none`. `FACT` (`FIX-01`): по РАЗРЕШЁННЫМ текстам пяти реализаций плюс `solid@1` — **102 строковых литерала, 1 срабатывание** (`power3.out`, объявлен), ложных НОЛЬ. Юнит, а не греп в `tests/lints/`, — по цене И по точности: юнит видит разрешённый `mountSource` вместе с интерполяциями (`TRANSFORM_ORDER`), а греп по исходнику их не видит и тащит четыре ложных из `freeze.js`. Вставка Н1в (`ease: 'elastic.out'`) показана красной дважды: пробой внутри теста и живым нарушением Н3 протокола `FIX-01`. Остаток формы вынесен долгом №190 | [`H-06`](impl/H-06/report.md) §6 (Н1в), транскрипт [`violation-transcript.txt`](impl/H-06/violation-transcript.txt) | **`TS-02`**/`E-00` — греп-линт по `mountSource`: «имя, похожее на кривую gsap (`<слово>.<слово>` либо `none`), обязано быть в реестре» | цена: одна регулярка в `tests/lints/` плюс решение, что считать «похожим на кривую»; цена бездействия: гейт выдаёт запись «шаблон детерминирован» про кадры, нарисованные кривой, которой в реестре нет, — то есть V13 охраняет не то, что D5 объявляет | `closed` |
| 185 | **Две строки видимости R2 в `runtime.js` — no-op ПО КАРТИНКЕ, а их охранник стережёт работу рендерера, а не наш код.** ИЗМЕРЕНО (`H-06`, протокол нарушений Н6, три пробы): (1) удаление обеих строк доезжает до композиции (`capsTimeline.set` в `index.html` — 0); (2) кадры БЕЗ них всё равно переключаются на границе групп (кадры 0..5 — один md5, 6..11 — другой); (3) кадры С ними — ПОБАЙТОВО те же. Значит видимость групп держит САМ `hyperframes@0.8.5` по атрибутам `data-start`/`data-duration`, которые `runtime.js` ставил и ДО правки R2. Отсюда: находка прежней сессии «эти атрибуты не читал НИКТО, все группы показывались друг на друге» неверна, а [`captions-visibility.test.ts`](../packages/renderer-hyperframes/test/captions-visibility.test.ts) остаётся зелёным при удалённых строках. Ни строки, ни охранник в этой сессии НЕ ТРОГАЛИСЬ: R2 — решение владельца, его пересмотр есть переделка Ш1–Ш4 **ЗАКРЫТ `FIX-01` (2026-08-29) ВАРИАНТОМ (а) ВЛАДЕЛЬЦА: СТРОКИ СНЯТЫ.** Снято ТРИ строки, а не две, и причина названа: вместе с двумя `capsTimeline.set` уходит `el.style.opacity = '0'` — оставленная в одиночку, она сделала бы группы невидимыми ВОВСЕ, то есть это было бы другое нарушение, а не снятие no-op. Следом снят `capsTimeline`, ставший мёртвым. Охранник ОСТАВЛЕН и переосмыслен: [`captions-visibility.test.ts`](../packages/renderer-hyperframes/test/captions-visibility.test.ts) стережёт теперь ВЕНДОРНОЕ поведение — связку «атрибуты `data-start`/`data-duration` в разметке → переключение групп в кадрах», — и версия вендора названа В ТЕКСТЕ ЧИСЛОМ: «пришпилено `hyperframes@0.8.5`; покраснение при обновлении вендора = сигнал, не поломка». Новый смысл ДОКАЗАН нарушением: на снятии трёх строк `opacity` файл был зелёным (Н6 протокола `H-06`), на снятии `data-start` он краснеет на кадрах (Н4 протокола `FIX-01`) | [`H-06`](impl/H-06/report.md) §6 (Н6), транскрипт [`violation-transcript.txt`](impl/H-06/violation-transcript.txt) | **владелец** (оставить как второй эшелон либо снять) + **`H-02`** (там же живёт **R13**, которому нужна та же видимость) | цена: решение владельца плюс переписанный охранник — он обязан отличать НАШУ видимость от рендереровой (например, снимая атрибуты `data-*` и требуя, чтобы картинка не изменилась); цена бездействия: строки выглядят несущими, тест выглядит их охранником, и оба утверждения ложны — следующий, кто их тронет, узнает это не от теста | `closed` |
| 186 | **Эмфаза `bold` НЕРАЗЛИЧИМА на единственном font-record проекта, и тест закреплён на этом факте.** ИЗМЕРЕНО (`H-06`): кадры внутри и вне окна `captionEmphasis@1` совпадают ПОБАЙТОВО. Причина — v1-правило «ровно один font-record», а этот record есть `DejaVuSans-Bold.ttf`: `@font-face` без дескриптора веса объявляет его гарнитурой веса `normal`, и синтетическое утолщение поверх уже жирных глифов Chrome не накладывает. Та же проба БЕЗ `font-family` (системная гарнитура) кадры РАЗЛИЧАЕТ — механизм рабочий, невыразим вход. Утверждение теста стоит `bold == normal` С КОММЕНТАРИЕМ: его покраснение будет означать «у проекта появилась гарнитура с настоящими начертаниями», а не поломку | [`H-06`](impl/H-06/report.md) §7, [`captions-visibility.test.ts`](../packages/renderer-hyperframes/test/captions-visibility.test.ts) шапка | **№13** / `M-02` / `H-07` — вместе с выбором шрифта канала | цена: второй font-record (или дескрипторы веса у одного) плюс переворот одного утверждения теста на `false`; цена бездействия: единственный визуальный эффект `captionEmphasis@1` не наблюдаем в кадрах, и его охраняет только юнит по механизму | `open` |
| 187 | **Настоящий шрифт гейта приезжает ИЗ МАШИНЫ, а не записью проекта.** `makeTemplateFixture` читает `/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf` напрямую и падает, если его нет (падает, а не пропускает — тот же порядок, что у браузерных файлов). Подделка не годится по измерению: с 12-байтовым `TTF_STUB` `@font-face` не грузится, `font-display: block` держит текст невидимым ~3 с, и в кадры въезжает ТАЙМЕР вместо типографики | [`renderer-hyperframes/test/fixture.ts`](../packages/renderer-hyperframes/test/fixture.ts) §`H-06` | **№13** / `M-02` — когда шрифт канала выбран, он обязан приезжать записью проекта (`asset-record/1`, sha в `store.lock`), а не из `/usr/share/fonts` | цена: ingest настоящего файла (это и есть работа №13); цена бездействия: гейт и `H-02` неповторимы на машине без DejaVu, и `engineFingerprint` о шрифте молчит — он описывает движок, а не проект | `open` |
| 188 | **Числа раскладки полосы субтитров выбраны `H-06` и названы ВРЕМЕННЫМИ.** Ширина 960 и отступ 60 перенесены из `sp3f`, кегль 72 — величина №125, а позиция полосы (`bottomPx: 240`) и высота плашки (`plateHeightPx: 480`) не названы НИ ОДНИМ документом проекта: без них невыразимо условие применимости **R13** («полоса лежит на непрозрачной плашке»), поэтому они выбраны так, чтобы две строки кегля 72 умещались в плашку целиком **ДОПОЛНЕНО `H-07` (2026-08-31): числа ПЕРЕЕХАЛИ и ПРЕДЛОЖЕНЫ ЗАНОВО, статус не меняется.** Место жительства сменилось — раскладка полосы стала свойством ТРЕКА и живёт в [`composition/runtime.js`](../packages/renderer-hyperframes/src/composition/runtime.js), константа `BAND`; у шаблона чисел раскладки не осталось ни одного (охранник — `templates.test.ts`). Предложение `H-07`, выбранное по глазу под безопасные зоны Shorts и показанное кадрами: ширина **920** (поля по 80), низ **500** px над краем кадра, кегль **68**, межстрочный **1.22**, плашка — НЕПРОЗРАЧНАЯ (`#05070c`, решение владельца `H-07`, вариант «б»: условие применимости **R13** остаётся в силе), скруглённая (радиус 28), с растушёвкой края тенью того же цвета (`0 0 36px 18px`), и она ОБНИМАЕТ текст, а не занимает всю полосу. ИЗМЕРЕНО на готовом ролике демо (кадр 15.6 c, поиск по точному цвету плашки): плашка `y 1297…1430` (высота 134), `x 225…854`, низ на **489** px над краем кадра, центр строки — **71.0 %** высоты, центр по X — **540** при кадре 540. То есть попадание в названную владельцем зону 68–74 % проверено числом, а не глазом. **ЧТО ЭТО ДОБАВЛЯЕТ ПРИБОРУ `H-02`:** плашка теперь переменной ширины (обнимает текст), и кроп полосы для **R13** обязан быть по ВЫСОТЕ, а не по прямоугольнику фиксированной ширины | [`captionEmphasis@1`](../packages/renderer-hyperframes/src/templates/captionEmphasis@1.ts) шапка (было), [`runtime.js`](../packages/renderer-hyperframes/src/composition/runtime.js) `BAND` (стало), измерение — [`H-07`](impl/H-07/report.md) §4 | **`H-02`** (прибор R13 предъявит к ним требования) / **владелец** (буква — по кадрам `H-07`) | цена: два числа и решение, кто их назначает — шаблон или профиль; цена бездействия: первый же прибор R13 будет измерять раскладку, которую никто не утверждал **ЗАКРЫТ `V-06` (2026-08-31) — числами владельца: кегль **68**, межстрочный **1.22**, ширина **920**, низ **500**** («полоса ок», решение владельца по кадрам `H-07`, шаг 0 задачи `V-06`). Числа уже стоят в [`composition/runtime.js`](../packages/renderer-hyperframes/src/composition/runtime.js) (константа `BAND`) — предложение `H-07` принято как есть, правки кода не потребовалось. Что это НЕ закрывает: прибор **R13** (`H-02`) по-прежнему обязан мерить кроп полосы по ВЫСОТЕ, а не по прямоугольнику фиксированной ширины (плашка обнимает текст) — это требование живёт в строке `H-02`, а не здесь | `closed` |
| 189 | **Критерий roadmap `H-06` «каждый шаблон прошёл гейт на `final` (10) и `draftHalf` (3)» на `bed@1` НЕИСПОЛНИМ ПО ПОСТРОЕНИЮ.** Он аудио-домена: в `RenderIR.clips` не попадает никогда (`CROSSING_TRACKS` — только `caption`/`visual`/`effect`), а его реализация есть ОТКАЗ (решение владельца, развилка «б», вариант б3). Гейт на нём даёт `error` («гейта не было») — и это ПРОВЕРЕНО тестом, потому что альтернатива (пустой `mount`) дала бы PASS на чёрных кадрах, то есть ложно-зелёный №164. Значит критерий исполнен на ЧЕТЫРЁХ визуальных из пяти, и пятый не «не сделан», а «не имеет смысла» | [`bed@1`](../packages/renderer-hyperframes/src/templates/bed@1.ts) шапка, [`H-06`](impl/H-06/report.md) §5 | **владелец** — правка формулировки критерия в roadmap §4.8 («каждый ВИЗУАЛЬНЫЙ шаблон») | цена: одно слово в roadmap; цена бездействия: критерий готовности читается невыполненным, хотя выполнен, — и следующая сессия либо припишет `bed@1` фиктивную запись, либо будет искать ошибку там, где её нет | `open` |
| 190 | **Форма «похожести на имя кривой» ловит только имена С ТОЧКОЙ, и обе её границы измерены.** Охранник №184 фильтрует литералы `mountSource` по форме `<слово>.<слово>` (со скобками) плюс точное `none`. В одну сторону он НЕ ловит односложные имена: `'linear'` — алиас линейной кривой у gsap — неотличим от значения CSS, и ловить такие значило бы краснеть на половине текста шаблона. В другую сторону под форму попал бы литерал вида `'style.transform'`; сегодня ложных **ноль** — измерено: 102 строковых литерала по шести `mountSource`, 1 срабатывание, и оно настоящее. То есть долг сегодня без наблюдаемой цены, но граница правила выбрана нами, а не D5 | [`FIX-01`](impl/FIX-01/report.md) §4 | **владелец** (формулировка D5: что считается именем кривой) — исполнение при `TS-02`/`H-07` | цена: расширение формы — одна регулярка, но сперва решение, какие имена входят; цена бездействия: кривая, названная односложно, доедет до кадров тем же путём, каким доезжала `elastic.out` | `open` |
| 191 | **`manifest.scale` в `manifest.json` композиции после №182 не читает НИКТО.** Правка №182 сняла единственного потребителя: `index.html` больше не раскрывает число множителем, а `runtime.js` берёт из `window.__VPE_MANIFEST` только `fps`, `assets`, `fonts` и `compositionId` (греп). Поле остаётся в каталоге композиции и входит в `bundle.hash`. Это ровно тот образец, который `H-06` уже поймал на `data-start` групп: данные, которые «кто-то, наверное, читает», а не читает никто. Вреда сегодня нет — размер выхода несут `width`/`height` и `data-width`/`data-height`, — но поле выглядит несущим | [`FIX-01`](impl/FIX-01/report.md) §2 | **`L-01`** (там композиция собирается сборкой) либо `H-02` | цена снятия: `bundle.hash` ВСЕХ композиций меняется, то есть кэш сегментов инвалидируется целиком — снимать стоит вместе с другой правкой композиции, а не отдельным коммитом; цена бездействия: следующий читатель примет число за вход рендерера | `open` |
| 192 | **Три путевых поля файла запроса гейта — ПЛЕЙСХОЛДЕРЫ, и по файлу это видно только по имени каталога.** В `packages/renderer-hyperframes/gate-requests/*.json` поля `tmpDir`, `outputPath` и `bundle.path` несут `.gate-run/…`: гейт их всё равно перекрывает под своим `runRoot` (`requestForRun` в [`gate.ts`](../packages/renderer-hyperframes/src/gate.ts)), поэтому значения не читает никто, кроме валидатора формы. Настоящий путь в файле ровно один — ассет (`assets/pattern-32.png`), плюс абсолютный системный шрифт. Читатель, не знающий про `requestForRun`, поверит, что каталог `.gate-run` где-то создаётся | [`GATE-PREP`](impl/GATE-PREP/report.md) §2 | — | условие открытия: ПЕРВЫЙ потребитель этих файлов вне гейта (например, прогон одиночного сегмента из файла запроса) — там плейсхолдеры перестанут быть безобидными и обязаны стать настоящими путями; цена сегодня: одна строка в шапке фикстуры и эта строка | `open` |
| 193 | **Список случаев гейта живёт в ДВУХ местах.** `CASES` в [`templates-gate.test.ts`](../packages/renderer-hyperframes/test/templates-gate.test.ts) (живой браузерный гейт `H-06`) и `GATE_REQUEST_CASES` в [`test/fixture.ts`](../packages/renderer-hyperframes/test/fixture.ts) (источник восьми файлов запросов) описывают одни и те же четыре композиции — клипы, `z`, `params` — и сегодня совпадают дословно. Расхождение означало бы, что владелец снимает записи на композиции, которую браузерный тест не мерил ни разу. Слияние — одна строка (`import { GATE_REQUEST_CASES }` вместо литерала), НЕ сделано намеренно: дифф `GATE-PREP` согласован с владельцем поадресно, а этот файл в него не входил | [`GATE-PREP`](impl/GATE-PREP/report.md) §5 | первая задача, которая и так правит `templates-gate.test.ts` | условие открытия: правка клипов в одном месте из двух; цена слияния — одна строка, цена бездействия — гейт мимо измерения | `open` |
| 194 | **Команда сверяет запрос с профилем по ТРЁМ полям **K4**, а в измеряемую пару входит и четвёртое — `executionProfile.workers`.** `assertRequestMatchesProfile` в [`template-gate.ts`](../packages/cli/src/template-gate.ts) сверяет `browserGpu`, `scale`, `imageFormat` — то, что читает адаптер как ПИКСЕЛЬНЫЙ профиль. Но `workers` уезжает в рендерер параллелизмом ([`argv.ts:122`](../packages/renderer-hyperframes/src/argv.ts)), а `FACT` (SP-3c §1.1): `w = 4` под нагрузкой дала 3 варианта из 9 там, где `w ≤ 2` не разошлась ни разу — то есть параллелизм и есть та величина, ради которой гейт вообще снимается. Запрос с `workers: 1` на профиле, объявляющем `4`, сегодня пройдёт молча и даст запись про пару, которой не существует. В файлах `gate-requests/*.json` число проставлено ИЗ yaml руками и стережётся только побайтовой сверкой с билдером | [`GATE-PREP`](impl/GATE-PREP/report.md) §4 | `L-01` либо первая задача, правящая `template-gate.ts` | условие открытия: первый запрос гейта, собранный не из фикстуры проекта; цена — четыре строки в существующей сверке (то же сообщение, четвёртое поле) | `open` |
| 195 | **`fixtures/minimal` не собирается живьём ничем, кроме подстановки чужих байтов под её адреса.** Записи ассетов фикстуры объявляют СИНТЕТИЧЕСКИЕ sha (`0000…0001`…`0005`), байтов с такими адресами не существует и существовать не может, а живая сборка требует настоящих: адаптер читает файл ассета и определяет формат по магическим байтам (`R3`, `magic.ts`). Прогоны `L-01` (юнит и браузерный e2e) кладут настоящий PNG и настоящий DejaVu Sans Bold ПОД АДРЕСАМИ ФИКСТУРЫ — это законно ровно потому, что CAS содержимое не перехэширует (осознанная граница `M-01`: целостность — предмет `vpe store verify`), но означает, что «сборка фикстуры» и «сборка проекта» расходятся в одном месте, и расхождение держится тестовым помощником, а не файлом фикстуры | [`L-01`](impl/L-01/report.md) §5 | `F-01` (прогон AC4 на фикстуре) либо `M-02` | цена: решение владельца — класть ли в фикстуру НАСТОЯЩИЕ маленькие ассеты (и тогда её sha станут настоящими, а `store.lock` непустым) или оставить синтетику и признать, что фикстура — спецификация формата, а не собираемый проект; цена бездействия: каждый следующий прогон изобретает подстановку заново  **Заметка `L-02` (2026-08-30), статус НЕ тронут: появившийся `vpe store verify` этот долг НЕ ловит и по построению не может.** Проверка спрашивает `store.lock`, а `store.lock` фикстуры пуст (`entries: []`) — на `fixtures/minimal` она печатает «0 записей, всё на месте» и даёт код 0, хотя пять синтетических адресов `assets/records/*.json` не имеют байтов и иметь не могут. Каталог ассетов и дубли в требуемое множество не входят вовсе (долг №206). Практическое следствие для читателя строки: зелёный `verify` на фикстуре не означает, что фикстура собираема | `open` |
| 196 | **Вход `assertBuildMayStart` НЕ сверяет `bundleHash`, и сверить его сборке нечем.** Комментарий [`gate.ts`](../packages/templates-spec/src/gate.ts) ожидает, что половину «код шаблона» подаст `vpe build` («вызывающий, который её знает, ОБЯЗАН подать»), — но `bundleHash` записи описывает композицию ФИКСТУРЫ ГЕЙТА, а сборка знает только `bundle.hash` СВОЕГО сегмента: подай она его, ни один ролик не стартовал бы. ИЗМЕРЕНО протоколом Н1 (`L-01`): запись с заведомо чужим `bundleHash` сборку пропускает, код выхода 0. Дыра не теоретическая: `engineFingerprint` считается по версиям ВНЕШНИХ зависимостей (`fingerprintedPackages` — не `@vpe/*`), поэтому правка `runtime.js` или реализации шаблона отпечаток НЕ двигает, и записи, снятые до правки, остаются «действующими» для сборки. Предложение (не сделано, решение владельца): сборка сверяет `bundleHash` записи с `bundle.hash` из КОММИЧЕННОГО файла запроса `gate-requests/<шаблон>.<профиль>.json` — он в репозитории с `GATE-PREP` и описывает ровно ту композицию, на которой снят гейт | [`L-01`](impl/L-01/report.md) §4 | `L-02` либо первая задача, правящая `templates-spec/src/gate.ts` | цена: ~15 строк и превращение `gate-requests/*` во вход сборки; цена бездействия: правка шаблона без пересъёмки гейта проходит молча — ровно то, ради чего написана вторая половина `gateStaleness`  **Заметка `L-02` (2026-08-30), статус НЕ тронут.** `templates-spec/src/gate.ts` в этой сессии правился — ОДНА строка ТЕКСТА отказа по поимённому разрешению владельца («пара та же, ~~рисует~~ МОГ рисовать другое»), механизма и входа `assertBuildMayStart` правка не касается ни полем, ни условием. Адрес «первая задача, правящая `gate.ts`» этим НЕ израсходован: сверка `bundleHash` со сборкой по-прежнему не сделана | `open` |
| 197 | **Сборка внедряет `tts:mock@1` и НЕ сверяет его с `project.yaml → voice.providerId`.** Сверка есть ветвление по ИМЕНИ провайдера — запрещено ADR-0010 §8 (**V16**) и ловится линтом; реестра реализаций в v1 нет (живой провайдер — `V-06`). Поэтому проект, назвавший живого провайдера, соберётся моком, а провенанс дубля и `voiceKey` запишут имя ИЗ ПРОЕКТА, то есть в коммитимый артефакт уедет утверждение о провайдере, который не работал. Сегодня спрашивается СВОЙСТВО: `capabilities.requiresNetwork` — провайдер, которому нужна сеть, отвергается до первой оплаты | [`L-01`](impl/L-01/report.md) §4 | `V-06` | условие открытия: появление второй реализации провайдера; цена — реестр «id → реализация» вместо внедрения одной **ЗАКРЫТ `V-06` (2026-08-31) — реестром, а не ветвлением.** Провайдер разрешается по `project.yaml → voice.providerId` картой «id → реализация» ([`providers/registry.ts`](../packages/voice/src/providers/registry.ts)), и карта строится ИЗ САМИХ РЕАЛИЗАЦИЙ по их `capabilities.providerId`: литерала имени в файле нет ни одного, поиск — `Map.get`, а не `if`, поэтому оба охранника ADR-0010 §8 остались в силе БЕЗ нового исключения (проверено отдельным тестом в `adr0010-capability-branching`). Неизвестное имя — отказ с перечнем известных. **Вторая половина долга, найденная здесь же:** имя take-файла — `chunkKey`, то есть смена провайдера не меняла ни одного имени, и проект, переведённый на живого, собрался бы НА СТАРЫХ ДУБЛЯХ МОКА молча; теперь дубль с чужим `voiceKey` считается ПРОМАХОМ (перечисляется **K8**, пересъёмка — по `--allow-tts`) и называется строкой отчёта сборки. Тесты — [`build-provider.test.ts`](../packages/cli/test/build-provider.test.ts) (5 из 5, без сети: транспорт подменён) | `closed` |
| 198 | **Частичный промах `voice` переплачивает: стадия зовётся на ВЕСЬ план.** ИЗМЕРЕНО (`L-01`): `recordSpeechPlan` спрашивает индекс прогона и межсборочный кэш `M-05`, но take-файлы с диска НЕ читает. `L-01` кэш не подключает, поэтому сборка идёт по правилу «промахов нет — стадии нет»: все дубли на месте ⇒ `voice` не запускается вовсе, иначе запускается на весь план и синтезирует заново и то, что уже лежит. На `tts:mock@1` это бесплатно, на живом провайдере — деньги | [`L-01`](impl/L-01/report.md) §3 | `M-05` (долг №89) либо `V-06` | цена: одна строка `cache: stageVoiceCache(...)` плюс тест «прогретый прогон не зовёт источник»; цена бездействия: первая же правка одного абзаца оплачивает весь ролик | `open` |
| 199 | **ESLint не видел каталог `packages/cli/src/build/**` — и это нашёл протокол нарушений, а не ревью.** Игнор `**/build/**` в [`eslint.config.js`](../eslint.config.js) написан для каталогов сборки, но он матчит ЛЮБОЙ каталог с этим именем, включая исходники. Каталог переименован в `src/build-stages/`, и первый же линт нашёл в нём настоящее нарушение **V16** (ветвление по `providerId`), которое до переименования было невидимо. Общее правило не исправлено: следующий каталог `src/build/` в любом пакете снова выпадет из линта молча | [`L-01`](impl/L-01/report.md) §4, Н2 | `L-03` (`vpe check`) либо первая задача, правящая `eslint.config.js` | цена: сузить игнор до `packages/*/build/**` и корневого `build/**` (или до `dist`-подобных), плюс тест «в дереве нет исходников, невидимых линту»; цена бездействия: правило, действующее везде, кроме места, где о нём забыли | `open` |
| 200 | **Межсборочный кэш `M-05` сборкой не подключён ни на одной стадии.** `composeKey`/`segmentKey` и `StageCache` написаны и покрыты (`M-05`), но `vpe build` не спрашивает их ни разу: каждая сборка считает Timeline, IR и РЕНДЕРИТ сегменты заново. Для `L-01` это осознанно (в критериях задачи кэша нет), и молчаливым это не осталось: `BuildRecord` печатает `cacheHits: 0` | [`L-01`](impl/L-01/report.md) §3 | `F-01` (K3 «попадание == промах» на фикстуре) либо `G-04` | цена: подстановка `StageCache` в три точки и тест K3 на настоящей сборке; цена бездействия: полный рендер на каждую сборку — минуты стенки там, где нужен `-c copy` | `open` |
| 201 | **Отказ R12 наступает ПОСЛЕ оплаты дублей.** Список использованных шаблонов сборка берёт из готового IR, а IR требует Timeline, Timeline — дублей: значит проект, у которого гейт не снят, сначала платит за синтез и только потом слышит «сборка не стартует». Ранняя проверка возможна (шаблоны названы записями режиссуры и порождёнными `[img:]` до всякого звука), но это ВТОРОЙ вывод «какие шаблоны использованы» — ровно тот класс дубля, из которого вырос долг №168, и заводить его без решения владельца задача не стала. На `tts:mock@1` цена нулевая, на живом провайдере — деньги за ролик, который не соберётся | [`L-01`](impl/L-01/report.md) §8 | `V-06` либо первая задача, правящая порядок стадий сборки | цена: либо один общий вывод списка шаблонов, читаемый и до Timeline, и после, либо осознанное «платим и падаем»; цена бездействия: первый же живой прогон без снятого гейта оплачен зря | `open` |
| 202 | **Ответ одной точки входа канонический, другой — нет: одно значение, два порядка ключей.** `vpe render-segment` печатает `canonicalJson(response)`, бинарь `renderer-hyperframes/bin/render-segment.ts` — `JSON.stringify(response)`. Причина механическая и не поддаётся устранению на месте: `JSON.stringify` запрещён линтом во всём `packages/*/src/**` (ADR-0007 §3), а `bin/**` вне его области, и тело точки входа (`src/segment-entry.ts`) поэтому возвращает ЗНАЧЕНИЕ, а печатает каждая оболочка сама. Сегодня цена нулевая: ответ подпроцесса не хэшируется ничем и читается разбором, а не побайтно | [`L-02`](impl/L-02/report.md) §3 | первая задача, которой понадобится сверять stdout двух точек входа ПОБАЙТНО (например, golden ответа) | цена: перевести бинарь на `canonicalJson` (одна строка, `@vpe/core-model` у пакета уже в зависимостях) — тогда формы совпадут; условие открытия: появление потребителя, сравнивающего байты | `open` |
| 203 | **Тест бинаря `--gate-profile ac4` слабее своего намерения — ИЗМЕРЕНО.** [`subprocess.test.ts`](../packages/renderer-hyperframes/test/subprocess.test.ts) утверждает «профилей гейта ровно два», проверяя `rule === 'R12'` и наличие слов `final`/`draftHalf` в сообщении. Проба `L-02` (Н4) заменила в теле отказ на молчаливое умолчание `final` — и тест ОСТАЛСЯ ЗЕЛЁНЫМ: подменённый вызов всё равно упирается в **R12** (записи гейта на прод-паре нет), а его сообщение те же два слова содержит. То есть проверяется «пришёл какой-то R12», а не «чужой профиль отвергнут». Тот же случай на стороне команды красный ([`render-segment.test.ts`](../packages/cli/test/render-segment.test.ts)) | [`L-02`](impl/L-02/report.md) §6 | первая задача, правящая `subprocess.test.ts` | цена: сверять `details[0].at === '--gate-profile'` вместо слов в сообщении (одна строка); цена бездействия: правка разбора профиля в теле краснеет на одной точке входа из двух | `open` |
| 204 | **Перенос блоба идёт ЦЕЛИКОМ ЧЕРЕЗ ПАМЯТЬ.** `vpe store fetch|push` копирует байты как `target.put(await source.read(sha), kind)` — то есть блоб читается в `Uint8Array` полностью и полностью же пишется. Это прямое следствие интерфейса `Store` из пяти методов (ADR-0005 §8: `read` отдаёт байты, потока в сигнатуре нет), и для дубля TTS (секунды речи) цена незаметна. Для снимка страницы или будущего видео-ассета в сотни МБ это пик RSS размером с блоб | [`L-02`](impl/L-02/report.md) §4 | `G-03` (второй бэкенд, rclone — там перенос всё равно не наш) либо первая задача, кладущая в CAS блоб > ~100 МБ | цена: шестой метод интерфейса (`copyTo`) либо потоковая пара `openRead`/`openWrite` — то есть правка ADR-0005 §8, а её минимальность объявлена причиной, по которой третий бэкенд остаётся конфигурацией; цена бездействия: OOM на большом блобе | `open` |
| 205 | **P7 («реплик ≥ 2») не проверяет и не пополняет НИ ОДНА команда.** `vpe store verify` поле `replicas` не читает вовсе — он спрашивает только про наличие и целостность байтов в ОДНОМ сторе; `vpe store push`, создав вторую физическую копию, в `store.lock` этого не записывает, то есть `replicas` остаётся тем, что человек вписал руками. Пока обе команды такие, правило «для `kind: voice\|snapshot\|ai-image` реплик ≥ 2» не выполнимо механически: даже правильно исполненный `push` оставляет лок утверждающим «реплик 0» | [`L-02`](impl/L-02/report.md) §4 | `G-03` (там же, где появляется второй бэкенд и `storeVerifyMaxAgeDays`) | цена: `push` дописывает идентификатор приёмника в `replicas` (и тогда нужен ИДЕНТИФИКАТОР бэкенда, а не путь — сегодня его нет: `--to` это каталог), `verify` считает реплики по видам; цена бездействия: единственная копия оплаченного PCM физически одна, а лок об этом молчит | `open` |
| 206 | **Требуемое множество sha берётся ТОЛЬКО из `store.lock`: отставший лок даёт зелёную проверку.** `vpe store verify` читает два файла — `project.yaml` (ради `store.path`) и `store.lock`, — и о том, что дерево проекта ссылается на байты ещё где-то (`assets/records/*.json`, `voice/takes/*.json`), не знает. Следствие измерено на фикстуре: `fixtures/minimal` объявляет пять синтетических адресов ассетов и пустой `entries: []` — проверка зелёная при нуле байтов (долг №195). Правило **P6** от этого исполнено буквально («все sha ИЗ `store.lock`»), но человек читает его шире — «клон проверен» | [`L-02`](impl/L-02/report.md) §4 | `M-02`/`F-01` (там, где записи ассетов и дубли становятся входом сборки фикстуры) либо `G-02` (`vpe open --full`) | цена: один вывод «какие байты нужны проекту», читаемый и командой стора, и сборкой, — то есть ровно тот класс второго вывода, из которого вырос долг №168, и заводить его без решения владельца задача не стала; цена бездействия: `verify` отвечает про лок, а спрашивают его про проект | `open` |
| 207 | **У ТРЕКА СУБТИТРОВ НЕТ СВОЕГО КАНАЛА ШРИФТА: `font-family` ПОЛОСЫ ДАЁТ КЛИП.** `H-07` перенёс раскладку полосы (позиция, кегль, межстрочный, плашка, цвет, тень) в `runtime.js` — она стала свойством ТРЕКА, — но семейство осталось у `captionEmphasis@1`, и это вынужденно: ссылку на шрифт несёт только клип (`ctx.fonts` + `ctx.fontOf`), а у трека `IR.captions` поля шрифта нет ни в IR, ни в профиле. Следствие названо вслух и ИЗМЕРЕНО: сегмент БЕЗ клипа эмфазы получает полосу на месте, в нужном кегле и на плашке, но СИСТЕМНЫМ шрифтом. Демо `H-07` обходит это авторски — по одному `captionEmphasis@1` на сцену (решение владельца), — то есть обход стоит записи в режиссуре на каждую сцену | [`H-07`](impl/H-07/report.md) §3 | **`compile`** — «поле роли шрифта в профиле»; тот же адрес уже назван ТЕКСТОМ ОТКАЗА компилятора ([`contract.ts:116`](../packages/compile/src/timeline/contract.ts)): «Укажите шрифт роли (поле профиля — долг с адресом `H-07`/№13)» | цена: поле профиля + чтение его треком; цена бездействия: у ролика без клипа эмфазы в сегменте типографика чужая, и заметить это можно только глазами | `open` |
| 208 | **СВЕРКА «`text` ГРУППЫ = `tokens.join(' ')`» ЖИВЁТ В БРАУЗЕРЕ, А НЕ В КОМПИЛЯТОРЕ.** `H-07` сделал пословную разметку субтитров (без неё эмфаза активного слова невыразима) и поставил на вход проверку: `runtime.js` бросает, если слова группы не складываются в её строку. Гарантия эта КОМПИЛЯТОРСКАЯ (`timeline/captions.ts`, `textOf` = `tokens.map(surface).join(' ')`), а предъявляется на рендере — то есть о разъехавшемся входе сборка узнаёт после старта браузера, а не до него. Измерено попутно: тестовая фикстура рендерера ЭТО ПРАВИЛО НАРУШАЛА (группа `and the sea` при одном токене `sea`) и была зелёной, потому что мерить его было нечем | [`H-07`](impl/H-07/report.md) §2 | `H-02` (прибор **R13** — первый, кому границы групп и слов нужны числами) либо первая задача, трогающая `compile/src/timeline/captions.ts` | цена: тот же ассерт на стороне `compile`, где величина и рождается; цена бездействия: класс ошибок «слова потерялись» виден только в кадрах | `open` |
| 209 | **ЭМФАЗА ОПИРАЕТСЯ НА ВЕНДОРНОЕ ВЫВЕДЕНИЕ ДИАПАЗОНА ОСИ `wght` ИЗ `fvar`, И ОХРАННИКА У ЭТОГО НЕТ.** `@font-face`, который печатает [`materialize.ts`](../packages/renderer-hyperframes/src/materialize.ts), диапазона весов НЕ объявляет; по букве CSS Fonts 4 дескриптор `font-weight` тогда равен `normal`, ось должна быть прижата к 400, а `font-weight: bold` — уйти в синтетическое утолщение. ИЗМЕРЕНО (`H-07`, проба П1, три пробы подряд), что на `chrome-headless-shell 152.0.7928.2` это НЕ так: (1) у `Montserrat[wght]` умолчание оси — 100 (Thin), а кадр не волосяной ⇒ ось выставляется в 400 по `font-weight: normal`; (2) добавление `font-weight: 100 900` в `@font-face` меняет `bundle.hash` сегмента (`2a8af058fbdd…` → `f4d47c7909fd…`), то есть патч ДОЕЗЖАЕТ; (3) кадр при этом ПОБАЙТОВО тот же (md5 `a14aa373…`). Значит браузер берёт диапазон из самого файла, и «вес по оси вариативного шрифта» (решение владельца `H-07`) исполняется — но исполняет его ВЕНДОР, а не наш код | [`H-07`](impl/H-07/report.md) §5, транскрипт [`violation-transcript.txt`](impl/H-07/violation-transcript.txt) П1 | `H-02` — вместе с прибором **R13**: там же, где меряется полоса, дёшево померить и «начертание активного слова отличается от базового» на НАСТОЯЩЕМ шрифте проекта | цена: одно утверждение в приборе `H-02` либо явный `font-weight: 100 900` в `@font-face` (измерено — сегодня no-op, но перестанет им быть, если вендор передумает); цена бездействия: обновление Chrome может увести эмфазу в синтетику молча — картинка останется похожей, а решение владельца окажется неисполненным | `open` |
| 210 | **ЗНАЧЕНИЕ ЛИЦЕНЗИИ НЕ СУДИТ НИКТО: запись с `status: all-rights-reserved` собирается целиком.** ИЗМЕРЕНО (`H-07`, нарушение Н3, две половины). Н3а: снятое поле `provenance.work` — ОТКАЗ на чтении каталога ассетов, `exit 2`, до всего. Н3б: поле на месте, значение запрещает использование — **сборка проходит, `exit 0`**, и `bundle.hash` всех трёх сегментов совпадает с нормальным прогоном побайтово, то есть provenance в композицию не входит и заметить подмену НИЖЕ по конвейеру нечем. Это не дефект: схема `asset-record/1` объявляет `status` свободной строкой сознательно (её шапка: «enum статусов прав протух бы раньше первого ролика»), а судить значение обязан Policy Guard — правила **PG-D1**/**PG-D2**, обе BLOCK. Долг заводится, чтобы отсутствие охранника было записано ЧИСЛОМ прогона, а не подразумевалось | [`H-07`](impl/H-07/report.md) §6 (Н3), транскрипт [`violation-transcript.txt`](impl/H-07/violation-transcript.txt) | **`CP-06`** (Policy Guard, домен D) — там, где `status` впервые становится входом правила, а не строкой файла | цена: правило домена D над `provenance.*.status`; цена бездействия: ассет с запрещающей лицензией доезжает до готового ролика, и единственный, кто это заметит, — человек, читающий записи глазами | `open` |
| 211 | **ДЕМО-ПРОЕКТ `examples/vertical-v1` НЕ ПОКРЫТ НИ ОДНИМ ОХРАННИКОМ.** Он и заведён как то, на что СМОТРЯТ (DoD Charter §8 п. 5), а не как вход прибора: `fixtures/minimal` остаётся единственной фикстурой охранников, и `H-07` её не трогал ни символом. Цена этого решения названа: правка `runtime.js`, шаблона или профиля может сломать демо, и узнаем мы об этом на следующей ручной пересборке, а не на прогоне. Сегодня частичная защита есть — `captions-visibility.test.ts` меряет полосу пиксельно на синтетическом входе, — но ни фотографий, ни вариативного шрифта, ни трёх сегментов подряд в ней нет | [`H-07`](impl/H-07/report.md) §7 | `F-01` (`fixtures/reference` — там появляется вторая живая сборка и вопрос «что из этого стережётся») либо `H-02` | цена: живой прогон демо в CI — минуты и 6 МБ ассетов в сторе, которых в репозитории нет; цена бездействия: демо тихо протухает между сессиями, а владелец узнаёт об этом на телефоне | `open` |
| 212 | **ФОТОГРАФИЯ В ПОЛНЫЙ РАЗМЕР СТОИТ ПИК RSS В ЕДИНИЦЫ ГИГАБАЙТ НА СЕГМЕНТ.** ИЗМЕРЕНО (`H-07`, `build/reports/timings.txt` живой сборки демо на `final`, `workers: 4`): `seg:street` — `peakRssBytes` 3.26 ГБ, `seg:work` — 2.97 ГБ, `seg:road` — **3.86 ГБ**; полный рендер трёх сегментов — 112.5 с на 497 кадров. Тяжелее всех сегмент с самым большим снимком (3840×4992 = 19.2 Мпикс): растеризатор держит распакованный кадр и промежуточные буферы Ken Burns на каждый воркер. Демо `H-07` уже платит за это авторски — вместо мастер-файлов Викисклада (до 6205×8066) взяты рендеры шириной 3840, и причина записана прямо в `provenance.work.note` записи. Но правила «до какого размера ужимать ассет» нет ни у профиля, ни у записи, и на машине с меньшей памятью тот же проект уйдёт в своп молча | [`H-07`](impl/H-07/report.md) §7 | `F-01` (`fixtures/reference` — 45–60 с и 6–8 сцен, то есть тот же расход на порядок дольше) либо `A-01` (там ассеты приходят пачками) | цена: поле профиля «максимальная сторона ассета» плюс производная запись с `derivedFrom` (механика уже есть в `asset-record/1`); цена бездействия: сборка ролика упирается в память машины, а не в бюджет AC2, и узнаётся это на прогоне | `open` |
| 213 | **КЛАСС ГОЛОСА СНИМАЕТСЯ ДЛЯ ГОЛОСА ПРОЕКТА, А НЕ ДЛЯ ГОЛОСА РОЛИ.** Снимок аккаунта (`accountSnapshot`, `V-06`) спрашивает `GET /v1/voices/{id}` РОВНО ОДИН раз — для `project.yaml → voice.voiceId`, — а провенанс каждого дубля пишет полученный класс всем чанкам. Роль (ADR-0010 §3a-bis) имеет право перекрыть голос: `voice_id` роли — своя переменная окружения, и её класс может быть другим (`premade` против `professional`). Тогда take-файл чанка с ролью утверждает класс ЧУЖОГО голоса — та же болезнь, от которой лечит `V-06` в целом («артефакт утверждает то, чего не было»), только на одно поле. Пока роли не назначаются записями режиссуры (`A-02` не сделана), случай недостижим: в демо и фикстуре голос один | [`V-06`](impl/V-06/report.md) §6 | **`A-02`** (там появляются назначения ролей) либо раньше, если проект заведёт вторую переменную голоса | цена: снимок на КАЖДЫЙ различный `voiceId` плана (бесплатные вызовы, но по одному на голос) и провенанс, собираемый чанком, а не прогоном | `open` |
| 214 | **СТАВКА ТАРИФА ЖИВЁТ В ОКРУЖЕНИИ (`ELEVENLABS_RATE_PER_CODEPOINT`), А НЕ В ПРОФИЛЕ.** Решение владельца (2026-08-31, вопрос 3 плана `V-06`, вариант «а»+«б»): множитель приезжает снимком аккаунта, потому что константы в коде запрещает ADR-0010 §2, а `UNKNOWN` (SP-2b.7) не даёт вывести его из ответов API. Цена названа: окружение НЕ ВЕРСИОНИРУЕТСЯ и не диффится — два прогона с разной ставкой различимы только по `planRateAtGeneration` в уже записанных дублях, и «кто поменял 0.55 на 1.0» история не хранит. Правильное место — поле профиля (`audio-profile/1`), но это правка `schema`, а её задача `V-06` не трогает ни символом | [`V-06`](impl/V-06/report.md) §3 | **`schema`** (ближайшая задача, правящая семейства; кандидат в правку ADR-0010 §2 записан заметкой там же) | цена: одно поле схемы, миграция профилей и решение, обязательно ли оно | `open` |
| 215 | **СБОРКА НЕ СВЕРЯЕТ СПИСАНИЕ: охранник множителя тарифа зовёт только живой тест.** `assertBilledRate`/`checkBilledRate` написаны, покрыты юнитами и вызываются в [`live-elevenlabs.test.ts`](../packages/voice/test/live-elevenlabs.test.ts); `vpe build` окно `GET /v1/usage/character-stats` после прогона НЕ читает. Причина не в лени: `FACT` (SP-2) окно оседает 20–40 с, то есть сборка обязана была бы ЖДАТЬ полминуты после последнего дубля ради проверки, которая на попадании кэша не нужна вовсе. Следствие, названное вслух: смена множителя обнаружится на ближайшем живом тесте, а не на ближайшей сборке | [`V-06`](impl/V-06/report.md) §4 | **`G-03`/`G-04`** (ночной прогон — место, где ждать полминуты не жалко) либо отдельная команда `vpe voice settle` | цена: ожидание в сборке против отложенного обнаружения; сегодня выбрано второе | `open` |
| 216 | **TAKE-ФАЙЛ БЕЗ `planRateAtGeneration` БОЛЬШЕ НЕ ЧИТАЕТСЯ, А МИГРАЦИИ НЕТ.** Читатель [`take-file.ts`](../packages/voice/src/plan/take-file.ts) требует поле (значение `null` законно, ОТСУТСТВИЕ — нет: «поля нет» означает файл другого движка, и подставить за него `null` значило бы принять на веру, что дубль ничего не стоил). Дубли этого репозитория переписаны сборкой в той же сессии, поэтому промаха нет ни одного; но снимок проекта, снятый ДО `V-06`, текущим движком не откроется — а именно это обязан проверять `G-02` | [`V-06`](impl/V-06/report.md) §3 | **`G-02`** (там снимок старого проекта обязан открыться), вместе с долгом №83 (у take-файла нет семейства и версии) | цена: либо версия формата у take-файла, либо разовая миграция каталога дублей | `open` |
| 217 | **`tts:mock@1` НЕ УМЕЕТ ИМИТИРОВАТЬ ЛИД-ИН, И ПОЭТОМУ ЦЕЛЫЙ КЛАСС ДЕФЕКТОВ ПРОВЕРЯЕТСЯ ТОЛЬКО ЗА ДЕНЬГИ.** У мока `leadInMs: 0` по построению, и это записано в его шапке как достоинство («T7 обязан работать и при нуле»). Цена этого достоинства измерена `V-06`: расхождение двух приборов — провайдер относит ведущую тишину к первому символу (`start[0] = 0.000`), а акустический детектор находит речь на 40–220 мс — на моке НЕВЫРАЗИМО, поэтому ассерт `anchors.ts` (привязка до лид-ина) прожил от `CP-01` до первого живого дубля и упал на нём же. Правка формулы покрыта тестом, который вносит расхождение РУКАМИ (правкой привязки), — это проверяет формулу, но не провайдера. Нужен параметр профиля мока: «лид-ин N мс тишины перед первым символом, alignment при этом начинается с нуля» | [`V-06`](impl/V-06/report.md) §12 | **`voice/mock`** — ближайшая задача, правящая `providers/mock.ts`; пока — `A-03`, где мок станет материалом калибровки алигнера | цена: один параметр `MockProfile` и ветка в `synthPcm`; цена бездействия — каждый следующий дефект этого класса ловится живым прогоном, то есть деньгами | `open` |
| 218 | **СБОРКА ПИШЕТ `store.lock` В КОНЦЕ, А НЕ ПОСЛЕ СТАДИИ `voice` — ОПЛАЧЕННЫЕ БЛОБЫ МОГУТ ОСТАТЬСЯ БЕЗ ЗАПИСИ.** ИЗМЕРЕНО (`V-06`, живая сборка демо): синтез прошёл, четыре дубля легли в CAS и в `voice/takes/`, а сборка упала на КОМПИЛЯЦИИ — и `store.lock` не был записан вовсе. Следующая сборка стадию `voice` пропускает по попаданию `voiceKey` (правильно — платить второй раз нельзя), но вместе с ней пропускает и `upsertEntry`: записи об оплаченных блобах не появятся уже никогда. Итог: `vpe store verify` о них не знает, `vpe store push` их не реплицирует, а `P7` («реплик ≥ 2» для невосстановимых байтов) их не видит. В этой сессии четыре записи вписаны РУКАМИ из take-файлов (sha256 из дубля, size с диска; `vpe store verify` затем сверил все восемь побайтно) | [`V-06`](impl/V-06/report.md) §11 | **`cli/build`** — запись `store.lock` обязана идти сразу после стадии `voice`, до рендера; либо отдельная команда «пересобрать `store.lock` сканом `voice/takes/` и CAS» (родня `voiceCacheFromTakes`, `M-05`) | цена: одна запись файла в середине сборки против потери учёта оплаченного при любом падении после синтеза | `open` |
| 219 | **ЗЕРНО `grade@1` РАСТЕРИЗУЕТСЯ НА КАЖДЫЙ КАДР ЗАНОВО, ХОТЯ `seed` ФИКСИРОВАН И КАРТИНКА ШУМА ОДНА И ТА ЖЕ.** ИЗМЕРЕНО (`E-07`, дифференциальный прибор `H-06`, 60 кадров, 5 повторов, `final` 1080×1920, шум 0.2102 мс/кадр): `grade@1` с зерном `0.15` — **545.44 мс/кадр**, без зерна — **133.97**, то есть зерно одно стоит **411.5 мс/кадр**; `wallMs` сегмента ×2.98, кадры на диске ×8.2 (206 → 1681 КБ/кадр, то есть **3.0 ГБ** промежуточных PNG на 60-секундный ролик). Цена ломает AC2 в одиночку: Charter AC2 даёт 500 мс/кадр на ВСЮ композицию, зерно просит 546. **Дефолт поэтому `grain: 0`** (решение владельца `E-07`: «решает измерение, не вкус»); параметр остаётся и РАБОТАЕТ — гейт `final` N = 10 с зерном дал **PASS** (один `framemd5` `b9eef71e75dc377b`, один `sha256` `b97d127db32e8366`), то есть `feTurbulence` на этой паре воспроизводим, и выключено зерно по ЦЕНЕ, а не по детерминизму. Форма починки названа: шум зависит только от констант шаблона, значит его можно растеризовать ОДИН раз (в `canvas`/`data:`-URI на монтировании) и переиспользовать всеми кадрами — тогда цена станет ценой наложения, а не генерации | [`E-07`](impl/E-07/report.md) §5 | **`E-07fix`** — первая задача, правящая `grade@1`; раньше неё вопрос не открывается | условие открытия: владелец хочет зерно в канале. Цена починки — одна растеризация на монтировании; цена бездействия — зерном нельзя пользоваться на роликах длиннее пробы | `open` |
| 220 | **`msPerFrameBudget` — ОДНО ЧИСЛО НА ШАБЛОН, А ЦЕНА `grade@1` РАЗЛИЧАЕТСЯ ПО `params` ВЧЕТВЕРО.** Измерено обе ветви (`E-07`): **545.44** мс/кадр с зерном `0.15` и **133.97** без него. Манифест держит одно значение и `params` не видит, поэтому объявлена ОЦЕНКА СВЕРХУ — **546** (ADR-0008 «Бюджет AC2»: поле есть оценка сверху, и брать её надо по самому дорогому достижимому вызову; объявить 134 значило бы спрятать превышение AC2 у того единственного вызова, ради которого отчёт бюджета и существует). Следствие названо: на ДЕФОЛТНЫХ `params` отчёт сборки завышает вчетверо, и демо печатает пик 1773 мс/кадр там, где настоящая цена прогона дала +25 с на 502 кадра (≈50 мс/кадр при `workers: 4`). Это первый шаблон, у которого бюджет зависит от параметра, — до него зависимости не было ни у одного из пяти | [`E-07`](impl/E-07/report.md) §5 | **`E-05`** — там по решению владельца 9 (RM1) включается ПАДЕНИЕ по сумме бюджета, и падать по завышенной вчетверо оценке нельзя; кандидат в правку ADR-0008 («Бюджет AC2») записан заметкой там же | цена: либо `msPerFrameBudget` становится функцией `params` (правка `TemplateManifest`, то есть схемы манифеста и всех шести спеков), либо остаётся оценкой сверху с записанной ценой ложных тревог | `open` |
| 221 | **ПОРОГ 250 мс/кадр ПЕРЕСТАЛ РАЗЛИЧАТЬ: С `grade@1` ЕГО ПРЕВЫШАЕТ ЛЮБАЯ СЦЕНА.** ИЗМЕРЕНО (`E-07`, живая сборка демо `examples/vertical-v1`): пик **1773 мс/кадр** против порога 250, и превышение стоит во ВСЕХ пяти строках отчёта бюджета — включая ту, где кроме грейда лежат только `still@1` (1) и `captionEmphasis@1` (1). Один клип `grade@1` (546 по №220) кладёт порог сам, без чьей-либо помощи. Сегодня это ПЕЧАТЬ, а не падение (решение владельца 9, RM1), и потому пока безвредно; но предупреждение, которое горит всегда, перестаёт быть предупреждением. **Опасность названа числом:** после регистрации пятого эффекта (`E-05`) решение 9 включает ПАДЕНИЕ при компиляции — и в тот день ни один ролик с грейдом не соберётся | [`E-07`](impl/E-07/report.md) §5, `examples/vertical-v1/build/reports/budget.txt` | **`E-05`** — вместе с включением падения; решение о величине порога принимает владелец | цена: либо порог поднимается по измеренной цене шести шаблонов, либо сумма перестаёт быть арифметической (цена слоёв НЕ аддитивна — `FACT` SP-3f §2), либо падение не включается | `open` |
| 222 | **РАЗНИЦА «РЕЕСТР ⧵ ФИКСТУРА» НАЗВАНА СПИСКОМ В ДВУХ МЕСТАХ.** Решение владельца `E-07`: двусторонняя сверка имён шаблонов сохранена, а разница перечислена поимённо (`NOT_IN_FIXTURE = ['grade@1']`) — чтобы седьмой молча добавленный шаблон краснел ровно как раньше. Список живёт в ДВУХ файлах: [`templates-spec/test/fixture-specs.test.ts`](../packages/templates-spec/test/fixture-specs.test.ts) и [`renderer-hyperframes/test/templates.test.ts`](../packages/renderer-hyperframes/test/templates.test.ts). Слить их нечем: пакеты разные и `templates-spec` рендерера не видит (**M6**) — та же причина, по которой в двух местах живут `easingIds` и списки случаев гейта (долг №193). Цена: каждый следующий шаблон среза `mvp` (`E-01`…`E-06`) правит ОБА, и забыть один — значит получить красный тест в чужом пакете | [`E-07`](impl/E-07/report.md) §3 | **`E-01`** — первый следующий шаблон, которого фикстура не зовёт; там станет видно, растёт ли список или пора заводить общее место | цена: сегодня две строки; при пяти шаблонах `mvp` — два списка по пять имён | `open` |
| 223 | **`shaderBg@1` В РОЛИ «ШАБЛОНА, КОТОРОГО НЕТ В РЕЕСТРЕ» — ОТЛОЖЕННАЯ МИНА `E-01`.** Три файла подменяют имя вызова на `shaderBg@1`, чтобы получить отказ «шаблона нет в реестре»: [`templates-spec/test/gate.test.ts:128–129`](../packages/templates-spec/test/gate.test.ts), [`templates-spec/test/registry.test.ts:117`](../packages/templates-spec/test/registry.test.ts), [`renderer-hyperframes/test/templates.test.ts:90`](../packages/renderer-hyperframes/test/templates.test.ts). Но `shaderBg` — это **`E-01`** (roadmap §5, семь имён перечислены дословно в [`templates-spec/src/name.ts`](../packages/templates-spec/src/name.ts) в комментарии к грамматике), то есть шаблон, который будет реализован. **Класс дефекта ИЗМЕРЕН `E-07` на живом примере, а не предположен:** ровно так сломался `compile/test/template-contract.test.ts`, бравший на ту же роль имя `grade@1` — один ассерт покраснел, а **три соседних остались ЗЕЛЁНЫМИ ПО ДРУГОЙ ПРИЧИНЕ** (`params` подменённых записей не проходят строгую схему появившегося шаблона, отказ случается, правило то же — и тест «шаблона нет в реестре» молча стал тестом «параметры не те»). Тихая половина опаснее красной: красную видно, зелёную — нет. Лечение показано там же и стоит одного литерала на файл: имя, которого в roadmap §5 нет и не будет (`noSuchTemplate@1`, правка `E-07` по точечному разрешению владельца) | [`E-07`](impl/E-07/report.md) §12 и §14 | **`E-01`** — задача, реализующая `shaderBg@1`; раньше неё дефект не проявляется | цена: три строковых литерала. Цена бездействия — на `E-01` покраснеет один ассерт из трёх файлов, а два других станут зелёными не о том, и это придётся обнаруживать чтением | `open` |
