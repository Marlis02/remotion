# PROGRESS — IMPL-FIX-01 (пакет из четырёх долгов: №182, №185, №181, №184) · старт 2026-08-29T17:44:29+06:00
Якорь `date -Iseconds` поставлен ДО чтения репозитория.
План: 5 шагов. Бюджет: 60 мин без зелёного теста ⇒ стоп и отчёт. Preflight браузера — отдельным якорем, вне часов.
- `17:44` [0/5] старт — чтение долгов, отчёта H-06, кода
- `17:48` PREFLIGHT (вне часов, якорь 2026-08-29T17:45:00+06:00): pull «Already up to date», frozen install «Already up to date», build чисто, браузер `linux-152.0.7928.2` один в нашем корне. Дерево чисто, `a374ccf` == origin/main.
- `17:52` [0/5] ЗОНД №182 — дефект воспроизведён ЧИСЛАМИ (`out/probe-scale.mjs`, `solid@1`, доля нечёрных пикселей по квадрантам): scale 1 → PNG 1080x1920, ЛВ/ПВ/ЛН/ПН = 100/100/100/100; scale 0.5 → PNG 540x960, **100/0/0/0** — содержимое занимает 270x480, то есть масштаб применён ДВАЖДЫ (0.5 x 0.5). Дальше — измерение, какая простановка лишняя.
- `17:58` [0/5] ИЗМЕРЕНО, какая простановка масштаба лишняя. Опыт А (html/body в БАЗОВОЙ геометрии + наш `transform: scale()` снят) → scale 0.5 даёт 540x960 и 100/100/100/100. Опыт Б (html/body оставлены масштабированными, снят только `transform`) → тоже 100/100/100/100. Вывод: **необходимая половина правки — снятие нашего `transform: scale()`**; масштаб раскрывает САМ рендерер. Правку html/body до базовой геометрии беру за когерентность (при `scale === 1` она ничего не меняет), опыт Б доказывает, что она не несущая. Дерево возвращено к оригиналу, `git status` чист.
- `17:59` [0/5] прочитаны: №181 (охранник `assertRequestCallsTemplate`, cli/src/template-gate.ts:158), №184 (юнит Н1 `templates.test.ts:106`, фильтр `isEasingId` — дыра), №185 (runtime.js:204 `capsTimeline` + три строки 228–231; после снятия `capsTimeline` становится мёртвым). Замер формы «похожести» для №184: литералы вида `<слово>.<слово>` по файлам рендер-пути дают 5 попаданий — 4 из `freeze.js` (`Array.prototype` и соседи) и одно настоящее (`power3.out` в `flash@1`); в текстах ШАБЛОНОВ ложных нет.

---

## План (жду «ок» владельца)

Порядок — рекомендованный: сначала всё, что меняет композицию (одна инвалидация `bundle.hash`
на пакет), потом охранники. Пары хэшей сниму по шагам, чтобы вклад каждой правки был раздельно
виден в отчёте.

**Ш1 — №182, масштаб (~20 мин).** Снять `transform: scale()` с `#root` (измерено: необходимая
половина) и привести `html, body` к базовой геометрии для когерентности; `data-width`/
`data-height`/`meta viewport` остаются МАСШТАБИРОВАННЫМИ — ими рендерер и узнаёт размер выхода.
Оба комментария в `materialize.ts` («раскрытие `scale` — обязанность адаптера») переписать по
измерению конвенцией зачёркивания. Доказательство — НОВЫЙ браузерный файл
`test/scale-render.test.ts` (с таймаутом): рендер `solid@1` на `scale: 0.5`, декод кадра через
`decodeRgb` (второго PNG-декодера не завожу — решение владельца `H-04`), ассерт «доля нечёрных
пикселей в КАЖДОМ из четырёх квадрантов = 100 %» плюс контроль на `scale: 1`. Форма ассерта
выбрана по цене: квадранты ловят и сжатие в угол, и сдвиг, а PSNR тут не нужен — эталона нет.

**Ш2 — №185, снятие строк R2 (~15 мин).** Убрать три строки (`el.style.opacity = '0'` и два
`capsTimeline.set`) и мёртвый после них `capsTimeline`; переписать комментарий R2 на измеренное.
`captions-visibility.test.ts` остаётся с НОВЫМ смыслом — стережёт ВЕНДОРНОЕ поведение
`hyperframes@0.8.5`, с комментарием владельца дословно. `bundle.hash` до/после — в отчёт.

**Ш3 — №181, смягчение охранника команды (~25 мин).** `assertRequestCallsTemplate` →
«названный обязан присутствовать хотя бы одним клипом; остальные обязаны быть ИЗ БИБЛИОТЕКИ;
пустой запрос и запрос без названного — отказ (код 1)». Юниты (не браузер): смешанный запрос
проходит и запись пишется по НАЗВАННОМУ; запрос без названного — отказ; клип вне библиотеки —
отказ. Плюс ОДИН живой контур командой на смешанном `still+kenburns` ⇒ PASS, запись в tmp,
путь напечатан (репетиция ручных гейтов).

**Ш4 — №184, линт кривых в `mountSource` (~10 мин).** Инверсия фильтра в юните Н1
`templates.test.ts`, а не новый файл в `tests/lints/` — по цене и по точности: юнит видит
РАЗРЕШЁННЫЙ текст `mountSource` (включая интерполяции вроде `TRANSFORM_ORDER`), а греп по
исходнику их не видит и тащит 4 ложных из `freeze.js`. Форма «похожести»: литерал вида
`<слово>.<слово>` либо точное `none`, обязан быть в `EASING_REGISTRY`; ложные срабатывания
замерю по всем пяти реализациям и назову числом в отчёте.

**Ш5 — протокол нарушений, доки, приёмка (~50 мин).** Н1 двойной масштаб возвращён руками ⇒
`scale-render` красный; Н2 запрос без названного ⇒ отказ команды; Н3 `elastic.out` в
`mountSource` ⇒ охранник №184 красный; Н4 вендорная видимость сломана снятием `data-start` ⇒
`captions-visibility` красный (доказательство нового смысла). Резерв/откат только `cp`,
porcelain после каждого, транскрипт. Доки: DEBTS (№181/182/184/185 закрыты, новые со №187),
invariants (переходы, если появятся), `docs/impl/FIX-01/report.md` с парами `bundle.hash` и
таблицей «долг → правка → чем охраняется». Приёмка: build → lint → typecheck → полный `test`
(на этой машине 8 м 10 с) → сверка диффа.

**Вопросов, требующих остановки, нет.** Два замечания к сведению — в тексте ответа.

---

- `18:05` ВЛАДЕЛЕЦ: «ок», план принят целиком, Ш1→Ш5 без остановок. Оба решения по цене приняты, основания-измерения перенести в `report.md`. Снятие трёх строк №185 принято с моей формулировкой. Поправки: (1) Ш1 — правка `html/body` отдельным осмысленным куском диффа и отдельной строкой отчёта со ссылкой на опыт Б («не несущая, взята за когерентность»); (2) Ш2 — в комментарии `captions-visibility.test.ts` явная строка «пришпилено: hyperframes@0.8.5; покраснение при обновлении вендора = сигнал, не поломка».
- `18:05` [1/5] Ш1 начат — снимаю `bundle.hash` ДО правок
- `18:18` [1/5] Ш1 (№182) ГОТОВО. Правка `materialize.ts` двумя РАЗДЕЛЬНЫМИ кусками: (а) несущий — снят `#root { transform: scale() }`; (б) за когерентность — `html/body` в базовой геометрии, с абзацем в JSDoc `indexHtml` и ссылкой на опыт Б. Плюс переписаны два комментария конвенцией зачёркивания. Новый браузерный файл `test/scale-render.test.ts` — **3 passed за 3.4 c** (scale 0.5 → 540x960 и 100/100/100/100; scale 1 → 1080x1920 и то же; проба прибора отличает сжатое в угол `100/0/0/0`). `bundle.hash` (`solid@1`): scale 1 `702dc5e6…db53` → **НЕ ИЗМЕНИЛСЯ** (правка не трогает полный профиль ни байтом); scale 0.5 `9b582b40…5219` → `6ef8e42e…7696`; scale 0.25 `c6dcdaa4…0b8b` → `a8ad91d2…420a`.
- `18:24` [2/5] Ш2 (№185) ГОТОВО. Из `runtime.js` сняты ТРИ строки (`el.style.opacity = '0'` и два `capsTimeline.set`) и мёртвый после них `capsTimeline`; грепом кода хвостов нет (остались только упоминания в прозе комментария). Блок R2 переписан на измеренное: видимость держит `hyperframes@0.8.5` по `data-*`. `captions-visibility.test.ts` — новый смысл (стережёт ВЕНДОРНОЕ поведение), в шапке явная строка «пришпилено: hyperframes@0.8.5; покраснение при обновлении вендора = сигнал, не поломка», текст ассерта называет обе проверяемые причины; заодно поправлен ставший ложным комментарий «почему scale 1» (причина теперь — разрешение прибора, а не дефект №182). **2 passed за 4.2 c.** `bundle.hash` после Ш2 (меняются ВСЕ три — runtime.js входит в каждую композицию): scale 1 `702dc5e6…db53` → `851e5e49…de0c`; 0.5 `6ef8e42e…7696` → `f114b20a…9138`; 0.25 `a8ad91d2…420a` → `24546a99…85c8`.
- `18:24` [3/5] Ш3 (№181) начат
- `18:27` [3/5] Ш3 (№181) ГОТОВО. `assertRequestCallsTemplate` → `assertRequestCarriesTemplate(request, template, libraryNames)`: названный обязан быть хотя бы одним клипом; соседи обязаны быть ИЗ БИБЛИОТЕКИ; пустой запрос и запрос без названного — отказ. Юниты (`cli/test/template-gate.test.ts`, **17 passed**): смешанный `still+kenburns` ПРОХОДИТ и запись ложится по НАЗВАННОМУ (`still@1.gates.json` есть, `kenburns@1.gates.json` нет); запрос без названного — отказ; сосед вне библиотеки (`solid@1`) — отказ. **ЖИВАЯ РЕПЕТИЦИЯ КОМАНДЫ** на смешанном запросе (scale 0.5, workers 4, 6 кадров, профиль `gate-profiles/draftHalf.yaml`): `vpe template gate kenburns@1` ⇒ **ГЕЙТ: PASS**, N=3, три прогона дали один `sha256` `666084110f838c12…2819` и один `framemd5` `5473d656235cd9bd…08a5`, `wallMs` 1390/1415/1401, отпечаток `61db2ca8…63a0`; запись создана `/tmp/vpe-fix01-gates-CXB6/kenburns@1.gates.json`, путь напечатан, код выхода 0. Это первый гейт `kenburns@1`, снятый КОМАНДОЙ, — прежним правилом он был неисполним.
- `18:27` [4/5] Ш4 (№184) начат
- `18:29` [4/5] Ш4 (№184) ГОТОВО. Юнит Н1 в `templates.test.ts` инвертирован: фильтр «похоже на кривую» вместо `isEasingId`, форма `<слово>.<слово>` с необязательными скобками плюс точное `none`. ЗАМЕР формы по РАЗРЕШЁННЫМ текстам пяти реализаций + `solid@1`: **102 литерала, 1 срабатывание** (`power3.out`, объявлен) — ложных ноль. Контроль форм: `back.out(1.7)`✓ (иначе `back.out(2.5)` прошёл бы), `none`✓, `cover`/`#ffffff`/`50%`/`bold`✗. `elastic.out` ловится и членства не проходит. **36 passed.**
- `18:29` [5/5] Ш5 начат — протокол нарушений Н1–Н4
- `18:34` [5/5] ПРОТОКОЛ НАРУШЕНИЙ ГОТОВ: четыре пробы, **четыре красных**, находок нет. Н1 двойной масштаб → `scale-render` дал 100/0/0/0; Н2 гейт назван по шаблону вне запроса → отказ R12, код 1, записей 0 (откатывать нечего — нарушение во ВХОДЕ); Н3 `elastic.out` литералом → юнит Н1 краснеет на членстве в D5 (дифф нарушения ровно 1 строка); Н4 снят `data-start` у групп → `captions-visibility` краснеет на кадрах 3 и 6 — это и есть доказательство НОВОГО смысла охранника (на снятии трёх строк opacity он был зелёным). Откаты только `cp`, porcelain после каждого — восемь строк пакета, резервов нет; контрольные прогоны затронутых файлов зелены. Транскрипт — `docs/impl/FIX-01/violation-transcript.txt` (168 строк).
- `18:40` [5/5] доки: DEBTS — №181/182/184/185 ЗАКРЫТЫ; новые заведены с **№190**, а не со №187: номера 187/188/189 уже заняты долгами `H-06` (шрифт из машины, числа полосы, критерий на `bed@1`) — проверено грепом, взять №187 значило бы столкновение. Заведены №190 (границы формы «похожести» кривой) и №191 (`manifest.scale` после №182 не читает никто). invariants — ревизия `FIX-01`: переходов НЕТ, счёт `guarded` 64; построчно R12/R13/D5/R2.
- `18:41` [5/5] полный `pnpm test` — начат, ожидаю ~8 мин
- `18:47` [5/5] ПРИЁМКА. `pnpm test` — **147 файлов, 2265 passed / 2 skipped / 0 failed, 491.8 c** (было 146/2260/2). Разность сходится нацело: +1 файл `scale-render.test.ts`, +5 тестов = +3 его утверждения +2 от охранника команды (один прежний тест заменён тремя). `pnpm typecheck` — чисто. `pnpm lint` — чисто.
- `18:49` [5/5] ГОТОВО. Дифф поверх `a374ccf` — восемь правленых файлов (+252 −81) и четыре новых: `test/scale-render.test.ts`, `docs/impl/FIX-01/{report.md,violation-transcript.txt,measurements.txt}`. Ни одного `.fix01bak`, worktree один, `fixtures/` не тронуты, лок не менялся, закрытые зоны не тронуты.
**Итог:** четыре долга закрыты (№181 смягчение по формулировке владельца + живая репетиция гейта `kenburns@1`; №182 снятие второй простановки масштаба, лишняя половина названа опытом Б; №184 инверсия фильтра, форма измерена — 102 литерала / 1 срабатывание / 0 ложных; №185 три строки сняты, охранник переосмыслен на вендорное поведение с версией в тексте). Протокол — четыре пробы, четыре красных, находок нет. Заведены №190 и №191; нумерация начата с №190, а не со №187 — 187/188/189 заняты долгами `H-06` (проверено грепом). Приёмка зелёная целиком: 147 файлов, 2265 passed / 2 skipped / 0 failed; typecheck и lint чисты.
**ЖДУ ВЛАДЕЛЬЦА:** коммит и пуш — за вами. Имя — `FIX-01 — масштаб, снятие R2-строк, смешанные запросы гейта, линт кривых в mountSource (№181/182/184/185)`.
Детали — `docs/impl/FIX-01/{report.md,violation-transcript.txt,measurements.txt}`.
