# PROGRESS — IMPL-NIGHT-04 (этап A ч.2: VID-02c → SP-VID-DUR) · старт 2026-09-12T02:17+06:00
План: 9 шагов — [0] база install/build/lint/test; [1] чтение VID-02a/b кода и долгов; [2] геометрия окна (пропорция видео); [3] одна формула прямоугольника (стадия+рантайм), move; [4] покадровая дыра только в окне клипа; [5] radius (измерить цену в ffmpeg); [6] loop; [7] пресеты+гейты+демо; [8] кэш-охранник №272 + документы + коммит-1; [9] SP-VID-DUR спайк 10 минут + документы + коммит-2. Бюджет: 5 ч (VID-02c ≤ 3 ч).
- `02:17` [0/9] база: install/build/lint/test начато
- `02:50` [0/9] база: build чисто, lint 0; полный vitest ещё идёт (фон, один процесс)
- `02:50` [1/9] чтение готово. РЕШЕНИЕ СЕССИИ (пересмотреть): браузер НЕ МОЖЕТ считать окно врезки — пропорция видео живёт в паспорте ассета (`intrinsic`), а в IR её нет и быть не должно. Значит «одна формула» реализуется сильнее, чем просил текст задания: прямоугольник считает ОДНА чистая функция на Node, из неё строятся и выражения ffmpeg, и ТАБЛИЦА ШАГОВ дыры, которая едет в композицию данными. Второй реализации формулы нет вовсе.
- `02:50` [1/9] РЕШЕНИЕ СЕССИИ (пересмотреть): `move` — линейный по номеру кадра, без кривой. Причина та же, что у `zooms` в VID-02a: реестр кривых D5 принадлежит браузеру, а прямоугольник считает Node и исполняет ffmpeg; вторая реализация `power2.inOut` была бы ровно тем расхождением, что запрещает ADR-0002.
- `03:20` [0/9] НОРМА записана с оговоркой: фоновый полный прогон стартовал ДО правок и читал файлы по ходу — он загрязнён моими же изменениями (одна ложная краснота `video-underlay-determinism`, воспроизводится только на полу-правленном дереве; отдельный прогон после правок — 1/1 зелёный). Норма принята как «0 failed» по чистому дереву 2cfc415 (build чисто, lint 0); полный прогон пересниму в конце ОДНИМ процессом.
- `03:20` [2/9] геометрия окна (пропорция видео), videoRectAt/move/loop/radius в плане и в графе ffmpeg — написано, src обоих пакетов компилируется
- `03:45` [3-6/9] стадия+рантайм+спек готовы: покадровая дыра из таблицы в манифесте, move/radius/loop в графе ffmpeg, схема параметров. Дальше — пресеты, тесты, живая проверка.
- `05:10` [3/9] ПЕРЕЕЗД ИЗМЕРЕН ЖИВЬЁМ: граф ffmpeg и чистая функция совпали на ВСЕХ 24 кадрах до пикселя (0 расхождений). Две находки измерением: у `crop` стороны НЕ покадровые (окно ехало, но не росло) — заменено маской альфы; у `overlay` переменная `n` опережает выходной кадр на 1 — поправка названа константой.
- `05:10` [5/9] ЦЕНА (60 кадров 1080×1920, медиана 3): угол 651 мс · угол+radius24 1364 мс (×2.10) · переезд 13807 мс (×21.2) · переезд+radius 31210 мс (×47.9). Дешёвая замена geq четырьмя drawbox измерена (713 мс, ×19.4 дешевле), но НЕ заработала по горизонтали — в дереве осталась правильность, оптимизация ушла долгом с ценой.
- `05:45` [4/9] браузерный охранник дыры зелёный: вне окна клипа слои ниже ЦЕЛЫ (№271), radius скругляет саму дыру (№269). Найдено измерением: твин обязан накрывать ВЕСЬ сегмент, иначе дыра стоит и вне окна.
- `07:05` [8/9] VID-02c закончен: гейты пересняты (draftHalf PASS N=3, final PASS N=10), демо 9/9 пресетов, 12 кадров в docs/impl/VID-02c/frames/, отчёт, DEBTS (закрыты №268/269/271/272, сужен №270, новые №284–№287), roadmap, ai-scenarist, spec export, 00-PROCESS. Полный прогон тестов идёт фоном.
- `07:05` [9/9] СТАРТ SP-VID-DUR — спайк 10-минутного ролика
- `07:38` [8/9] полный прогон: 2800 passed, 3 skipped, **1 failed** — и краснота МОЯ: охранник Н1 (греп кривых по mountSource) увидел `ease: 'none'` у твина покадровой дыры. Починено объявлением `easingIds: ['none']` с объяснением, что это не кривая движения. Перепрогон файла — 46/46.
- `08:30` [9/9] SP-VID-DUR готов. Проба 599.7 с (49 сцен, 17 991 кадр). ЧИСЛА: холодный draftHalf ≈20 мин, прогретый 16.97 с, правка одной сцены 47.06 с (1 промах / 48 попаданий), пик RSS 279–292 МБ, финал 64.3 МБ, диск build/tmp 12 ГБ. ГЛАВНЫЙ ЛИМИТ — кадры сегментов не удаляются после энкода (долг №288, правка ≈10 строк). Вердикт: 10 минут можно сейчас, 20 — после этой правки. `final`/`verify ac4` не гонялись — расчёт назван экстраполяцией.
- `08:30` РЕШЕНИЕ СЕССИИ (пересмотреть): №288 не чинится в этой сессии — правка трогает рендер-путь, только что прошедший полный прогон, и переснять его в остаток сессии было бы нечем.
- `09:25` ПРИЁМКА: `pnpm lint` 0 · `pnpm build` чисто · полный прогон **2801 passed, 0 failed, 3 skipped** (189 файлов, 706 с). Вторая моя краснота — `purity.test.ts` «кривые объявляют ровно три шаблона» — починена вместе с первой (`easingIds: ['none']` у `video@1` — не кривая движения, а имя в тюне покадровой дыры).
**Итог:** VID-02c — видео закрыто: окно в пропорции видео (дефект VID-02a), дыра по кадрам только в окне клипа, move/radius/loop, одна формула прямоугольника, гейты и демо пересняты, кэш-охранник №272. SP-VID-DUR — 10-минутный ролик собирается: холодный черновик 20 мин, прогретый 17 с, правка сцены 47 с; единственный лимит — 12 ГБ неудалённых кадров (долг №288). Бюджет 5 ч превышен: по стенке ~7 ч 10 мин. Детали — docs/impl/VID-02c/report.md, docs/impl/SP-VID-DUR/report.md.
