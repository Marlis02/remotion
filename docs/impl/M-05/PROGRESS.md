# PROGRESS — IMPL-M-05 «кэш стадий и ключи» · старт 2026-08-25T07:20Z
План: 6 шагов (чтение → план владельцу → механизм кэша+ключи → cacheKeyView данными+golden →
матрица мутации+сверка → интеграция voice, доки). Бюджет: сети и платных вызовов нет.
- `07:20` [0/6] чтение начато — roadmap M-05, ADR-0006 целиком, K1/K2/K3/K5/K6/K10/V15/P6,
  долги 56/59/68/87/89/105, код `voice/plan/*`, `media/store/*`, схемы профилей, границы ADR-0009
- `07:35` [0/6] чтение готово за 15 мин — найдены три структурных факта, определяющих план:
  (1) `media` не резолвит `@vpe/voice`, `voice` не резолвит `@vpe/schema` (симлинки проверены) —
      матрица не может лежать в одном файле; (2) `.cache` игнорируется git, а `voiceKey` в
      take-файле ОТСУТСТВУЕТ ⇒ реестр `voiceKey → sha256` сегодня невосстановим сканом;
      (3) `compileProfile` входит в `segmentKey` целиком по ADR-0006 §5, но K5 требует, чтобы
      `projectSampleRate` в ключ немого сегмента не входил
- `07:35` ЖДУ ВЛАДЕЛЬЦА: план из 6 шагов и 6 вопросов §4 — ниже в чате, до «ок» кода нет
- `07:52` [1/6] владелец дал «ок» на план и все шесть ответов: views JSON + resolveJsonModule;
  canonical → media (core-model НЕ открывается); take-файл получает `voiceKey` + манифест;
  категория `upstream` + `maxChunkChars` в `SpeechPlan`; GC не входит; статусы приняты.
  Дополнение: оговорка «у `voice` `profileId` не применим» — в шапку `layout.ts` и в отчёт
- `07:52` [2/6] начато: механизм кэша, ключи, перенос канонической формы — ожидаю ~40 мин
- `08:41` [2/6] готово за 49 мин — механизм кэша (`.cache/<stage>[/<profileId>]`, манифест,
  `writeAtomic`), три ключа, `cacheKeyView` тремя JSON, перенос канонической формы в `media`,
  интеграция `voice`. `tsc --build` зелёный, 1435/1435 тестов зелёные (было 1432 + 3 красных)
- `08:41` НАХОДКА №1: линт `V-04` (долг №85) покраснел на первой редакции кэша — запись несла
  измеренные края. Он ПРАВ: правка `.cache/voice/<key>` подставила бы в коммитимый take-файл
  выдуманный лид-ин. Исправлен КОД, не тест: в кэше живёт только невоспроизводимое
  (`sha256`, `numSamples`, `sampleRate`, `alignment`), края и вердикт приёмки пересчитываются
  на попадании теми же функциями, что на промахе. K3 стал свойством построения
- `08:41` НАХОДКА №2: take-файл без блока `bind` межсборочный кэш НЕ восстанавливает — в нём
  нет ответа провайдера, а он невоспроизводим. Пропуск ГРОМКИЙ, списком в результате
- `08:41` [3/6] начато: матрица мутации, golden `cacheKeyView`, тесты K3/K5, протокол нарушений
- `09:06` [3/6] механизм + матрица + golden: 59 тестов зелёные. Матрица покрывает **62 поля**
  трёх схем (33 в ключе), недостижимых нет — необязательные поля добраны вторым образцом
  (`render.ac4.yaml`). Счёт печатается в golden `cache-matrix.txt`
- `09:06` НАХОДКА №3: `pixelProfile.jpegQuality` необязателен (запрещён при `png`) — проектор
  падал. «Поля нет» и «поле есть» обязаны быть РАЗНЫМИ входами ключа: заведён признак
  `optional` в данных view и обёртка-список (`[]` против `[value]`), инъективная и при `null`
- `09:06` НАХОДКА №4: первая редакция матрицы сравнивала мутанты ВТОРОГО образца с базой
  первого — краснела бы на каждом поле `ac4`. Поймано самой матрицей; база теперь своя у
  каждого образца
- `09:06` [4/6] начато: матрица стадии `voice` (категория `upstream`, долг №87), K3 холодный/
  прогретый, протокол нарушений
- `09:22` [4/6] готово — матрица `voice` (9 строк upstream), K3 холодный/прогретый, линт
  единственной канонической формы. Полный прогон: 90 файлов, **1510 тестов зелёные**
- `09:22` НАХОДКА №5 (матрица): применимая роль ВСЕГДА перекрывает `project.yaml → voice.voiceId`
  — `voice_id` в `voice-roles/1` обязателен. Строка матрицы проверяется в условии, где поле
  действует (база без назначения роли); свойство записано отдельным утверждением
- `09:22` [5/6] протокол нарушений: 9 нарушений, 9 красных, 9 откатов — записан в
  docs/impl/M-05/violation-transcript.txt. НАХОДКА №6: тесты `voice` видят данные `media`
  только после `tsc --build` (резолв в `dist/`) — ограничение в отчёт §7
- `09:22` [6/6] начато: отчёт, ревизия invariants.md, долги
