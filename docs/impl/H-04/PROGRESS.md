# PROGRESS — H-04 гейт детерминизма шаблона (V13) · старт 2026-08-28T20:20:32+06:00
- `20:25` ЯКОРЬ (измерен ДО чтения): `pnpm test` целиком — 129 файлов / 2099 тестов зелёные, wall 245.4 s (`real 4m06.9`), node v25.6.1, pnpm 11.9.0 (PATH через ~/.nvm/versions/node/v25.6.1/bin — `pnpm` не в дефолтном PATH). Размеры входов: `renderer-hyperframes/src` 14 файлов, `run.ts` 611 стр., `materialize.ts` 426, `validate.ts` 422; `templates-spec/src/gate.ts` 171, `manifest.ts` 289.
- `20:28` ЧТЕНИЕ готово (~23 мин): roadmap §3 H-04/E-00 + §4.9 H-04/H-05/E-00; ADR-0008 «Гейт детерминизма шаблона» (процедура 1–5, три класса, область действия, «постоянная часть конвейера»); invariants R12/R14 + шапка (счёт 62); DEBTS №156–167; код `templates-spec/src/{gate,manifest,spec,registry}.ts`, `renderer-hyperframes/src/{run,contract,fingerprint}.ts`, `test/{solid,fixture,render,isolation-render}.ts`, `media/src/assemble/{artifact,framemd5}.ts`; спайки `sp3f/determinism.mjs`, `sp3f/where.mjs`, `sp3/lib/media.mjs`.
- `20:28` ИЗМЕРЕНО (вопрос задания «включён ли bitexact в профилях»): `encoder.bitexact: true` во ВСЕХ трёх профилях фикстуры (`render.final.yaml:37`, `render.draft.yaml:36`, `render.ac4.yaml:32`); `media/src/assemble/encode.ts:190` разворачивает его в `-fflags +bitexact -flags:v +bitexact`. ⇒ FLAKY-по-контейнеру НЕ является нормой на этой паре; ожидание — PASS (мини-гейт N=2 в `render.test.ts` его уже подтверждал).
- `20:28` ИЗМЕРЕНО (граница пакетов): `@vpe/media` у рендерера — **devDependency**, карта ADR-0009 даёт `renderer-hyperframes → {core-model, templates-spec}` и охраняется `tests/boundaries/adr0009-graph.test.ts`. ⇒ `runGate` НЕ МОЖЕТ звать `buildSegmentArtifact`/`framemd5Of` напрямую: кодирование+измерение приходит ВХОДОМ-портом, а склеивает его вызывающий (`E-00`/тест, у которых media есть).
- `20:28` ИЗМЕРЕНО: `renderSegment` вызывается в 24 местах (`test/{render,r2-r3,isolation-render,subprocess}.test.ts`, `bin/render-segment.ts`) — это цена дефолта `gate: 'require'` (вопрос владельца 2).
**План (6 шагов), жду «ок»:**
- [1/6] `src/gate.ts` — `runGate`: N из `GATE_RUNS` (не литерал), профиль-не-пара → отказ; N последовательных `renderSegment` (свежий `tmpDir`, тот же `outputPath`-шаблон); проба отпечатка ДО и ПОСЛЕ; сверки между прогонами (`bundle.hash` → error, `frameCount` → error, `browserLaunchLine` → FLAKY-с-причиной, №161); классификация ПОРЯДКОМ framemd5 → sha256; `GateRecord` через `GateRecordSchema`; `formatGateOutcome`.
- [2/6] `src/where.ts` — `whereReport(runA, runB, ir)`: расходящиеся кадры из ПОКАДРОВЫХ строк framemd5 (сравнение уже есть в данных, второй прибор не нужен); кадр → клипы IR, чьи окна его накрывают; bbox ненулевой разности + PSNR на опорных кадрах по PNG двух прогонов.
- [3/6] R12-контур в `renderSegment` (по ответу на вопрос 2) + правка существующих вызовов.
- [4/6] Юниты: `test/gate.test.ts` (фейковый рендер+энкодер: PASS / FAIL / FLAKY / error×3 / N из схемы / ac4-отказ / запись проходит схему TS-01 и пускает `assertBuildMayStart`, чужой отпечаток — не пускает) и `test/where.test.ts` (синтетические framemd5-листинги + IR с тремя клипами; bbox/PSNR на синтетических PNG).
- [5/6] Браузер (машина владельца): `test/gate-render.test.ts` — `runGate(solid@1, draftHalf)` N=3 живьём + композиция с исключением ⇒ `error`, не PASS (№164).
- [6/6] Протокол нарушений (5 позиций) + `docs/impl/H-04/{report.md,PROGRESS.md,violation-transcript.txt}`, `invariants.md` (R12 по факту), долги №161/164, новые с №168.
- `21:12` РЕШЕНИЯ ВЛАДЕЛЬЦА: (б) `<id>@<n>.gates.json`, схема — `GateRecordSchema` (массив, ≤1 на профиль), отсутствие файла = `gates: []`; `require` дефолтом с текстом отказа, называющим ОБА выхода, `why` в `skip` — непустой; `bin/render-segment` — флаг `--gate-skip <причина>`; PSNR/bbox — в рендерер, порт `GateMedia` с фейком в юните (приём `pcmSource` из `CP-05`), склейка с `buildSegmentArtifact` — в browser-тесте + README как обязанность `E-00`. Кадры 2× без пересъёмки и декод ffmpeg rawvideo — утверждены.
- `21:12` [1/6] начато — `src/gate.ts`.
- `21:25` [1/6]+[3/6]+часть [4/6] готово за 13 мин: `src/gate.ts` (`runGate`, три класса + `error`, N из `GATE_RUNS`), `src/where.ts`, опция `gate` в `RenderOptions` (дефолт-отказ `R12`), `--gate-skip`/`--gate-profile` в `bin/render-segment`, 24 существующих вызова помечены `gate: GATE_SKIP` с причиной. `test/gate.test.ts` — **14/14 зелёные** (0.74 s). `subprocess.test.ts` — 8/8 зелёные, из них 3 новых: подпроцесс БЕЗ `--gate-skip` отказывает `R12` (и текст называет `solid@1` + команду), пустая причина — тоже `R12`, `--gate-profile ac4` — отказ.
- `21:25` НАХОДКА (в отчёт, долг №168): `RenderIrSegment.clips[].frames` типизирован как `FrameInterval = {frameStart, frameEnd}` (`core-model/src/time/interval.ts:37`, компилятор кладёт именно её — `compile/src/render-ir/build.ts:160`), а рантайм композиции читает `clip.frames.start/end` (`composition/runtime.js:141`) и фикстура `H-01` написана по форме РАНТАЙМА. Зелено сегодня только потому, что компилятор и адаптер ещё не встречались на одном значении (`L-01`). `where.windowOf` читает обе формы и называет причину.
- `21:28` [5/6] начато — живой гейт (3 прогона × solid@1 + R12-контур + №164), ожидаю ~5–10 мин
- `21:31` [5/6] ЖИВОЙ ГЕЙТ ЗЕЛЁНЫЙ на этой машине (браузер есть, `unshare`/`ip` есть): `runGate(solid@1, draftHalf)` N=3 ⇒ **PASS**, 1 sha256 и 1 framemd5 из 3; `wallMs` 2858 / 1363 / 1334 (первый прогон платит холодный старт); отпечаток `3217a4cf…3cb95c`; весь файл (3 теста: PASS + R12-контур + №164) — 59.1 s. Запись прошла схему `TS-01` и открыла сборку сегмента; чужой отпечаток — не открыл.
- `21:31` [4/6] `test/where.test.ts` — 11/11 зелёные (0.23 s): кадры 7–9 → клип `r:mid` (75 % окна) против фона (25 %), bbox 3×2 с maxLevel 4, PSNR +inf на равных кадрах, обе формы `frames` (`frameStart`/`start`).
- `21:31` [6/6] начато — полный прогон + lint в фоне, затем протокол нарушений и доки.
- `21:44` [6/6] протокол нарушений: пять нарушений внесены в `src/gate.ts` (резерв/откат `cp`), КАЖДОЕ поймано — Н1 (одна величина) 3 теста, Н2 (FAIL пишет запись) 1, Н3 (`ac4` как пара) 1, Н4 (порядок перевёрнут) 4, Н5 (N литералом) 1; после отката 25/25 зелёные. Транскрипт — `docs/impl/H-04/violation-transcript.txt`.
- `21:44` ПОЛНЫЙ ПРОГОН: **132 файла / 2130 тестов зелёные, 294.6 s** (было 129/2099/245.4 s); `pnpm lint` 0 ошибок; `pnpm typecheck` чисто.
- `21:47` Доки: `docs/impl/H-04/report.md`, `invariants.md` (R12 `named → guarded`, счёт 62 → 63, ревизия), `DEBTS.md` (№161 сужен вторично, №164 проверен делом, новые №168–172), README пакета (порт `GateMedia` + опция `gate`).
**Итог:** гейт V13 снимается библиотечной функцией `runGate` через тот же `renderSegment` (N из схемы, порядок framemd5→sha256, проба отпечатка до/после, четыре между-прогонные сверки); `where` раскладывает расхождение по клипам IR и меряет bbox/PSNR; сборка сегмента БЕЗ решения о гейте не стартует по умолчанию. Живой гейт на этой машине: PASS, запись целиком в отчёте. Детали — `docs/impl/H-04/report.md`.
