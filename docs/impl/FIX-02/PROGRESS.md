# PROGRESS — IMPL-NIGHT-02 (FIX-02 + VID-02a) · старт 2026-09-11
План: шаг 0 (install/build/lint/test) → FIX-02 (измерить/исправить/охранник/пере-гейт/доки) → VID-02a. Бюджет: 5 ч стенки, FIX-02 ≤ 1.5 ч.
01:43 [0/N] шаг 0: pnpm lint + pnpm test начаты
- `01:43` [0/8] шаг 0 начат — install/build/lint/test
- `01:54` [0/8] готово: install ok, `pnpm -r build` ok, `pnpm lint` **0**, `pnpm test` **2639 passed / 3 skipped / 0 failed** (178 файлов, 635.9 с). Норма задания «2642» = ВСЕГО тестов; фактическая раскладка записана.
- `01:55` [1/8] FIX-02 §1.2: прибор-линейка `work/fix-02/` (4 угловых квадрата + полоса на `bottom:500`), рендер на scale 1 и 0.5 — начато
- `01:57` [1/8] готово: ДИАГНОЗ. При `scale 0.5` кадр 540×960, но содержимое НЕ уменьшено: ЛВ-квадрат 100×100 (ждали 50×50), три других угла и полоса — ВНЕ кадра. То есть рендерер уменьшает ВЬЮПОРТ и не трогает СОДЕРЖИМОЕ; диагноз `FIX-01` «масштаб дважды» неверен во второй половине.
- `01:58` [2/8] правка: единственный CSS-масштаб на `#root` в `materialize.ts` — начато
- `02:10` [2/8] ИЗМЕРЕНИЕ ПРОТИВ ПЛАНА: `transform: scale()` на `#root` НЕ ГОДИТСЯ — браузер красит только ту часть страницы, что лежит внутри вьюпорта в НЕмасштабированных координатах: фон растянут на 270×480 из 540×960 (тот самый «100/0/0/0» `FIX-01`, но причина в отсечении отрисовки, а не во втором масштабе). Пробую `zoom` (он меняет РАСКЛАДКУ, значит вьюпорт совпадает).
- `02:35` [2/8] ПРИЧИНА НАЙДЕНА В ВЕНДОРЕ: `RUNTIME_IIFE` HyperFrames 0.8.5 перезаписывает `#root.style.width/height` инлайном на `data-width`/`data-height` (540×960) и ставит `overflow:hidden` — слои 1080×1920 обрезаются. Поэтому масштаб поставлен на `.layer` (`transform: scale(s); transform-origin: 0 0`), не на `#root`.
- `02:36` [2/8] готово: линейка на `scale 0.5` — ВСЕ пять маркеров на местах x·0.5 ± 0 px, полоса 540×60 на y=650, кадр занят целиком (0,0…539,959). Прямой скриншот Chrome тем же html подтверждает верную картинку.
- `02:12` [3/8] готово: охранник `scale-render.test.ts` переписан пиксельным (линейка `test/markers.ts`, 8 утверждений), `captions-visibility` получил третье — на `scale: 0.5`. Протокол нарушений: Н1 (масштаба нет) — 7 красных; Н2 (масштаб на `#root`) — 5 красных; Н3 (масштаб дважды) — 6 красных; контроль зелёный 11/11.
- `02:13` [4/8] пере-гейт `draftHalf` шести шаблонов — начато (запросы уже перегенерированы: изменились ровно 6 draftHalf-файлов, все 6 `final` побайтно равны)
- `02:14` [4/8] готово: шесть гейтов `draftHalf` — PASS, N=3, отпечаток `3217a4cf…`. Изменились ровно 6 записей `draftHalf`; все 6 записей `final` побайтно равны (diff пуст).
- `02:19` [5/8] готово: `verify ac4` на `examples/ai-test-1` (scale 0.25) — ПРОГОНЫ РАВНЫ, нулевой порог, все 9 величин `=`; стенка **209 с**.
- `02:20` [6/8] демо всех семи шаблонов + кадры — начато
- `02:26` [6/8] готово: демо всех семи — 7 из 7 за 46 с; кадры в `docs/impl/FIX-02/frames/`. `captionEmphasis@1` на `draftHalf` больше не чёрное поле: 185 кадров из 210 несут картинку (было `YMAX=16` по ВСЕМ).
- `02:33` НАХОДКА: первая сборка проекта с ПРОГРЕТЫМ кэшем `draftHalf` отказывает правилом K3 — `bundle.hash` в `segmentKey` не входит, а композиция черновика изменилась. Это ПРОЕКТНОЕ поведение (`CACHE-01`), лечится `rm -rf <проект>/.cache/segment/draftHalf`. Кэши трёх проектов вычищены, сборки перезапущены.
- `02:46` [7/8] готово: `examples/pompeii-30` (877 кадров, 55 с) и `work/asset-test` (1119 кадров, 60 с) пересобраны на `draftHalf` — кадрирование полное, субтитры на месте; кадры в `docs/impl/FIX-02/frames/`.
- `02:48` [8/8] доки FIX-02 готовы (report, roadmap, invariants, DEBTS №265/№182/№191 закрыты/переоценены, новые №266/№267, gate-runbook §0-ter). Полный прогон тестов — начат.
- `03:05` [8/8] ЧАСТЬ 1 ГОТОВА. `pnpm test` — **2645 passed / 3 skipped / 0 failed** (было 2639/3/0; +6 сходится нацело: scale-render 3→8, captions-visibility 2→3). `pnpm lint` 0. Стенка FIX-02: 01:43→03:05 = **1 ч 22 мин**.
- `03:06` ЧАСТЬ 2 — VID-02a. Начинаю с измерения развилки §2.3 (дыра или два прохода).
- `03:12` VID-02a §2.3 РАЗВИЛКА РЕШЕНА ИЗМЕРЕНИЕМ — **ДЫРА**, не два прохода. `backdrop-filter: opacity(0)` дыры НЕ даёт (кадр побайтно равен варианту `none`), а `clip-path: path(evenodd, …)` на слоях ниже по `z` даёт РОВНО дыру: внутри прямоугольника RGBA `0,0,0,0`, снаружи пиксели не тронуты. Ставится инъекцией `<style>` — прецедент `captionEmphasis@1`. Цена: дыра СТАТИЧНА, значит ручка `move` (переезд прямоугольника) в эту версию не входит.
- `03:12` Побочно измерено (долг №267): MAD «уменьшенный final vs draftHalf» на НАСТОЯЩЕМ содержимом — фото `still@1` **0.1947**, фото+`grade@1` с зерном **1.8939**. И проверено, что трансформа `.layer` из FIX-02 НЕ сломала `backdrop-filter` у `grade@1`: средний RGB кадра совпадает на обоих профилях (118.5/113.7/107.5), без грейда — 123.3/123.3/123.3.
- `03:40` VID-02a: собраны спек `video@1`, реализация (прозрачный слой + дыра `clip-path`), стадия `compositeVideoUnderlay` в `@vpe/media`, разворот плана `video-plan.ts`, врезка стадии в `renderSegments`. Всё собирается. Дальше — первая живая сборка на `work/asset-test` с `demo-video`.
- `03:52` VID-02a: гейты `video@1` сняты — `draftHalf` N=3 PASS (7 с), `final` N=10 PASS (16 с), различных framemd5/sha256 по 1. Видео гейта — синтетический mp4 64×36, 24 кадра, **2724 байта**. Запросы гейта порождены билдером; чужие 12 файлов не тронуты. Побочно починена курица-и-яйцо в `gate-requests.test.ts`: список владельцев шахматки считался из ГОТОВЫХ файлов запросов, и для восьмого шаблона файл тестов не стартовал вовсе (ENOENT на сборе).
- `04:28` НАЙДЕНА РЕГРЕССИЯ FIX-02, найдена ПО ДОРОГЕ к VID-02a: `kenburns@1` анимирует слой НИЖЕ себя через GSAP (`x`/`y`/`scale`), а GSAP пишет ИНЛАЙНОВЫЙ `transform` — и он затирал CSS-масштаб слоя из FIX-02. Измерено: MAD «уменьшенный final vs draftHalf» на связке `still@1`+`kenburns@1` = **34.41** (без кенбёрнса 0.19). Охранник не ловил: линейка `markers@1` ничего не анимирует.
- `04:30` ЛЕЧЕНИЕ: масштаб слоя ставится `zoom`, а не `transform` — GSAP пишет `transform`, и два свойства не спорят. Измерено после правки: маркеры ±0 px, MAD с кенбёрнсом **0.3184**. Гейты `draftHalf` придётся снять ЗАНОВО (композиция снова изменилась).
- `05:05` VID-02a: гейты пересняты на `zoom`-композиции — семь `draftHalf` PASS N=3; шесть записей `final` за всю сессию НЕ изменились ни байтом (diff с исходным срезом пуст). Демо `video@1` собрано: 4 пресета из 4, 452 кадра, 17.4 с. По дороге вылечен off-by-one: `loop` повторяет кадр, не трогая PTS, и `overlay` терял кадр — лечится перенумерацией `setpts=N/fps/TB`.
- `06:40` Бюджет 5 ч исчерпан. Состояние: обе части сделаны, оба гейта `video@1` PASS, демо 8/8, живая проверка снята кадром. Осталось: финальный полный прогон (последние правки счётчиков «семь → восемь» проверены поштучно, но одним процессом ещё не прогонялись) и перенос PROGRESS.
- `07:35` ФИНАЛ: `pnpm lint` **0**, `pnpm test` **2700 passed / 3 skipped / 0 failed** (182 файла, 652 с). Было до сессии 2639/3/0 → +61. Разложение: FIX-02 +7 (scale-render 3→9, captions-visibility 2→3), VID-02a +32 (video-underlay 18, video-plan 13, детерминизм 1), плюс +22 от восьмого шаблона в существующих переборах по каталогу (спеки, реестры, гейт-запросы, демо, spec export).
