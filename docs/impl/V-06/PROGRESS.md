# PROGRESS — IMPL-V-06 провайдер ElevenLabs · старт 2026-08-31T13:33:55+06:00
План: preflight → чтение → план владельцу → провайдер → выбор по providerId → приёмка → ролик → доки.
Бюджет: ≤ 4000 code points отправлено (живые вызовы).
- `13:33` [0/N] якорь снят; node v25.6.1; `.env` есть, в `.gitignore` (строка 1). Значение не читаю.
- `13:56` [0/N] чтение закончено: ADR-0010 §1/§2/§4–§8, roadmap V-06, DEBTS 13/73/88/118/188/197, voice/providers/*, plan/record.ts, acceptance/ladder.ts, cli/build-stages/pipeline.ts, охранники V9 и ADR-0010 §8. Пишу загрузчик `.env` (preflight).
- `13:38` PREFLIGHT: `.env` подхватывается? НЕТ — ни `bin/vpe.ts`, ни vitest его не читают; написан `packages/cli/bin/env-file.ts` (свой разбор `KEY=value`, окружение важнее файла, значений не печатает).
- `13:39` PREFLIGHT: сеть до `api.elevenlabs.io` — ДА (ответ JSON получен). `ELEVENLABS_VOICE_ID` виден процессу — ДА (20 символов, формат id голоса). `ELEVENLABS_API_KEY` виден — ДА, но это НЕ ключ: `GET /v1/user/subscription` → HTTP 400 `invalid_api_key` / `api_key_id_used_as_api_key` («API keys start with sk_»). Тариф не читается, живой вызов невозможен.
- `13:40` ЖДУ ВЛАДЕЛЬЦА: нужен настоящий `sk_…` в `ELEVENLABS_API_KEY`; плюс два вопроса плана. Офлайн-часть делаю не дожидаясь.
- `13:58` [1/6] код провайдера готов: `providers/{http,elevenlabs,usage,registry,source}.ts`, ставка в provenance, стичинг текстом через `conditionedOn`, реестр по `providerId`, `.env` и транспорт в `bin/`. `tsc --build` зелёный. Живых вызовов: 0. Отправлено code points: 0.
- `14:12` [2/6] офлайн-тесты зелёные: `elevenlabs` 16, `provider-registry` 11, `billing-rate` 14; линты V9 и ADR-0010 §8 обновлены (реестр НЕ пополнен — сеть входом), новый линт секретов Н1 (4). Полный прогон до правок: 2 красных — оба мои, починены. `.env` в тестах: секреты да, `ELEVENLABS_LIVE` нет (проверено пробой). Живых вызовов: 0.
- `14:30` [3/6] протокол нарушений: Н1 RED (линт секретов), Н2 RED (юнит тела запроса), Н3 RED (охранник ставки), Н4 RED (V9-линт), Н4b RED (снят ранний охранник сборки — отказ приходит слоем ниже, но теряет инструкцию). Все откаты зелёные. ADR-0010: §1 (два места), §2 (planRateAtGeneration), §4 — правки внесены. Живых вызовов: 0.
- `15:02` [4/6] доки: ADR-0010 (§1 ×2, §2, §4), DEBTS (закрыты 118/197/13/188, сужен 88, заведены 213–216), roadmap (SP-2 №13 закрыт), invariants (ревизия V-06, переходов нет, счёт 67), отчёт и транскрипт. `eslint .` был красным на `JSON.stringify` в теле запроса — заменено на `canonicalJson` (ADR-0007 §3), теперь чист. Живых вызовов: 0.
- `15:02` ЖДУ ВЛАДЕЛЬЦА: в `.env` по-прежнему ID ключа (64 символа, без `sk_`), `ELEVENLABS_RATE_PER_CODEPOINT` нет. Пункты 5 задания (ролик) и живой тест — заблокированы.
- `15:12` [5/6] полный прогон: **159 файлов, 2401 зелёных, 3 skipped, 2 красных** — обе красноты в НОВОМ линте секретов, и находка настоящая: транскрипт нарушений цитировал поддельный ключ `sk_…` дословно, охранник его поймал (отличить подделку от настоящего он не умеет и не должен). Цитата убрана, линт зелёный. `eslint .` и `tsc --build` чисты.
- `15:22` [5/6] по вопросу владельца про ставку: заведён ЧЕТВЁРТЫЙ вердикт охранника `not-declared` — необъявленная ставка больше не подменяется единицей (на Creator это красило бы здоровый прогон). Юнит +1, живой тест печатает причину. `tsc`, `eslint`, целевые прогоны зелёные.
- `15:31` PREFLIGHT-2: **ключ валиден: ДА** (`sk_…`, 51 символ; значение не печатается). Тариф `creator`, `character_count` 2268 из 125173 — ровно число SP-2b, с тех пор не потрачено ничего. Права проверены тремя бесплатными вызовами: User (тариф) 200, Voices Read (`Michael C. Vincent`, класс **professional**) 200, usage/character-stats 200. `ELEVENLABS_RATE_PER_CODEPOINT=0.55` задана.
- `15:31` [6/6] НАЧАТО: живой тест — ОДИН платный вызов, текст 81 code point. Ожидаю ~1 мин (в нём два ожидания по 25 с ради окна usage).
- `15:32` [6/6] готово за ~31 с — **живой дубль ПРИНЯТ приёмкой V-02**: `charIdentity: true`, `uniqueRatio 1.000`, `maxEqualRun 1`, `tailResidual 8 сэмплов` (допуск 24), лид-ин **4080 сэмплов = 170.0 мс**. Расход: отправлено **81** code point, списано **45**; ставка 0.55 обещала 45 → вердикт `match`, фактическая 0.5556. Окно usage осело за ~25–50 с. Накопительно: отправлено 81, списано 45.
- `15:33` НАЧАТО: живая сборка демо `examples/vertical-v1` на `--profile final` (браузер, 3 сегмента). Ожидаю ~3–6 мин, расход ~460 code points.
- `15:40` ОШИБКА: живая сборка демо упала ДО синтеза — строгий читатель take-файла отверг мокшные дубли `H-07` (`planRateAtGeneration` нет вовсе). Это долг №216, сработавший в ту же сессию, в которой заведён. Что делаю: отказ получил НАЗВАННУЮ причину и инструкцию (смена формата, а не порча файла); мокшные дубли демо удаляю — они всё равно промах по `voiceKey`, то есть пересъёмка неизбежна и ничего оплаченного не теряется; их записи `kind: voice` из `store.lock` убираю (на них больше никто не ссылается).
- `15:42` НАЧАТО (повтор): живая сборка демо на `--profile final`. Дублей на диске нет — платятся все три чанка, ~460 code points.
- `15:46` [6/6] дубли ОПЛАЧЕНЫ И ПРИНЯТЫ: 4 чанка, отправлено **271** code point (75/47/109/40), все `accepted`, `ratio 1.000`, `tailResidual` −9…+7 при допуске 24, лид-ины 960…5280 сэмплов (40…220 мс), в провенансе `creator` / `professional` / ставка 0.55. Накопительно: отправлено **352**, списано ≈**194**.
- `15:46` ОШИБКА: сборка упала ПОСЛЕ синтеза, на компиляции — `ADR-0003 T7`, `anchors.ts`: у первого чанка привязка первого слова (0 в сыром PCM) лежит ДО лид-ина (960). Это НЕ порча данных: провайдер относит ведущую тишину к первому символу (`start = 0.000`), а акустический детектор T7 находит речь на 40 мс. На `tts:mock@1` лид-ин 0 по построению, поэтому ассерт никогда этого не видел. Правка — в `packages/compile` (зона, запрещённая заданию).
- `15:47` ЖДУ ВЛАДЕЛЬЦА: решение по правилу пересечения привязок с интервалом речи (три варианта в чате). Оплаченные дубли на диске, повторная съёмка не потребуется ни при каком варианте.
- `16:31` [7/8] правка `compile/timeline/anchors.ts` по разрешению владельца: пересечение привязки с окном речи, пустое ⇒ `absent`. Мёртвый отказ `ADR-0003 T7` из функции убран. Тесты: новый файл `anchors-speech-window.test.ts` (3 из 3) + прежние `anchors.test.ts` (11 из 11) зелёные. `tsc` и `eslint` чисты.
- `16:33` [8/8] ГОТОВО: **`final.mp4` собран на живом голосе** — 19.6 МБ, 1080×1920, 502 кадра, 16.733 с, аудио AAC 48 кГц моно. Стадия `voice` не звала источник ни разу (попадание `voiceKey`), то есть пересъёмки не было. `store.lock`: четыре записи оплаченных блобов вписаны руками (sha из дубля, size с диска) — `vpe store verify` сверил все восемь побайтно, код 0.
- `16:34` Долги №217 (мок не умеет лид-ин) и №218 (store.lock пишется в конце) заведены. Расход итоговый: отправлено **352** code points, списано ≈**194** из бюджета 4000.
- `16:50` ФИНАЛ: полный прогон — **160 файлов (159 зелёных, 1 пропущен), 2407 тестов зелёных, 3 skipped, 0 красных**, 571.1 с. `tsc --build` и `eslint .` чисты. Проверка перед пушем: `sk_` в диффе рабочего дерева — **0**; `.env` ни среди отслеживаемых, ни среди неотслеживаемых файлов — **нет**.
**Итог:** живой провайдер `tts:elevenlabs@1` написан и предъявлен делом: живой дубль прошёл приёмку `V-02`, ставка тарифа подтверждена окном `usage/character-stats`, провайдер выбирается по `providerId` проекта, а первый ролик с живым голосом собран (`final.mp4`, 16.733 с). Первый живой дубль нашёл дефект, который мок показать не мог, — правка `compile` сделана точечно по разрешению владельца. Расход: 352 отправленных code point, ≈194 списанных из 4000. Детали — `docs/impl/V-06/report.md`, транскрипт — `docs/impl/V-06/violation-transcript.txt`.
