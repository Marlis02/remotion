# PROGRESS — IMPL-NIGHT-05 этап B ч.1: kineticType@1 + довески №288/№290 · старт 2026-09-12T04:21Z
План: [0] базовый прогон → [1] №288 кадры → [2] №290 охранник → коммит A → [3] чтение → [4] kineticType@1 → [5] пресеты/демо → [6] гейты → [7] живая ct-test-1 → [8] доки → коммит B. Бюджет: 3 ч стенки, стоп 2:45.
- `04:21` [0/8] старт; `vpe-docker prepare` — ok (node v25.6.1, pnpm 11.9.0, ffmpeg 7.0.2)
- `04:22` [0/8] базовый прогон `vpe-docker test` — начат, ожидаю ~10–15 мин
- `04:24` [1/8] №288: `renderSegments` сносит `request.tmpDir` после `cache.put`; флаг `--keep-frames` (build/argv/verify-ac4/template-demo); 5 литералов `BuildArgs` в тестах; `build.test.ts` «каталог композиции» получил `keepFrames: true` (читает tmpDir после сборки)
- `04:28` [1/8] охранник `packages/cli/test/keep-frames.test.ts` написан (кадров нет / с флагом есть / sha256 тот же)
- `04:29` [2/8] №290 сверен по файлам: `video@1` bundleHash записей = bundle.hash запросов (bb741de5…/99f8a109…). Охранник `packages/templates-spec/test/gates-on-current-bundle.test.ts` — по всем папкам шаблонов
- `04:34` [0/8] базовый прогон готов за 739.9 с: **2798 passed / 7 failed / 3 skipped**. ВСЕ 7 — гонка: тесты подхватили созданную по ходу папку `kineticType@1` (реестр не сгенерирован, запросов гейта нет). За вычетом 4 порождённых ею проверок норма = **2801 / 0 / 3**, как и заявлено.
- `04:36` [4/8] spec.ts + impl.ts `kineticType@1` написаны. РЕШЕНИЕ СЕССИИ, ПЕРЕСМОТРЕТЬ: субтитры и `baseWidth` шаблон берёт из `window.__VPE_IR` / `window.__VPE_MANIFEST` (той же дверью, что `video@1`), а не полем `ctx` — правка `runtime.js` сдвинула бы `bundle.hash` всех восьми шаблонов. Цена: зависимость не видна в контракте; долг.
- `04:38` [4/8] реестры сгенерированы; запросы гейта порождены. **ЧУЖИЕ ЗАПРОСЫ НЕ СДВИНУЛИСЬ НИ ОДИН** (`git status`: только `templates/index.ts` и новая папка) — записи гейта семи шаблонов остаются действующими. Шрифт гейта скопирован в свою папку (как у pattern-32.png у трёх шаблонов).
- `04:44` [4/8] переписи обновлены (8→9 реализаций/спеков, 4→5 с кривыми, 1→2 со шрифтом, 7→8 без declareDuration, 6→7 читателей ctx.frames); `style.transform` заменён flex-колонкой (охранник №173); охранник №290 честно красный на `kineticType@1` — записей гейта ещё нет
- `04:45` [6/8] снимаю гейт draftHalf в образе — ожидаю ~30 с
- `04:44` [6/8] ГЕЙТЫ PASS оба: draftHalf N=3 sha e81f0b16… (1.5–3.7 с/прогон), final N=10 sha a5edd071… (1.45–3.7 с/прогон), различных framemd5 — по одному. engineFingerprint 3217a4cf… (тот же).
- `04:44` [5/8] собираю демо шести пресетов — ожидаю ~1–2 мин
- `04:47` [5/8] демо собрано и ПОСМОТРЕНО глазами: punch-word / counter / typewriter / formula — как задумано; stack на длинной кириллической фразе УШЁЛ за правый и верхний край. Это не дефект шаблона, а цена «кегль под длину фразы не подбирается»: проза демо укорочена, кегли `stack-caps-bold` 150→120 и `punch-word` 210→170 (кириллица шире латиницы). Гейты пересняты: draftHalf 7cc1ea2a…, final aaa0d032… — оба PASS.
- `04:50` [7/8] ЖИВАЯ ПРОВЕРКА `work/ct-test-1` (3 сцены, живой голос): сборка draftHalf прошла с тремя записями `kineticType@1` (stack-caps-bold на sc:street, punch-word на b:evict, counter-money на b:shop).
- `04:50` [1/8] №288 ИЗМЕРЕН НА ЖИВОМ ПРОЕКТЕ: `build/tmp` после обычной сборки — **12 КБ** (один `concat.txt`, ноль PNG); та же сборка с `--keep-frames --no-cache` — **689 МБ** (1511 PNG) на 39 с черновика. `final.mp4` ОБЕИХ сборок побайтово один: sha256 e2645f1d… — АС4 не задет.
- `04:54` [7/8] живые кадры сняты и посмотрены: punch-word «DOORWAY» встаёт РОВНО на подсвеченном слове субтитра; stack-caps-bold «NOBODY» — на `b:evict` в такт; counter-money бежит поверх видео. НАЙДЕНО ЖИВЬЁМ: на `z: 20` счётчик лежал ПОД `video@1` (его z 25) и в кадре его не было вовсе, хотя в IR он стоял — исправлено `z: 30`, записать долгом/правилом.
- `04:55` [7/8] собираю один кадр на `final` 1080×1920 и стенку сегмента с kineticType против без — ожидаю ~3–5 мин
- `04:59` [7/8] СТЕНКА (draftHalf, `--no-cache`, тот же проект): БЕЗ kineticType 57 708 мс (24 912 / 7 227 / 12 131 по сегментам), С ним 57 032 мс (24 382 / 6 646 / 12 180). Разность −676 мс, то есть ОТРИЦАТЕЛЬНАЯ — цена шаблона НИЖЕ ШУМА ПРИБОРА на этом проекте. Кадр `final` 1080×1920 снят (`live-04`).
- `05:03` [6/8] браузерные охранники зелёные, 8 из 8 (16.1 с): время появления в пикселях (кадры 0/4/5 — 774 чернил, с кадра 6 — 2 071 836… — второе слово РОВНО на своём кадре), вход двигает геометрию а не включает текст, раскладка шести пресетов внутри safe-area. Первая редакция мерила ТЁМНОЕ прибором субтитров и была красной на фоне — заведён `kinetic-ink.ts` (белое на чёрном), причина записана в его шапке.
- `05:05` [6/8] охранник 6 (кэш: правка `sizePx` → промах ровно своего сегмента) и охранник 7 (кривая вне реестра D5 → отказ схемы) зелёные; `kinetic-type-spec.test.ts` — 20 утверждений схемы.
- `05:06` [8/8] полный прогон в образе — начат, ожидаю ~13 мин; параллельно пишу доки
- `05:18` [8/8] прогон готов за 772.6 с: 2880 passed / 4 failed / 3 skipped. Все 4 — переписи и одна моя ошибка прибора: реестр 8→9 (materialize), «1 из 8 шаблонов» (template list), пример spec export (гонка с правкой по ходу) и `keep-frames.test` — он считал кадры ПЕРЕЧИСЛЕНИЕМ `tmp/segments/*`, а уборка сносит сами каталоги, то есть список пустел и ответ был бы зелёным при любой сборке. Прибор заменён обходом `tmp` по PNG; порядок прогонов перевёрнут (сначала с флагом — эталон). Все четыре зелены точечным прогоном.
- `05:23` [8/8] ПРОТОКОЛ НАРУШЕНИЙ: шесть охранников сломаны правкой КОДА и показаны красными (расписание +1 кадр — 3 табличных и 3 пиксельных; знаменатель счётчика — 3; вертикаль всегда сверху — все 6 пресетов за верхнюю safe-area; уборка кадров выключена — 66 PNG против 0; bundleHash записи гейта подменён — video@1; `easing` открыт до `z.string()` — elastic.out прошёл). Все откаты `diff`-чисты. lint 0.
- `05:24` [8/8] контрольный полный прогон в образе — начат
- `05:36` [8/8] КОНТРОЛЬНЫЙ ПОЛНЫЙ ПРОГОН В ОБРАЗЕ: **2886 passed / 0 failed / 3 skipped** за 777.0 с (было 2801/0/3 ⇒ +85 утверждений). `eslint .` — 0. `vpe template list`: девять шаблонов, `kineticType@1` — `final:PASS draftHalf:PASS`, бюджет 4, шесть пресетов, демо есть; «записей гейта: 8 из 9» (девятый — `bed@1`, аудио-домен, долг №189).
**Итог:** довески закрыты измерением (№288 — 689 МБ → 12 КБ, финал побайтово тот же; №290 — переснимать не надо, `bundleHash` записей равен `bundle.hash` запросов, плюс охранник по всем шаблонам). `kineticType@1` сдан целиком: четыре режима, шесть пресетов, гейты PASS на обоих профилях, демо и живая проверка на `work/ct-test-1`, восемь охранников, шесть из них показаны красными. Норма 2886/0/3. Детали — docs/impl/KT-01/{report.md,PROGRESS.md,violation-transcript.txt,frames/}.
