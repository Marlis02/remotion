# PROGRESS — IMPL-GATE-PREP (запросы гейта файлами + runbook) · старт 2026-08-29T19:52:11+06:00
- `19:52` якорь снят ДО чтения; стоп-правило 60 мин без зелёного теста
- `20:0x` чтение готово: `fixture.ts` (билдеры `makeTemplateFixture`/`readyRequest`), `template-gate.ts` (три входа, охранник №181, K4-сверка), `gate.ts` (`requestForRun` перекрывает `tmpDir`/`outputPath`/`bundle.path`), `materialize.ts` (`bundle.hash` = перечень каталога, абсолютных путей НЕ содержит), профили `draftHalf.yaml`/`render.final.yaml`, DEBTS (№192 свободен — проверено грепом)

────────────────────────── ПЛАН (жду «ок») ──────────────────────────
Ш1. Генератор в `packages/renderer-hyperframes/test/fixture.ts` (первая копия, №179; третьей
    не будет): восемь случаев `4 шаблона × {draftHalf, final}` + функция, которая берёт
    `makeTemplateFixture`, ставит СТАБИЛЬНЫЕ пути и досчитывает `bundle.hash` через
    `readyRequest` (материализация, браузера не требует).
Ш2. Зондом порождаю восемь `gate-requests/<t>@1.<profileId>.json` (`canonicalJson` + `\n`)
    и кладу ассет файлом `gate-requests/assets/pattern-32.png` (байты `PNG_PATTERN_32`).
    Восемь `bundle.hash` — в журнал.
Ш3. Юнит `test/gate-requests.test.ts`, БЕЗ браузера: (а) каждый файл побайтово равен
    порождению билдера; (б) тройка K4 равна yaml-профилю через `readFamily`; (в) каждый файл
    проходит охранник №181 для своего названного шаблона. Красное — с именем файла.
Ш4. Смоук живьём: `pnpm build`, затем `vpe template gate` на ЧЕТЫРЁХ `draftHalf`-файлах с
    `--gates-dir <tmp>` ⇒ 4 × PASS; таблицы прогонов в журнал. `final` живьём НЕ гоняю.
Ш5. `docs/gate-runbook.md` (восемь команд полными путями, время, критерий успеха, что
    коммитить, что делать при FLAKY/FAIL/error, строка про `bed@1` №189), DEBTS №192,
    `docs/impl/GATE-PREP/report.md`, протокол нарушения (порча `scale` в одном файле ⇒
    красный юнит с именем; откат `cp`, porcelain), приёмка build/typecheck/lint/test.
- `20:05` [1-2/5] Ш1+Ш2 готово: генератор в `test/fixture.ts` (GATE_REQUEST_CASES/PROFILES/PATHS, `buildGateRequestFile`), восемь файлов + `assets/pattern-32.png` порождены режимом `VPE_GATE_REQUESTS_UPDATE=1`
- `20:14` [3/5] Ш3 ЗЕЛЁНЫЙ: `gate-requests.test.ts` — 18 тестов, 0.75 с, БЕЗ браузера; повтор без флага воспроизвёл файлы байт в байт. `bundle.hash`: still 1566…908f/61c5…c372, kenburns 86e5…5a3a/f9f0…c06a, flash 0fc4…e0b1/5d39…9c10, captionEmphasis 1c63…8ba8/cd5a…74d5 (draftHalf/final)
- `20:16` [Г] правка CLI по разрешению владельца: `resolveRequestPaths` в `template-gate.ts` (пять путевых полей, резолв от каталога ФАЙЛА запроса); юнит `cli/test/gate-requests-cli.test.ts` — 18 тестов зелёные (резолв от файла с контролем «не от cwd», абсолютные как прежде, K4 против yaml через `readFamily` ×8, приём всех восьми файлов командой ×8)
- `20:17` [4/5] Ш4 начат: `pnpm build`, дальше живой смоук четырёх `draftHalf` — ожидаю ~1–3 мин на четыре гейта (репетиция FIX-01: 1.4 с на прогон при 6 кадрах)
- `20:18` [4/5] Ш4 ГОТОВО за ~25 с: 4 × PASS живьём (`draftHalf`, N=3, 12 кадров, 540×960). still 697b51ed/330031a7 · kenburns ae36ce83/e1883220 · flash 33075dd4/3b4a6715 · captionEmphasis d9bece6e/6a50c931; прогоны 1482–1670 мс, ≈6 с на гейт; engineFingerprint 61db2ca8…5e1363a0 един. Записи — только в tmp, дерево исходников чисто (`*.gates.json` нет)
- `20:20` [5/5] Ш5: runbook написан (8 команд от корня, время, три признака успеха, таблица FLAKY/FAIL/error, строка про bed@1); протокол Н1 (порча scale руками) — ТРИ красных охранника (побайтовая сверка, K4-vs-yaml, R12 команды), откат по хэшу, контроль 36/36 зелёных
- `20:21` приёмка запущена: lint + полный `pnpm test` (браузерные файлы на этой машине живые — ожидаю ~8–10 мин)
- `20:26` доки готовы: `docs/gate-runbook.md`, `docs/impl/GATE-PREP/report.md` + `violation-transcript.txt`, DEBTS №192 (плейсхолдеры путей), №193 (список случаев в двух местах), №194 (`workers` в пару входит, но не сверяется). Кандидат из задания долгом не стал — это работающий охранник, показан красным. `pnpm build` чисто, `pnpm lint` чисто; жду полный `pnpm test`
- `20:29` ОШИБКА приёмки: полный `pnpm test` — 2 красных, оба в `tests/lints/nul-in-sources.test.ts`: бинарный ассет `gate-requests/assets/pattern-32.png` содержит 19 байт NUL, а список исключений линта был пуст
- `20:30` ЧИНЮ ПО ПРОТОКОЛУ САМОГО ЛИНТА (его шапка: «если появится бинарная фикстура, она обязана быть НАЗВАНА здесь — то есть попасть в дифф и в ревью, а не проскочить молча»): файл внесён в `ALLOWED` с ответом «почему не может быть текстовым» (байты входят в `bundle.hash`; расширение по магическим байтам; однопиксельная замена = ложно-зелёный №164). Устаревшая строка шапки «список пуст» помечена как изменённая. ДЕВЯТЫЙ адрес диффа — назову владельцу вслух
- `20:30` линт зелёный (3/3); перезапущен полный прогон
- `20:38` [приёмка] полный `pnpm test` ЗЕЛЁНЫЙ: 149 файлов, 2301 passed | 2 skipped (2303), 0 failed, 484.1 с. Счёт сошёлся: было 147/2265+2 ⇒ +2 файла и +36 тестов (18+18) — ровно два новых юнита
**Итог:** восемь файлов запросов гейта порождены билдерами и сверяются побайтово; правка CLI (Г) — относительные пути от каталога файла запроса; смоук 4 × PASS живьём (≈6 с на гейт); runbook на восемь команд; долги №192/193/194; девятый адрес диффа — линт NUL (бинарный ассет назван по протоколу самого линта). Записи гейта НЕ сняты — это шаг владельца. Детали — docs/impl/GATE-PREP/report.md.
