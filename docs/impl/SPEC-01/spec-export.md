# Спецификация движка для ИИ-сценариста

Снято командой `vpe spec export`. Схема выгрузки — `vpe-spec-export/1`, версия реестра шаблонов — `1`.

Это ПОЛНЫЕ правила игры: по ним пишут сценарий, не открывая код. Всё, чего здесь нет, движок отвергает — не «поймёт по-своему», а останавливает сборку с адресом ошибки.

## 1. Грамматика: проза и якоря

### Два файла, и это правило, а не соглашение

`source/NN-имя.md` — **только проза** с беспараметрическими маркерами. `direction/NN-имя.yaml` — **вся параметрика**. Параметр в прозе невыразим: грамматики для него нет (ADR-0002 §1). Имена файлов пары совпадают.

**Проза — по-английски, и только по-английски** (Charter V12): английский — язык `source/*.md`, текста в TTS и субтитров. Второго языка контента не будет. Комментарии в `direction/*.yaml` — по-русски, как вся документация проекта.

Заголовки структуры: `# chapter: id` и `## scene: id`. Их `id` становятся якорями `ch:id` и `sc:id`; переименование заголовка — ошибка компиляции, если на якорь кто-то ссылается (ADR-0004 §9).

### Маркеры прозы — закрытый список из ПЯТИ, плюс заголовки главы и сцены (ADR-0002 §2, нормативная таблица)

Шестого маркера не существует: расширение списка требует нового ADR. Колонка «стоит денег» — про перегенерацию TTS, и она есть ровно у двух строк.

**ИМЯ ЯКОРЯ — ЗАКРЫТАЯ ГРАММАТИКА, И НА НЕЙ СПОТЫКАЮТСЯ ПЕРВЫМ ДЕЛОМ.** Имя бита, сцены, главы и alias картинки: первый символ — буква или цифра, дальше буквы, цифры, `-` и `_`. Ни пробелов, ни точек, ни двоеточий. `[beat: the turn]` — отказ лексера с колонкой; `[beat: the-turn]` — законно.

`[pause: Nms]` — целое число миллисекунд с единицей `ms` и **N ≥ 1**. Ноль писать нельзя: внутри абзаца он режет чанк и стоит денег ради нуля сэмплов тишины, а на границе сцены обнуляет тишину движка, то есть отменяет кандидата на разрез (ADR-0002 §2).

`[say: display | spoken]` — слева то, что увидит зритель в субтитре, справа то, что произнесёт голос. Разделитель — `|`; без него отказ.

| маркер | в TTS | режет чанк | стоит денег | якорь | где допустим |
|---|---|---|---|---|---|
| `[beat: name]` | нет | нет | нет | `b:name` | любое место |
| `[pause: Nms]` на границе абзаца | нет | нет | нет | нет | между абзацами |
| `[pause: Nms]` внутри абзаца | нет | да, только на границе предложения | **да** | нет | между предложениями |
| `[say: d \| s]` | `s` (в субтитр идёт `d`) | нет | да, если меняется `s` | `w:` внутренний | любое место |
| `[img: alias]` | нет | нет | нет | `b:img-<alias>-<n>` | начало предложения |
| `[emph]` | **нет** — маркер чисто визуальный | нет | нет | нет | любое место |
| `# chapter:` / `## scene:` | нет | да | нет | `ch:` / `sc:` | начало строки |

### Линт прозы: почему в тексте нет цифр (ADR-0002 §3)

В прозе ЗАПРЕЩЕНЫ: цифры, `%`, `$`, `№`, римские цифры, сокращения с точкой, URL, `**жирный**`, списки, инлайн-код. Ошибка вида `файл:строка:колонка`. Причина не стилистическая: `FACT` (r1 §1.4) — маппинг «исходный текст ↔ нормализованный» провайдер TTS не отдаёт ни на каком языке, а без него привязка слов к времени не строится как тождество.

Число пишется СЛОВАМИ: `nineteen hundred`, `ten thousand`, `nineteen thirty-six`. Если словами нельзя — escape-hatch `[say: 23% | twenty-three percent]`: слева то, что увидит зритель в субтитре, справа то, что произнесёт голос.

Область запрета — ТОЛЬКО проза. В `direction/*.yaml` цифры и URL законны.

### Якоря: чем адресуется время (ADR-0004 §1, §2, §5a)

Пространства имён: `b:` — бит (`[beat: name]`), `sc:` / `ch:` — сцена и глава по заголовкам, `r:` — ссылка на запись режиссуры по её `recordId`, `w:` — токен исходника, **внутреннее**.

**ЗАПРЕТ, КОТОРЫЙ НЕЛЬЗЯ ОБОЙТИ:** ни одна запись режиссуры не имеет права ссылаться на `w:`. Только `b:` / `sc:` / `ch:` / `r:`. Ссылка на слово «съезжала» бы при правке соседнего слова, и правка применялась бы к чужому месту молча.

`at` — где запись начинается, `until` — где кончается; обе формы — `{ kind: anchor, anchor: "…" }`. `until` на scope-якоре означает КОНЕЦ этой области (`until: sc:street` = конец сцены `street`). `until`, не указанный вовсе, означает конец области, содержащей `at`.

`b:` без произносимого соседа — не ошибка: его позиция есть конец последнего речевого клипа своей сцены. Сцена без единого речевого клипа — ошибка со списком.

### `[img: alias]` — единственный сахар, и он разворачивается компилятором (ADR-0002 §4)

Маркер минтит **неявный бит** `b:img-<alias>-<n>`, где `<n>` — порядковый номер `[img:]` с этим alias среди всех `[img:]` ФАЙЛА, начиная с единицы. Компилятор сам порождает запись `still@1` — `track: visual`, `z: 0`, от этого бита до следующего `[img:]` той же сцены либо до её конца.

**СЛЕДСТВИЕ, БЕЗ КОТОРОГО РЕЖИССУРА НЕ РАБОТАЕТ:** эффект над картинкой (`kenburns@1`, `parallax25@1`) обязан быть написан ТОЙ ЖЕ парой якорей, что и порождённая запись, — `at: b:img-<alias>-1`, `until: sc:<сцена>`. Он двигает соседний снизу слой, и разъехавшееся окно оставило бы его без цели.

Alias'ы каталога придумывает НЕ автор сценария: они лежат в `assets/aliases.yaml` проекта, и список даёт владелец темой. Alias, которого в каталоге нет, — отказ сборки, а не пустой кадр.

Живая проза целиком — `examples/vertical-v1/source/01-archive.md`:

```markdown
schema: source-dialect/1

# chapter: main

## scene: street

[img: street] A city street in nineteen hundred. [beat: work] Ten thousand people before noon, and the archive kept none of their names.

## scene: work

[img: mechanic] Twenty years on, a man leans into a steam pump. [pause: 300ms] The pump is gone. The photograph is not.

## scene: road

[img: mother] Nineteen thirty-six. A road, a mother, a pause. [beat: close] The country finally [emph] looked.
```

## 2. Каталог шаблонов — 7 шт., и это весь набор

Поля ниже прочитаны из спеков и манифестов: диапазоны и enum'ы — интроспекцией схем `params`, бюджеты и роли — из манифеста, статус гейта — из записей рядом со спеком. Переписанных руками параметров в этом разделе нет ни одного.

Реестр easing (**закрыт**, 6 кривых): `power1.inOut`, `power2.inOut`, `power3.out`, `back.out(1.7)`, `sine.inOut`, `none`. Порядок трансформаций — translate → scale.

### `kenburns@1`

* **назначение:** Медленный ход по кадру: наезд/отъезд и смещение от `from` к `to`. Собственной картинки у него нет — он двигает СОСЕДНИЙ СНИЗУ слой, поэтому окно якорей обязано совпасть с окном записи `[img:]` той же сцены, иначе двигать нечего. Уместен на спокойной сцене, которую держит одно фото: горизонтальный снимок в кадре 9:16 ведётся по ГОРИЗОНТАЛИ — так виден сюжет, а не растянутые лица (решение владельца `H-07`). Дорог, и дорого в нём ровно то, что он делает: ресэмплинг полноэкранного фото на КАЖДОМ кадре — по одному вызову на сцену.
* **ассеты (роли):** не просит
* **шрифты (роли):** не просит
* **длительность:** задаёт автор: `until` записи либо конец области
* **кривые, которые принимает:** `power2.inOut`
* **бюджет, мс/кадр:** 33 — оценка СВЕРХУ по самому дорогому вызову
* **источники случайности (`purposes`):** нет
* **звуковые признаки:** нет
* **гейт детерминизма:** final: PASS (N = 10), draftHalf: PASS (N = 3); сводный класс — PASS

`params` — форма из схемы шаблона (JSON Schema, вход):

```json
{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"easing":{"enum":["power2.inOut"],"type":"string"},"from":{"additionalProperties":false,"properties":{"scale":{"type":"number"},"x":{"type":"number"},"y":{"type":"number"}},"required":["scale","x","y"],"type":"object"},"to":{"additionalProperties":false,"properties":{"scale":{"type":"number"},"x":{"type":"number"},"y":{"type":"number"}},"required":["scale","x","y"],"type":"object"}},"required":["from","to","easing"],"type":"object"}
```

**Проверки сверх JSON Schema** (заданы `.refine` — их текст и есть текст отказа):

* `from.scale` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3)
* `from.x` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3)
* `from.y` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3)
* `to.scale` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3)
* `to.x` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3)
* `to.y` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3)

### `flash@1`

* **назначение:** Короткая вспышка на дорожке `effect` — метка момента, а не украшение. Длительность задаёт САМ шаблон параметром `durationSamples`, поэтому `until` записи не нужен: она ставится одной точкой `at` на бит. Кривая затухания у него ровно одна и в `params` НЕ едет (`power3.out`): форма затухания есть свойство эффекта, а не места, куда его поставили. Не чаще одного раза на сцену и только на том слове, ради которого сцена написана.
* **ассеты (роли):** не просит
* **шрифты (роли):** не просит
* **длительность:** **объявляет сам** — `until` записи не нужен
* **кривые, которые принимает:** `power3.out`
* **бюджет, мс/кадр:** 30 — оценка СВЕРХУ по самому дорогому вызову
* **источники случайности (`purposes`):** нет
* **звуковые признаки:** нет
* **гейт детерминизма:** final: PASS (N = 10), draftHalf: PASS (N = 3); сводный класс — PASS

`params` — форма из схемы шаблона (JSON Schema, вход):

```json
{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"durationSamples":{"exclusiveMinimum":0,"maximum":9007199254740991,"type":"integer"},"strengthPct":{"exclusiveMinimum":0,"maximum":100,"type":"integer"}},"required":["strengthPct","durationSamples"],"type":"object"}
```

### `bed@1`

* **назначение:** Музыкальная подложка — ЕДИНСТВЕННЫЙ шаблон аудио-домена: кадров он не рисует вовсе, и его бюджет мс/кадр равен нулю по построению, а не по недосмотру. `inPoint` — точка ВНУТРИ той же подложки, и его alias обязан совпасть с `asset`: разные означали бы второй ассет, которого шаблон не объявляет, и схема такую пару отвергает. В v1 музыка НЕ микшируется — клип лежит в `AudioPlan.music[]` как есть, а `gainDb` и `duckUnderSpeechDb` объявлены и типизированы, но звука пока не меняют (долг №141).
* **ассеты (роли):** `asset`
* **шрифты (роли):** не просит
* **длительность:** задаёт автор: `until` записи либо конец области
* **кривые, которые принимает:** ни одной (твинов нет)
* **бюджет, мс/кадр:** 0 — оценка СВЕРХУ по самому дорогому вызову
* **источники случайности (`purposes`):** нет
* **звуковые признаки:** нет
* **гейт детерминизма:** — (записей нет; `UNGATED` означает «проверки НЕ выполнялись», а не «чисто»); сводный класс — UNGATED

`params` — форма из схемы шаблона (JSON Schema, вход):

```json
{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"asset":{"minLength":1,"type":"string"},"duckUnderSpeechDb":{"type":"number"},"gainDb":{"type":"number"},"inPoint":{"additionalProperties":false,"properties":{"asset":{"minLength":1,"type":"string"},"kind":{"const":"mediaTime","type":"string"},"offsetSamples":{"maximum":9007199254740991,"minimum":0,"type":"integer"}},"required":["kind","asset","offsetSamples"],"type":"object"}},"required":["asset","inPoint","gainDb","duckUnderSpeechDb"],"type":"object"}
```

**Проверки сверх JSON Schema** (заданы `.refine` — их текст и есть текст отказа):

* `params` целиком — ещё 1 перекрёстная проверка, чей текст строится внутри неё самой; его печатает отказ компилятора
* `gainDb` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3)
* `duckUnderSpeechDb` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3)

### `still@1`

* **назначение:** Статичная картинка — то, во что компилятор разворачивает маркер `[img: alias]` прозы. ЭТУ ЗАПИСЬ АВТОР ОБЫЧНО НЕ ПИШЕТ: она порождается сама — `track: visual`, `z: 0`, от неявного бита `b:img-<alias>-<n>` до следующего `[img:]` той же сцены либо до её конца. Писать её руками стоит только там, где нужен другой `z` или окно, не совпадающее с порождённым. `fit` необязателен и имеет ровно одно значение — `cover`; вертикаль берётся кадрированием, а не растяжением.
* **ассеты (роли):** `asset`
* **шрифты (роли):** не просит
* **длительность:** задаёт автор: `until` записи либо конец области
* **кривые, которые принимает:** ни одной (твинов нет)
* **бюджет, мс/кадр:** 1 — оценка СВЕРХУ по самому дорогому вызову
* **источники случайности (`purposes`):** нет
* **звуковые признаки:** нет
* **гейт детерминизма:** final: PASS (N = 10), draftHalf: PASS (N = 3); сводный класс — PASS

`params` — форма из схемы шаблона (JSON Schema, вход):

```json
{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"asset":{"minLength":1,"type":"string"},"fit":{"enum":["cover"],"type":"string"}},"required":["asset"],"type":"object"}
```

### `captionEmphasis@1`

* **назначение:** Визуальная эмфаза внутри полосы субтитров и ЕДИНСТВЕННЫЙ шаблон, объявляющий шрифт (роль `caption`). Маркер `[emph]` прозы — чисто визуальный: голосовой эмфазы в v1 нет, TTS этого маркера не видит и денег он не стоит. Ставится ПО ОДНОМУ НА СЦЕНУ, окном на сам scope-якорь сцены (`at` и `until` — один и тот же `sc:<id>`): сегмент без такого клипа получает полосу на месте и в нужном кегле, но СИСТЕМНЫМ шрифтом (долг №207).
* **ассеты (роли):** не просит
* **шрифты (роли):** `caption`
* **длительность:** задаёт автор: `until` записи либо конец области
* **кривые, которые принимает:** ни одной (твинов нет)
* **бюджет, мс/кадр:** 1 — оценка СВЕРХУ по самому дорогому вызову
* **источники случайности (`purposes`):** нет
* **звуковые признаки:** нет
* **гейт детерминизма:** final: PASS (N = 10), draftHalf: PASS (N = 3); сводный класс — PASS

`params` — форма из схемы шаблона (JSON Schema, вход):

```json
{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"style":{"enum":["bold"],"type":"string"}},"required":["style"],"type":"object"}
```

### `grade@1`

* **назначение:** Единый тон поверх сцены — цветокоррекция, виньетка и зерно CSS-фильтрами, без LUT-ассета и без единой кривой. В КАНАЛЕ ОН ВЫКЛЮЧЕН: владелец посмотрел ролик и снял вызовы (`ENV-01`, дословно — «тон незаметен, готовые изображения буду давать уже обработанными»), поэтому новый сценарий его не зовёт, пока владелец не попросит тон отдельно. Все шесть полей необязательны — законно тронуть одну ось и оставить остальные. Самый дорогой шаблон библиотеки, и дорог он `grain`: объявленная оценка сверху вчетверо выше цены вызова без зерна.
* **ассеты (роли):** не просит
* **шрифты (роли):** не просит
* **длительность:** задаёт автор: `until` записи либо конец области
* **кривые, которые принимает:** ни одной (твинов нет)
* **бюджет, мс/кадр:** 546 — оценка СВЕРХУ по самому дорогому вызову
* **источники случайности (`purposes`):** нет
* **звуковые признаки:** нет
* **гейт детерминизма:** final: PASS (N = 10), draftHalf: PASS (N = 3); сводный класс — PASS

`params` — форма из схемы шаблона (JSON Schema, вход):

```json
{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"contrast":{"maximum":4,"minimum":0,"type":"number"},"grain":{"maximum":1,"minimum":0,"type":"number"},"hueRotate":{"maximum":360,"minimum":-360,"type":"number"},"saturate":{"maximum":4,"minimum":0,"type":"number"},"sepia":{"maximum":1,"minimum":0,"type":"number"},"vignette":{"maximum":1,"minimum":0,"type":"number"}},"type":"object"}
```

### `parallax25@1`

* **назначение:** 2.5D-параллакс на слоях, которые нарезал АВТОР: каждый слой — отдельный alias каталога со своим provenance, порядок списка от ДАЛЬНЕГО к БЛИЖНЕМУ. Оценки глубины и сегментации в движке нет и не будет — слои приезжают готовыми файлами, и обратный порядок дал бы законный, компилирующийся и НЕВЕРНЫЙ кадр. Зовётся только на снимке с ОДНИМ выделенным объектом: измерено (`E-02` §7) — на толпе без такового три модели сегментации из трёх дали 0.1–0.2 % кадра, то есть слоя не получается вовсе, а на фигуре — 27.6 и 46.6 % и чистая вырезка. Один alias — законный вход и осмысленный кадр, но это ЛОЖНЫЙ параллакс, и он вчетверо дороже настоящего: платит размытие дальнего плана.
* **ассеты (роли):** `layer0`, `layer1`, `layer2`, `layer3`
* **шрифты (роли):** не просит
* **длительность:** задаёт автор: `until` записи либо конец области
* **кривые, которые принимает:** `power2.inOut`, `none`
* **бюджет, мс/кадр:** 44 — оценка СВЕРХУ по самому дорогому вызову
* **источники случайности (`purposes`):** нет
* **звуковые признаки:** нет
* **гейт детерминизма:** final: PASS (N = 10), draftHalf: PASS (N = 3); сводный класс — PASS

`params` — форма из схемы шаблона (JSON Schema, вход):

```json
{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"depthSpread":{"type":"number"},"drift":{"type":"number"},"easing":{"enum":["power2.inOut","none"],"type":"string"},"layers":{"items":{"minLength":1,"type":"string"},"maxItems":4,"minItems":1,"type":"array"},"scale":{"type":"number"}},"required":["layers","drift","depthSpread","easing"],"type":"object"}
```

**Проверки сверх JSON Schema** (заданы `.refine` — их текст и есть текст отказа):

* `drift` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3); `drift`: доля кадра от 0 до 0.2 включительно (решение владельца `E-02`)
* `depthSpread` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3); `depthSpread`: во сколько раз ДАЛЬНИЙ слой медленнее ближнего, то есть `>= 1`. Значение меньше единицы означало бы фон, едущий быстрее переднего плана, — это не параллакс, а его зеркало
* `scale` — геометрия: ожидалось конечное число (ADR-0007 §3); `-0` не является геометрической величиной (ADR-0007 §3); `scale`: положительный множитель («дыхание» кадра). Ноль и отрицательные схлопывают или отражают слой, а не масштабируют его

## 3. Правила канала — числами

| величина | значение | источник |
|---|---|---|
| кадр | 1080 × 1920 (9:16, вертикаль) | profiles/compile.yaml проекта |
| fps | 30 (решение владельца 3, RM2 — не умолчание) | ADR-0003; profiles/compile.yaml |
| частота проекта | 24000 Гц; единица авторского слоя — сэмпл | ADR-0003 T1 |
| длина ролика | не более 1800 кадров = 60 с | profiles/compile.yaml `maxDurationFrames`; PG-E1 (BLOCK) |
| безопасные зоны | сверху 180, снизу 320, слева и справа по 60 px | profiles/compile.yaml `safeAreas` (зона интерфейса Shorts) |
| субтитры: слов в группе | от 1 до 3 | profiles/compile.yaml `captions.tokensPerGroupMin/Max` |
| субтитры: потолок символов группы | 21 | profiles/compile.yaml `captions.maxGroupChars` |
| полоса субтитров | ширина 920 при отступе 80; низ полосы 500 px над краем кадра; кегль 68; межстрочный 1.22 | docs/impl/H-07/report.md §4 — выбрано по кадрам, измерено на готовом ролике |
| полоса: где она встаёт | центр строки на 71.0 % высоты кадра (измерено на кадре 15.6 с) | docs/impl/H-07/report.md §4 |
| бюджет AC2 | 500 мс/кадр на ВСЮ композицию (60-сек Short ≤ 15 минут) | Charter AC2; ADR-0008 «Бюджет AC2» |
| порог отчёта по сцене | 250 мс/кадр — сумма `msPerFrameBudget` пересекающихся клипов; превышение печатается и НЕ роняет сборку | решение владельца 9 (RM1); packages/cli/src/budget.ts |
| тишина движка | абзац 4320, сцена 7680, глава 14400 сэмплов (180 / 320 / 600 мс) | profiles/compile.yaml; ADR-0003 T8 |
| минимальный сегмент | 45 кадров (1.5 с) — более мелкие кандидаты на разрез компилятор объединяет | profiles/compile.yaml `minSegmentDurationFrames` |
| кривые | реестр ЗАКРЫТ, в нём ровно 6 кривых; порядок трансформаций — translate → scale | packages/templates-spec/src/easing.ts (**D5**, ADR-0007 §3) |
| язык контента | английский; русского контента не будет | Charter V12 |

## 4. Форма ответа

### Что вернуть

РОВНО ДВА ФАЙЛА на ролик, с совпадающими именами: `source/01-имя.md` и `direction/01-имя.yaml`. Ни `project.yaml`, ни `publish.yaml`, ни `aliases.yaml` — их владелец уже написал, и переписывать их не нужно.

**`source/*.md` — только по-английски.** Это входное условие проекта (Charter V12), а не предпочтение: русского контента не будет. Комментарии внутри `direction/*.yaml` — по-русски.

Каждый файл начинается строкой `schema:` своего семейства: `source-dialect/1` и `direction/1` соответственно. Без неё файл не читается вовсе.

Скелет `source/NN-имя.md`:

```markdown
schema: source-dialect/1

# chapter: <id главы латиницей>

## scene: <id сцены латиницей>

[img: <alias>] <проза по-английски, числа СЛОВАМИ>. [beat: <id бита>] <ещё проза>.

## scene: <id второй сцены>

[img: <alias>] <проза>. [pause: 300ms] <проза с [emph] на ударном слове>.
```

Скелет `direction/NN-имя.yaml`:

```yaml
schema: direction/1
records:
-
  recordId: "1a7c0e33"          # 4 случайных байта: 8 строчных hex
  at:
    kind: anchor
    anchor: "b:img-<alias>-1"   # только b:/sc:/ch:/r:, никогда w:
  until:
    kind: anchor
    anchor: "sc:<id сцены>"     # until на scope-якоре = его конец
  track: visual                 # speech·music·sfx·caption·visual·effect
  z: 10
  template: "kenburns@1"        # имя ИЗ КАТАЛОГА, иного не существует
  params:
    # ровно те поля, которые объявляет схема шаблона; лишнее — отказ с путём
```

### Поля записи режиссуры

`recordId` — 4 случайных байта в hex, ровно 8 строчных символов; у каждой записи свой. `at` / `until` — `{ kind: anchor, anchor: "…" }`. `track` — одно из шести имён дорожек: `speech`, `music`, `sfx`, `caption`, `visual`, `effect`. `z` — целое, порядок слоёв. `template` — имя вызова из каталога §2. `params` — параметры ЭТОГО шаблона, и никакие другие: лишнее поле есть отказ схемы с путём.

Порядок `z`, которым собран живой ролик: картинка 0 (порождённая `[img:]`), эффект над ней 10, вспышка 20, грейд 25, субтитры 30.

### Полный пример: по одной записи на каждый шаблон

Все записи ниже скопированы из живых файлов репозитория — источник назван у каждой; YAML напечатан каноническим писателем движка, то есть эта форма гарантированно проходит схему `direction/1`.

* `kenburns@1` — examples/vertical-v1/direction/01-archive.yaml, запись `1a7c0e33`. Окно — ТА ЖЕ пара якорей, что у порождённой записи `[img: street]`. Горизонтальный снимок ведётся по горизонтали (`y` не меняется), наезд с 1.06 до 1.14.
* `parallax25@1` — examples/vertical-v1/direction/01-archive.yaml, запись `4d8ea15b`. Два слоя, дальний — САМ оригинал без альфы, ближний — вырезка поверх него. Порядок списка от дальнего к ближнему есть часть контракта.
* `captionEmphasis@1` — examples/vertical-v1/direction/01-archive.yaml, запись `9f31b204`. Окно — сам scope-якорь сцены: `at` и `until` совпадают. По одному на сцену.
* `flash@1` — examples/vertical-v1/direction/01-archive.yaml, запись `b8340c6a`. `until` НЕТ вовсе: длительность объявляет сам шаблон полем `durationSamples` (3600 сэмплов = 150 мс при 24000). Стоит на бите `close` — том слове, ради которого написана сцена.
* `still@1` — fixtures/minimal/direction/01-intro.yaml, запись `5d6e1130`. **ЭТУ ЗАПИСЬ ОБЫЧНО НЕ ПИШУТ:** её порождает компилятор из `[img: alias]` с `track: visual`, `z: 0` и окном до следующего `[img:]`. Руками — только там, где нужен другой `z` или другое окно, как здесь.
* `bed@1` — fixtures/minimal/direction/01-intro.yaml, запись `c81a05f7`. Дорожка `music`, окно — до конца ГЛАВЫ. `inPoint.asset` совпадает с `asset`: точка внутри той же подложки. В v1 микса нет — числа объявлены, но звука пока не меняют.
* `grade@1` — examples/vertical-v1/direction/01-archive.yaml на коммите `0e509d4` (`E-07`), запись `7e21c40a`; снят из демо решением владельца `ENV-01`. **ЭТОТ ШАБЛОН В КАНАЛЕ ВЫКЛЮЧЕН** — владелец снял тон дословно: «тон незаметен, готовые изображения буду давать уже обработанными». Пример приведён, чтобы форма была известна, а не чтобы его звали. `z: 25` — над картинкой и под субтитрами; `grain: 0` — цена, а не вкус.

```yaml
schema: direction/1
records:
-
  recordId: "1a7c0e33"
  at:
    kind: anchor
    anchor: "b:img-street-1"
  until:
    kind: anchor
    anchor: "sc:street"
  track: visual
  z: 10
  template: "kenburns@1"
  params:
    easing: power2.inOut
    from:
      scale: 1.06
      x: -0.05
      y: 0
    to:
      scale: 1.14
      x: 0.05
      y: 0
-
  recordId: "4d8ea15b"
  at:
    kind: anchor
    anchor: "b:img-mechanic-1"
  until:
    kind: anchor
    anchor: "sc:work"
  track: visual
  z: 10
  template: "parallax25@1"
  params:
    depthSpread: 2.6
    drift: 0.045
    easing: power2.inOut
    layers:
    - mechanic
    - mechanic-figure
    scale: 1.06
-
  recordId: "9f31b204"
  at:
    kind: anchor
    anchor: "sc:street"
  until:
    kind: anchor
    anchor: "sc:street"
  track: caption
  z: 30
  template: "captionEmphasis@1"
  params:
    style: bold
-
  recordId: "b8340c6a"
  at:
    kind: anchor
    anchor: "b:close"
  track: effect
  z: 20
  template: "flash@1"
  params:
    durationSamples: 3600
    strengthPct: 22
-
  recordId: "5d6e1130"
  at:
    kind: anchor
    anchor: "b:count"
  track: visual
  z: 15
  template: "still@1"
  params:
    asset: ledger
    fit: cover
-
  recordId: "c81a05f7"
  at:
    kind: anchor
    anchor: "sc:turn"
  until:
    kind: anchor
    anchor: "ch:main"
  track: music
  z: 0
  template: "bed@1"
  params:
    asset: pad-loop
    duckUnderSpeechDb: -6
    gainDb: -18
    inPoint:
      asset: pad-loop
      kind: mediaTime
      offsetSamples: 96000
-
  recordId: "7e21c40a"
  at:
    kind: anchor
    anchor: "sc:street"
  until:
    kind: anchor
    anchor: "sc:street"
  track: effect
  z: 25
  template: "grade@1"
  params:
    contrast: 1.08
    grain: 0
    hueRotate: -6
    saturate: 0.85
    sepia: 0.28
    vignette: 0.35
```

## 5. Чего не существует

* **Эффектов вне каталога не существует.** Каталог §2 — весь набор, и числа шаблонов здесь нет намеренно: оно печатается там, где считается. Имя, которого в каталоге нет, реестр не разрешает — сборка не стартует. Не «похожий эффект», не «сделай сам».
* **Кривых ровно 6, и реестр закрыт:** `power1.inOut`, `power2.inOut`, `power3.out`, `back.out(1.7)`, `sine.inOut`, `none`. Кривая называется ПО ИМЕНИ РЕНДЕРЕРА: `easeInOutCubic` или `inOutCubic` отвергаются даже тогда, когда кривая с таким смыслом в реестре есть. Сверх того шаблон принимает лишь ТЕ кривые, которые объявил сам (колонка `easing` каталога), а не весь реестр.
* **Alias картинок не выдумывать.** Список alias'ов даёт владелец вместе с темой; alias, которого нет в `assets/aliases.yaml`, — отказ сборки. Нет списка — спросить, а не придумать правдоподобное имя.
* **Цифр в прозе нет.** Ни года, ни процента, ни `$`. Словами либо через `[say: d | s]`.
* **Ссылок на `w:` нет.** `at`/`until` — только `b:` / `sc:` / `ch:` / `r:`.
* **Параметров сверх схемы нет.** Каждая схема `.strict()`: поле, которого шаблон не читает, — это либо опечатка, либо поведение, которого никто не решал. Молчания не будет: отказ назовёт путь к полю.
* **Всё сверх перечисленного — отказ компилятора,** а не «движок как-нибудь поймёт». Проверка результата одна: `vpe build --project <кат> --profile draftHalf`.
