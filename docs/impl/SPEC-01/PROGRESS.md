# PROGRESS — IMPL-SPEC-01 `vpe spec export` · старт 2026-09-01T01:20:58+06:00
План: пишу после чтения. Бюджет: 60 минут без зелёного теста — стоп и отчёт.
- `01:20` [0/N] якорь снят, начинаю чтение (ADR-0002/0004, templates-spec, cli, examples, отчёты H-07/E-07/E-02)
- `01:40` [0/N] чтение закончено (Charter, ADR-0002/0004, семь спеков, cli/argv/run/template-list, direction-схема, H-07/E-07/E-02 числа); проверено: `z.toJSONSchema` в zod 4.4.3 работает на всех семи схемах, сообщения `.refine` извлекаются интроспекцией
- `01:40` [0/N] преflight: `tsc --build` + полный `vitest run` — базовый прогон, чужой красный
- `01:56` [0/N] базовый прогон готов за 615 с: **1 красный из 2439**, ЧУЖОЙ — `renderer-hyperframes/test/library.test.ts` («ПЯТЬ шаблонов … PASS»): коммит владельца `3f80766` (01:06, за 15 мин до сессии) добавил `parallax25@1.gates.json` одним файлом, поимённый список в тесте не обновлял. Доказательство: `git show --stat 3f80766` = 1 файл, `git log -- library.test.ts` = последняя правка `dec76ac`. Это ОЖИДАЕМОЕ поведение охранника (долг №227), правка — одна строка в пакете, который задача трогать не должна
- `01:58` [0/N] ЖДУ ВЛАДЕЛЬЦА: план и четыре вопроса одним блоком
- `02:05` ВЛАДЕЛЕЦ: ок плану. В1 — да (границы `.refine` интроспекцией + юнит). В2 — с источником у каждого примера. В3 — русский. В4 — (б) точечное разрешение на литерал в `library.test.ts`, №227 не закрывать. Часы пошли.
- `02:05` [1/4] правка по разрешению: `'parallax25@1'` в поимённый список `library.test.ts`
- `02:07` [1/4] готово: `library.test.ts` зелёный (8/8). Имя теста и комментарий по-прежнему говорят «ПЯТЬ» — разрешение было точечным («ничего кроме литерала»), расхождение назову в отчёте, №227 не закрыт
- `02:07` [2/4] начато: `guidance` в `TemplateSpec` + семь спеков + четыре тестовых спека; интроспекция схем
- `02:22` [2/4] готово: `guidance` у семи спеков (тестовые четыре наследуют спредом), `params-schema.ts` — `introspectParams`; измерено: `.refine`-границы вынимаются у 11 полей трёх шаблонов, перекрёстная проверка `bed@1` видна как «checks без текста»
- `02:22` [3/4] начато: `vpe spec export` — argv, spec-export.ts, run.ts
- `02:48` [3/4] готово: `vpe spec export` печатает markdown и `--json` (парсится, 7 шаблонов, 7 примеров); YAML примеров печатает канонический писатель `@vpe/schema`
- `02:48` [4/4] начато: охранник `spec-export.test.ts`, `docs/ai-scenarist.md`, отчёт, транскрипт нарушений
- `03:05` [4/4] нарушения Н1–Н4: четыре пробы, четыре красных; восстановлено, porcelain чист от следов проб
- `03:06` ВТОРОЙ ЧУЖОЙ КРАСНЫЙ — линт: `packages/cli/test/request-dedup.test.ts:65` (`as unknown as` — правило S-01 долг №3). Файл идентичен HEAD (`git diff --quiet HEAD` — да), последняя правка `71fb1b6` (E-02). Мои файлы линт проходят (exit=0). Точечного разрешения на него нет — НЕ трогаю, называю в отчёте
- `03:12` [4/4] `docs/ai-scenarist.md`, транскрипт, выгрузка `docs/impl/SPEC-01/spec-export.md` (439 строк), долги №233–235. Пакетные тесты cli+templates-spec+compile: 544/544. Полный прогон запущен
- `03:25` вычитка выгрузки глазами: добавлены три правила, которых не хватало для «пишу, не открывая код» — грамматика имени якоря, `N ≥ 1` у `[pause:]`, разделитель `[say:]`. Грамматика имени проверена на `publicAnchor()`: пробел/точка/ведущий дефис — отказ, дефис и подчёркивание внутри — принято
- `03:26` полный прогон перезапущен на финальных исходниках
- `03:45` проба Н5 (сверх задания): первая редакция проверки чисел канала искала `30 ` во всём markdown и осталась бы зелёной после пропажи строки fps. Охранник переписан на поиск в `doc.channel`; проба это доказала. Транскрипт и отчёт обновлены — пять проб, пять красных
- `03:50` проба П1: утверждение «восьмой шаблон попадёт в выгрузку сам» проверено делом — синтетический `zoomblur@1` даёт восемь карточек с guidance и схемой. Записано в транскрипт и отчёт
- `03:58` ПОЛНЫЙ ПРОГОН ЗЕЛЁНЫЙ: 163 файла PASS, 2486 passed, 3 skipped, **0 failed** (578 с). База была 2435+1 красный+3 skipped при 163 файлах ⇒ +50 тестов и +1 файл, ровно охранник задачи; чужой красный ушёл из счёта
- `03:59` контрольный прогон на финальном диске: cli+templates-spec — 342/342, 20 файлов
**Итог:** `vpe spec export` выгружает правила движка markdown'ом и `--json`; параметры — только интроспекцией схем (включая границы `.refine` их собственным текстом); `guidance` обязателен у семи шаблонов; охранник 50 проверок; пять проб нарушений красные, одна нашла слабость моего же охранника. Чужой линт `request-dedup.test.ts` НЕ тронут — вход шага 0 следующей задачи. Детали — docs/impl/SPEC-01/report.md, выгрузка — docs/impl/SPEC-01/spec-export.md, инструкция — docs/ai-scenarist.md.
