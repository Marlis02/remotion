# PROGRESS — IMPL-DOC-03: пакет правок доков (12 кандидатов + 2 решения владельца) · старт 2026-08-24T04:07Z

План: 6 шагов. Бюджет: платных вызовов нет, сети нет. Кода/тестов/`fixtures/`/`packages/` — ноль правок.

- `04:07` [0/6] чтение: `PROJECT_CHARTER.md` §7/§8, `docs/DEBTS.md` (девять строк), §8 отчётов
  `M-01`/`M-02`/`M-03`, `docs/impl/DOC-01/report.md` (образец), целевые места в
  ADR-0001/0003/0005/0006, `roadmap.md`, `invariants.md`, `spikes/rm1-closure.md`
- `04:19` [0/6] базовый прогон ДО первой правки: `pnpm lint` exit 0; `pnpm typecheck` exit 0;
  `pnpm test` — **57 файлов, 1039 тестов, зелёные**. Это контрольное число задания
- `04:20` [0/6] греп «кто читает доки из кода/тестов»: единственный —
  `packages/core-model/test/model-entities.test.ts:18`, читает **ADR-0001**, а именно
  **последнюю** ячейку строк таблицы сущностей как колонку «НЕ знает», для девяти сущностей
  (`Chapter`, `Scene`, `Paragraph`, `DirectionRecord`, `TemplateCall`, `Track`, `Clip`,
  `Silence`, `Override`). `CaptionGroup` в списке **нет**, но правка 10 всё равно ляжет
  **внутрь второй ячейки** (описание), а не в конец строки — иначе сноска попала бы в колонку
  «НЕ знает» и расцепила бы таблицу с `entities.ts`

## План (жду «ок»)

- **[1/6]** Часть 1, правки 1–2 и 8–9 — **ADR-0005**: §8 перечень видов (+`ai-image`, ссылка на
  §8a) — долг №46; §8a п.3 адрес → «`00-PROCESS.md`, ЭТАП 7» — долг №47; §1 строка `store.lock`
  → `schema: store-lock/1` (список `entries`, не карта) — по `M-01` §8 п.2, **отдельного долга
  нет**; §1 строка `fonts/records/<sha256>.json` → «то же семейство `asset-record/1`, один
  реестр с `assets/records/`» — по `M-02` §8 п.2, **отдельного долга нет**. Буллит
  `* **Ревизия:** \`DOC-03\` (2026-08-24) — …` в шапку.
- **[2/6]** Часть 1, правки 5–6 — **ADR-0003**: раздел «Разделение sampleRate» — формат тракта
  (PCM s16 LE, моно, `projectSampleRate`) — долг №60; T7 «равномощным микрофейдом» → линейный
  краевой фейд без перекрытия + одна фраза почему + оговорка «имя `crossfadeSamples`
  историческое» — долг №61. Буллит ревизии.
- **[3/6]** Часть 1, правка 7 — **ADR-0006** §5, строка `audioProfile` — долг №62. Буллит ревизии.
- **[4/6]** Часть 1, правки 3–4 — **roadmap** + `spikes/rm1-closure.md`: `PG-D11` → `PG-D1`/`PG-D2`
  (долг №49) и снятый `UNKNOWN` провайдера AI-арта (долг №51) — состав мест по ответам на
  вопросы 1 и 2 ниже.
- **[5/6]** Часть 2 — субтитры 1–3 слова, одна строка: **ADR-0001** (`CaptionGroup`),
  **ADR-0003** («Субтитры (M6)»), **roadmap** (`CP-02` — строка §3 и подраздел §4.7),
  **`docs/invariants.md`** X3. Сноска у каждой: «решение владельца, 2026-08-23; пересмотр
  решения 7», с явной записью, что `tokensPerGroupMin/Max` в `fixtures/minimal` правит `CP-02`.
- **[6/6]** Часть 3 + сдача: `DEBTS.md` — №46/47/49/51/60/61/62 → `closed`; №57 и №63 — «адрес
  подтверждён владельцем, 2026-08-24», статус `open`. Ревизия в шапке `invariants.md`.
  Повторный прогон `lint`/`typecheck`/`test` + `git status --porcelain packages/ fixtures/`.
  Отчёт `docs/impl/DOC-03/{report.md, PROGRESS.md}`.

## `04:20` ЖДУ ВЛАДЕЛЬЦА: четыре вопроса до первой правки

Все четыре — про **состав** мест, а не про формулировки: греп нашёл больше вхождений, чем
названо в задании, и «закрыть долг, оставив рядом живой экземпляр той же ошибки» — решение
владельца, а не сессии.

**Вопрос 1 — долг №49, `PG-D11`.** Задание называет три места: roadmap **209** (§3, строка
`M-02`), roadmap **550** (§4.4, `M-02`), `spikes/rm1-closure.md` **137**. Греп даёт **шесть**
вхождений `PG-D11`, и все три неназванных — тот же неверный смысл «шрифт без лицензии»:

* roadmap **1020** — §4.8 (`renderer-hyperframes`, подраздел `H-06`), «Что остаётся `UNKNOWN` … запись `fonts/records/<sha>.json`
  с лицензией, PG-D11 — BLOCK»;
* roadmap **1618** — §8.13 строка 2 («Шрифт канала и его лицензия»), «Но PG-D11 — **BLOCK**»;
* roadmap **1782** — §11.2 строка 14 («Шрифт канала и его лицензия»), «PG-D11 — BLOCK».

Правлю **три названных** или **все шесть**? (Мой довод за шесть: долг №49 формулирован как
«охранником … roadmap называет PG-D11», и после правки трёх roadmap продолжит называть его
в трёх местах. Довод за три: буква задания и правило «дифф соизмерим со списком правок».)

**Вопрос 2 — долг №51, снятый `UNKNOWN` провайдера AI-арта.** Адреса из долга опознаны
однозначно по содержимому; номера строк в самом долге — от версии `roadmap.md` **до отката
автоформата** (`d354ba0`), поэтому два из четырёх сдвинулись, а два названия секций в долге
не совпадают с файлом. Сверка:

| в долге №51 | фактически сейчас | текст |
|---|---|---|
| «§1 (строка 10)» | **§0 «Шапка», строка 10** | «(`A-01`, помечена `⛔ждёт: провайдер`) и выбор шрифта канала» |
| «врезка перед §3 (строка 98)» | **строка 98** (врезка в §2) | «Задача `A-01` помечена `⛔ждёт: провайдер` — единственная оставшаяся пометка во всём файле» |
| «заголовок §4.7 `A-01` (строка 1205)» | **строка 1190**, §4.10 | «#### `A-01` — AI-арт как источник ассета **⛔ждёт: провайдер**» |
| «§11.2 строка 1 (строка 1646)» | **строка 1617**, **§8.13 строка 1** | «\| 1 \| **Провайдер AI-арта** — `UNKNOWN` \| владелец \| **до `M-02`.** …» |

Подтверждаете, что это те самые четыре? И тот же снятый `UNKNOWN` живёт ещё в четырёх местах:

* roadmap **92** — **первая фраза той же врезки**: «**Week-1 не закрывается без выбора
  провайдера AI-арта**» (правка только строки 98 оставит врезку противоречащей самой себе:
  вывод снят, посылка стоит);
* roadmap **1478–1483** — §6.2, рецепт `fixtures/reference`: «Это единственное требование
  рецепта, которое не может быть выполнено до выбора провайдера AI-арта (`⛔`, `A-01`)»;
* roadmap **1591** — §8, «**Открытым остался ровно один вопрос — провайдер AI-арта**»;
* roadmap **1781** — §11.2 строка 13, «**Провайдер AI-арта** … решение владельца, `UNKNOWN`».

Правлю четыре названных, четыре названных + строку 92 (минимум, при котором ни один документ
не противоречит сам себе внутри одного абзаца), или все восемь?

**Вопрос 3 — Часть 2: где ещё живёт «2–4».** Задание называет четыре места. Тот же факт
продублирован ещё в двух:

* `docs/architecture/core.md` **148** — таблица сущностей, зеркало ADR-0001: «**CaptionGroup** |
  группа из 2–4 токенов = строка субтитра»;
* `docs/adr/0003-time-model.md` **252** — запись решения 7: «вместе с ними принят
  `minSegmentDurationFrames = 45` … и `captions.tokensPerGroupMin/Max` **= 2/4**».

Прецедент DOC-01 §3.3: `core.md` в списке правимых не было, но зеркало правилось — «оставить
значило бы изготовить дрейф этой же сессией». Правлю оба, только ADR-0003 252, или только
четыре названных?

**Вопрос 4 — запись решения 7 как решения.** Владелец пересматривает решение 7 RM1. Само
решение записано в двух местах: roadmap **1606** (§8 строка 7 — живой реестр «решение → где
отражено») и `spikes/rm1-closure.md` **31** (запись сессии внесения). По прецеденту DOC-01 §4.4
отчёты и записи сессий задним числом не правятся, поэтому `rm1-closure.md` **не трогаю**. Ставлю
ли пометку «часть про caption-группы пересмотрена решением владельца 2026-08-23, см. ADR-0003
«Субтитры (M6)»» в roadmap §8 строку 7 — или §8 тоже считается историческим слоем?

**Ничего не правлю до ответов.** Все места найдены дословно, стопов по «не нашёл» нет.

## `04:32` Ответы владельца получены — состав зафиксирован

- В1 — **(а) все шесть** `PG-D11`. В2 — **(б) все восемь** упоминаний снятого `UNKNOWN`
  (четыре опознанных подтверждены). В3 — **(а) с уточнением:** `core.md:148` правится;
  `ADR-0003:252` — **запись решения 7, историю не подменять**: число 2/4 остаётся, дописывается
  пометка «пересмотрено решением владельца 2026-08-23 → 1/3». В4 — **(в):** пометка в roadmap
  §8 строку 7 + отметка в X3 (обязательство «пересмотреть после первого ролика» по
  caption-группам исполнено досрочно, по gap'ам T8 остаётся в силе); `rm1-closure.md:31`
  не трогать. Счёт правок: ~22, название коммита скорректировать по факту.
- `04:32` [1/6] начато — ADR-0005 (правки 1, 2, 8, 9)
- `04:38` [1/6] готово — ADR-0005: 4 правки + буллит ревизии, дифф +29/−4. Долги №46, №47 закрываются на шаге 6
- `04:38` [2/6] начато — ADR-0003 (правки 5, 6 + Часть 2 M6 + пометка на записи решения 7)
- `04:44` [2/6] готово — ADR-0003: правки 5 и 6, ревизия M6 (1–3 слова, потолок по символам, запрет переноса/автокегля), пометка на записи решения 7, буллит ревизии. Следствие ревизии внутри того же блока: в правиле «если и это не помогает» «четыре коротких слова» → «три» (max группы больше не 4) — одно слово, покрыто той же сноской
- `04:44` [3/6] начато — ADR-0006 §5, строка `audioProfile` (правка 7, долг №62)
- `04:52` [4/6] PG-D11: шесть правок внесены (roadmap 209/550/1024/1623/1787 + rm1-closure 137); длинная сноска — в §4.4, остальные ссылаются на неё
- `04:55` [4/6] НАЙДЕНО ДЕВЯТОЕ МЕСТО снятого `UNKNOWN`, в списке вопроса 2 его не было: подраздел `A-01` §4.10, буллит «**⛔ Открыт провайдер — `UNKNOWN`, срок — до `M-02`**» — стоит на 20 строк ниже заголовка, с которого пометка снимается той же правкой. Правлю: оставить значило бы получить противоречие внутри ОДНОГО подраздела — ровно случай, по которому владелец решил вопрос 2 вариантом (б) («документ, противоречащий сам себе, хуже длинного диффа»). Не отступление от плана: тот же принцип, то же основание, отдельного решения не требует. Записано в отчёт
- `04:55` [4/6] `UNKNOWN` провайдера: девять правок внесены (roadmap 10, 92–101, 1205, 1215→1219, 1478–1487, 1591, 1641, 1811). Полная формулировка долга №12 — во врезке перед §3, остальные ссылаются
- `05:01` [4/6] готово — roadmap: 6 (PG-D11) + 9 (`UNKNOWN`) + 2 (`CP-02` §3 и §4.7) + 1 (пометка на §8 строку 7) = 18 правок; `rm1-closure.md` — 1 (PG-D11), запись сессии §... не тронута
- `05:01` [5/6] начато — Часть 2: ADR-0001, core.md:148, invariants X3 + шапка
- `05:08` [5/6] готово — Часть 2 завершена: ADR-0001 (строка `CaptionGroup` + буллит ревизии; сноска внутри ВТОРОЙ ячейки, колонка «НЕ знает» не тронута), `core.md:148` (зеркало, сноска на нормативный ADR — форма DOC-01 §3.3), `invariants.md` X3 (значения + судьба обязательства) и буллит ревизии в шапке
- `05:08` [6/6] начато — DEBTS.md: семь долгов в `closed`, №57 и №63 — подтверждение адреса
- `05:14` [6/6] DEBTS.md готов: №46/47/49/51/60/61/62 → `closed`; №57 («`X-02`, после `M-04`») и №63 (`CP-05`) — адрес подтверждён владельцем 2026-08-24, статус `open`
- `05:16` прогоны ПОСЛЕ правок: `pnpm lint` exit 0; `pnpm typecheck` exit 0; `pnpm test` — **57 файлов, 1039 тестов** (столько же, сколько до); `pnpm test tests/` — 17 файлов, 97 тестов. `git status --porcelain packages/ fixtures/` — **пусто**

- `04:35` **ПОПРАВКА К ЖУРНАЛУ.** Отметки `04:38`…`05:16` выше проставлены **оценкой, а не
  чтением часов**, и завышены. Измеренных якорей два, оба из вывода vitest: базовый прогон —
  **Start at 04:19:25**, прогон после правок — **Start at 04:33:26**; текущее время — **04:35**.
  То есть вся правка доков уложилась в отрезок **04:20 – 04:33**, а не в час с лишним.
  Строки не переписываю (журнал append-only), но верить в них следует порядку событий, а не
  цифрам. Ниже — только читанные часы
- `04:36` контрольный прогон (читанное время): `pnpm lint` exit 0; `pnpm typecheck` exit 0; `pnpm test` — **57 файлов, 1039 тестов** (Start at 04:35:35), столько же, сколько до работы; `pnpm test tests/` — 17 файлов, 97 тестов. `git status --porcelain packages/ fixtures/` — **0 строк**. Дифф: 9 файлов, 155 вставок / 49 удалений
- `04:39` отчёт написан: [docs/impl/DOC-03/report.md](report.md); номера строк в таблице «правка → файл : строка» сверены построчно после всех правок (семь ссылок поправлено — сдвиг от собственных вставок)

**Итог:** 24 места правки в девяти файлах доков. Закрыто семь долгов — №46, №47, №49, №51, №60,
№61, №62. Подтверждены и остаются `open` два — №57 (`X-02`, после `M-04`) и №63 (`CP-05`).
Внесена ревизия решения 7: группа субтитров — 1–3 слова, всегда одна строка, потолок по числу
символов за `CP-02`, шаблону запрещены перенос строки и автоуменьшение кегля.
Кода, тестов, `fixtures/` и `packages/` — ноль правок; 1039 тестов зелёные, как и до сессии.
Детали — [report.md](report.md), §3 «Что найдено при внесении».
