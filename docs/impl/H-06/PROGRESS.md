# PROGRESS — IMPL-H-06-resume · старт 2026-08-29T12:18:01+06:00
Якорь `date -Iseconds` поставлен ДО чтения репозитория.
План: Ш0 (верификация снимка 0d1a3cc на этой машине) → план ниже черты → жду «ок» → Ш5 (бюджеты) → Ш6 (нарушения+доки). Бюджет: 60 мин без зелёного теста ⇒ стоп и отчёт. Preflight браузера — отдельным якорем, вне часов.
- `12:18` [0/3] старт Ш0 — читаю снимок, ставлю зависимости
- `12:19` PREFLIGHT (вне часов, отдельный якорь 2026-08-29T12:18:55+06:00): `pnpm install --frozen-lockfile` → «Already up to date» (1.0s); `pnpm build` (tsc --build) → чисто, 5.6s. Браузер ЖИВ: наш корень `~/.cache/hyperframes/chrome/chrome-headless-shell/` содержит РОВНО одну установку `linux-152.0.7928.2`, бинарь отвечает `Google Chrome for Testing 152.0.7928.2`. Рядом чужой puppeteer-кэш `linux-152.0.7977.42` — та самая раскладка, на которой H-03 поймал расхождение каналов; резолвер `browser.ts` его не читает.
- `12:19` [1/3] Ш0: старт полного прогона `pnpm test` — ожидаю ~несколько минут
- `12:34` [1/3] Ш0 ГОТОВО за 15 мин. `pnpm test` — **146 файлов PASS, 2260 passed / 2 skipped / 0 failed, 486.9 c**. Расхождения с ожиданием НЕТ: на приёмной машине (без Chrome) норма — 31 красный в 9 браузерных файлах, здесь браузер есть, поэтому браузерные зелёные. Контрольный гейт `still@1` (draftHalf, N=3) — **PASS**: три прогона дали один `sha256` `7bac50d3…bc9c` и один `framemd5` `f49fbb77…b2bd`, 12 кадров, `wallMs` 1846/1809/1799, отпечаток `61db2ca8…63a0`. Браузер этой машины жив.
- `12:35` запущен отдельный прогон 10 браузерных файлов — за счётом по файлам (для отчёта)

---

## План (жду «ок» владельца)

**Ш5 — измерение `msPerFrameBudget` (≈25 мин).** Прибор — в `packages/renderer-hyperframes/out/`
(каталог в `.gitignore` и в ignores eslint ⇒ в дифф не попадает; сырой лог уедет в
`docs/impl/H-06/`). Метод дифференциальный, как в плане прежней сессии: реестр = пять прод-реализаций
плюс тестовый `noop@1` с ПУСТЫМ mount; на каждый шаблон два запроса, различающиеся РОВНО одним
клипом (`T` против `noop@1`) при тех же ассетах, шрифте, окне, кадрах и профиле; `msPerFrame =
(median(wallMs_T) − median(wallMs_noop)) / кадров`. Шум прибора меряется тем же прибором: разброс
серии `noop@1` против самой себя. `kenburns@1` — на паре `[still, kenburns]` против `[still, noop]`
(один стоять не может по построению). `bed@1` не рендерится вовсе — 0 остаётся измерением по
построению. Правило д1: `Math.ceil`; разность ниже шума ИЛИ отрицательная ⇒ 1 у визуальных,
0 у `bed`, с пометкой «измерено `H-06`: ниже шума прибора». Правка — пять манифестов
`templates-spec/src/templates/*@1.ts` конвенцией зачёркивания, затем `pnpm build` по цепочке и
прогон `templates-spec` + `renderer-hyperframes`.

**Ш6а — протокол нарушений Н1–Н6 (≈20 мин).** Каждое — падающим, резерв/откат ТОЛЬКО `cp`,
`git status --porcelain` после каждого отката, транскрипт — `docs/impl/H-06/violation-transcript.txt`.
Н1 easing вне реестра в реализации; Н2 `Date.now` в mountSource (живой рендер, guard D4);
Н3 перевёрнутый порядок transform (самопроверка `а1` обязана уронить); Н4 реализация без спека;
Н5 правка шаблона без пересборки ⇒ чужой `bundleHash` не пускает; Н6 удаление двух строк R2 ⇒
`captions-visibility` краснеет. Браузер нужен Н2/Н3/Н5/Н6 — это прогоны, а не юниты.

**Ш6б — доки (≈25 мин).** `DEBTS.md`: №154 закрыт с условием «`G-04` > 15 мин на final ⇒ вариант (б)»
и ссылкой на `2e10ecd`; №173 закрыт двусторонним охранником; №148 закрыт правилом д1; новые со
**№181** (охранник команды под смешанные запросы → `E-00`; дефект масштаба → `H-01fix`; судьба
draft-пары → `L-01`; плюс накопленное снимком). `invariants.md` — ревизия `H-06` переходами построчно
до/после. `docs/impl/H-06/report.md` — адреса переноса из `sp3f/src/index.html` «откуда → куда» и что
НЕ перенесено; `bundle.hash` до/после правки runtime (значение «до» — из `git worktree` на `5d1f91d`);
таблица гейтов «шаблон · класс · верная/кривая картинка (scale)»; находки прежней сессии
(Н-A/Н-B/Н-C, bold, масштаб) сводкой; название финального коммита.

**Ш7 — приёмка (≈10 мин).** Полный `pnpm test` + `pnpm lint` + `pnpm typecheck`; `git status --porcelain`
и `git diff --stat` поверх `0d1a3cc` — обязан быть только: манифесты templates-spec, DEBTS, invariants,
docs/impl/H-06/*. Греп долгов `H-06` до/после.

**Вопросов, требующих остановки, нет.** Одно замечание к сведению — в тексте ответа.

---

- `12:41` ВЛАДЕЛЕЦ: «ок», план принят целиком, Ш5→Ш6а→Ш6б→Ш7 без остановок. Способ снятия `bundle.hash` «до» на `solid@1` через worktree принят; основание (изолирован вклад `runtime.js`) назвать в отчёте при самой паре хэшей. Два уточнения: (1) медианы серий и измеренный шум прибора — ЧИСЛАМИ в `report.md` рядом с бюджетами, чтобы правило «ниже шума» проверялось из отчёта без пересчёта; (2) Н5: после отката porcelain пуст И контрольный ЗЕЛЁНЫЙ прогон затронутого файла — в транскрипт («откатил» проверяется поведением).
- `12:41` [2/4] Ш5 начат — прибор бюджетов
- `12:42` Ш0 добор: отдельный прогон 10 браузерных файлов — **10 файлов PASS, 51 passed / 1 skipped, 351.9 c**. По файлам: where 11, isolation-render 9, subprocess 8, templates-gate 6, render 5, fingerprint-browser 4, gate-render 3, cli/template-gate-render 2, captions-visibility 2, templates-gate-final 1.
- `12:47` Ш5: прибор написан (`packages/renderer-hyperframes/out/measure-budget.mjs`, вне git и вне eslint). Пробы: 6 кадров / scale 0.25 — ВСЁ тонет в шуме запуска (константа ~1.5 с, шум 65.6 мс = 10.9 мс/кадр); 30 кадров / scale 1 (геометрия `final`) — разности ВИДНЫ: kenburns 29.96, flash 20.09, still 1.66, captionEmphasis 1.64 мс/кадр при шуме 5.09. Профиль измерения — `final` (1080×1920, scale 1): бюджет объявлен ОЦЕНКОЙ СВЕРХУ, и мерить его на четверти линейного размера значило бы занизить в ~16 раз по пикселям. Основной прогон: 60 кадров, 5 повторов — начато, ожидаю ~5 мин
- `12:49` [2/4] Ш5 ИЗМЕРЕНО за 4 мин (60 кадров, 5 повторов, геометрия `final`). Шум прибора = |медиана A − медиана B| двух независимых серий одного базового запроса = **70.7 мс на 60 кадров = 1.178 мс/кадр**. Разности медиан: `still@1` 56.1 мс ⇒ **0.936** мс/кадр (НИЖЕ шума); `kenburns@1` 1976.7 ⇒ **32.944**; `flash@1` 1783.6 ⇒ **29.727**; `captionEmphasis@1` 53.2 ⇒ **0.887** (НИЖЕ шума); `bed@1` — рендера нет по построению. По д1: still 1, kenburns 33, flash 30, captionEmphasis 1, bed 0. Сырой лог + текст прибора → `docs/impl/H-06/budget-measurement.txt`
- `12:52` Ш5 ПОСЛЕДСТВИЕ, требующее слова владельца (работу не останавливаю, Ш6а иду): новые числа уронили ДВА ассерта-потребителя в `packages/compile/test/` — объявленной ЗАКРЫТОЙ зоне и вне приёмного диффа. `template-contract.test.ts:146` ждёт `msPerFrameBudget` у `kenburns@1` = 2 (стало 33); `compile-ir.test.ts:244` ждёт `seg:intro maxMsPerFrame` = 3 (стало 34 = still 1 + kenburns 33). `seg:turn` = 3 не изменился. Правка арифметическая, поведение кода не меняется; прецедент точечного разрешения — закрытие долга №171. Вопрос задам текстом после Ш6а.
- `12:52` [3/4] Ш6а начат — протокол нарушений Н1–Н6
- `13:05` Ш6а НАХОДКА (важная): нарушение **Н6 НЕ КРАСНЕЕТ**. Удалил обе строки видимости R2 из `runtime.js` — `captions-visibility.test.ts` остался ЗЕЛЁНЫМ (2 passed). Разобрано тремя измерениями: правка доехала до композиции (`capsTimeline.set` в index.html = 0), кадры БЕЗ строк переключаются на границе групп (0..5 → 5e9a142bf12a, 6..11 → 8cc681a93915), кадры С ними — ПОБАЙТОВО те же. Видимость держит САМ рендерер по `data-start`/`data-duration`. Следствия: находка прежней сессии «эти атрибуты не читал никто» неверна; две строки R2 — no-op по картинке; охранник Н6 стережёт работу рендерера, а не наш код. Строки НЕ удаляю (переделка Ш1–Ш4 запрещена), долг заведу.
- `13:10` [3/4] Ш6а ГОТОВО: одиннадцать проб, девять красных, ДВЕ находки. Н1а tsc TS1360; Н1б юнит Н1; **Н1в — кривая вне реестра литералом в mountSource не ловится НИЧЕМ, гейт даёт PASS на ДРУГИХ кадрах** (sha 'elastic.out' cd853a1f… против объявленной power3.out bb18d149…); Н2а линт рендерера --strict; Н2б guard D4 с именем клипа; Н3а самопроверка а1 (gsap собрал 'translate(1px, 1px) scale(2, 2)' — FACT SP-3c §6.2 подтверждён живьём); Н3б юнит №173; Н4 юнит сверки в обе стороны; Н5а правило R2 адаптера до браузера (32c29155… против 12c182ea…); Н5б gateStaleness; **Н6 — охранник ЗЕЛЁН, строки R2 no-op по картинке**. Откаты только `cp`, porcelain после каждого — те же пять строк, картинка каждого шаблона побайтово доработочная. Транскрипт — `docs/impl/H-06/violation-transcript.txt`
- `13:10` [4/4] Ш6б начат — bundle.hash до/после через worktree, затем доки
- `13:14` Ш6б: `bundle.hash` на синтетическом `solid@1` — **до (`5d1f91d`, worktree): `03c550b38695f3ea…ab93`; после (снимок + Ш5): `2848f02d842e585e…a7f0`**. Изоляция вклада `runtime.js` ДОКАЗАНА, а не заявлена: дамп `index.html` обеих композиций различается РОВНО на 27 строк, и все 27 — внутри `runtime.js` (комментарий R2, `guardedTimeline`, три строки видимости). Worktree удалён.
- `13:20` Ш6б: DEBTS готов — №148/№154/№173 закрыты; №151 переадресован (адрес израсходован, списки не расширены), №167 сужен измерением (отложенных вызовов у пяти шаблонов НОЛЬ), №168/№174/№176/№177 помечены; девять новых — №181 охранник команды→`E-00`, №182 масштаб→`H-01fix`, №183 draft-пара→`L-01`, №184 кривая вне реестра в тексте mountSource, №185 no-op строк R2, №186 bold неразличим, №187 системный шрифт, №188 числа полосы, №189 критерий на `bed@1`. invariants: ревизия `H-06` — переходов НЕТ, счёт 64, построчно R12/R13/R14/D4/D5; пометки в строках R12 и R13.
- `13:25` [4/4] Ш6б ГОТОВО: `docs/impl/H-06/report.md` (171 строка) — Ш0-числа, метод и числа Ш5 (медианы, разбросы, шум), таблица гейтов «шаблон · класс · картинка (scale)», пара `bundle.hash` с доказательством изоляции, перенос из `sp3f` «откуда → куда» и что НЕ перенесено, протокол с двумя находками, сводка находок прежней сессии, инварианты, долги, название коммита. Ш7 — приёмка.
- `13:30` [4/4] Ш7 ПРИЁМКА. `pnpm typecheck` — чисто. `pnpm lint` — чисто. `pnpm test` — **2 failed | 2258 passed | 2 skipped (2262), 146 файлов, 484 c**. Красных ровно два, и оба названы ДО прогона: `packages/compile/test/template-contract.test.ts` и `compile-ir.test.ts` — ассерты доизмерительных бюджетов. **Новых красных Ш5–Ш6 НЕТ ни одного**, браузерный файл протокол не добавил. Дифф поверх `0d1a3cc`: 7 файлов — пять манифестов templates-spec, DEBTS, invariants — плюс три новых в `docs/impl/H-06/`; ни `.h06bak`, ни worktree, ни следов проб. Долги грепом `H-06`: 12 строк → 22; строк всего 180 → 189; `closed` 50 → 53.
**Итог:** Ш0 подтвердил снимок (2260 passed на машине с браузером); бюджеты заменены измерением (`kenburns@1` 33, `flash@1` 30 — прежние оценки были занижены на порядок; `still@1`/`captionEmphasis@1` по 1 ниже шума 1.178 мс/кадр; `bed@1` 0 по построению); протокол нарушений дал девять красных и ДВЕ находки — кривая вне реестра D5 проезжает в кадры мимо всех охранников (№184) и две строки видимости R2 суть no-op, а их охранник стережёт работу рендерера (№185). Доки закрыты: три долга закрыто, пять помечено, девять заведено со №181.
**ЖДУ ВЛАДЕЛЬЦА:** точечное разрешение на два арифметических ассерта в `packages/compile/test/` (закрытая зона) — без него репозиторий несёт два красных. Детали — `docs/impl/H-06/report.md` §10 п. 4.
Детали — `docs/impl/H-06/{report.md,violation-transcript.txt,budget-measurement.txt}`.
