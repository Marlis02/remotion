# PROGRESS — IMPL-DAY-01 (CACHE-02 → CAPTION-01 → VID-02b) · старт 2026-09-11T14:11+06
План: шаг0 baseline → ЗАДАЧА1 CACHE-02 → ЗАДАЧА2 CAPTION-01 → ЗАДАЧА3 VID-02b (если ≥1ч). Бюджет: стоп 18:30, ноль денег, ноль сети.
- `14:11` [0/4] старт; Charter и 00-PROCESS прочитаны
- `14:12` [0/4] `pnpm install --frozen-lockfile` + `pnpm -r build` + `pnpm lint` — готово, lint 0 нарушений
- `14:12` [0/4] `pnpm test` на ХОСТЕ начат
- `14:16` [0/4] ОШИБКА прибора: на хосте `asset-add.test.ts` 17 failed — тест зовёт `/usr/local/bin/ffmpeg` (путь ОБРАЗА), на хосте ffmpeg в `/usr/bin`. Хост не тот прибор; прогон снят
- `14:16` [0/4] `./scripts/vpe-docker test` начат — ЭТО способ измерения нормы (образ `vpe-env:env-02` на месте)
- `14:32` [0/4] НОРМА ИЗМЕРЕНА в образе `vpe-env:env-02`: **2700 passed / 3 skipped / 0 failed**, 682.27 с. Совпала с заявленной 2700/3/0. Способ — образ (на хосте прибор врёт: `/usr/local/bin/ffmpeg`)
- `14:32` [1/4] ЗАДАЧА 1 CACHE-02 начата
- `14:35` [1/4] CACHE-02: код правлен (views/segment.json +bundleHash, keys.ts, render.ts — вердикт «промах по композиции» СНЯТ, stage-cache.ts комментарий, тесты). Билд зелёный
- `14:35` [1/4] golden `cache-key-view.txt` перегенерирован: fields 39 → **40**, сдвиг ровно +1 поле (`02. bundleHash : text`) и перенумерация. `cache-matrix.txt` НЕ сдвинулся (поле прямое, обходом схем не достигается)
- `14:36` [1/4] media+cache-segment+blast-radius: 336 passed / 0 failed
- `14:40` [1/4] СТЕНКА `work/ct-test-1`: сборка draftHalf БЕЗ `rm -rf .cache` поверх старого кэша — ПРОШЛА (exit 0), три сегмента `кэш: промах`, final.mp4 sha `8daa9ef2c0c048c9…` РАВЕН прежнему. Прогретая следом: 3/3 `попадание`, тот же sha. Записей в манифесте 3 → 6 (старые на месте)
- `14:41` [1/4] полный прогон в образе начат
- `14:49` [1/4] полный прогон в образе: **2703 passed / 3 skipped / 0 failed** (норма была 2700/3/0; +3 — три новых охранника вместо одного снятого). `pnpm lint` 0
- `14:52` [1/4] протокол нарушений Н1–Н4 снят, все откаты проверены `diff` — чисто. Документы: report/PROGRESS/violation-transcript, roadmap (своя строка + пометка в CACHE-01), DEBTS (№272 сужен, №155 пометка, №196 заметка, новый **№273**), invariants K3 уточнён, ADR-0006 §2 формула дополнена, 00-PROCESS вердикты
- `14:53` [1/4] **ЗАДАЧА 1 CACHE-02 ЗАКОНЧЕНА.** Коммит 1 готов
**Итог:** `bundleHash` — восьмое слагаемое `segmentKey`; вердикт «промах по композиции» снят; сборка поверх старого кэша проходит промахом, а не отказом K3.
Норма 2703/3/0 в образе `vpe-env:env-02`, lint 0. Детали — report.md, violation-transcript.txt.
