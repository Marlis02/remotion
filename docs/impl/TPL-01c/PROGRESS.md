# PROGRESS — IMPL-NIGHT-01 (SP-VID + TPL-01c) · старт 2026-09-10T02:50+06:00
План: шаг0 (eslint work/**, build, lint, test) → A1..A6 (SP-VID, ≤1.5 ч) → B1..B6 (TPL-01c) → отчёт. Бюджет: 4 ч стенки, денег 0, сети 0.
- `02:50` [0/3] шаг 0 начат — node v25.6.1, pnpm 11.9.0, ffmpeg 7.0.2-static ok
- `02:54` [0/3] шаг 0: eslint `work/**` добавлен, install+build ok, `pnpm lint` — 0 проблем; `pnpm test` идёт фоном
- `03:01` [0/3] шаг 0 готов за 11 мин — `pnpm test` 2575 passed / 3 skipped / 0 failed (2578 всего), 615 с, exit 0. Норма задания «2578 passed/3 skipped» = ТОТ ЖЕ прогон (2578 — всего), расхождений нет
- `03:50` [2/3] ЧАСТЬ B (TPL-01c) начата: скелет демо-проекта, `demo.json` в папке шаблона, команда `vpe template demo`
- `03:55` [2/3] СЕМЬ ДЕМО СОБРАНЫ, 7 из 7, все 10 пресетов показаны: bed 135 кадр/3.4 с, captionEmphasis 210/4.3, flash 168/3.8, grade 125/7.4, kenburns 299/9.0 (3 сегмента, 3 пресета), parallax25 228/6.8 (2/2), still 146/3.5
- `03:55` НАХОДКА: `captionEmphasis@1` на первом варианте прозы упал «ADR-0003 T5: элемент `correction:seg:demo` длиной 0» — поправка кадровой границы вышла нулевой при нулевой хвостовой добивке. Чужой красный, не мой: лечится длиной прозы. Долг №257
- `04:03` [2/3] охранники готовы: юнит `packages/cli/test/template-demo.test.ts` (30 тестов, вычисляется по папкам), браузерный `packages/cli/test/template-demo-e2e.test.ts` (1 тест, 20.5 с, kenburns@1 два прогона → тот же sha), скелет `packages/cli/test/demo-project.test.ts` (11), зона NUL расширена с обеими компенсациями
- `04:08` [2/3] доки TPL-01c: отчёт, транскрипт из 12 проб (все — настоящие отказы), roadmap, gate-runbook §4-ter шаг 3-тер, 00-PROCESS, ADR-0009, долги №257–№258. `pnpm lint` — 0. Ждём полного прогона
- `04:14` [3/3] ПОЛНЫЙ ПРОГОН ЗЕЛЁНЫЙ: 2617 passed / 3 skipped / 0 failed (2620 всего), 651 с, exit 0. Было 2575/3/0 (2578) ⇒ +42 = 30 (template-demo) + 11 (demo-project) + 1 (браузерный e2e). Файлов 174 → 177
- `04:41` [3/3] ПОВТОРНЫЙ ПОЛНЫЙ ПРОГОН (после снятия мёртвого экспорта) ЗЕЛЁНЫЙ: 2617 passed / 3 skipped / 0 failed, 648 с, exit 0. `pnpm lint` — 0 проблем. Сырые выводы A3/A4/A6 сохранены в docs/impl/SP-VID/raw/

**Итог:** семь демо из семи, десять пресетов из десяти, весь каталог собирается за ≈38 с на
`draftHalf`; собирает их та же функция `build`, что и `vpe build`, поэтому все прошли **R12**.
Файл демо — `demo.json`, а не `demo.yaml` (решение сессии, разбор — отчёт §3.1). Полный
прогон 2617 passed / 3 skipped / 0 failed. Детали — `docs/impl/TPL-01c/report.md`.
