# PROGRESS — IMPL-CP-07 (компилятор потребляет контракт шаблона) · старт 2026-08-27T22:32:49+06:00
План: пишется после чтения (§1). Бюджет: стоп-правило 90 минут без зелёного теста; платных вызовов нет.
- `22:32` [0/N] якорь снят до чтения (`date -Iseconds` = 2026-08-27T22:32:49+06:00); git clean на `main` @ afdf05c
- `22:38` [0/7] чтение закончено за 5 м 16 с (ADR-0008 «Декларация»/«Контракт»/«V3×V9», ADR-0007 §1/§5, ADR-0002 §4, ADR-0005 §1; TS-01 отчёт целиком, CP-04 §3/§4/§7, CP-01 §5 в.7/8; invariants R3/K6/D1/D2/T3/R12; DEBTS 119/120/135/136/139/141/35/138/146/148/150; код `templates-spec/src/**`, `compile/src/{timeline,render-ir,audio}/**`, `core-model/model/render-ir.ts`, `media/assets/catalog.ts`, фикстура) — план ниже, ЖДУ ВЛАДЕЛЬЦА

## План (7 шагов) — ждёт «ок»

1. `templates-spec`: необязательный `declareDuration?(params): Samples | null` в `TemplateSpec`; реализация у `flash@1` (`params.durationSamples`); тесты пакета (объявляют четверо из пяти — нет; объявляет только `flash@1`).
2. `compile/src/timeline/contract.ts` — НОВАЯ чистая функция `templateContracts({records, generated, catalog, registry, registryVersionOfProfile})`: сверка версии реестра (K6, до первой записи) → `parseTemplateName` → `registry.resolve` → `paramsSchema.safeParse` → `declareAssets`+`resolveAlias` → `declareFonts`+запись `kind:'font'` → `declareDuration` → «порождённая запись + непустые `purposes` ⇒ ошибка». ОДИН `CompileError` со списком на все записи (`recordId`, шаблон, путь поля zod).
3. Timeline: `ComposeInput.registry`, `CompileProfileInput.templateRegistryVersion`; `compose` зовёт п.2 ПЕРВЫМ шагом; `records.ts` кладёт `assets`/`fonts`/`declaredDuration`/`purposes` в обе ветви `ClipFill`, особая ветка `resolveAlias` у `[img:]` УДАЛЯЕТСЯ (один путь), длительность из объявленного параметра; `dumpTimeline`.
4. IR: `core-model/model/render-ir.ts` — `IrFontRef` + `IrClip.fonts` + `RenderIrSegment.fonts`/`assets` (вопрос **A**); `IrClipSource.fonts/purposes`; `materializeSeeds` по `purposes` (`purpose = templateId` удаляется); объединение по сегменту, сортировка `(sha256, role)`; ассерт R3 в `compileIr`; сумма `msPerFrameBudget` по кадрам (вопрос **F**); `dumpIr`.
5. Аудио: `AudioMusicClip.assets` (№141 — sha `pad-loop` в `AudioPlan.music[0]`).
6. Тесты: новый `test/template-contract.test.ts` (фикстура + семь отказов); правки `compile-ir.test.ts` (D1 переписан на синтетическом спеке с `purposes: ['jitter']`, K4-строка `templateRegistryVersion` → ОШИБКА, fonts/assets/seeds, новый `segmentIrHash`), `records.test.ts` (`flash@1` = 4800/6 кадров), `dump.test.ts`, `audio.test.ts`, `project.ts` (реестр); греп-охранник «в `compile/src/**` нет `params.asset`/`params.durationSamples`/`params.inPoint`»; D2-греп расширен.
7. Протокол нарушений (6 сценариев, откат `cp`-резервом), отчёт `docs/impl/CP-07/`, `invariants.md` (R3/K6), `DEBTS.md` (девять адресатов, новые с №152).
- `22:44` [0/7] «ОК с поправками» получен: A — четыре правки типов; B/C/D/E/F/G приняты как рекомендовано; П1 — порядок проверок (`declare*` не зовутся на записи, не прошедшей схему); П2 — R3-ассерт В ОБЕ СТОРОНЫ. Начинаю шаг 1.
- `22:51` [1-5/7] шаги 1–5 готовы за 11 м: `declareDuration` в контракте + `flash@1`; `contract.ts` (порядок П1); `ComposeInput.registry`, `CompileProfileInput.templateRegistryVersion`, один путь у `[img:]`, длительность из объявленного параметра; типы `core-model` (4 правки), seeds по purposes, объединение по сегменту, R3-ассерт в обе стороны (П2), `budgets`; `AudioMusicClip.assets`. `tsc` по `src/**` зелёный, осталось 5 ошибок в тестах — их правит шаг 6.
- `23:11` [6/7] шаг 6 готов за 20 м: `template-contract.test.ts` (17 тестов), D1/D2 переписаны на синтетическом `jitter@1`, K4-строка `templateRegistryVersion` → ОШИБКА, греп-охранник `cp07-template-params`, `declareDuration` в тестах пакета. `pnpm test` — **112 файлов / 1899 тестов / 104.60 с** (было 110/1869); typecheck и lint зелёные. Стоп-правило не сработало: первый зелёный прогон `compile` — на 31-й минуте.
- `23:24` [7/7] протокол нарушений: семь прогонов (Н1 греп `params.asset`; Н2 версия реестра; Н3 alias подменён; Н4 `declareDuration` снят; Н5 `purposes`→`templateId`; Н6 «первый попавшийся» шрифт; Н7 лишний файл в запросе + контрольный Н7-bis односторонний ассерт). Все откачены `cp`; контроль зелёный (1899 тестов). Хэши измерены ФАКТИЧЕСКИ: `git archive HEAD` в отдельный каталог дал `CP-04`-значения `acf91306…`/`5bcdb8bd…` — реконструкция совпала побайтово.
- `23:31` [7/7] отчёт, `invariants.md` (R3/K6/D1/D2 — пометки, счёт 56), `DEBTS.md` (девять адресатов; закрыты №119/120/135/139, №141 → `X-02`, №35 сужен до `L-03`, №136/138 проверены; новые №152/153). Финальный контроль: `tsc` / `eslint` / `vitest` — зелёные, 112 файлов / 1899 тестов / 104.87 с.
**Итог:** компилятор зовёт контракт шаблона — `params` по схеме спека, `declareAssets` → sha (один путь и для `[img:]`), `declareFonts` → шрифты в IR, `declareDuration` (`flash@1` = 4800 сэмплов / 6 кадров), seed'ы по `purposes` (на фикстуре пусто — измерение), версия реестра по значению (**K6**), R3-ассерт в обе стороны, бюджет печатается. `segmentIrHash` обоих сегментов сменился; пять вкладов измерены по отдельности, базовая строка `CP-04` — прогон `git archive HEAD`, то есть FACT. Стоп-правило не сработало: первый зелёный прогон `compile` — на 31-й минуте. Детали — `docs/impl/CP-07/{report.md, violation-transcript.txt}`.
