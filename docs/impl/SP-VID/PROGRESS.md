# PROGRESS — IMPL-NIGHT-01 (SP-VID + TPL-01c) · старт 2026-09-10T02:50+06:00
План: шаг0 (eslint work/**, build, lint, test) → A1..A6 (SP-VID, ≤1.5 ч) → B1..B6 (TPL-01c) → отчёт. Бюджет: 4 ч стенки, денег 0, сети 0.
- `02:50` [0/3] шаг 0 начат — node v25.6.1, pnpm 11.9.0, ffmpeg 7.0.2-static ok
- `02:54` [0/3] шаг 0: eslint `work/**` добавлен, install+build ok, `pnpm lint` — 0 проблем; `pnpm test` идёт фоном
- `03:01` [0/3] шаг 0 готов за 11 мин — `pnpm test` 2575 passed / 3 skipped / 0 failed (2578 всего), 615 с, exit 0. Норма задания «2578 passed/3 skipped» = ТОТ ЖЕ прогон (2578 — всего), расхождений нет
- `03:02` [1/3] ЧАСТЬ A начата — A1 (Chrome, прозрачность), ожидаю ~2 мин
- `03:04` [1/3] A1 готово за 2 мин — pix_fmt `rgba`, альфа=0 на 92.99 % пикселей, кадры `asis` и `clear` ПОБАЙТОВО РАВНЫ: `background:#000` нашего index.html рендерер перебивает своим `!important`
- `03:14` ОШИБКА: A2 первый вариант графа не завершался — `overlay ... eof_action=repeat` при БЕСКОНЕЧНОМ `loop=-1` у графики даёт бесконечный выход (436 МБ за 12 мин, прогон 1 не закончился). Прогон убит; правлю граф: графика конечная (`trim`) + `shortest=1`, стоп-кадр — `loop` вместо `split/trim/concat` (тот буферизовал ~1 ГБ)
- `03:16` [1/3] A2 готово — 4 прогона × 10.7 с, sha256 ОДИН (`f7516dec…`), framemd5 ОДИН, 660 кадров/22.000 с, 20 597 625 Б. Зум `zoompan` стоит +0.4 с на 20 с (6.11 → 6.49) — не он был причиной зависания
- `03:21` [1/3] A3 готово — `zoompan` каноничной формы (`z='min(zoom+шаг,…)'`) НЕ ЗУМИРУЕТ (YDIF 0.00081, 78.5 % кадров без движения); рабочая форма — по `on`. Ровность (σ/среднее): zoompan 0.219, scale+lanczos 0.178, scale+bicubic 0.170, супер×2 0.105. Цена на 5 с: 2.35 / 2.52 / 2.35 / 4.26 с. Найдено: у `crop` w/h считаются ОДИН раз — «crop по n» не зумирует вовсе
- `03:25` [1/3] A4 готово — `prores_ks` и `qtrle` альфу пишут (bbox 400×400 совпал на 150/150 кадрах, два прогона побайтно равны); `libvpx-vp9` альфу МОЛЧА ТЕРЯЕТ (ffprobe `yuv420p`, декод `Requested planes not available`); PNG с альфой — 12 677 Б/кадр
- `03:26` [1/3] A6 готово — duck: soft −1.20 дБ, mid −6.30 дБ, hard −14.70 дБ (LUFS фона под голосом против своей же паузы)
- `03:26` [1/3] A5 начато (ffmpeg 3 стадии × 3 прогона на 60 с + Chrome 1800 кадров) — ожидаю ~10 мин
- `03:37` ОШИБКА: тот же дефект графа в A5 (`eof_action=repeat` + бесконечная графика) — 422 МБ за 11 мин. Убито, исправлено тем же способом, перезапуск
- `03:46` [1/3] A5 готово — ffmpeg на 60 с: только энкодер 307.1 мс/с, overlay 359.9, +zoompan 421.3, +scale-зум 847.1; Chrome 1800 кадров: final 22.6 мс/кадр (678 мс/с), draftHalf 11.5 мс/кадр (346 мс/с). ЧАСТЬ A измерена целиком
- `03:50` [1/3] ЧАСТЬ A ЗАКОНЧЕНА за 48 мин: отчёт `docs/impl/SP-VID/report.md`, приборы в `docs/impl/SP-VID/scripts/`, строка roadmap, долги №254–№256
- `04:15` НАХОДКА (повтор A2): 2 различных sha256 из 4 при ОДНОМ framemd5 — видео побитово равно, разъехался ЗВУК. Разбираю по одному
- `04:19` [1/3] A2-бис: виновник — `sidechaincompress` (2 sha из 8 в одиночку; `amix+aresample` без него 1 из 8; AAC-энкодер 1 из 8, и при `-threads 1` тоже 1). НЕ лечат: `-filter_threads 1` (2 из 8), фиксированная нарезка `asetnsamples=1024` (3 из 8), подача обоих входов из WAV (2 из 8). ЛЕЧИТ явная длина после компрессора (`atrim`): 1 из 8 на звуке и 1 из 4 на полном A2, sha тот же `f7516dec…`
- `04:26` [1/3] A2-бис закончен, N = 32 на вариант: `sidechaincompress` — 4 исхода из 32; без него `amix+aresample` — 1 из 32; AAC-энкодер — 1 из 32. Не лечат `-filter_complex_threads 1` (3), `-threads 1` (3), `atrim` (3), `asetnsamples=1024` (4). Расхождение слышимое: 0.19 % сэмплов, макс |Δ| 6.03 % шкалы. Отчёт SP-VID, roadmap и долг №259 обновлены

**Итог:** гипотеза «(а)» подтверждена по видео и оговорена по звуку. A1 — прозрачность уже
есть и стоит ноль; A2 — видео побитово равно во всех 20 прогонах ночи, а `sidechaincompress`
недетерминирован (A2-бис, 4 исхода из 32); A3 — ровность зума числом (σ/среднее); A4 — два
рабочих альфа-кодека и один молча ломающийся; A5 — ≈1099 мс на секунду ролика на `final`;
A6 — три пресета duck. Строка `03:16` («sha256 ОДИН») исправлена строками `04:15`–`04:26`:
журнал не переписывается задним числом. Детали — `docs/impl/SP-VID/report.md`.
