# PROGRESS — IMPL-H-05 (изоляция рендера: netns, freeze D4, --strict, резолвер браузера) · старт 2026-08-28T09:26:03Z
Якорь ДО чтения (измерено): `unshare` util-linux 2.39.3 `/usr/bin/unshare`, `ip` iproute2-6.1.0 `/usr/sbin/ip`;
`unshare -rn --map-root-user sh -c 'ip link set lo up; …'` → lo UP 127.0.0.1/8, `TZ=UTC LC_ALL=C` ПРОНЕСЕНЫ, exit=0, <1 с.
№160 подтверждён на диске: наш корень `~/.cache/hyperframes/chrome/chrome-headless-shell/linux-152.0.7928.2` (одна установка)
+ чужой `~/.cache/puppeteer/chrome-headless-shell` рядом. HEAD=2e10ecd, node v25.6.1.
- `09:26` [0/N] якорь снят — читаю roadmap H-05, ADR-0008/0007, invariants R1/D4, спайки sp3c/sp3d, код renderer-hyperframes
- `09:36` [0/5] чтение закончено (roadmap H-05, ADR-0008/0007 §4, invariants R1/D4/R12/R14, sp3c §4 + raw, DEBTS 156–163, run/argv/materialize/proctree/contract, runtime.js, gsap.min.js, hyperframes@0.8.5 dist/cli.js). ИЗМЕРЕНИЯ ДО ПЛАНА:
    * M1 `--strict` СУЩЕСТВУЕТ у `render` («Fail render on lint errors»), есть и `--strict-all`. При `--quiet` текст отказа НЕ печатается (`presentRenderLintAbort` → `if (plan.effectiveQuiet) return`) — код выхода мерить отдельно.
    * M2 (№160, причина найдена в коде CLI) У РЕНДЕРЕРА ДВА РАЗНЫХ ПОРЯДКА РЕЗОЛВА: команда `browser path` → `findFromCache()` = puppeteer-кэш ПЕРВЫМ (`dist/cli.js:114408`), а фактический запуск → `resolveHeadlessShellPath()` = env `PRODUCER_HEADLESS_SHELL_PATH`, затем env `HYPERFRAMES_BROWSER_PATH`, затем hyperframes-кэш, и лишь потом puppeteer (`dist/cli.js:65272`). Подтверждение в СЫРЫХ данных спайка: `sp3c/.../network-isolation.json` — `versions.chromeHeadlessShell = 152.0.7977.42`, а в логе того же прогона `[BrowserManager] Browser launched (HeadlessChrome/152.0.7928.2…)`. То есть preflight/отпечаток мерил ОДИН бинарь, а рендерил ДРУГОЙ. Законный рычаг есть: `HYPERFRAMES_BROWSER_PATH`.
    * M3 раскладка нашего корня: `~/.cache/hyperframes/chrome/chrome-headless-shell/linux-<ver>/chrome-headless-shell-linux64/chrome-headless-shell` (одна установка, 262 МБ); чужая — `~/.cache/puppeteer/chrome-headless-shell/linux-152.0.7977.42/…` (261 МБ).
    * M4 (D4, меняет размещение guard'а) GSAP ЗАХВАТЫВАЕТ `Date.now` НА ЗАГРУЗКЕ: `It=(O=Date.now,…)` в `gsap.min.js`, тикер зовёт `O()` сразу. Guard, поставленный ДО gsap, убивает композицию на загрузке. `performance.now`/`Intl`/`toLocaleString`/`localeCompare` у gsap — 0 вхождений, `Math.random` — только в opt-in `utils.shuffle/random`. Инжектируемый рантайм САМОГО HyperFrames (`RUNTIME_IIFE`, 385 КБ): `Date.now`×8, `performance.now`×1, `Math.random`×2, `Intl.`×3 — риск, что бросок заденет его; проверяется только прогоном.
    * M5 (ulimit RSS) `prlimit --as` — НЕ потолок RSS: `--as=256M` убивает node на инициализации V8 (`Fatal process out of memory: SegmentedTable::InitializeTable`), `--as=64M` — segfault, при копеечном фактическом RSS. `ulimit -m` (RLIMIT_RSS) ядром Linux не обеспечивается (`unlimited`, 7.0.0-30-generic). Реальный потолок только cgroup v2 (`systemd-run --user --scope -p MemoryMax=`; контроллер `memory` есть) — это зависимость от systemd-сессии и SIGKILL вместо диагностики.
    * M6 поля `isolation`/лимита памяти в `executionProfile` НЕТ (`schema/src/profiles/render-profile.ts`: `workers`, `chapterParallelism`, `segmentTimeoutMs` — `.strict()`), а `@vpe/schema` — закрытая зона задания ⇒ вход изоляции идёт в `RenderOptions` (§4 п. 1 задания).
План (5 шагов):
  1. `src/browser.ts` — детерминированный резолвер по НАШЕМУ корню (0 → preflight-ошибка, 1 → путь, ≥2 → отказ списком); `run.ts`/отпечаток зовут его; `renderEnv` пришпиливает `HYPERFRAMES_BROWSER_PATH` и гасит `PRODUCER_HEADLESS_SHELL_PATH`. Тесты на подставном `HOME` (№160/№162).
  2. `src/isolation.ts` — `netnsCommand(argv, env)` (`unshare -rn --map-root-user sh -c 'ip link set lo up; exec …'`, env явным проносом), preflight `unshare`/`ip`; `RenderOptions.isolation`; свой TMPDIR внутри `request.tmpDir`; обёртка только вокруг ЗАПУСКА CLI.
  3. `composition/freeze.js` + встраивание (место — по ответу на вопрос 2); `bundle.hash` меняется, старое/новое в отчёт; тесты в `node:vm`.
  4. `--strict` безусловно в `renderArgs`; измерить быстрый отказ кривой композиции (и что печатается при `--quiet`).
  5. Браузерные прогоны на этой машине: R1 (внешний URL в netns → падение; mp4 в netns == mp4 вне по sha256), `Math.random` в шаблоне → бросок, `--strict` → быстрый отказ; протокол нарушений; отчёт/инварианты/долги.
- `09:36` ЖДУ ВЛАДЕЛЬЦА: три вопроса §4 (дефолт изоляции; размещение freeze с учётом M4; ulimit RSS с учётом M5).
- `09:52` РЕШЕНИЯ ВЛАДЕЛЬЦА: (1) дефолт `netns`, нет `unshare`/`ip` — отказ preflight'а, выключение только явным `isolation:'none'`; долг адресом «`none` на macOS». (2) freeze — вариант (а) «после GSAP, до шаблонов» с ИЗМЕРЕННЫМ откатом на (б); при откате взведение обязано покрывать и МОДУЛЬНЫЙ код шаблона (регистрация в `__VPE_TEMPLATES`), не только `mount`/колбэки; размещение и временная дыра пишутся ДОСЛОВНО в клетку D4, не только в отчёт. (3) потолка памяти нет, долг сужается измерением дословно + адресом; кандидат в правку roadmap §4.9 `H-05` («ulimit RSS» механизмом не существует). П1: сверка контура тремя точками (отпечаток == env запуска == версия из `[BrowserManager] Browser launched`), кандидат в ADR-0006 §3 «preflight-канал `browser path` лжив». П2: при отказе `--strict` текст линт-ошибок обязан дойти до автора — «exit 1 без объяснения» отказом не считается.
- `09:53` [1/5] начато: измерение `--strict` + `--quiet` на заведомо кривой композиции (браузер не нужен — линт статический)
- `09:58` [1/5] измерено (П2, браузер не нужен): `render --strict --quiet` на кривой разметке → **exit 1 за 3.0 с, 0 байт объяснения** (`presentRenderLintAbort` глотает текст при `--quiet`); `render --strict` без `--quiet` → exit 1 за 2.1 с, 811 байт с тремя `✗` и «Aborting render due to lint issues (--strict mode)»; `hyperframes lint <dir>` → exit 1 за 2.3 с, тот же текст, статически, без браузера. РЕШЕНИЕ: `--quiet` остаётся (он слагаемое `engineFingerprint`, снятие сменило бы отпечаток), а на ненулевом коде адаптер зовёт `lint` В ТОЙ ЖЕ изоляции и кладёт его текст в отказ — цена платится только на падении.
- `09:53` [2/5] код готов (`browser.ts`, `isolation.ts`, `composition/freeze.js`, правки `argv/run/materialize/runtime.js/contract/errors/fingerprint/index`), `tsc --noEmit` чист; `argv.test.ts` — 19 зелёных, голден-вектор принял `--strict`. Иду в новые тесты (браузер не нужен).
- `09:59` [3/5] ИЗМЕРЕНО, ПЕРЕХОД (а)→(б) ПО ФАКТУ: guard, поставленный после GSAP но глобально, СЛОМАЛ рантайм самого HyperFrames. Прогон `subprocess.test.ts` (настоящий браузер): `[Browser:PAGEERROR] D4 (ADR-0007 §4): `Date.now` в композиции запрещён — код композиции вне клипа`, следом `[FrameCapture] window.__hf not ready after 45000ms … State: __hf=true, seek=false, player=false, renderReady=true, duration=undefined` — то есть инжектируемый рантайм рендерера читает часы на своей инициализации, и без них не выставляет `seek`/`duration`. Рендер упал за 46.8 с вместо ~5 с. Это ровно риск, названный в вопросе 2 (`Date.now`×8 в `RUNTIME_IIFE`). Перехожу на вариант (б) владельца: guard установлен глобально, но БРОСАЕТ только во ВЗВЕДЁННОМ окне; взвод покрывает (1) модульный код шаблонов — построение `__VPE_TEMPLATES`, (2) вызов `mount`, (3) колбэки таймлайна, переданные шаблоном.
- `10:20` [4/5] браузерные прогоны на этой машине (все ИЗМЕРЕНЫ):
    * R1 негативная фикстура: композиция обратилась по адресу ВНЕ namespace ⇒ рендер ПАДАЕТ (`ADR-0008 композиция`, `NetworkError … Failed to load http://127.0.0.1:<порт>/probe.txt`), 46.7 с. ТОТ ЖЕ шаблон с `isolation:'none'` ⇒ проходит — то есть падение про namespace, а не про шаблон.
    * По дороге найдено и закрыто: исключение в композиции рендерер отказом НЕ СЧИТАЕТ — прогон завершался кодом 0 и отдавал ровно N ЧЁРНЫХ кадров. Добавлен охранник `pageErrorsOf` (`[Browser:PAGEERROR]` из лога рендерера) — без него оба критерия готовности были бы недостижимы.
    * Ложный контроль пойман и снят: без `access-control-allow-origin` браузер блокировал кросс-origin XHR САМ, и негативный контроль краснел одинаково в обоих режимах, доказывая работу CORS, а не изоляции.
    * R1 побайтово: mp4 из netns и вне него — равные `sha256` и `framemd5Sha256` (`solid@1`, 6 кадров, профиль `ac4`).
    * D4: динамическая форма (`window['Ma'+'th']['ran'+'dom']`) — отказ РАНТАЙМ-ГУАРДА с именем API и адресом клипа (`randomizer@1`, `r:aaaa0001`); открытая форма — отказ `--strict` статически, до браузера (`non_deterministic_code`).
    * №160 контур: резолвер → `~/.cache/hyperframes/…/linux-152.0.7928.2/…`; `hyperframes browser path` → `~/.cache/puppeteer/…/linux-152.0.7977.42/…` (ЧУЖАЯ, канал лжив); отпечаток `chrome` → `Google Chrome for Testing 152.0.7928.2`; запущено `HeadlessChrome/152.0.7928.2`. Три точки сошлись на пришпиленной.
    * `bundle.hash` фикстуры: без guard`а `e30e511d…901d` → с guard`ом `89a65c8a…142f`; `index.html` 24186 байт, из них guard 10315.
    * `engineFingerprint`: до `--strict` `a48cdce5…ea89` → после `61db2ca8…63a0`; `launch.args` = `render -o --format png-sequence --quiet --strict`.
- `10:31` [5/5] измерения для отчёта сняты: цена изоляции — 2.397 с в namespace против 2.726 с вне (30 кадров, тот же каталог, sha256 первого PNG совпал побайтово). Протокол нарушений: шесть нарушений (Н1 lo не поднят, Н2 env не пронесён, Н3 заморозка подменяет константой, Н4 `--strict` снят, Н5 резолвер вернулся к `browser path`, Н6 охранник PAGEERROR снят) — все шесть краснят целевые тесты; Н5 воспроизвёл №160 живьём («резолвер выбрал …7928.2…, а запустился 152.0.7977.42»), Н6 показал зелёный-по-неверному (N чёрных кадров с кодом 0).
- `10:34` полный прогон репозитория: 2101 тест, 2 красных — ОБА мои же охранники поймали мои изменения: `d4-clock-boundary` (форма часов уехала в строковый литерал `materialize.ts`) и `fingerprint-live` (голден строки запуска не знал про `--strict`). Оба поправлены по существу, а не ослаблены. `pnpm lint` — чист после трёх правок (`catch {}` без переменной, стрелка вместо алиаса `this`, лишний eslint-disable).
- `10:40` документы: `docs/impl/H-05/{report.md, violation-transcript.txt}`; `docs/DEBTS.md` — №2/157/160/162 закрыты, №161 сужен, новые №164–167; `docs/invariants.md` — R1 `measured → guarded`, D4 `named → guarded`, счёт 60 → 62, обе клетки несут ДОСЛОВНО область действия и непокрытое.
- `10:37` ФИНАЛЬНЫЙ ПОЛНЫЙ ПРОГОН: **129 файлов, 2101 тест — 2099 зелёных, 2 пропущенных, 0 красных**; `pnpm lint` чист; `tsc --noEmit` чист.
**Итог:** R1 и D4 переведены в `guarded` (счёт 60 → 62) — изоляция исполнена ОС (netns по умолчанию, preflight с пробой, негативная фикстура падает, mp4 в/вне namespace побайтово равны), заморозка глобалей бросает во взведённом окне (вариант «б» — по измерению, безусловный бросок ломает рантайм рендерера). `--strict` включён с доставкой текста линта; выбор браузера детерминирован и сверен контуром из трёх точек. Два измерения переписали замысел: №2 (вариант «а» невозможен) и находка «исключение композиции — не отказ», без закрытия которой оба критерия готовности недостижимы. Долги №2/157/160/162 закрыты, №161 сужен, новые №164–167. Потолка памяти нет — механизма не существует (измерено).
Детали — docs/impl/H-05/report.md, violation-transcript.txt.
- `10:57` sha256 обоих mp4 сняты значениями для отчёта: `280dcc1d…6c95` в namespace и вне него (одно и то же), `framemd5Sha256` `46cc2107…3118`. Печать `console.log` из тестов убрана: вывод vitest её не показывает, а «печать», которой не видно, — мусор, а не измерение; вместо неё в тесте №160 теперь УТВЕРЖДЕНИЕ, что запустился бинарь НАШЕГО резолвера, а не тот, который назвал лживый канал `browser path`.
- `10:46` КОНТРОЛЬНЫЙ полный прогон после всех правок: **129 файлов, 2101 тест — 2099 зелёных, 2 пропущенных, 0 красных**; `pnpm lint` чист.

# PROGRESS — H-05fix (правка приёмки) · старт 2026-08-28T11:0x UTC
- `11:0x` дефект ВОСПРОИЗВЕДЁН локально прогоном с подставным пустым `HOME`: `fingerprint-live.test.ts` — 1 красный из 10, `expected 'в $HOME/.cache/hyperframes/chrome/…' to contain 'browser path'`. Ветка `absent` исполняется только там, где браузера нет, — на машине с Chrome берётся `else`, и расхождение невидимо.
- `11:0x` причина в `fingerprint.ts` дополнена третьим утверждением (почему чужой puppeteer-кэш не годится); ассерт перецелен на три подстроки — `preflight`, `не является браузером`, `$HOME/.cache/hyperframes` — то есть не ослаблен, а усилен.
- `11:0x` греп по `test/**`: ещё три места со старой формулировкой резолвера, ни одно не падало — фикстура причины в `fingerprint.test.ts`, роль теста-«находки» в `fingerprint-browser.test.ts` (после `H-05` это охранник исправления, а не свидетельство дефекта), счёт подпроцессов пробы в обосновании таймаута (было «четыре, включая `browser path`», стало три).
- `11:0x` проверено ОБЕИМИ ветками: пустой `HOME` — 10/10; настоящий `HOME` — 28/28 по двум файлам отпечатка.
- `11:07` полный прогон после фикса: **129 файлов, 2101 тест — 2099 зелёных, 2 пропущенных, 0 красных**; `pnpm lint` и `tsc --noEmit` чисты.
