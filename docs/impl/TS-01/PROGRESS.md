# PROGRESS — IMPL-TS-01 (templates-spec: контракт шаблона, манифест, реестр) · старт 2026-08-27T21:30:54+06:00
План: пока не утверждён — сначала чтение (Charter, 00-PROCESS, roadmap TS-01/TS-02/E-00/H-01, ADR-0008/0007/0006/0001/0009, invariants R3/R12/R14/K6, код core-model/schema/fixtures, DEBTS ×17), затем план 3–7 шагов ниже черты и ожидание «ок». Бюджет: стоп-правило 90 минут без зелёного теста.
- `21:30` [0/N] якорь измерен `date -Iseconds` ДО чтения — 2026-08-27T21:30:54+06:00
- `21:31` [0/N] чтение начато: Charter (§4 V1–V13, §7, §8), 00-PROCESS
- `21:40` [0/N] прочитано: roadmap §3 (`TS-01`/`TS-02`/`E-00`/`H-01`/`H-04`/`E-01…07`) + §4.8 `TS-01`/`TS-02`; ADR-0008 («Контракт», «Декларация ресурсов», «Оспариваю V3», «Гейт» Процедура+Классы+Область, «Бюджет AC2», таблица кадров/с); ADR-0007 §1/§3; ADR-0006 §3/§4; invariants R3/R12/R14/D5/K6/M6 + шапка (счёт 56); DEBTS — 16 строк с адресом `TS-01` (+№142 чужая)
- `21:45` [0/N] код прочитан: `core-model/model/entities.ts` (`TemplateCall`, `TemplateParams`, `DirectionRecordBase`), `direction.ts` (`assertNoGridPoint` — скан, долг №35), `render-ir.ts` (`IrAssetRef.role`, `fonts: never[]`), `schema/types/brands.ts`, `schema/families/compile-profile.ts`, `fixtures/minimal/direction/01-intro.yaml` (5 вызовов), `aliases.yaml`, запись шрифта, `packages/templates-spec/*` (пустой: `src/index.ts` = `export {}`), `packages/voice/tsconfig.json` (образец `rootDir: '.'`), `tests/boundaries/adr0009-graph.test.ts`
- `21:46` ИЗМЕРЕНО: `pnpm why zod` → `zod@4.4.3`, ровно одна версия, владелец — `@vpe/schema`. Вторая версия в лок не приедет, если поставить `4.4.3`
- `21:46` ИЗМЕРЕНО: `node` вне PATH сессии; рабочий бинарь — `~/.nvm/versions/node/v25.6.1/bin` (v25.6.1, pnpm 11.9.0 — совпадает с `engines`/`packageManager`)
- `21:52` [0/N] чтение закончено. План ниже черты — ЖДУ ВЛАДЕЛЬЦА («ок»)

---

## План (6 шагов), стоп-правило 90 минут без зелёного теста

1. **Каркас пакета.** `zod@4.4.3` (та же версия, что у `@vpe/schema`) в
   `packages/templates-spec/package.json`; `rootDir: '.'` + `include: test/**` по образцу
   `voice` (долг №6 — тот же, новая строка не заводится); `pnpm install --offline`;
   `pnpm build`. Контроль: `pnpm why zod` обязан остаться «Found 1 version».
2. **Ядро контракта** — `src/`: `errors.ts`, `name.ts` (грамматика имени + `local:`),
   `refs.ts` (`AssetRef {alias, role}`, `FontRef {role, family?}`), `manifest.ts` (zod-схема
   манифеста, `GateRecord`, N=10/3, `determinismClassOf`), `spec.ts` (`TemplateSpec`,
   `requestFiles` — вход R3), `registry.ts` (`createRegistry`, `TEMPLATE_REGISTRY_VERSION`),
   `gate.ts` (`assertBuildMayStart` — вход R12), `index.ts`.
3. **Пять спеков фикстуры** `src/templates/{kenburns,still,flash,captionEmphasis,bed}@1.ts`
   + `templates/index.ts`.
4. **Тесты** `packages/templates-spec/test/*` (грамматика, пять схем × фикстура, реестр,
   гейт, чистота `declare*`) + `tests/boundaries/templates-spec-imports.test.ts`;
   прогон `pnpm typecheck` / `pnpm lint` / `pnpm test`.
5. **Протокол нарушений** — пять падающих (спек без манифеста; гейт по `profileId` без
   сверки отпечатка; `N = 5` на `final`; `gridPoint` через схему `still@1`; `declareAssets`
   читает `fs`), откат `cp`-резервом. Измерение `assertBuildMayStart` на фикстуре — дословно.
6. **Доки и сдача** — `docs/impl/TS-01/{report.md, PROGRESS.md, violation-transcript.txt}`;
   пометки R3/R12 в `invariants.md` (счёт 56 не меняется); семнадцать долгов со статусами;
   новые с №145; кандидаты правок roadmap (§3 `CP-06`, §4.8 `determinismClass`) и ADR
   (0008 — alias-форма `AssetRef`; 0007 §1 — `purposes` в манифесте) — в отчёт, ADR не править.

## Вопросы владельцу (шесть по заданию + шесть находок чтения)

1. Манифест — **данные пакета** (рекомендую «а») или семейство `template-manifest/1`.
2. Вызов контракта из компилятора — новая задача **`CP-06`** (кандидат в roadmap §3).
3. `determinismClass` — **производное** от `gates` (`worst`, без записей `UNGATED`), в данных
   не хранится; кандидат в правку roadmap §4.8.
4. `AssetRef` — **alias-форма**, в `core-model` тип не заводится (№120 фиксируется, не устраняется).
5. `kenburns@1.declareAssets` → **пусто**, `declaredAssets: []`.
6. `engineFingerprint` записи гейта — **строка целиком**, сравнение строгим равенством.

Находки чтения, которые входят в те же ответы:
7. `local:` ⟺ `forkedFrom` присутствует (ADR-0008: локальный шаблон и есть форк) — отдельного
   поля `namespace` в манифесте НЕ завожу.
8. Роль ассета = **имя параметра**, держащего ассет (ADR-0002 §4; так уже в
   `compile-ir.ts:79` — `role: 'asset'`) ⇒ №138 закрывается без смены `segmentIrHash`.
9. Ни один из пяти шаблонов фикстуры случайности не требует ⇒ `purposes: []` у всех; это
   ИЗМЕРЕНИЕ, сужающее №135 (при подключении контракта `seeds` станет ПУСТЫМ, а не сменит ключ).
10. `bed@1` кадров не рисует (аудио-домен), а реестр требует `msPerFrameBudget > 0` — число
    будет фикцией; новый долг с адресом `E-00`/`X-02`.
11. `bed@1.inPoint.asset` схемой обязан равняться `params.asset` — иначе пришлось бы выдумать
    вторую роль ассета.
12. Класс результата гейта пишу дословно по ADR-0008: `PASS` / `FAIL` / `FLAKY-по-контейнеру`.

- `21:38` **ПОПРАВКА К ЖУРНАЛУ (append-only, старые строки не стёрты).** Отметки
  `21:31`/`21:40`/`21:45`/`21:46`/`21:52` выше — **НЕ измерены**, они проставлены на глаз.
  Измеренных якорей на отрезке чтения ровно два: старт `2026-08-27T21:30:54+06:00` и
  `2026-08-27T21:38:35+06:00` (момент записи плана). То есть всё чтение уложилось в **7 м 41 с**,
  а не в 22 минуты, как читались те отметки. Дальше каждая строка получает `date -Iseconds`.
- `21:38` ЖДУ ВЛАДЕЛЬЦА: «ок» на план и на двенадцать ответов выше.
- `21:43` ОК ВЛАДЕЛЬЦА получен (2026-08-27T21:43:53+06:00). Поправки: П1 — `msPerFrameBudget: z.int().nonnegative()`, `bed@1` = 0, правило «> 0» уезжает в `CP-06`/`E-00`; долг с точной формулировкой. П2 — тест сверки `TEMPLATE_REGISTRY_VERSION` с `fixtures/minimal/profiles/compile.yaml` регуляркой. Находка 9 — строка в отчёт про тест D1 из `CP-04`. Начинаю шаг 1/6
- `21:56` [1/6] готово: `zod@4.4.3`, `rootDir: '.'`, `pnpm install --offline` за 2.1 с; лок +3 строки, `pnpm why zod` — одна версия, два владельца
- `21:56` [2-3/6] готово: 11 модулей `src/` (errors, name, refs, params, manifest, spec, registry, gate, index + 5 спеков + templates/index). `pnpm typecheck` ЗЕЛЁНЫЙ с первого прогона (2026-08-27T21:56:16+06:00)
- `21:56` НАЙДЕНО (измерение, не догадка): порождённая `[img:]`-запись даёт `params: { asset }` БЕЗ `fit` (`core-model/src/anchors/img.ts:86`), а фикстура — с `fit: cover`. Значит `fit` у `still@1` ОБЯЗАН быть необязательным: сделай его обязательным — и восемь записей из восьми на ролике AC1 не прошли бы собственный контракт
- `21:56` [4/6] начато: тесты пакета + `tests/boundaries`
- `22:03` [4/6] готово (2026-08-27T22:03:47+06:00): 6 файлов тестов пакета (152 теста) + `tests/boundaries/templates-spec-imports.test.ts` (5). `pnpm typecheck` зелёный, `pnpm lint` зелёный, `pnpm test` — **110 файлов / 1869 тестов, 99.54 с**
- `22:03` ЛИНТ ПОЙМАЛ СВОЁ: `JSON.stringify` в `gate.ts` (ADR-0007 §3 / `S-01`) — заменён сборкой имени из полей; в тесте — на `structuredClone`
- `22:03` [5/6] начато: протокол нарушений (пять падающих)
- `22:08` [5/6] готово (2026-08-27T22:08:08+06:00): протокол — шесть прогонов (Н1, Н1b, Н2…Н5), все откачены `cp`-резервом, контроль зелёный (1869 тестов, 99.64 с)
- `22:08` ИЗМЕРЕНО (Н1): снятие ПЕРВОГО эшелона «манифеста нет» шаблон не пропускает — ловит второй (`safeParse(undefined)`); первый эшелон стережёт ТЕКСТ отказа, не отказ. Н1b со снятыми обоими: спек регистрируется, 5 тестов красных
- `22:08` ИЗМЕРЕНО (Н5): `declareAssets` с `fs` — тест «два вызова равны» ЗЕЛЁНЫЙ (24/24), греп-охранник КРАСНЫЙ. Чистоту стережёт греп, а не равенство результатов
- `22:08` [6/6] начато: доки
- `22:16` [6/6] доки: `docs/impl/TS-01/report.md` (392 строки), `violation-transcript.txt`; `invariants.md` — ревизия + пометки R3/R12/K6/D5, счёт 56 БЕЗ изменений; `DEBTS.md` — 16 строк обновлены, 7 новых (№145–151), 144 → 151
**Итог:** `templates-spec` получил контракт шаблона, манифест и реестр; закрыты долги №37 и №138 (последний — БЕЗ смены `segmentIrHash`), №35 сужен адресом `CP-06`, шесть долгов сужены с адресом `CP-06`, №36 → `L-03`, №48 → `H-01`, №92/№128 переадресованы, №13/№54/№125 остаются владельцу. Входы R3/R12 написаны и покрыты, вызывающих нет — счёт `guarded` 56 не меняется. На фикстуре `assertBuildMayStart` ПАДАЕТ на всех пяти шаблонах, и это критерий. Детали — `docs/impl/TS-01/report.md`.
