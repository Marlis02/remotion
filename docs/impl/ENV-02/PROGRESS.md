# PROGRESS — ENV-02 образ воспроизводимой среды канала (Docker) · старт 2026-09-08
План: заход 1 — чтение (§1) + измерения (§2) + план и вопросы; заход 2 — Dockerfile, обёртка, прогон, документы.
- `23:19` [1/N] чтение §1 идёт. **НАХОДКА-БЛОКЕР:** на хосте НЕТ ни одного контейнерного рантайма — `docker`, `podman`, `nerdctl`, `buildah`, `lxc`, `apptainer` не найдены; `/var/run/docker.sock` нет; в snap-списке docker/lxd нет. Пользователь в группе `lxd`, но lxd не установлен. `unshare -rm` РАБОТАЕТ (userns разрешены: `unprivileged_userns_clone=1`, apparmor-ограничение 0). Значит собрать и ПРОГНАТЬ образ на этой машине нечем; §5 (измерения в образе) без установки рантайма неисполним. Вопрос владельцу — в блоке захода 1
- `23:23` [1/N] ИЗМЕРЕНО (§2.2) проба хоста скриптом вне репо: отпечаток `3217a4cfd040e68b51c4ac395c8cc16596394df19221a0e09ca58262463cb95c` — РАВЕН эталону `invariants.md`. Полей 11 + `probeVersion`; `pkg.*` — ДВА (`gsap 3.15.0`, `hyperframes 0.8.5`), не пять: `fingerprintedPackages` читает `dependencies` рендерера, а там ровно две не-`@vpe` записи
- `23:24` [1/N] ИЗМЕРЕНО (§2.3) шрифт `build-fixture.ts:31` = `/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf`, sha256 `d1c3ff99f1e1ce1827a33efd4dad81f40babda06bff9e43bd7591c86662a287b`, 705684 б, пакет `fonts-dejavu-core 2.37-2build1` (jammy). Хост — Ubuntu 22.04.5 LTS
- `23:26` [1/N] ИЗМЕРЕНО ffmpeg: 7.0.2 в `old-releases` НЕТ (там максимум 6.0.1); текущий `releases/ffmpeg-release-amd64-static.tar.xz` — это и есть **7.0.2** (release-readme, last-modified 2024-08-24), sha256 архива `abda8d77ce8309141f83ab8edf0596834087c52467f6badf376a6a2a4c87cf67`, 41888096 б. Бинари из архива **побайтово равны** `/usr/local/bin/ffmpeg`/`ffprobe` хоста (`cmp` чист, sha `e7e7fb30…`/`4f231a19…`)
- `23:31` [1/N] ИЗМЕРЕНО остальное для плана: Node `v25.6.1` linux-x64 tar.xz sha256 `97eec07e2a43c5a39e4968c1ae554461783914d872c27d856e98d2751094f5be` (29695012 б); `CHROME_VERSION = "152.0.7928.2"` пришпилен ВНУТРИ `hyperframes@0.8.5` (`dist/cli.js`) ⇒ `browser ensure` в образе возьмёт ту же; `fonts-dejavu-core=2.37-2build1` из пула jammy даёт `DejaVuSans-Bold.ttf` sha `d1c3ff99…287b`, 705684 б — ПОБАЙТОВО тот же файл, что у хоста; список системных библиотек снят `ldd` с самого `chrome-headless-shell` и отображён `dpkg -S` (44 пакета транзитивно, верхних ~20); максимум долга = **241**
- `23:31` НАХОДКА (правит посылку задания, решение НЕ меняет): `hyperframes@0.8.5` запускает Chrome с `--no-sandbox --disable-setuid-sandbox --disable-dev-shm-usage` БЕЗУСЛОВНО, хардкодом в `dist/cli.js`. Значит «под root Chrome требует `--no-sandbox`» здесь неверно — флаг стоит всегда, и на ноуте тоже; и `launch.args` отпечатка — это аргументы CLI `hyperframes`, а не Chrome, они замороженная константа `FIXED_RENDER_ARGS`. Не-root остаётся, но по ДРУГОЙ причине: владение файлами в примонтированном дереве + `git` откажется читать репозиторий чужого uid
- `23:31` НАХОДКА-2: рендер по умолчанию заворачивается в `unshare -rn --map-root-user` (`isolation.ts`, R1 `guarded`), и preflight ПРОБУЕТ право, а не наличие файла. В образе нужны `util-linux` + `iproute2`, а контейнер обязан пускать `CLONE_NEWUSER` (штатный seccomp-профиль Docker его запрещает) — иначе рендер откажет на preflight
- `23:32` [1/N] ИЗМЕРЕНО (§2.1) норма хоста: `pnpm install --frozen-lockfile` 1.26 с («Already up to date»), `pnpm -r build` 1.74 с (инкрементально), `pnpm test` — **169 файлов passed | 1 skipped (170); 2521 passed | 3 skipped (2524); 0 failed; 642.36 с**. Красных НЕТ. Единственный пропуск — `packages/voice/test/live-elevenlabs.test.ts` (живой ElevenLabs по `ELEVENLABS_LIVE`), то есть законный
- `23:32` ЖДУ ВЛАДЕЛЬЦА: план захода 2 + вопросы (главный — контейнерного рантайма на машине нет)
- `23:33` ОК ВЛАДЕЛЬЦА: В1 — вариант (А), Docker поставлен; В2 — `seccomp=unconfined` + `--network none`, долг №242 на точечный профиль; В3 — принято, `pnpm -r build` перед КАЖДОЙ командой; В4 — `docker/Dockerfile`; В5 — ai-test-1 на оплаченных дублях, `--allow-tts` не давать. Поправки П1/П2 — в отчёт, решений не меняют. Пере-гейт при несовпадении — стоп. Бюджет 90 мин
- `23:33` [1/7] начато: проверка рантайма и каркас Dockerfile
- `23:42` [1/7] `docker/Dockerfile` + `docker/Dockerfile.dockerignore` написаны; ПЕРВАЯ сборка образа начата — ожидаю ~5-10 мин (Chrome 263 МБ + Node 30 МБ + ffmpeg 42 МБ + apt)
- `23:44` [1/7] ОШИБКА: buildx не установлен, работает легаси-билдер — `--progress` не поддержан, `Dockerfile.dockerignore` не читается. Исправлено НЕ корневым `.dockerignore`, а минимальным контекстом: обёртка собирает временный каталог из ОДНОГО файла (манифест рендерера). В образ по построению не может попасть ничего сверх перечисленного
- `23:45` [2/7] обёртка `scripts/vpe-docker` + `docker/probe.mjs` написаны; ПЕРВАЯ сборка образа начата заново
- `23:46` [2/7] ОШИБКА сборки образа: `node: error while loading shared libraries: libatomic.so.1` — Node 25 на jammy требует `libatomic1`, в базовом `ubuntu:22.04` его нет. Добавлен в apt-список, пересборка
- `23:47` [2/7] ОШИБКА-2: `corepack: not found` — в дистрибутиве Node 25 корепака больше нет (выведен из комплекта). Ставится явно `npm install -g corepack@latest`; `npm i -g pnpm` отвергнут — он поставил бы pnpm МИМО `packageManager`. Пересборка
- `23:49` [3/7] образ дошёл до `hyperframes browser ensure` — качает Chrome Headless Shell **v152.0.7928.2** (114.8 МБ), версия та самая. Параллельно начата ЭТАЛОННАЯ сборка `ai-test-1` на хосте в `work/env-02/build-host` (тайминг хоста снят при частичном наложении на сетевую фазу сборки образа — сказано в отчёте)
- `23:51` [7/7] в `docs/DEBTS.md` внесены новые долги №242 (точечный seccomp-профиль вместо `unconfined` — заказан владельцем), №243 (ffmpeg 7.0.2 только по подвижной ссылке, нужен зеркало-архив), №244 (R14 говорит «пять версий», полей `pkg.*` два — поправка П2). Статусы №224/№225 — после измерения пробы в образе
- `23:53` [3/7] ЭТАЛОН ХОСТА готов: `vpe build ai-test-1 --profile final` за **4 мин 00.96 с** (3 сегмента 476+392+251), `final.mp4` 40130228 б, sha256 `83799253fa9a25299bbf8bbd0275c07ba12c8b5c462719e10908bb807c450db4` — **побайтово равен** сборке владельца от 2026-09-01, лежащей в `examples/ai-test-1/build/`. То есть хост воспроизводит сам себя через неделю
- `23:54` [3/7] ОШИБКА-3: `hyperframes browser ensure` в сборке образа ВИСНЕТ — скачивание доходит до 100 % (114.8 МБ), после чего процесс за 5 минут тратит 4 с CPU, каталог `linux-152.0.7928.2` остаётся ПУСТ. Сборка прервана, диагностирую отдельным контейнером
- `23:55` [3/7] ПРИЧИНА НАЙДЕНА диагностическим контейнером: `Extraction failed: no zip archiver is available. Install unzip`. В образе не было `unzip`; спиннер продолжал крутиться после отказа — отсюда вид зависания. Отдельно замечено: сам инструмент предлагает обход `HYPERFRAMES_BROWSER_PATH=/usr/bin/google-chrome` («any Chrome build works») — ровно подмену растеризатора, запрещённую долгом №160. `unzip` добавлен, пересборка
- `00:01` [3/7] ОБРАЗ СОБРАН: `vpe-env:env-02`, **1.74 ГБ**, сборка **3 мин 18.04 с** (apt-слой пересобирался из-за `unzip`; базовый `ubuntu:22.04` был в кэше). Chrome распакован — «Ready to render», каталог один: `linux-152.0.7928.2`
- `00:01` [4/7] начато: prepare + проба в образе
- `00:02` [4/7] ОШИБКА-4: `pnpm install` в контейнере отказал — `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`: в `node_modules/.modules.yaml` записан `storeDir` ХОСТА, контейнерный другой, и pnpm хочет снести каталог (1.3 ГБ) и скачать заново; следующий `pnpm install` на хосте захотел бы снести обратно. Установка сделана ПО УСЛОВИЮ (есть `node_modules` — не трогать; нет — ставить). Основание: состав дерева задаёт ЛОК, а не установщик; `installedVersion` читает версии из `node_modules/<имя>/package.json` и от машины-установщика не зависит
- `00:06` [4/7] ОШИБКА-5 и её причина: pnpm 11 зовёт `install` перед КАЖДЫМ скриптом (`runDepsStatusCheck`) — ни `--config.verifyDepsBeforeRun=false`, ни переменная окружения проверку не снимают (проверены обе формы). Обёртка переведена на прямые бинари дерева: `node_modules/.bin/tsc --build` и `node_modules/.bin/vitest run`. Подмены нет — корневой скрипт `build` это буквально `cross-env TZ=UTC LC_ALL=C tsc --build`, а `TZ`/`LC_ALL` стоят переменными образа. `prepare` зелёный за 1.93 с
- `00:07` [4/7] **КРИТЕРИЙ ПРИЁМКИ ИСПОЛНЕН: проба в образе даёт `3217a4cfd040e68b51c4ac395c8cc16596394df19221a0e09ca58262463cb95c` — РАВЕН эталону ноута.** Все 11 полей совпали поимённо, включая `chrome 152.0.7928.2`, обе строки ffmpeg/ffprobe 7.0.2-static, `node v25.6.1`, `hostClass local`, `launch.args`/`launch.env`. Пере-гейт не нужен
- `00:07` [5/7] начато: сборка ai-test-1 в образе + гейт still@1 — ожидаю ~5 мин
- `00:09` [5/7] **СБОРКА В ОБРАЗЕ: `final.mp4` ПОБАЙТОВО РАВЕН хостовому и коммитнутому** — sha256 `83799253fa9a25299bbf8bbd0275c07ba12c8b5c462719e10908bb807c450db4`, 40130228 б, `cmp` чист. Время: образ **3 мин 59.81 с**, хост **4 мин 00.96 с**
- `00:09` [5/7] **ГЕЙТ `still@1 draftHalf` В ОБРАЗЕ: PASS · N = 3**, различных framemd5 1, различных sha256 1, 5.33 с. Запись легла во временный каталог; коммитнутые `*.gates.json` не тронуты. Сверка с записью репозитория: `class`, `N`, `bundleHash`, `engineFingerprint`, `framemd5`, `sha256` — ВСЕ ШЕСТЬ РАВНЫ ⇒ **запись действует**
- `00:10` [6/7] начато: норма тестов в образе — ожидаю ~11 мин
- `00:20` [6/7] НОРМА ТЕСТОВ В ОБРАЗЕ (первый прогон): **168 файлов passed | 1 failed | 1 skipped (170); 2520 passed | 1 failed | 3 skipped (2524); 624.55 с**. Красный ОДИН: `tests/lints/v06-secrets-not-in-tree.test.ts` — `EACCES: permission denied, mkdtemp '/vpe-secrets-XXXXXX'`. Причина не в тесте: он кладёт песочницу РЯДОМ с репозиторием (`path.join(ROOT, '..', …)`), а при монтировании в `/repo` родитель — корень контейнера, и не-root в него не пишет. Лечится монтированием на уровень глубже (`/home/vpe/repo`), тест не трогается
- `00:22` [6/7] образ пересобран за **1 мин 05.98 с** (`WORKDIR /home/vpe/repo`), отпечаток после переезда тот же — `3217a4cf…3cb95c`. Перезапуск прогона тестов
- `00:33` [6/7] **НОРМА ТЕСТОВ В ОБРАЗЕ = НОРМА ХОСТА: 169 файлов passed | 1 skipped (170); 2521 passed | 3 skipped (2524); 0 failed; 625.96 с** (хост — 642.36 с). Единственный пропуск тот же законный: `live-elevenlabs.test.ts` по `ELEVENLABS_LIVE`. Красных нет
- `00:33` [7/7] начато: финализация документов
**Итог:** критерий приёмки `ENV-02` в части отпечатка ИСПОЛНЕН измерением: проба в образе поле в поле равна пробе ноута, `engineFingerprint` `3217a4cf…3cb95c` — тот же; пере-гейт не нужен. Сверх того побайтово совпал `final.mp4` (образ = хост = коммит) и совпали все шесть полей записи гейта; норма тестов в образе равна норме ноута (2521/3/0). НЕ сделано: `verify ac4` в образе (≈8 мин сверх бюджета, долг №239), пара гейта `final/N=10`, проверка на ВТОРОЙ машине (долг №245). Детали — docs/impl/ENV-02/report.md.
