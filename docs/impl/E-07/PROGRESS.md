# PROGRESS — IMPL-E-07 grade@1 · старт 2026-08-31T16:57:53+06:00
План: чтение → план владельцу → спек+реализация → гейты/запросы/runbook → демо+кадры → доки. Бюджет: 60 мин без зелёного теста ⇒ стоп.
- `16:57` [0/6] якорь снят, дерево чистое (b14a653)
- `17:0X` [1/6] чтение: roadmap E-07 + решение 11, H-06 report, FIX-01 §184, flash@1 спек/реализация, runtime.js z-порядок, fixture.ts, гейт-тесты, runbook — прочитано
- `17:05` [1/6] preflight: node v25.6.1, pnpm 11.9.0, `pnpm build` чисто, браузер `chrome-headless-shell 152.0.7928.2` на месте (вне часов)
- `17:10` [1/6] ПРОБА ФОРМЫ (вне кода, 3 рендера 1080×1920): `backdrop-filter` РАБОТАЕТ (центр 123/123/123 → 136/130/123); виньетка работает (угол 173/165/156 → 113/108/102); `feTurbulence` рисует и ДВА прогона побайтово равны; `url(#f)` внутри `backdrop-filter` этот Chrome ИГНОРИРУЕТ (байты кадра равны варианту без зерна); `mix-blend-mode` у ребёнка ИЗОЛИРУЕТ host и обнуляет backdrop соседа ⇒ тон обязан стоять на САМОМ host. Цена зерна в байтах: кадр PNG 31 КБ → 2.7 МБ
- `17:12` ЖДУ ВЛАДЕЛЬЦА: план + 4 вопроса выданы
- `17:16` [2/6] «ок» владельца: tmp-каталог записей; названная разница ['grade@1']; z: 25; backdrop-filter на host. Плюс НОВОЕ ТРЕБОВАНИЕ: измерить цену зерна (wallMs сегмента и объём tmp, зерно 0.15 против 0) на `final`; ломает AC2/диск ⇒ дефолт зерна 0 + долг с числом
- `17:16` [2/6] начато: спек + реализация `grade@1`
- `17:26` [2/6] готово: спек + реализация + обе регистрации; `pnpm build` чисто. Проба на настоящей реализации (кадр 1080×1920): тон 123.2/123.2/123.2 → 135.8/129.5/121.2; виньетка угол 171.0 → 112.1; зерно даёт +0.22 к sd (5.18 → 5.40 на чистом зерне) и 31 КБ → 1.1 МБ на кадр; тон под зерном сохраняется (134.3/129.0/121.9). Кроп 1:1 глазами — тёплый тон и мелкое зерно на месте
- `17:26` [3/6] начато: охранники — счётчики и названная разница ['grade@1']
- `17:32` [3/6] ПЕРВЫЙ ЗЕЛЁНЫЙ: `templates-spec` + `templates.test.ts` — 9 файлов, 226 passed. Правлено 4 ожидания счёта (registry, fixture-specs ×2, gate) + двусторонняя сверка с названной разницей ['grade@1'] в двух местах
- `17:38` [3/6] НАЙДЕНО ОТСТУПЛЕНИЕ (вопрос владельцу, код НЕ пишу): `packages/compile/test/template-contract.test.ts` использует имя `grade@1` ЧЕТЫРЬМЯ литералами как «шаблон, которого НЕТ в реестре». Один ассерт красный (`Зарегистрированы`), три зелены по ДРУГОЙ причине (params не проходят строгую схему grade@1) — то есть лгут. `compile` — закрытая зона задания. Правка = четыре строковых литерала на имя, которого в реестре нет
- `17:45` [4/6] готово: гейт `draftHalf` в ОБЕ копии списка (№193); гейт `final` N=10 с зерном вторым `it` в `templates-gate-final.test.ts`; перегенерация запросов — **прежние ВОСЕМЬ файлов побайтово те же** (sha256 до/после совпали, porcelain = 2 новые строки); runbook +2 команды
- `17:45` [4/6] готово: НОВЫЙ БРАУЗЕРНЫЙ ФАЙЛ `test/grade-captions.test.ts` (2 теста) — Н2 зелёный: текст под грейдом R−B −0.07, над грейдом 18.31; грейд красит слой под собой (0.00 → тёплый). Находка: над ПУСТОТОЙ backdrop-filter не красит ничего (фон страницы R−B 0.00 в обоих) — основание смешанного запроса
- `17:46` [5/6] начато: измерение бюджета и цены зерна
- `17:56` [5/6] ИЗМЕРЕНО (60 кадров, 5 повторов, final 1080×1920, шум 0.2102 мс/кадр):
    `grade@1` с зерном 0.15 — **545.44 мс/кадр** (бюджет по д1 = 546); без зерна — **133.97 мс/кадр** (134)
    ЦЕНА ЗЕРНА: wallMs ×2.98 (12430 → 37085 мс на 60 кадров), кадры на диске ×8.2 (206 → 1681 КБ/кадр)
    ⇒ AC2 СЛОМАН ЗЕРНОМ: 546 мс/кадр × 1800 кадров = 16.4 мин при потолке 15 мин, то есть одно
    только зерно съедает весь бюджет ролика; порог сцены 250 мс/кадр превышен зерном в одиночку.
    РЕШЕНИЕ ПО ПРАВИЛУ ВЛАДЕЛЬЦА: дефолт зерна = 0, параметр остаётся, долг с числом
- `18:02` [5/6] ГЕЙТЫ ЖИВЬЁМ, ОБА PASS:
    `grade@1` draftHalf N=3 — sha256 `0e8448a61a97b1f7`, framemd5 `408cb94c961ca563`, 12 кадров, 1931/1934/2003 мс
    `grade@1` (зерно 0.15) final N=10 — sha256 `b97d127db32e8366`, framemd5 `b9eef71e75dc377b`, 6 кадров, 2703…2816 мс
    ⇒ `feTurbulence` НА ЭТОЙ МАШИНЕ ВОСПРОИЗВОДИМ; зерно выключается по ЦЕНЕ, а не по детерминизму
- `18:03` [6/6] начато: демо — клип на трек effect в трёх сценах, пересборка, кадры
- `18:08` [6/6] запись гейта `grade@1` final снята во ВРЕМЕННЫЙ каталог (`--gates-dir`), PASS N=10, sha256 `b97d127db32e8366` — тот же, что дал vitest; дерево исходников `.gates.json` не получило (porcelain чист по ним)
- `18:09` [6/6] начато: `vpe build --profile final` БЕЗ `--allow-tts`, ожидаю ~3–4 мин
- `18:14` [6/6] ДЕМО ПЕРЕСОБРАНО: `vpe build --profile final` БЕЗ `--allow-tts`, 3 сегмента (179/170/153 = 502 кадра), 2 мин 25 с, final.mp4 19.7 МБ. **СЕТЬ: «обращений к источнику 0»** (reports/voice.txt). Бюджет сцены: пик 1773 мс/кадр — ПЕЧАТЬ, не падение. Кадры до/после — 3 пары, плюс пара 1:1 на зерно
- `18:26` [6/6] ПРОТОКОЛ НАРУШЕНИЙ: пять проб, все красные — Н1 линт D5; Н2 пиксельный тест (18.31 против порога 1); Н3 отказ спека до рендера, exit 1; Н4а статический линт `non_deterministic_code`; Н4б guard D4 с именем шаблона И клипа. Откаты чистые, `.e07bak` в дереве нет (резерв в scratchpad)
- `18:27` [6/6] начато: DEBTS №219–222, invariants, отчёт
- `18:52` ПОЛНЫЙ ПРОГОН №1: 161 файл, **2 failed | 2415 passed | 3 skipped**, 626 с. Красных два: (1) `compile/test/template-contract.test.ts` — ОТСТУПЛЕНИЕ, ждёт владельца; (2) `renderer-hyperframes/test/materialize.test.ts` «реестр НАПОЛНЕН пятью» — мой недосмотр, счёт 5 → 6, ПОЧИНЕНО, файл зелёный (23 passed)
- `19:12` ПОЛНЫЙ ПРОГОН №2 запущен — подтвердить, что красный ровно один
- `19:23` ПОЛНЫЙ ПРОГОН №2: 161 файл, **1 failed | 2416 passed | 3 skipped**, 630 с. Красный РОВНО ОДИН — `compile/test/template-contract.test.ts`, то самое отступление; ждёт слова владельца (а/б/в). Новых красных правка E-07 не оставила
- `19:24` ЖДУ ВЛАДЕЛЬЦА: имя «шаблона, которого нет в реестре» в закрытой зоне `compile` — вариант (а) shaderBg@1 / (б) noSuchTemplate@1 / (в) не править
- `19:30` РАЗРЕШЕНИЕ ВЛАДЕЛЬЦА (точечное, закрытая зона `compile`): вариант (б), ровно четыре литерала 'grade@1' → 'noSuchTemplate@1' в `template-contract.test.ts`, строки 157/159/319/333. Плюс завести №223 (shaderBg@1 в роли несуществующего, адрес E-01)
- `19:45` правка §12.1 применена: `git diff --numstat` = `4 4`, один файл зоны, файл зелёный (17/17)
- `19:56` ПОЛНЫЙ ПРОГОН №3: **160 файлов PASS, 2417 passed | 3 skipped | 0 failed**, 634 с, exit 0. ПОЛНОСТЬЮ ЗЕЛЕНО
- `19:57` доки: DEBTS №219–№223, invariants (ревизия E-07, guarded 67), отчёт, транскрипт, кадры
**Итог:** `grade@1` в библиотеке шестым; оба гейта PASS (`draftHalf` N=3, `final` N=10 с зерном);
зерно воспроизводимо, но выключено дефолтом по ЦЕНЕ (545.44 против 133.97 мс/кадр, диск ×8.2);
демо перегрейжено и пересобрано без сети; протокол Н1–Н4 — пять красных; репозиторий зелёный.
Детали — docs/impl/E-07/report.md.
