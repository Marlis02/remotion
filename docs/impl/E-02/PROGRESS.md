# PROGRESS — IMPL-E-02 parallax25@1 · старт 2026-08-31T21:40:24+06:00
План: якорь → шаг 0 → чтение → план+вопросы → жду «ок». Бюджет: 60 мин без зелёного = стоп.
- `21:40` [0/N] якорь снят до чтения
- `21:44` [0] ШАГ 0: `library.test.ts` был красный (`4` против `5`) — счёт 4→5 + `grade@1` в список; файл зелёный 8/8, porcelain = 1 файл
- `21:44` [0] preflight (вне часов): node v25.6.1, pnpm 11.9.0, ffmpeg 7.0.2-static, chrome-headless-shell 152.0.7928.2 — эталон среды на месте
- `21:52` [1/5] чтение закончено: roadmap §3/§5, схема `derivedFrom`, спеки/реализации `still@1`/`kenburns@1`/`grade@1`, `fixture.ts`, `gate-requests.test.ts`, `runtime.js`, каталог ассетов, демо
- `21:52` ЖДУ ВЛАДЕЛЬЦА: план 5 шагов + 4 вопроса выданы
- `21:55` [1/5] «ок» владельца: В1(а) rembg в venv; В2(а) дальний слой = оригинал, производный — с derivedFrom; В3 чтение формы подтверждено; В4 still@1 под параллаксом приемлем + долг с адресом CP-*
- `21:55` [1/5] начато: спек `parallax25@1` + реализация + обе регистрации. ЧАСЫ ПОШЛИ
- `22:35` [1/5] готово: спек + реализация + обе регистрации, `pnpm build` чисто
- `22:44` [1/5] ПЕРВЫЙ ЗЕЛЁНЫЙ: `templates-spec` 185/185 + `templates.test.ts`/`materialize.test.ts` 66/66. Правлено 6 ожиданий счёта (registry, fixture-specs ×2, purity, templates ×3, materialize) + названная разница ['grade@1','parallax25@1'] в ДВУХ файлах (№222)
- `22:45` [2/5] начато: гейт-инфраструктура — билдер на N ассетов, два синтетических PNG с альфой, CASES в обеих копиях, перегенерация запросов
- `22:30` ПОПРАВКА К ЖУРНАЛУ: три строки выше (`21:55`, `22:35`, `22:44`, `22:45`) записаны на глаз и опережают часы; сверено `date -Iseconds` = 22:29. Дальше время только из `date`
- `22:47` [2/5] запросы перегенерированы: ДЕСЯТЬ прежних файлов побайтово те же (sha до/после совпали строка в строку), porcelain — ровно 4 новых (2 json + 2 png слоёв) ⇒ прежние записи гейта ДЕЙСТВУЮЩИЕ
- `22:38` [2/5] Н4 ЗЕЛЁНЫЙ (браузерный, новый файл `parallax-cover.test.ts`, 3.5 с): штатная — восемь углов из восьми ≥ 132.0 при пороге 60; копия БЕЗ запаса — первый кадр правые углы 0.0, последний кадр левые 0.0. Двусторонний охранник + «кадры движутся»
- `22:39` [2/5] начато: живой гейт `draftHalf` N=3 на шести случаях, ожидаю ~4 мин
- `22:41` [2/5] ГЕЙТ `draftHalf` N=3 ЖИВЬЁМ, PASS: `parallax25@1` sha256 `caec071e5b3efed3`, framemd5 `8b495cd8b509ffa5`, 12 кадров, 1516/1539/1541 мс. Файл 8/8 за 127 с
- `22:44` [3/5] начато: прибор бюджета (60 кадров, 5 повторов, final 1080×1920, шум тем же прибором), ожидаю ~15–25 мин
- `22:46` [3/5] ИЗМЕРЕНО (60 кадров, 5 повторов, final 1080×1920, шум 0.8061 мс/кадр):
    `parallax25@1` на ДВУХ слоях — **10.6918 мс/кадр** (бюджет 11); на ОДНОМ (вырожденный, blur 18px) — **43.3854** (44)
    ⇒ размытие одно стоит ≈32.7 мс/кадр, втрое дороже всей остальной работы шаблона. Ожидание «один слой дешевле» ОПРОВЕРГНУТО
    В МАНИФЕСТ едет 44 (оценка СВЕРХУ: `layers: [alias]` — законный вызов). AC2 не ломается (потолок 500, порог сцены 250)
    3–4 слоя не измерены (прибор ограничен двумя байтовыми слоями) — INFERENCE ≈21, долг №229
- `22:47` [3/5] временный прибор удалён; 12 файлов запросов от правки бюджета не сдвинулись (31/31)
- `22:47` [4/5] начато: демо — rembg в venv, нарезка Mulberry Street, записи с derivedFrom (вне часов)
- `22:52` [4/5] rembg 2.0.69 поставлен в venv scratchpad (34 пакета, onnxruntime 1.23.2, python 3.10.12); систему не трогал. Источник: 3840×2835 JPEG RGB
- `23:05` [4/5] ОТСТУПЛЕНИЕ: rembg НЕ РЕЖЕТ Mulberry Street. Три модели, все впустую: u2net — alpha>128 на 0.1 % кадра (bbox 390×246 из 3840×2835), u2net_human_seg — 0.1 %, isnet-general-use — 0.2 % (призрак пожарной лестницы, глазами — не слой). Причина: u2net-класс сегментирует ОДИН выделенный объект, а на снимке толпа без него
- `23:05` [4/5] контрольный опыт на двух других PD-снимках демо: `mechanic` (Хайн) — u2net даёт 46.6 % кадра, вырезка чистая глазами; `mother` (Ланге) — 27.6 %, тоже чистая. Инструмент РАБОТАЕТ, не работает он на НАЗВАННОМ снимке
- `23:05` ЖДУ ВЛАДЕЛЬЦА: смена снимка демо — отступление от решения `E-02`; кода демо не пишу
- `23:12` [2/5] ГЕЙТ `final` N=10 ЖИВЬЁМ, PASS: `parallax25@1` sha256 `894886fb5fdd62bb`, framemd5 `d5cf21ede7c43abc`, 6 кадров, 1367…1407 мс; отпечаток `3217a4cf…` (эталон канала). Файл 3/3 за 68 с
- `23:12` [5/5] начато: runbook (+2 команды, счета 10→12, 5→6 файлов) — готово; дальше протокол Н1–Н3
- `23:20` [5/5] ПРОТОКОЛ НАРУШЕНИЙ: Н1 красный (схема: 5-й слой, drift вне [0,0.2], depthSpread<1, лишнее поле, кривая вне объявленных); Н3 красный (самопроверка TRANSFORM_ORDER на обратном порядке И на пропавшем translate; плюс пропавшая роль слоя); Н4 красный (пиксельный охранник углов, числа сняты); **Н2 НЕ КРАСНЕЕТ НИЧЕГО** — производный файл с `derivedFrom: null` каталог принимает и читает сочинённый статус лицензии из его же записи. Находка класса №210, долг №230
- `23:13` [5/5] ПОЛНЫЙ ПРОГОН №1: 162 файла, **3 failed | 2452 passed | 1 skipped**. Оба красных — МОИ и починены: (1) NUL-линт на двух PNG слоёв ⇒ две строки в `ALLOWED` с названной причиной («предмет этих файлов — альфа-канал»); (2) `cli/test/template-gate.test.ts` «записей гейта: 1 из 6» ⇒ 1 из 7. Оба файла зелёные (20/20)
- `23:13` ПОБОЧНО ИЗМЕРЕНО тем же красным: `vpe template list` печатает СЕМЬ шаблонов, `parallax25@1` — бюджет 44, easing `power2.inOut,none`, гейт `—` (записи снимает владелец). Условие приёмки «семь шаблонов» выполнено
- `23:13` [5/5] долги №227–№231 заведены
- `23:23` ПОЛНЫЙ ПРОГОН №2: **161 файл PASS | 1 skipped | 0 failed**, exit 0. ПОЛНОСТЬЮ ЗЕЛЕНО (браузерные и живые гейты включительно)
- `23:30` [5/5] доки: roadmap §3/§5 (24 → 44, «входит в базовую стоимость» опровергнута), invariants (ревизия E-02, guarded 67, дыра V10), долги №227–231, отчёт, транскрипт
- `23:30` ЖДУ ВЛАДЕЛЬЦА: демо (§7 отчёта). Всё остальное зелено и закрыто
