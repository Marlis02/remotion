# PROGRESS — демо «Помпеи», 30 с · старт 2026-09-10T00:00Z
План: 7 шагов — разведка кода, ассеты (PD), сценарий en, режиссура, полоса субтитров, TTS (платно), сборка. Бюджет: один живой прогон TTS (~80 слов).
- `--:--` [1/7] разведка движка начата
- `--:--` [1/7] готово — FACT: шаблонов 7 (`vpe template list`), аудио-шаблон один — `bed@1`; sfx-шаблона нет
- `--:--` НАЙДЕНО: микса музыки в v1 НЕТ. `compile/src/audio/types.ts` §«чего в этих типах нет»: музыка едет ДАННЫМИ (`music[]`, решение владельца 1 «а», `CP-05`), `mixSaturating` в зоне не зовётся ни разу. Дорожка ролика = речь + тишины движка. Значит «фон музыка + эффекты» в выходной mp4 физически не попадут.
- `--:--` НАЙДЕНО: оформление полосы субтитров — НЕ параметр проекта. Плашка и тень зашиты в `renderer-hyperframes/src/composition/runtime.js` (`BAND.plateColor`, `BAND.textShadow`, долги №188/№125), цвет активного слова — `ACCENT_COLOR = '#ffb347'` в `captionEmphasis@1/impl.ts`. Правка меняет `bundleHash` → записи гейта V13 у всех 7 шаблонов становятся несвежими.
- `--:--` ЖДУ ВЛАДЕЛЬЦА: (1) музыка/эффекты — как поступить; (2) полоса субтитров — правкой канала или переопределением на проект.
- `--:--` РЕШЕНИЕ ВЛАДЕЛЬЦА: (1) без музыки и sfx — только голос; (2) полосу субтитров НЕ трогаем, собираем на том, что движок умеет сейчас (плашка+тень+оранжевый акцент остаются).
- `--:--` [2/7] калибровка темпа речи и подбор PD-ассетов — начато
- `--:--` [2/7] готово — 3 ассета PD заведены (`vpe asset add`): last-day 3840×2696 (Брюллов 1833), excavation 2000×1405 (Соммер №10800), cast 3810×3070 (Соммер №1279). Все три — public-domain, источники в записях.
- `--:--` [3/7] сценарий (en, V12) ~83 слова, 3 сцены; режиссура: kenburns×3, captionEmphasis×3, flash на бите `plaster`. Расчёт по темпу живого голоса 2.84 слов/с (замер по takes vertical-v1) ⇒ ~30 с.
- `--:--` [4/7] сухая сборка на draftHalf БЕЗ сети — начато
- `--:--` [4/7] готово — parse/compile прошли без сети, 4 чанка речи; отказ K8 штатный.
- `--:--` [5/7] ПЛАТНЫЙ ШАГ начат: живой ElevenLabs, 4 чанка (~450 знаков), профиль draftHalf. Ключ и id голоса — из окружения (CLAUDE.md §2).
- `--:--` [5/7] готово — живой TTS: 4 дубля, речь 27.77 с, ролик draftHalf 27.1 с, 813 кадров. Денег потрачено: один прогон синтеза ~450 знаков.
- `--:--` НАЙДЕНО: на draftHalf субтитров в кадре НЕТ. Воспроизведено вне моего проекта: `vpe template demo captionEmphasis@1` даёт чёрный кадр (YMAX=16 по всем кадрам), пересборка `examples/vertical-v1` — тоже без полосы. Композиция при этом верна: открыл bundle в Chrome — `#captions` есть, 11 групп, белый текст, bottom 500px, шрифт загружен. Пиксельный охранник `captions-visibility.test.ts` ЗЕЛЁНЫЙ — он гоняет scale 1 (final). Гипотеза: дефект виден только на scale 0.5.
- `--:--` [6/7] сборка на final для проверки гипотезы — начато
- `--:--` [6/7] готово — на final субтитры И кадрирование в порядке. Значит дефект в профиле draftHalf (scale 0.5), не в проекте: на нём в кадр попадает только ЛЕВЫЙ ВЕРХНИЙ КВАДРАНТ композиции, полоса субтитров (bottom 500 px) остаётся за кадром. Охранник `scale-render.test.ts` этого не ловит: он считает долю НЕЧЁРНЫХ по квадрантам, а полноэкранный слой картинки её держит равной 100/100/100/100 и при обрезке.
- `--:--` [7/7] правки по кадру: params сцен 2 и 3 (печатная подпись и поля отпечатков), +1 фраза (~30 с). ПЛАТНЫЙ ШАГ: один новый чанк синтеза.
- `--:--` [7/7] готово — final: 877 кадров, 29.233 с, 28.0 МБ; расход по всем дублям 495 code points, списано 272, обращений к источнику за задачу 2.
**Итог:** ролик собран, три сцены на PD-ассетах, живой голос. Музыки нет (микса нет в v1), субтитры в оформлении канала — обе части по решению владельца. Найден дефект `draftHalf` — долг №265. Детали — report.md.
