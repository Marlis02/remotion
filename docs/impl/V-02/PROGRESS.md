# PROGRESS — IMPL-V-02 приёмка дубля (пакет `voice`) · старт 2026-08-24T09:06:34Z
План: 6 шагов. Бюджет: сеть/ключи не нужны (V9), платных вызовов НЕТ ни одного.
1. Дочитать источники (roadmap V-02 + §4.5, analyze.mjs построчно, ADR-0010 §1/§5/§6/§9,
   профиль `takeAcceptance` + `TakeAcceptanceSchema`, `packages/voice/src/*` из `V-01`,
   отчёт `V-01` §«Отклонения от спайка», инварианты V1/V2/V8, долги SP-2 №8).
2. Базовый прогон `typecheck` + `lint` + `test` — зафиксировать зелёное и число тестов.
3. Код: `packages/voice/src/acceptance/` (`health.ts`, `ladder.ts`); правки `types.ts`
   (`rejectReason` — union, `TakeAcceptance`), `errors.ts` (правило M12), `mock.ts`
   (`takeHealth` → делегат приёмки; статусы V8 в `tokenIntervals`/`makeTake`), `index.ts`.
4. Тесты: перенесённая семантика `analyze.mjs`, пороги из профиля, эмодзи-FACT, лестница,
   `alignment: null`, V8 на обоих классах, критерий roadmap (два прогона ⇒ один `chunkKey`).
5. Протокол нарушений: каждый новый охранник показан падающим, откат после каждого.
6. Доки: `docs/impl/V-02/{report.md,PROGRESS.md,violation-transcript.txt}`, ревизия
   `invariants.md` (V1/V2/V8), долги (SP-2 №8 + новые с №75), статус в roadmap, имя коммита.
- `09:06` [1/6] источники дочитаны — analyze.mjs (139 строк), mock.ts (556), types.ts (230),
  ADR-0010 §1/§5/§6, профиль (0.9 / 8 / 2), invariants V1/V2/V8, roadmap §7.3 SP-2 №8
- `09:20` [2/6] базовый прогон зелёный: 68 файлов / 1188 тестов, `voice` 61; tsc и eslint чисты
- `09:22` [3/6] код готов: `acceptance/health.ts` (+`ladder.ts`), `types.ts` (union причин,
  `TokenBinding` — размеченное объединение), `errors.ts` (`ADR-0010 §1 (M12)`), `mock.ts`
  (делегат + статусы), `index.ts`; 61 тест `V-01` зелёный после механической правки вызовов
- `09:23` [4/6] `acceptance.test.ts` — 20 тестов зелёные (эмодзи-FACT воспроизведён: 0.976/run 2
  и 0.956/run 3 на строках 41 и 45 code points)
- `09:26` [4/6] `ladder.test.ts` (8) + `token-status.test.ts` (8) + охранник-греп
  `tests/lints/adr0010-take-acceptance.test.ts` (8) — весь репозиторий зелёный: 72 / 1230
- `09:35` [5/6] протокол нарушений: 22 внесения, все красные, откат после каждого;
  ДВЕ НАХОДКИ — (1) снятие эпсилона `start > end + 1e-9` было зелено (грубый случай краснеет
  и без эпсилона) — закрыто «дрожью» 5e-10; (2) умолчание параметра порогов в `mock.ts`
  оставляло зелёными все 103 теста пакета — охранник дополнен тремя формами возврата чисел.
  После закрытия обеих: 72 файла / 1232 теста зелёные, tsc и eslint чисты
- `09:45` [6/6] доки: `docs/impl/V-02/{report.md, violation-transcript.txt}`; `invariants.md` —
  ревизия + V1/V2/V8 → `guarded` (29 → 32, `named` 63 → 60); `DEBTS.md` — №75–81, №73
  переадресован `V-06`; roadmap — §4.5 статус `V-02`, §7.3 долг SP-2 №8 закрыт
**Итог:** приёмка дубля живёт в `packages/voice/src/acceptance/`; пороги невыразимы в коде
(обязательный параметр + греп-охранник из трёх частей), лестница M12 чанк не делит, интервал
`[t, t]` для `absent` не типизируется, эмодзи-`FACT` воспроизведён до третьего знака.
72 файла / 1232 теста зелёные, `tsc` и `eslint` чисты; платных вызовов и сетевых — ноль.
Детали — `docs/impl/V-02/report.md`, протокол — `docs/impl/V-02/violation-transcript.txt`.
