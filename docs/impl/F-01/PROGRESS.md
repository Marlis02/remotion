# PROGRESS — IMPL-F-01 (AC4 / AC4-b) · старт 2026-09-01T22:50:43+06:00
План: шаг 0 (точечные разрешения) → чтение → план 3–5 шагов + вопросы → ждать «ок». Бюджет: 60 мин без зелёного = стоп.
- `22:50` [0/N] якорь снят, PROGRESS начат
- `22:51` [0] шаг 0 готово: `request-dedup.test.ts` — аннотация `sha: Sha256` вынесена из `as`-выражения (селектор смотрит поддерево); eslint файла 0, тесты 11/11 зелёные
- `22:52` [1/N] чтение начато (roadmap F-01, ac4-профиль, Charter AC4 rev5, ADR-0007 §10, DEBTS, build-stages, assemble, ai-test-1)
- `22:59` [1/N] чтение готово (roadmap §3/§4 F-01, Charter AC4 rev5, ADR-0007 §7/§8/§10, ac4-профиль, DEBTS, build-stages, media/assemble, voice/providers, ai-test-1)
- `22:59` НАХОДКИ preflight: (а) `maxProbeDurationFrames` не читает НИКТО; (б) `profiles.renderAc4` объявлен схемой и не читается никем — `readRenderProfile` знает только final/draft; (в) `--profile` у build принимает только final|draftHalf (GATE_PROFILES); (г) браузер и hyperframes на месте, ai-test-1 собран живьём сегодня (fp 3217a4cf…, 3 сегмента, 1119 кадров); (д) voice.txt печатает «попаданий кэша 0» и не знает про billedUnits; (е) `fetch` в `bin/http.ts` не обёрнут — `cause` теряется
- `22:59` ЖДУ ВЛАДЕЛЬЦА: план 5 шагов + 3 вопроса выданы в чат
- `23:01` [2/5] «ок» владельца: В1 skip-гейт, В2 ADR-0007 §8 буквально, В3 вариант А. Начат шаг 1 — вход на ac4-профиль и `vpe verify ac4`
- `23:12` [2/5] шаг 1 готово по коду: `ac4.ts` (лист: имя профиля, причина skip, разбор framemd5), `readRenderProfile` читает `profiles.renderAc4` + явный файл, `build` пропускает R12 на ac4 с печатью причины, команда `vpe verify ac4`. tsc --build чисто, eslint cli чисто
- `23:22` [3/5] ИЗМЕРЕНО (шов, SP-3 №5): на готовой сборке `ai-test-1` последовательность хэшей кадров `final.mp4` = склейка последовательностей трёх сегментов (476+392+251 = 1119), второго энкода на шве НЕТ
- `23:22` ИЗМЕРЕНО (AC4-b, предварительно): IR `seg:two` в проекте [S0,S,S2] и в проекте [S] совпадает ПОЛНОСТЬЮ, кроме `segmentDurationInFrames` (48 против 38): хвостовой gap сцены принадлежит сегменту (T6). Следствие для D12 — сравнивать надо ОДИН И ТОТ ЖЕ IR, отрендеренный в составе и отдельно (формулировка задачи), а не пересобранную обрезку
- `23:22` [3/5] начат живой прогон AC4 на фикстуре (браузер, профиль ac4, 65 кадров) — ожидаю минуты
- `23:24` [3/5] AC4 на фикстуре ЗЕЛЁНЫЙ с первого прогона: два живых прогона (браузер, ac4-профиль, 65 кадров) равны по framemd5, байтам финала и PCM; 10.5 с на файл теста
- `23:25` [3/5] начат прогон `vpe verify ac4 --project examples/ai-test-1` (живой, 2×1119 кадров) — ожидаю ~3 мин
- `23:31` [3/5] ГОТОВО AC4 НА НАСТОЯЩЕМ РОЛИКЕ: `vpe verify ac4 --project examples/ai-test-1` — 2 прогона × 1119 кадров, ВСЕ девять величин равны (final sha256, framemd5 финала, PCM дорожки, sha256+framemd5 трёх сегментов), шов 476+392+251=1119 без второго энкода, EXIT=0, стенка 2:58.78
- `23:32` [4/5] запущен браузерный тест AC4-b (D12 + шов на фикстуре) в фоне; параллельно — диагностика сети и золото blast-radius
- `23:33` [4/5] AC4-b ЗЕЛЁНЫЙ (16.5 с): IR сегмента в составе и в обрезке различается ровно длиной (48/38), тот же IR отдельно и в составе даёт побайтово равный .mts, шов на фикстуре сошёлся
- `23:33` [4/5] диагностика сети готова: `callTransport` + `causeChain` + `NETWORK_HINT` в `voice/providers/http.ts`, оба вызова транспорта (синтез и снимок аккаунта) через неё; юнит 8/8 зелёный
- `23:37` [4/5] золото blast-radius снято и зелёное: правка `to.scale` у `kenburns@1` ⇒ ПРОМАХ ровно у `seg:one`, `seg:two`/`seg:three` равны, три `voiceKey` равны. Юнит расхода голоса 8/8
- `23:38` [5/5] запущен второй прогон `vpe verify ac4` на ai-test-1 (повтор + свежий voice.txt нового формата) в фоне
- `23:50` [5/5] Н1–Н4 показаны красными руками и восстановлены: Н1 — часы в стадии звука ⇒ расходятся `final.mp4 · sha256` и PCM при равных кадрах; Н2 — чужой сосед ⇒ красный на `bundleHash`; Н3 — испорченное золото ⇒ красный с дифом строки `seg:two`; Н4 — `fetch` без `cause` ⇒ 4 красных в диагностике
- `23:50` [5/5] реестры обновлены: DEBTS №236–241, инварианты D10/D11/D12/K9 → guarded (67 → 71), roadmap §2.1 AC4, §3 F-01, §4 F-01, §7.3 долги SP-3 №5 и №6 закрыты
- `23:51` [5/5] запущен полный прогон репозитория (фон), пишу отчёт
- `00:06` [5/5] ПОЛНЫЙ ПРОГОН ЧИСТЫЙ: 169 файлов, 2520 тестов зелёных, 3 skipped, 0 красных, 607 с. `tsc --build` и `eslint .` — чисто
- `00:09` [5/5] перепрогон файлов, правленных после старта (voice + tests/lints + три юнита cli): 50 файлов, 525 тестов, 0 красных
**Итог:** AC4 и AC4-b закрыты измерением, а не объявлением: `vpe verify ac4` на `examples/ai-test-1` дал побайтово равный `final.mp4` в четырёх сборках (1119 кадров), шов `concat -c copy` впервые проверен на кадрах рендерера, сегмент побайтово равен себе отдельно и в составе. Долги SP-3 №5 и №6 закрыты, заведены №236–241, D10/D11/D12/K9 → `guarded`. Две дыры владельца закрыты: расход голоса печатается из take-файлов (632 отправлено / 347 списано, попаданий 4/4), недошедший `fetch` объясняется хостом, `cause` и подсказкой VPN/DNS. Детали — `docs/impl/F-01/report.md`, вывод команды — `verify-ac4-ai-test-1.txt`, протокол нарушений — `violation-transcript.txt`.
