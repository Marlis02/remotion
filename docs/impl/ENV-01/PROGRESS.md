# PROGRESS — IMPL-ENV-01 (шрифт гейта в репо №187, эталон среды канала) · старт 2026-08-31T20:46:34+06:00
План: якорь → чтение → план 3–4 шага → «ок» → реализация. Бюджет: стоп-правило 60 мин без зелёного теста.
- `20:46` [0/N] якорь снят, начинаю чтение (DEBTS №187/192/209, fixture.ts, template-gate.ts, invariants, roadmap, runbook, direction)
- `21:05` [0/4] чтение готово. ИЗМЕРЕНО до плана: системный DejaVuSans-Bold.ttf = `d1c3ff99…287b`, 705684 байта — СОВПАЛ с sha в двух файлах запросов `captionEmphasis@1.*`; ffmpeg на PATH = 7.0.2-static (`/usr/local/bin`), node v25.6.1; `bundle.hash` десяти запросов выписаны; `resolveRequestPaths` (cli/src/template-gate.ts) резолвит `fonts[].path` — шрифт входит в пять полей; `materialize.ts` кладёт шрифт как `fonts/<sha>.<ext>` ⇒ `font.path` в `bundle.hash` НЕ входит (основание для «хэши не сдвинутся»); лицензия шрифта в системе — `/usr/share/doc/fonts-dejavu-core/copyright` (DEP-5, стенза `Files: *` = Bitstream Vera); макс. номер долга = 223
- `21:05` ЖДУ ВЛАДЕЛЬЦА: план 4 шагов + два вопроса (имя константы / форма файла лицензии)
- `21:12` ОК ВЛАДЕЛЬЦА: план целиком; В1 — `GATE_FONT_PATH`/`gateFontBytes()`, `build-fixture.ts:31` НЕ трогать (долг с адресом); В2 — `copyright` целиком, обе стензы поимённо в отчёте; контрольный опыт `unshare -rm` — двумя числами. Дальше без остановок
- `21:12` [1/4] начато: контрольный опыт ДО правки — оба теста в mount-namespace без системного DejaVu, ожидаю ~3 мин
- `21:16` [1/4] КОНТРОЛЬНЫЙ ОПЫТ ДО ПРАВКИ (`unshare -rm`, оба теста, 40 утверждений):
    К0 эта машина как есть — **40 passed / 0 failed**, 1.48 с
    К1 системного DejaVu НЕТ вовсе — **10 failed / 30 passed**: падает билдер (`systemFontBytes`), красны все десять сверок
    К2 по системному пути ДРУГОЙ валидный DejaVu (`DejaVuSans.ttf`, sha `690243ad…`) — **2 failed / 38 passed**: красны ровно два файла `captionEmphasis@1.*`, и `bundle.hash` в них уехал `58e06db5…` → `07eadae8…`. К2 — это условие приёмной машины (у неё `5c1247ac`), и именно её два красных задача обязана погасить
- `21:22` [1/4] готово: байты в репо — `gate-requests/assets/DejaVuSans-Bold.ttf` (705684 б, sha `d1c3ff99…287b`, `cmp` с системным — байт в байт), лицензия `DejaVuSans-Bold.LICENSE.txt` (3859 б, sha `63d3ba75…fbc9`, дословно `/usr/share/doc/fonts-dejavu-core/copyright`, две стензы: `Files: *` → bitstream-vera, `Files: debian/*` → GPL-2+). NUL: в шрифте 86278, в лицензии 0 ⇒ в `ALLOWED` идёт только `.ttf`. Линт `nul-in-sources` — 3/3 зелёных
- `21:22` [2/4] начато: фикстура и тест — шрифт становится ассетом каталога запросов
- `21:38` [2/4] готово. `SYSTEM_FONT_PATH`/`systemFontBytes()` → `GATE_FONT_PATH`/`gateFontBytes()` (чтение из репо + сверка sha, отказ называет файл и обе величины); `GATE_REQUEST_PATHS.font = 'assets/DejaVuSans-Bold.ttf'`; в тесте утверждение перевёрнуто (относителен + резолвится + sha) плюс два новых теста на байты шрифта.
    **ДЕСЯТЬ `bundle.hash` — ВСЕ РАВНЫ** (`diff` до/после пуст). Дифф ровно два файла по одной строке, расхождение полей ровно одно: `fonts[0].path` `/usr/share/fonts/…` → `assets/DejaVuSans-Bold.ttf`. `*.gates.json` — ни одного изменения в porcelain
    КОНТРОЛЬНЫЙ ОПЫТ ПОСЛЕ: К0 **42 passed / 0 failed**, К1 (шрифта нет) **42/0**, К2 (другой DejaVu) **42/0**. Было 40/0, 10 red, 2 red
    ПРОТОКОЛ НАРУШЕНИЙ: Н1 (байт шрифта испорчен, offset 700000) — **13 красных**, оба сообщения называют файл и обе sha; Н2 (путь шрифта возвращён на системный абсолютный + перегенерация) — **2 красных**, «шрифт обязан быть ОТНОСИТЕЛЬНЫМ» на обоих `captionEmphasis@1`. Откаты чистые (sha шрифта вернулся, файлы перегенерированы, хэши снова равны)
    `pnpm typecheck` и `pnpm lint` — зелёные, вывод пуст
- `21:38` [3/4] начато: демо без грейда, пересборка `vpe build --profile final` без `--allow-tts`, ожидаю ~3 мин
- `21:47` [3/4] готово: три клипа `grade@1` сняты (дифф `7 87` в одном yaml), демо ПЕРЕСОБРАНО `vpe build --profile final` БЕЗ `--allow-tts` за **1 мин 51 с**: 3 сегмента 179/170/153 = **502 кадра**, final.mp4 19.6 МБ, 16.733 с, 1080×1920. **СЕТЬ: «чанков 4, обращений к источнику 0»** (reports/voice.txt). Бюджет сцены: пик **135 мс/кадр** против 1773 у `E-07` с грейдом. `store.lock` и `anchors.lock.jsonl` сборка не тронула (porcelain по `examples/` — один yaml + README)
- `21:47` [4/4] начато: доки, долги, отчёт
- `22:05` [4/4] доки готовы: invariants (ревизия `ENV-01` с таблицей эталона + зачёркивание ffmpeg 6.1.1 в ревизии `H-03`), roadmap (пометка у долга шва §7.3 + строка `ENV-02` сверх карты), gate-runbook (проверка шрифта снята, ffmpeg = 7.0.2-static, контрольный прогон 42), DEBTS (№187 → closed, №192 уточнён, новые №224/№225/№226), отчёт `docs/impl/ENV-01/report.md`
- `22:05` НАХОДКА (не объём задачи, заведена долгом №226): отпечаток `3217a4cf…` стоит и в `impl/H-04/report.md:43` (до `--strict`), и на этой машине (с `--strict`) — две пробы с разными `launch.args` не дают один blake3. На задачу не влияет: записи несут `3217a4cf…`, машина даёт `3217a4cf…`
- `22:06` ПОЛНЫЙ ПРОГОН запущен — ожидаю ~11 мин
- `22:16` ПОЛНЫЙ ПРОГОН: 161 файл, **1 failed | 2418 passed | 3 skipped**, 556.7 с. Красный РОВНО ОДИН и он НЕ МОЙ: `renderer-hyperframes/test/library.test.ts` ждёт ЧЕТЫРЕ шаблона с записями гейта, а их ПЯТЬ — записи `grade@1` положил коммит владельца `50fe84c` («Записи гейта grade@1 после E-07»), файл есть в HEAD. Сессия не трогала ни одного входа этого теста (`git status` по `templates-spec`/`library.*` пуст). Арифметика подтверждает: `E-07` закончил на 2417 passed / 0 failed, я добавил 2 теста ⇒ 2419 = 2418 + 1
- `22:16` ЖДУ ВЛАДЕЛЬЦА: чинить ли `library.test.ts` (одна строка, счёт 4 → 5 + `grade@1` в список) — это ВНЕ объёма задачи («не трогает templates-spec»), и по правилу «отступление — стоп и вопрос ДО кода» сам не правлю
**Итог:** долг №187 закрыт байтами в репозитории; десять `bundle.hash` до/после равны поштучно, записи гейта не тронуты; юнит перестал зависеть от машины — контрольный опыт в mount-namespace 42/42 во всех трёх конфигурациях (было 40/0, 10 red, 2 red); эталон среды канала записан измеренным (отпечаток `3217a4cf…`); демо пересобрано без грейда и без сети. Детали — docs/impl/ENV-01/report.md.
- `22:3x` ОТВЕТ ВЛАДЕЛЬЦА / ПРИЁМКА: сверка запросов зелёная на машине с другим системным DejaVu — критерий №187 исполнен делом. `library.test.ts` (4→5) — чинится шагом 0 СЛЕДУЮЩЕЙ задачи по поимённому разрешению, здесь не трогать. Задача ЗАКРЫТА.
