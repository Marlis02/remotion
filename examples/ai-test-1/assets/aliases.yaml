schema: aliases/1

# alias → sha256. Замена фото не трогает прозу (класс правок 5, ADR-0004).
# Три настоящих снимка public domain; лицензия каждого названа поимённо в его записи
# `assets/records/<sha256>.json` (долг №48 — про лицензии КОДА, здесь речь про ассеты).
street:   "846e37e15b6e5162a9035ab7a35842710f75ace93478de5ad349c39cec8b0314"
mechanic: "8e6317a20918ba603c4a5e4fd25ee6db60f8e5207a105f5f7031d74d135118d6"
mother:   "4e5fbcf1388c9fc925482c1ed8299f069bf0264ac7291851e42c98eb65de33eb"

# ── ЧЕТВЁРТЫЙ ALIAS — ПРОИЗВОДНЫЙ, И ЭТО ПЕРВЫЙ ТАКОЙ В РЕПОЗИТОРИИ (`E-02`, 2026-08-31) ──
# Ближний слой параллакса сцены `work`: вырезка механика с насосом, PNG с альфой, та же
# геометрия 2155×3000, что у источника. Права он НЕ несёт: его запись объявляет `derivedFrom`
# на `8e6317a2…`, и `resolveEffectiveLicense` читает лицензию оттуда (P11, ADR-0005 §9a).
# Инструмент и версия названы в записи поимённо — `rembg@2.0.69`, модель `u2net`.
#
# ДАЛЬНЕГО СЛОЯ ЗДЕСЬ НЕТ, И ЭТО НЕ ПРОПУСК: дальним планом стоит САМ `mechanic` выше.
# Вырезать фон отдельным файлом было бы нельзя честно — rembg отдаёт передний план, а фон с
# дырой на его месте потребовал бы дорисовки, которой на снимке 1920 года не было.
mechanic-figure: "98defdd0109639cd19392b60ed7bcdd18443086390dd140826012f91a89fd49a"
